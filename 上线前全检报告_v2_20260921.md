# 漫剧生成系统 · 上线前全检报告 v2（代码审查 + 安全审计 + QA 回归）

> **日期**：2026-09-21　|　**代码基线**：`zdljh/mjscxt` HEAD `3f0dacc`（与 `origin/main` 零分叉，工作树干净）
> **检查范围**：`362affe`（v1 全检基线）→ `3f0dacc` 的全部 25 个修复 commit + 全仓回归
> **方法**：① 全局门禁（py_compile/pyflakes/注入面 grep）② 代码审查（6 条 🟢 保持项不回潮 + 25 commit 修复实质抽查）③ 安全审计（F-01~F-09 逐条 + 新增代码安全面）④ QA 全量离线回归（18 脚本）

---

## 一、总体结论：**✅ 条件 Go（Conditional Go，纯本机单用户部署）**

- **QA 回归**：18/18 脚本全绿，无失败用例、无新回归（含 4 条 P0 专项 + P1-19 收敛验证）。
- **安全闸门**：F-02 / F-03 / F-05(P1-12) / F-08 / F-09 全部 ✅；F-01 主防线完整（`_project_or_400` 46 处收口 + 路径穿越拒绝 + abspath 双保险）。
- **保持项**：6 条 🟢 良好实践**零回潮**。
- **非阻塞遗留（4 项 P2，下迭代）**：F-06 交付面死文件未删 / 约 20 处写盘路由未走 `_project_or_400`（非逃逸型）/ `qc_client.py` 固定 `.tmp` 无锁 ×3 / `app.py` 直跑分支绕过 F-02 护栏。
- **未覆盖项（如实声明）**：主动渗透测试、`pip audit` CVE 比对、Windows 多用户 ACL 运行时效果（F-04/F-07）、P0-4/P0-5 专属离线回归脚本（仅代码走查确认）。

| 视角 | 结果 |
|---|---|
| 🔴 阻断项 | **0** |
| 🟠 非阻塞遗留（P2） | 4 |
| 🟡 运维/人工项 | 3（F-04/F-07 ACL、pip audit、上线 5 项人工走查） |
| 🟢 通过项 | 保持项 6 + 闸门 5 + 回归 18/18 |

---

## 二、全局门禁（提交级）

| 门禁 | 结果 |
|---|---|
| `git pull --ff-only` | ✅ Already up to date，HEAD=origin/main=`3f0dacc`，`0 0` 零分叉 |
| `py_compile app/*.py` | ✅ 全部编译通过 |
| `pyflakes app/*.py`（undefined name） | ✅ 0 命中 |
| 全仓 `shell=True`（FFmpeg/subprocess 注入面） | ✅ 0 处（含 .py/.js/.sh） |
| 工作树 | 仅 `?? deliverables/`（QA 临时目录，非代码产物，gitignore 建议项） |

## 三、代码审查：6 条 🟢 保持项不回潮核查

| 保持项 | 核查方法 | 结果 |
|---|---|---|
| 1. FFmpeg 全链路零 `shell=True` | `grep -rn "shell=True" app/*.py \| wc -l` | ✅ 0 处 |
| 2. agent 工具白名单无 RCE 原语 | 全仓 grep eval/exec 白名单 | ✅ 未回潮（新增代码无白名单外工具） |
| 3. `_serve_safe` / `_safe_upload_name` 防读穿越 | `grep -c def` 存在性 | ✅ 两函数均在（app.py） |
| 4. 密钥 Fernet 库权限 `0o600` | `secret_store.py:122/:227 os.chmod(path, 0o600)` | ✅ 两处均在 |
| 5. `serve.py` atexit + 信号优雅停机 | `grep -c atexit\|signal` = 18 处 | ✅ 在 |
| 6. `_safe_project` 无 `or` 回潮（死守卫） | 括号配平法精确扫描「结果层 `or`」 | ✅ **0 处**（34 处粗命中全部为参数层 `or ''`/`or 'project'` 既有写法或 docstring 警告文字） |

**修复实质抽查**（不只看 commit message，逐条对验收标准核代码）：

- P2-9（`consistency.py`）：降级 pHash 专用阈值 `PHASH_PASS=80 / PHASH_WARN=60`（env 可覆盖，非 0），判定标签带 `degraded_` 前缀 → 噪声图不再误判 pass ✅
- P2-7（`keyframe.py:549-579`）：`critical_issues` 现在经 4 元组 `verify_cb` 真实写入 `r["qc"]["critical_issues"]`，G1 止损 `_qc_retry_hopeless` 可消费 → 空壳修复 ✅
- P1-11（`pipeline.py:1417-1433`）：死信文件「.bak 快照 + 唯一临时名 + 损坏先恢复」✅
- P1-3（`video_postprocess.py:122-123`）：`probe_media` 已取 `sample_rate`/`channels`，拼接前音轨一致性判定生效 ✅
- H1（`comfyui_client.py:61-84`）：`_CALL_STATS` 读写全部包 `with _CALL_STATS_LOCK` ✅
- P1-5（`pipeline.py:795-900`）：时间轴基准改 ffprobe 实测（拼接成片实测 → 各段实测累加退化），不再用剧本 duration ✅
- P1-19 收尾（`dub_mix.py`，我方 commit `3f0dacc`）：`_shot_seq_num` 收敛为 `shot_key.shot_seq` 薄代理，写读两侧同口径（`S01-C02→1` 不再 `→102`）；回归 `verify_shot_key_unify` §5 改为行为锁定 ✅

## 四、安全审计：F-01 ~ F-09 核查表（独立 agent 审计 + 主审复核）

| 闸门 | 位置 | 结论 | 说明 |
|---|---|---|---|
| F-01 路径穿越收口 | `app.py:323-374`（46 处调用） | ✅ | 拒绝空/控制字符/无有效字符 + `..`/绝对路径/分隔符 + abspath 前缀双保险 |
| F-01 收口尾巴 | `app.py` 约 20 处写盘路由 | 🟠 P2 | `_safe_project(...) + 参数层 or 兜底`（如 :4236 `/api/final/video`），会把越界入参静默收敛进共享 `project` 命名空间——**非逃逸型**（无越界写盘），混淆视听 + 串项目风险，下轮统一改 `_project_or_400` |
| F-02 监听护栏 | `serve.py:94-136` | ✅ | 非回环 host 默认 fail-closed 拒绝启动；仅 `MJSCXT_ALLOW_NON_LOOPBACK` 显式开关放行（容器/受控网络场景），无编程旁路 |
| F-02 直跑绕过 | `app.py:10642` `python app/app.py` 分支 | 🟠 P2 | 直跑不经 serve.py 护栏（`APP_HOST=0.0.0.0` 时 debug 模式裸暴露）；非部署路径（Docker CMD 走 serve.py），建议直跑分支调 `_guard_host` 或文档标注 |
| F-03 明文密钥 | 全仓 `sk-` 扫描 | ✅ | 0 命中；`qc_config.backup_20260914.json` 不在树上；`ai_config.json` 本地 api_key 全空（已迁加密库）且 gitignore 排除 |
| F-05/P1-12 GPU 闸门 | `gpu_task_gate.py` | ✅ | 纯 `threading.Semaphore`，零 subprocess/shell、无状态文件（无固定 .tmp 截断面）、`max(1,…)` 防 env 设 0 死锁、`/free` 有 `has_other_running_gpu_tasks` 互斥守卫 |
| F-06 交付面 | 根目录 + templates | 🟠 P2 | `test_page.html` / `h3_ref_submit.py` / `index_old.html` / `index_v2.html` 仍被 git 跟踪；**路由零引用**（独立 grep 复核），纯死文件，实际暴露≈0，上线前删除即可 |
| F-09 .gitignore | `.gitignore:21-27` | ✅ | 失效否定行已删除并留 A-24 注释（`index.json` 明确不入库，靠 `project_store .bak` 恢复），`git check-ignore -v` 实测命中 `output/` 第 21 行 |
| F-08 部署收口 | `docker-compose.yml` | ✅ | `127.0.0.1:5000:5000` 已收口；Dockerfile `EXPOSE 5000` 仅文档性 |
| F-04/F-07 ACL | 代码层 | ✅（运维项） | `.env` / `.secret_key` / `output/secrets.enc` 均未被 git 跟踪；`PROJECT_TRASH_DIR` 路径无越权写风险，`chmod 0o700` 已写代码（Windows 下 no-op，部署机需 icacls 复核） |

## 五、QA 全量离线回归（18/18 全绿，无新回归）

由 QA 工程师独立执行，每脚本一次独立调用（避免 CLI 沙箱批量删除守卫假红），`runtime.json` 已重定向到临时目录并跑后还原，未污染用户暂停态。

| 脚本 | 通过/失败 | 覆盖 |
|---|---|---|
| verify_qc_bool_parse | 91 / 0 | 质检开关字符串语义 |
| verify_prompt_qc | 167 / 0 | 提示词预检 |
| verify_qc_summary_scan | 40 / 0 | 质检摘要扫描 |
| verify_qc_retry_stop | 34 / 0 | 尾帧止损（P2-7） |
| verify_ai_qc_sync | 36 / 0 | AI 质检同步 |
| verify_qc_ref_images | 38 / 0 | 参考图设定图（P1-19 锚点） |
| verify_atomic_write_locks | 77 / 0 | 落盘原子化（P1-1） |
| verify_project_delete_coverage | 17 / 0 | 项目删除覆盖 |
| verify_project_audit | 61 / 0 | 端点存在性 + 死代码不回潮 |
| verify_final_video_episode | 37 / 0 | 成片/时间轴（P1-5） |
| verify_camera_framing | 70 / 0 | 景别/机位 |
| verify_h3_sequential | 41 / 0 | H3 逐镜匹配（P1-7） |
| verify_qc_gate_declared_off | 28 / 0 | **P0-1** 半开质检态 fail-closed |
| verify_shot_content_dialogue | ALL PASS | **P0-2** 仅台词镜 + P1-16 |
| verify_negation_boundary | 40 / 0 | **P0-3** 否定语境 |
| verify_shot_key_unify | ALL PASS | **P1-19** 镜号收敛（含 dub_mix 行为锁定） |
| verify_cancel_interrupt | 41 / 0 | 托管暂停秒级生效 |
| verify_empty_shot_drop | 7 / 0 | 空壳镜头丢弃不变量 |

**4 条 P0 专项（P0-1/2/3）+ P1-19 全部通过**；P0-4/P0-5 无专属脚本，经代码走查确认（`pipeline.py:944-948` review 复位、`app.py:9607` 集级互斥）。

## 六、🟠 P2 非阻塞尾巴清单（下迭代处理，不阻断上线）

| 编号 | 位置 | 问题 | 建议 |
|---|---|---|---|
| T-1（F-06） | 根目录 `test_page.html`/`h3_ref_submit.py`、`app/templates/index_old.html`/`index_v2.html` | 调试/历史死文件仍被 git 跟踪（路由零引用） | 上线前 `git rm` 删除 |
| T-2 | `app.py` 约 20 处写盘路由（含 :4236） | `_safe_project + 参数层 or 兜底` 未走 `_project_or_400` | 统一改 `_project_or_400`（非逃逸型，仅口径） |
| T-3 | `qc_client.py:743/896/915` | `save_config/save_endpoint/reset_endpoint` 固定 `.tmp` + 模块无锁 | 抽 `atomic_write_json` 复用（同 P1-1 手法） |
| T-4 | `app.py:10642` 直跑分支 | `python app/app.py` 不经 serve.py F-02 护栏 | 直跑分支调 `_guard_host` 或文档标注仅本地开发 |

## 七、🟡 运维/人工项（脚本替代不了，上线前逐条走查）

1. **真实 3 集项目跑一遍托管**：分镜 → 尾帧 → 视频 → 成片 → 字幕全链路产物齐全，`deliverables.json` 与磁盘一致；
2. **抽查 3 个 `*.meta.json`（分镜/尾帧各 ≥1）+ 3 条质检记录**，确认 `seed`/`prompt`/`qc` 字段齐全（尾帧 meta 由 P2-5/P2-6 补上）；
3. **断质检接口**（`base_url` 改错）再跑一镜，确认 fail-closed（无产物入库 + 前端明确告警）——对应 P0-1 修复；
4. **点一次暂停**，确认秒级生效（`verify_cancel_interrupt` 已绿，实机再点一次）；
5. **跑完一轮检查 `output/`** 无未被任何清单引用的孤儿文件；
6. **F-04/F-07 ACL**：部署机上 `icacls` 收紧 `.env`/`.secret_key`/`secrets.enc` 至当前用户 + 回收站目录 0700；
7. **线上 5000 实例**：确认已重启加载 `3f0dacc`（`app/*.py` 改动必须重启才生效，重启前查 `/api/autopilot/status` 的 `current` 非空=有项目在跑）。

## 八、Go / No-Go 判定与条件

**判定：✅ Conditional Go**——核心 5 条 P0 + 5 条上线闸门（F-01/02/03/05/09）+ 6 条保持项 + 18/18 回归全绿，无阻断项。

**上线前置条件（必须全满足）**：
- [ ] 部署机重启线上实例加载 `3f0dacc`（第七节第 7 条）；
- [ ] 完成第七节 5 项人工走查；
- [ ] F-04/F-07 ACL 在部署机 `icacls` 收紧（运维）；
- [ ] （建议）先删 T-1 死文件再打包交付，缩小侦察面。

**可延后（不影响本机单用户上线）**：T-2 / T-3 / T-4 三项 P2 尾巴，下迭代统一收口。
**未覆盖（如实声明）**：主动渗透测试、`pip audit` 逐包 CVE 比对、Windows 多用户 ACL 运行时实测、P0-4/P0-5 专属离线回归脚本。

> **与 v1（362affe 基线）差异**：v1 标记的 🟠6/🟡10 中，P0×5、P1 主线、F 系列闸门、P1-19 收尾均已在前 25 个 commit 修复并经本次回归验证；v2 剩余的 4 个 P2 尾巴（T-1~T-4）为「已达标非阻断」级别，其中 T-1/F-06 建议上线前顺手清掉。
