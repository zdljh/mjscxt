# 全项目缺陷审计报告 · 提示词 / 图片 / 视频 / AI质检 + 全局状态机

- **审计对象**：漫剧生成系统（AI 漫剧自动生成）
- **代码基线**：`295933d`（`main`，与 `origin/main` 零分叉；工作树仅 `?? deliverables/`）
- **审计日期**：2026-09-21
- **审计方式**：只读。5 路并行深挖（提示词/图片/视频/质检/全局）+ 主理人独立复核；**所有条目均已亲自 Read 源码并原样引用**，其中 6 条用真实代码执行验证过（条目内标「实测」）。
- **规模**：后端 `app/*.py` 41,021 行（`app.py` 10,434 行）
- **结论**：**P0 阻断 5 条｜P1 严重 18 条｜P2 优化 14 条**；另有 4 项「历史审计声称已修，实为未修/部分修」。

---

## 一、项目整体流程简述 & 风险总览

### 1.1 端到端流水线（文本版）

```
[小说]  output/novels/<novel_id>/*.txt
   │ novel_parser.split_chapters()  → chapters[{index,title,text}]
   │ autopilot.chapters_of() 实时重切（每集=一章）
   ▼
┌─ 每集 9 步（pipeline.STEP_SEQUENCE）────────────────────────────────────────┐
│ 1 script      step_script       novel_to_script → output/scripts/<项目>/epNN_script.json
│               ↑ shots[{shot_id,duration,camera,location,description,visual_detail,
│                       dialogue,audio_cues,characters_in_shot,items_in_shot,style}]
│               门禁：原文覆盖率 coverage（≥ coverage_min_percent）+ audit_script
│ 2 assets      step_assets       _generate_asset_task → ComfyUI character/item/scene_gen
│               → output/assets/{characters,items,scenes}/<项目>/<名>/{base.png,<视角>.png}
│               门禁：qc_client.check_image（基础图 + 逐视角），不达标重画、阻断不入库
│ 3 storyboard  step_storyboard   _storyboard_worker → ComfyUI 分镜生成.json
│               → output/storyboards/<项目>[/epNN]/shot_XX.png + storyboard_manifest.json
│               门禁：prompt_qc 预检 + check_image（带角色/物品/场景设定图）
│ 4 keyframe?   step_keyframe     keyframe.generate_keyframes → ComfyUI Qwen Edit
│               → output/keyframes/<项目>[/epNN]/shot_XX_end.png（链式：上镜尾帧=下镜首帧）
│               门禁：_keyframe_qc_verifier
│ 5 video       step_video        _video_generate_worker → ComfyUI H3
│               模式 episode（整集一次生成）/ per_shot（逐镜）/ keyframe（首尾帧插值）
│               → output/videos/<项目>[/epNN]/shot_XX.mp4 或 epNN_full.mp4
│               门禁：check_video（抽帧送检 + 客观层 duration/fps/音轨）
│ 6 final       step_final        video_postprocess.concat_videos / add_subtitles
│               → output/final/<项目>/epNN_final.mp4
│ 7 tts         step_tts          build_dub_plan + _dub_worker → TTS
│               → output/dub/<项目>/lines/*.wav + epNN_dub_manifest.json
│ 8 mix         step_mix          _mix_prepare + _mix_worker(ffmpeg)
│               → output/mix/<项目>/epNN_dubbed.mp4
│ 9 upscale     step_upscale      upscale_client.VideoUpscaler（FlashVSR，全程 fail-open）
│               → output/upscale/<项目>/epNN_upscaled.mp4
└─────────────────────────────────────────────────────────────────────────────┘
   ▼
record_deliverable() → output/autopilot/<项目>/deliverables.json
   │ 人工验收 review ∈ {pending, accepted, rejected}
   └─ rejected → 期望「重生产」，实际见 P0-4（空转）
```

### 1.2 关键上下文传递关系

| 传递物 | 生产方 | 消费方 | 契约 |
|---|---|---|---|
| `ctx`（pipeline） | `run_episode()` | 全部 step | `{config, project_name, project_key, episode_no, novel_meta, chapter, script, steps, progress}` |
| `script.shots[]` | `novel_to_script._norm_shots` | 分镜/尾帧/视频/配音/字幕/NLE | `shot_id` 为整数序号；`duration = max(模型值, required_shot_duration())` |
| 资产图 | `_generate_asset_task` | 分镜图/H3/尾帧 | 路径约定 `output/assets/<kind>/<项目>/<名>/` |
| `storyboard_manifest.json` | `_storyboard_worker` / 单镜重做 | 画布/一致性/probe | 「正式目录有非空图 ⟺ 已过质检」**该不变量被 P0-1 破坏** |
| `epNN_dub_manifest.json` | `_dub_worker` | `probe_tts` / 混音 / 音频质检 | 配音完成度的**唯一**真相源（写入非原子，见 P1-10） |
| `deliverables.json` | `record_deliverable` | 人工验收 / 托管挑集 | `review` 复位靠 **path 判等**（见 P0-4） |
| 质检记录 | `_qc_record_verdict` | 总览 / 教训回流 | `output/qc/<项目>/<kind>_<shot>.json`，字段 `passed`/`time` |

### 1.3 风险总览

| 风险面 | 结论 |
|---|---|
| **质检闸门可信度** | ⚠️ 三处系统性削弱：① 「已声明开启但接口未就绪」时 3 条链路 fail-open（P0-1）；② 否定语境判定无分句边界，崩坏描述被吞（P0-3）；③ 缺 score 即放行（P1-17）。**资产链路是唯一 fail-closed 的**。 |
| **断点续跑可信度** | ⚠️ 前提是「正式目录有图 ⟺ 已过质检」，被 P0-1 破坏后会出现**未质检产物被洗白为 `success=True`**。 |
| **多集（epNN）隔离** | ⚠️ 普遍缺失：分镜复用守卫（P1-8）、混音取素材（P1-6）、NLE 导出（P2-13）、字幕时间轴（P1-5）均不带集号。 |
| **落盘原子性** | ⚠️ 仅 4 个模块收敛，**仍有 18 处固定 `.tmp` + 无锁**，含密钥库（P1-1）。 |
| **并发/幂等** | ⚠️ 单 GPU 无闸门（`TaskQueue` 从未接线，P1-12）；`run-once` 与托管无集级互斥（P0-5）；内部任务污染公共任务表（P1-9）。 |
| **数据安全** | ⚠️ 项目索引损坏 → 静默清空 → 写回空索引 → 全部项目永久消失（P1-2）；密钥库读改写竞态 + 静默清空（P1-1）。 |
| **长时无人值守** | ⚠️ 死信落盘失败 → 计数清零 → 无限重跑（P1-11）；磁盘无水位控制（P2-10）。 |

---

## 二、缺陷分级清单

- **P0 阻断（5 条）**：会导致流水线跑不通 / 产出完全异常 / 未质检产物进入交付 / 死循环
- **P1 严重（18 条）**：偶现异常、画风漂移、质检误判、数据错乱、跨集错配
- **P2 优化（14 条）**：性能、日志、可维护性、可观测性、资源治理

---

## 三、缺陷条目 · P0 阻断级

### 【P0-1】【质检闸门 · 跨模块】「已声明开启但接口未就绪」时，分镜/视频/尾帧未质检产物直接写入正式目录

- **类型**：业务流程逻辑缺陷（fail-open 口径不一致 + 断点续跑不变量破坏）
- **所属模块**：分镜图 / 视频（整集+逐镜）/ 尾帧 × AI 质检
- **文件路径:行号**
  - ❌ `app/app.py:3121-3123`（分镜，fail-open）
  - ❌ `app/app.py:3852-3855`（逐镜视频，fail-open）
  - ❌ `app/app.py:3661` + `app/app.py:3690-3694`（整集视频，`qc_fn=None` → `qc_passed=True`）
  - ❌ `app/app.py:5715-5719` + `app/keyframe.py:462-470`（尾帧，`verify_cb=None` → move 进正式目录）
  - ✅ 对照（正确做法）`app/app.py:2415-2422`、`app/app.py:2512-2520`（资产，fail-closed）
- **问题描述**：四条链路统一写 `if not qc_on:`，**都没有同时判断 `qc_declared`**。`qc_on = qc_client.image_qc_ready(cfg)` 在「用户开了质检但 base_url / api_key / model 不全或网关不通」时为 False：
  ```python
  # app/app.py:3121（分镜）
  if not qc_on:
      shutil.copy2(scratch_png, dst)   # 未质检的图直接写正式目录
      break
  ```
  ```python
  # app/app.py:3852（逐镜视频）
  if not qc_on:
      if os.path.exists(dst):
          os.remove(dst)
      shutil.move(v_scratch, dst)      # 未质检的视频直接入库
      break
  ```
  ```python
  # app/keyframe.py:462（尾帧）
  if verify_cb is None and r.get("ok") and os.path.abspath(scratch_end) != os.path.abspath(end_p):
      shutil.move(scratch_end, end_p)  # 未送检的尾帧进正式目录
  ```
  资产链路在同条件下置 `base_gate = {"accept": False, "blocked": True, "label": "质检接口未就绪"}` 并只留暂存 —— **三种口径并存**。
- **风险影响（次生伤害更致命）**
  1. 未质检产物进入正式目录，破坏 `app/app.py:2966` 明文声明的不变量「正式目录有图 ⟺ 已过质检」；
  2. 重试轮 `app/app.py:2968-2977` 的逐镜跳过逻辑据此把该镜**改写为 `success=True` / `label="沿用已达标图"`** —— 未质检产物被"洗白"；
  3. `pipeline.probe_storyboard` / `probe_video` 只看文件存在 → 判就绪 → 未质检图/视频进入成片并登记交付物；
  4. 尾帧在链式模式下是**下一镜首帧**，坏图顺链污染后续全部镜头；
  5. `app/app.py:3186-3189` 的报错文案写「未写入正式目录」，与事实相反，误导排障。
- **复现条件**：`qc_config.json` 设 `enabled=true, image_enabled=true`，把 `base_url` 改错（或留空 key/model）→ 跑一次分镜/视频/尾帧生成 → 正式目录出现产物，`output/qc/<项目>/` 无对应记录。
- **修复方案**：抽公共判定，四处统一为「declared-but-unavailable ⇒ fail-closed」：
  ```python
  # qc_client.py（新增）
  def qc_unavailable_but_declared(cfg: dict) -> bool:
      return bool(cfg.get("enabled") and cfg.get("image_enabled")) and not image_qc_ready(cfg)

  # app/app.py:3121（分镜）
  if not qc_on:
      if qc_client.qc_unavailable_but_declared(qc_cfg):
          item["qc_blocked"] = True
          item["error"] = "质检已开启但接口未就绪，未写入正式目录（暂存图见质检历史）"
          break                              # 保留暂存，不 copy2
      shutil.copy2(scratch_png, dst)         # 真正"未开启质检"才按原行为入库
      break
  ```
  视频/尾帧同理（不 move 到正式路径，改写 `qc_blocked/qc_unavailable` 并保留暂存）；同时修正 `app.py:3186-3189` 文案。
- **验收标准**：① 上述复现条件下正式目录**零新增**产物，任务结果出现 `qc_blocked / qc_unavailable` 且原因="质检接口未就绪"；② 重试轮不再出现 `label="沿用已达标图"` 且 `success=True` 的条目；③ 真「质检未开启（`image_enabled=false`）」时行为与旧版一致（正常入库）。
- **负责人建议**：**开发A**（本批最高优先，先改分镜 `:3121` 与逐镜视频 `:3852`，再补整集/尾帧）

---

### 【P0-2】【提示词预检 · 上游】空壳镜头判据上下游不一致 → 「仅台词」镜头被判死刑且不可自愈 → 整集在分镜步永久卡死

- **类型**：业务流程逻辑缺陷（判据分叉 + 不可自愈 fatal）
- **所属模块**：剧本归一化 × 提示词预检 × 分镜步门禁
- **文件路径:行号**：`app/novel_to_script.py:845-857`、`app/prompt_qc.py:227-232`、`app/prompt_qc.py:248-249`
- **问题描述**：上游「空壳」判据包含台词，下游预检判据**不含台词**：
  ```python
  # novel_to_script.py:845
  _has_vis = bool(str(s.get("description") or "").strip()
                  or str(s.get("visual_detail") or "").strip()
                  or str(s.get("storyboard_prompt_zh") or "").strip())
  ...
  # novel_to_script.py:855  ← 有台词就放行
  if not (_has_vis or _has_dlg):
      dropped_empty.append(...); continue
  ```
  ```python
  # prompt_qc.py:227  ← 只看画面三选一
  return bool(_txt(ctx.get("description")) or _txt(ctx.get("visual_detail"))
              or _txt(ctx.get("storyboard_prompt_zh")))
  # prompt_qc.py:248
  if not _has_shot_content(ctx):
      fatal.append("镜头缺少画面描述（description / visual_detail / storyboard_prompt_zh 均为空）")
  ```
  `_check_storyboard` 把它放进 `fatal`，而 `_repair_storyboard` 只补风格/无文字约束 → 复检仍 fatal → **不可自愈**。
- **风险影响**：该镜确定性永久失败（`_PromptQCBlocked`，`app/app.py:3025-3030`）→ `probe_storyboard` 永远缺 1 镜 → **整集在分镜步永久失败**，视频/成片全部拿不到；重跑必然复现同样失败；用户无任何可操作补救入口。这正是 `novel_to_script.py:836-841` 注释声称已修掉的卡死场景。
- **复现条件**：模型输出 `{"camera":"中景","location":"破庙","dialogue":[{"speaker":"林风","text":"谁在那里？"}]}`（漏写 description/visual_detail）→ 跑批量分镜。
- **修复方案**：统一判据，二选一（推荐 A）：
  - **A（上游补齐）**：`_norm_shots` 对「仅台词」镜头用台词补画面：`row["description"] = row["description"] or <台词文本>`；仍为空才丢弃。
  - **B（下游放宽）**：`_has_shot_content` 把 `dialogue` 计入并同步 `prompt_qc._rebuild_hint`；同时保证 `_check_storyboard` 仍能把「无画面约束」判为 **issues（可自愈）** 而非 fatal。
- **验收标准**：构造仅含 `dialogue` 的镜头跑批量分镜 → 无单镜永久失败，`pipeline.step_storyboard` 的 `probe.done=True`；真·四字段全空镜头仍被丢弃（回归 `verify_empty_shot_drop`）。
- **负责人建议**：**开发A**

---

### 【P0-3】【AI质检 · 代码侧硬闸】否定语境判定无分句边界 → 崩坏描述被吞 → 崩坏图/视频 `blocked=False` 正常入库

- **类型**：代码bug（硬闸被绕过）
- **所属模块**：AI 质检（`find_critical_issues` → `_finalize_verdict` / `_qc_gate`）
- **文件路径:行号**：`app/qc_client.py:160-169`、`app/qc_client.py:192`
- **问题描述**：否定语境只看关键词**前 6 个字**，不做分句截断，而否定词表包含 `不`、`缺少`、`无`：
  ```python
  _NEGATION_WORDS = ("无", "没有", "未", "不存在", "非", "不", "缺少", "未发现", "已消除")

  def _has_negative_context(text: str, idx: int, window: int = 6) -> bool:
      ctx = str(text or "")[max(0, idx - window):idx]
      return any(neg in ctx for neg in _NEGATION_WORDS)
  ```
  于是**前一个分句**的否定词会把**后一个分句**的真缺陷判成否定语境而丢弃。
- **风险影响**：`find_critical_issues` 是 `_finalize_verdict` 与 `_qc_gate`「P0 独立复核」共用的**唯一**关键缺陷硬闸。一旦漏检，崩坏图/视频 `blocked=False` → `_qc_gate` 返回 `accept=True` → 正常写入正式目录并进入成片。中文缺陷描述里「不自然 / 不协调 / 缺少细节」极其常见，触发概率高。
- **复现条件（实测，已执行）**：
  ```
  find_critical_issues(['五官不自然，面部畸变'])   -> []     # 「畸变」被前文「不」吞掉
  find_critical_issues(['比例不协调，人物崩坏'])   -> []     # 「崩坏」同上
  find_critical_issues(['画面缺少细节，手指异常']) -> []     # 「缺少」= 否定词
  find_critical_issues(['角色面部轻微畸变'])       -> ['角色面部轻微畸变']   # 无否定词才命中
  # 端到端：_qc_gate(...) -> {'accept': True, 'label': '质检达标'}
  ```
- **修复方案**：加分句边界 + 邻近距离约束：
  ```python
  _CLAUSE_BREAK = "，。；！？,.;!?、\n"

  def _has_negative_context(text: str, idx: int, window: int = 6) -> bool:
      s = str(text or "")
      ctx = s[max(0, idx - window):idx]
      for ch in _CLAUSE_BREAK:          # 回扫遇分句边界即截断
          p = ctx.rfind(ch)
          if p != -1:
              ctx = ctx[p + 1:]
      return any(neg in ctx for neg in _NEGATION_WORDS)
  ```
- **验收标准**：新增单测覆盖上述 4 条文案，**前 3 条必须命中**（返回非空）；`'无明显畸变'`、`'画面无崩坏'` 仍不命中；`verify_qc_bool_parse / verify_prompt_qc` 全绿。
- **负责人建议**：**开发A**

---

### 【P0-4】【全局状态机】成片「打回（rejected）」后幂等短路 → 空转、`review` 永不复位、最终误报「状态同步异常」

- **类型**：业务流程逻辑缺陷（状态机闭环失效）
- **所属模块**：托管（autopilot）× 交付物验收（pipeline）
- **文件路径:行号**：`app/pipeline.py:1318-1324`、`app/pipeline.py:1364-1376`、`app/autopilot.py:700-702`、`app/autopilot.py:747-751`
- **问题描述**
  1. 打回只写 `review` 字段 + 清死信，**不失效化任何产物**：
     ```python
     # pipeline.py:1364
     item.update({"review": review, "review_note": note, "reviewed_at": _now()})
     ...
     if review == "rejected":
         resolve_dead_letter(project_name, episode_no, note="成片被打回，重新生产")
     ```
  2. `record_deliverable` 的 review 复位靠 **path 判等**：
     ```python
     # pipeline.py:1321
     "review": "pending" if prev.get("path") != path else (prev.get("review") or "pending"),
     ```
  3. 重跑时 `probe_final` / `probe_mix` / `probe_upscale` 全部命中（均为**确定性路径**）→ 全步骤 `skipped` → 交付物 `path` 与上次**完全相同** → `review` 保留 `rejected`。
  4. 循环往复，直到 `_note_run` 连续 3 次「成功但 review 仍为 rejected」触发死信，报出**误导性**的「疑似状态同步异常，请检查交付物索引是否可写」。
- **风险影响**：24/7 无人值守的核心闭环（人只做验收/打回）彻底失效 —— 用户打回后拿不到新成片，看板永久显示「已打回，待重做」，期间反复空跑消耗 GPU，最终收到指向错误的告警。
- **复现条件**：某集已出片并登记 → POST `/api/autopilot/deliverables/review {review:"rejected"}` → 让托管轮转跑到该集 → `deliverables.json` 的 `review` 始终 `rejected`，成片 mtime 不变。
- **修复方案**（两处都要改）：
  ```python
  # ① 打回即失效化产物（改名封存，而非删除，便于人工回看）
  if review == "rejected":
      ts = time.strftime("%Y%m%d_%H%M%S")
      for p in (pipeline.mix_output_path(ctx), pipeline.final_path(ctx), pipeline.upscale_path(ctx)):
          if os.path.isfile(p):
              os.replace(p, f"{p}.rejected.{ts}")
      resolve_dead_letter(project_name, episode_no, note="成片被打回，产物已封存，待重生产")
  ```
  ```python
  # ② 复位判据改为「产物指纹」而非路径
  _fp = (os.path.getmtime(path), os.path.getsize(path)) if _nonempty(path) else None
  "review": "pending" if prev.get("fingerprint") != _fp else (prev.get("review") or "pending"),
  "fingerprint": _fp,
  ```
- **验收标准**：打回后该集下次生产必须产出**新内容**（`path` 或 mtime/size 变化），`review` 自动回到 `pending`；连续 2 轮内不再残留 `rejected`，也不再产生「状态同步异常」死信。
- **负责人建议**：**开发A**

---

### 【P0-5】【并发/幂等】`/api/autopilot/run-once` 与托管守护线程无集级互斥 → 同一集两条流水线并发写同一批路径

- **类型**：架构稳定性隐患（并发）
- **所属模块**：托管 × Flask 路由 × 总控 AI（`produce_episode`）
- **文件路径:行号**：`app/app.py:9298`、`app/autopilot.py:631`、`app/autopilot.py:729-734`
- **问题描述**：run-once 在 **Flask 请求线程里同步执行整集流水线**：
  ```python
  # app/app.py:9298
  result = pipeline.run_episode(cfg, project, ep, meta, chapter)   # 未传 should_stop
  ```
  而守护线程的防重跑守卫 `_rerun_allowed` **只认 `_LAST_RUN`**，该状态仅在 `_note_run`（生产**结束后**）更新：
  ```python
  # app/autopilot.py:729
  def _rerun_allowed(project: str, episode_no: int) -> bool:
      with _LOCK:
          if _LAST_RUN.get("key") != f"{project}#{episode_no}":
              return True
  ```
  run-once 全程不写 `_LAST_RUN`，`_episode_state` 此时仍是 `todo` → 守护线程在 `IDLE_SLEEP=20s`（或 `wake`）后会**再挑中同一集**。
- **风险影响**：① 单 GPU 上双倍烧卡、争抢显存（OOM）；② 两线程写同一批 `shot_NN.png` / `epNN_final.mp4` / dub manifest / mix 成片 → 半成品或错序成片，可能被 `_playable` 判就绪并登记为交付物；③ `record_deliverable` / `append_history` 重复登记；④ run-once 未传 `should_stop` → 用户点「暂停托管」对它**完全无效**。
- **复现条件**：项目已启用托管且处于 IDLE_SLEEP 窗口 → POST `/api/autopilot/run-once {project, episode_no}` → 20s 内 `autopilot.status().current` 与该集一致，日志可见两次「第N集剧本…」并行推进。
- **修复方案**：引入进程内集级锁 + 幂等拒绝：
  ```python
  # pipeline.py
  _EP_RUNNING: dict = {}
  _EP_LOCK = threading.Lock()

  @contextlib.contextmanager
  def episode_guard(project_name: str, episode_no: int):
      key = f"{project_name}#{int(episode_no)}"
      with _EP_LOCK:
          if key in _EP_RUNNING:
              raise PipelineError(f"该集已在生产中（{key}），拒绝重复启动")
          _EP_RUNNING[key] = time.time()
      try:
          yield
      finally:
          with _EP_LOCK:
              _EP_RUNNING.pop(key, None)
  ```
  `run_episode` 全程持锁（`autopilot._produce` 与 run-once 共用）；run-once 改为传 `should_stop=autopilot._halt_requested`。
- **验收标准**：并发触发「托管轮转 + run-once」同一集 → 第二次被显式拒绝（或复用），日志仅一轮生产，磁盘产物只被一条链路写；`/api/autopilot/pause` 能中断 run-once。
- **负责人建议**：**开发A**

---

## 三（续）、缺陷条目 · P1 严重级

### 【P1-1】【数据安全】落盘「固定 `.tmp` + 无锁」仍散落 18 处（含密钥库读改写竞态与静默清空整库）

- **类型**：架构稳定性隐患 / 代码bug　|　**所属模块**：持久化（secret_store / qc_client / llm_client / pipeline / autopilot / continuity / consistency / autonomous）
- **文件路径:行号**（实测统计 `grep -c "Lock()"` / 固定 tmp 处数）：
  ```
  已修（唯一 tmp + 锁）：project_store.py 1/1 ✅  ai_config.py 1/1 ✅  ai_chat.py 1/1 ✅  prompt_memory.py 2/1 ✅
  未修（固定 path+".tmp" 且无锁）：
    app/secret_store.py:152-162 ← 最高危（密钥库）
    app/qc_client.py:692, 845, 864        app/llm_client.py:217, 273
    app/pipeline.py:1261, 1278, 1323, 1369, 1403
    app/autopilot.py:148                  app/continuity.py:108
    app/consistency.py:364                app/autonomous.py:60
  ```
- **问题描述（最高危子项）**：
  ```python
  # secret_store.py:152
  def _write_store(self, data: dict) -> None:
      tmp = path + ".tmp"                       # ← 固定名，并发写者互相截断
      with open(tmp, "w", encoding="utf-8") as f:
          json.dump(data, f, ensure_ascii=False, indent=2)
      os.replace(tmp, path)

  # secret_store.py:148  读取失败 → 静默按空处理
  except Exception as e:
      logger.warning(f"密钥库读取失败（按空处理）：{e}")
      return {"version": 1, "secrets": {}}
  ```
  `set_api_key` 是典型 `_read_store() → 改 → _write_store()` 序列（`secret_store.py` 内确认）。并发下（多标签页/多会话保存 AI 设置）会丢密钥；若 `.tmp` 被交错截断导致解析失败，**下一次写入会把整个密钥库替换为单条记录**，仅留一行 warning。
- **风险影响**：① 已保存的 text/qc/chat 三把密钥集体静默失效；② `qc_config` 损坏 → 触发 G14 corrupt → 质检降级/跳过（与 P0-1 叠加放大）；③ `pipeline` 的 `deliverables.json` / `dead_letter.json` 损坏 → 交付记录丢失、「需人工介入」列表为空；④ `llm_client` 配置损坏 → 文本模型不可用。
- **复现条件**：两个会话/标签页同时保存 AI 设置；或让 `output/secrets.enc.tmp` 被并发写；随后检查 `secret_store.get_api_key('qc')` 是否为空。
- **修复方案**：把 `project_store.py:134-169` 的 `_atomic_replace` + 唯一临时名 + `@_locked` 抽为公共工具（建议 `app/atomic_io.py`），18 处统一替换：
  ```python
  # atomic_io.py（新）
  def write_json_atomic(path: str, data, *, fsync: bool = True) -> None:
      with path_lock(path):                     # 按 abspath 复用 RLock
          os.makedirs(os.path.dirname(os.path.abspath(path)), exist_ok=True)
          tmp = f"{path}.{os.getpid()}.{threading.get_ident()}.{os.urandom(3).hex()}.tmp"
          try:
              with open(tmp, "w", encoding="utf-8") as f:
                  json.dump(data, f, ensure_ascii=False, indent=2)
                  f.flush()
                  if fsync:
                      os.fsync(f.fileno())
              _replace_backoff(tmp, path)       # 复用 project_store._atomic_replace 的 8 次退避
          except Exception:
              try:
                  if os.path.exists(tmp):
                      os.remove(tmp)
              except OSError:
                  pass
              raise
  ```
  并单独修 `secret_store._read_store`：解析失败改 **fail-loud + 隔离**（备份 `path + ".corrupt.<ts>"`、抛 `SecretStoreCorrupt`），**绝不返回空 dict 供上层写回**。
- **验收标准**：① `grep -rn 'tmp = .*\.tmp"' app/*.py` 为空；② 并发 20 次 `set_api_key` 往返后密钥库仍含全部 key；③ 注入损坏 `secrets.enc` → `get_api_key` 抛明确异常并生成 `.corrupt.<ts>`，不返回空；④ 离线测试全绿。

---

### 【P1-2】【数据安全】项目索引损坏 → 静默清空 → 读改写写回空索引 → 全部项目永久消失（且托管静默停摆）

- **类型**：架构稳定性隐患（静默数据丢失放大）　|　**所属模块**：项目注册表（project_store）
- **文件路径:行号**：`app/project_store.py:114-131`、`:181-190`、`:257`/`:296`、`:302`/`:310`、`:474`/`:476`
- **问题描述**：`_read_json` 只对 `PermissionError` 退避重试；**JSON 解析失败直接返回默认值**，无日志、无隔离、无备份：
  ```python
  for i in range(6):
      try:
          with open(path, "r", encoding="utf-8") as f:
              return json.load(f)
      except PermissionError:
          time.sleep(0.02 * (i + 1))
      except (OSError, ValueError):
          return default          # ← 损坏 = 静默当"没有"
  ```
  `load_index()` 拿到 `None` 后构造空索引，而调用方（`create_project` / `rename` / `delete`）都是**读 → 改 → `save_index`** → 把空索引写回，**覆盖掉尚可人工修复的损坏文件**。全库无 `.bak` 轮转。
- **风险影响**：① 用户所有项目从列表消失（刷新也不回来）；② `autopilot.enabled_projects()` / `list_plans()` 同时被打空 → **托管静默停摆**，agent 侧 `get_status/get_plan` 回「没有已启用项目」，用户与 AI 都误判成「自己没开托管」；③ 唯一恢复入口 `migrate_legacy()`（`project_store.py:768-793`）**本身就是读改写同一索引**，并发/损坏时会二次覆盖。
- **复现条件**：把 `output/projects/projects_index.json` 截断成半截 JSON → 打开应用 → 项目列表为空 → 新建任意项目 → 检查文件已被空索引覆盖。
- **修复方案**：
  ```python
  # ① 解析失败 fail-loud + 隔离备份
  except (OSError, ValueError) as e:
      logger.error("项目索引解析失败（%s）：%s", path, e)
      try:
          shutil.copy2(path, f"{path}.corrupt.{time.strftime('%Y%m%d_%H%M%S')}")
      except OSError:
          pass
      raise
  # ② save_index 前备份上一版
  def save_index(index):
      if os.path.isfile(PROJECT_INDEX_PATH):
          shutil.copy2(PROJECT_INDEX_PATH, PROJECT_INDEX_PATH + ".bak")
      _write_json(PROJECT_INDEX_PATH, index)
  # ③ 只读路由捕获异常 → 5xx + 明确文案，不返回空列表
  ```
- **验收标准**：注入截断索引后 `GET /api/projects` 返回 5xx + 明确原因（而非空列表），并生成 `.corrupt.<ts>` 与 `.bak`；恢复原文件后列表完整；索引损坏期间新建/改名/删除**不得**写入覆盖。

---

### 【P1-3】【视频·成片】拼接前的音轨/视频一致性判定是死代码 → `-c copy` 拼出音轨错位成片且被判就绪

- **类型**：代码bug　|　**所属模块**：成片拼接 `video_postprocess.concat_videos`
- **文件路径:行号**：`app/video_postprocess.py:84-87`、`:302-312`、`:324-338`
- **问题描述**：判定读 `sample_rate` / `channels`，但 `probe_media` 的 ffprobe **根本没取这两个字段**：
  ```python
  # video_postprocess.py:84
  cmd = ["ffprobe", "-v", "error", "-show_entries",
         "stream=index,codec_type,codec_name,width,height:format=duration,format_name",
         "-of", "json", path]        # ← 无 sample_rate / channels / r_frame_rate
  # :302
  audio_params.add((p.get("audio_codec"), p.get("sample_rate"), p.get("channels")))
  need_reencode = has_any_audio and len(audio_params) > 1   # ← 后两项恒 None
  ```
  即 `(aac, None, None)` 与 `(aac, None, None)` 被判一致；视频侧参数（宽高/帧率/SAR）**完全未参与判定**；`-c copy` 后不校验产物，`returncode == 0` 即信任并返回。
- **风险影响**：正是 `ensure_audio_track` 想修的场景没被修住 —— H3 原生音轨（32k/mono aac）与补入静音轨（48k/stereo aac）**codec 相同但采样率/声道不同** → 判「一致」→ `-c copy` 拼接 → 音轨错位/变速/后半段异常；而 `probe_final` 的 `_playable`（只要 ffprobe 读出视频流且 `duration>0`，`app/pipeline.py:250-283`）**判就绪** → 坏成片被 `step_final` 标 ok 并登记交付物。
- **复现条件**：用 `32k/mono aac` 与 `48k/stereo aac`（同 codec）的两段 mp4 调 `concat_videos` → 不走 `_concat_reencode`，函数返回成功路径。
- **修复方案**：① `-show_entries` 补 `sample_rate,channels,r_frame_rate,avg_frame_rate`，`info["streams"]` 同步映射；② 一致性判定加视频参数 `("codec","width","height","fps")`；③ copy 成功后复核「总时长 ≈ Σ段时长（容差 `max(1.0, sum*0.02)`）+ 有视频流」，不符即 `return self._concat_reencode(...)`；④ 修 `:320-322` 的 concat list 路径转义（反斜杠与单引号）。
- **验收标准**：上述构造输入必须走重编码路径；任一段替换为不同分辨率/帧率时判定不一致并触发降级；`-c copy` 产物时长偏差 >2% 时自动降级而非静默成功。

---

### 【P1-4】【视频·成片】降级重编码硬编码 `1280:720@30` → 竖屏 9:16 项目被改成横屏加黑边（不可逆）

- **类型**：代码bug　|　**所属模块**：`video_postprocess._concat_reencode`
- **文件路径:行号**：`app/video_postprocess.py:374-386`
- **问题描述**：
  ```python
  per_stream.append(
      f"[{i}:v:0]scale=1280:720:force_original_aspect_ratio=decrease,"
      f"pad=1280:720:(ow-iw)/2:(oh-ih)/2,fps=30,format=yuv420p,setsar=1[v{i}]"
  )
  ```
  与竖屏设定冲突：`style_kit.DEFAULT_RATIO` 为 9:16，H3 实际以 544×960 出片。
- **风险影响**：只要降级路径被触发（P1-3 的编码不一致，或 `-c copy` 失败），竖屏成片被改写成 **1280×720 横屏画布 + 左右黑边**（544×960 → 408×720 居中），帧率强转 30；画幅信息丢失且**不可逆**（后续超分/混音/交付都基于这份），而 `probe_final` 依旧判就绪 → 交付物登记。属「静默画幅变更」。
- **复现条件**：让任一段编码与其它段不同，或使 demuxer 返回非 0，观察 `_concat_reencode` 产物宽高为 1280×720。
- **修复方案**：归一化目标取「首个片段实测宽高/帧率」或 `style_kit.resolve(style, default_ratio=DEFAULT_RATIO)["size"]`：
  ```python
  first = probe_media(video_paths[0])
  tw, th = first.get("width") or 544, first.get("height") or 960
  tfps = _parse_fps(first.get("r_frame_rate")) or 24
  per_stream.append(
      f"[{i}:v:0]scale={tw}:{th}:force_original_aspect_ratio=decrease,"
      f"pad={tw}:{th}:(ow-iw)/2:(oh-ih)/2,fps={tfps},format=yuv420p,setsar=1[v{i}]")
  ```
- **验收标准**：竖屏项目被迫重编码后仍为 9:16（544×960 或整数倍）、无黑边、帧率与输入一致；横屏项目不受影响。

---

### 【P1-5】【视频·时间轴】字幕/混音/质检 `expected_duration` 用剧本 duration 累加，与成片实测时长拼接基准分裂

- **类型**：代码bug / 业务流程逻辑缺陷　|　**所属模块**：成片字幕 / 音画对齐 / 视频质检
- **文件路径:行号**：`app/pipeline.py:741-746`、`app/video_postprocess.py:550-560`、`app/dub_mix.py:135-138`、`app/app.py:3619-3620`
- **问题描述**：成片按磁盘分段**实测时长**顺序拼接，字幕却按剧本 `duration` 累加：
  ```python
  # pipeline.py:741
  for s in shots:
      dur = float(s.get("duration") or 5)
      text = dialogue_text(s.get("dialogue"))
      if text:
          subs.append({"start": cur, "end": cur + dur, "text": text})
      cur += dur
  ```
  同根因另两处：`video_postprocess.py:550-560`（手工成片路径同样累加剧本时长）；`dub_mix.py:135-138` 整集模式 `real = None`，逐镜时长全走剧本而实际混入的是实测时长整片。质检侧 `expected_duration = sum(float(s.get("duration") or 0.0) for s in shots)`（`app/app.py:3619-3620`）使时长校验基准同样失真。
- **风险影响**：H3 按帧对齐出片，实测时长几乎必然 ≠ 剧本时长；差量随镜序**线性累积** → 越靠后的字幕/台词越滞后，末段台词落到片外被 `-shortest` 静默截掉（项目自身记录过 ep04「21 句 17 句落片外」）。**`video_mode` 默认就是 `episode`**（`app/pipeline.py:97`），即默认生产模式全程踩这条。
- **复现条件**：生成一集后令 Σ剧本 duration 与 Σ实测时长相差 >0.5s，烧录字幕后拖到后半段比对。
- **修复方案**：时间轴单一事实源，统一走实测（复用已按 ffprobe 对位的 `dub_mix.shot_timeline()`）：
  ```python
  tl = dub_mix.shot_timeline([{"shot_id": s.get("shot_id"),
        "path": os.path.join(vd["dir"], f"shot_{A._shot_seq(s.get('shot_id')):02d}.mp4")} for s in shots])
  for i, s in enumerate(shots):
      text = dialogue_text(s.get("dialogue"))
      if text:
          subs.append({"start": tl[i]["start"], "end": tl[i]["start"] + tl[i]["duration"], "text": text})
  ```
  整集模式按「实测整片时长 / Σ剧本时长」尾端比例校正；`expected_duration` 改用 `probe_media(final)["duration"]`。
- **验收标准**：构造「剧本 12×5s=60s、实测合计 66s」，字幕最后一条 `end` ≤ 成片时长且与画面误差 ≤0.3s；混音报告不再出现「末句结束超出视频时长」告警；视频质检时长偏差不再因基准失真误报。

---

### 【P1-6】【视频·混音】混音取素材三处全不带集号 → 第 2 集起时间轴/成片源系统性错配

- **类型**：业务流程逻辑缺陷　|　**所属模块**：音画对齐混音（`app._mix_prepare` 素材解析）
- **文件路径:行号**：`app/app.py:8506-8518`、`:8487-8503`、`:8531-8538`
- **问题描述**：
  ```python
  # app.py:8506
  def _mix_segments_dir(project_name: str) -> str:
      best, best_key = "", (-1, 0)
      for d in project_store.project_dirs(VIDEOS_DIR, project_name):   # ← 只返回顶层目录，不解析 epNN/
          vids = [f for f in os.listdir(d) if f.lower().endswith(".mp4")]
          key = (len(vids), max(os.path.getmtime(os.path.join(d, f)) for f in vids))
          if key > best_key:              # ← 挑"mp4 最多 + mtime 最新"
              best, best_key = d, key
      return best
  ```
  `_mix_resolve_video` 未显式传 `video_path` 时取项目成片目录里 **mtime 最新**的 mp4；`_mix_manifest` 在指定集清单缺失时**回落最新清单**。而写入侧 `_ep_dir`（`app/app.py:324-332`）把第 2 集起写进 `<项目>/epNN/`。
- **风险影响**：一次混音可能「第 1 集时间轴 × 第 2 集配音 × 最新那支成片」→ 逐句落点系统性错位（长台词被 `-shortest` 截尾），成片源可能取到别集。且 `build_entries` 在 shot_id 未命中时**静默顺排**（`app/dub_mix.py:208-209` `start = slot["start"] if slot else cursor`），全程无告警 —— 用户与托管都发现不了。
- **复现条件**：第 1 集产物平铺在 `output/videos/<项目>/`，第 2 集在 `output/videos/<项目>/ep02/`；对第 2 集调 `/api/mix/generate`（不传 `video_path`）或等托管跑到第 2 集 mix 步。
- **修复方案**：三处显式接收 `episode_no`，改用既有读侧口径 `_ep_read_dir(base, project, ep)`（`app/app.py:335-341`）；成片按 `ep{ep:02d}_final.mp4` 精确取；指定集清单缺失**报错**而非回落；`build_entries` 未命中记 warning 并暴露到混音报告 `warnings`。
- **验收标准**：第 2 集混音报告 `timeline.segment_count` 来自 `ep02/`；`entries` 每条 `shot_id` 均能在 timeline 命中（`unmatched=0`）；`video_source` 为该集成片；构造「第 1 集 mtime 更新」结果不变。

---

### 【P1-7】【提示词/图片/视频】H3 参考图锚点与文本声明双重错配（锚点恒取第 1 角色 + 尾帧被声明为角色锚点）

- **类型**：代码bug（资产固定失效 + 参考图语义错配）　|　**所属模块**：H3 提示词构建 × 视频链路
- **文件路径:行号**：`app/app.py:3487-3488`、`:3539`、`:3530-3536`、`app/comfyui_client.py:2076-2087`
- **问题描述**
  ```python
  # app.py:3488  主角锚点 = 传入角色列表第 1 项（项目级全量角色表，与本镜出场角色无关）
  main_char_img = _collect_reference_images(character_refs[:1], [])
  # app.py:3539
  refs = [sb_local] + main_char_img
  ```
  ```python
  # comfyui_client.py:2078  提示词文本侧同源错
  main = (char_refs or [{}])[0]
  if main.get("name"):
      picture_defs.append(("<Picture 2>",
          f"{main.get('name')} 的外观参考，定义其五官、发型、服装与画风，必须与 <Picture 1> 保持同一人物"))
  for ref in (char_refs or [])[:1]:
      subjects.append({"name": ref.get("name", "主角"), "appearance": _appearance(ref)})
  ```
  分镜图链路已用 `_match_shot_chars`（`app/app.py:2757`）按 `characters_in_shot` 逐镜选锚点，**视频链路没有**。且 keyframe 模式 `refs = [start_p] + [end_p]`（真实注入首帧+尾帧），提示词仍把 `<Picture 2>` 声明为「角色外观锚点」、`<Picture 1>` 声明为「本镜分镜图」——链式模式下 `<Picture 1>` 实际是**上一镜尾帧**。
- **风险影响**：① 只出现配角/反派的镜头被要求「保持主角外观与参考图一致」，且 `<Subject 2>` 不存在 → 换脸、配角每镜漂移、主角串镜；H3 是片源，代价最高。② keyframe 模式 H3 完全不知道存在「动作终点帧」→ 首尾帧插值核心能力失效，链式构图基准与实际图不符 → 连贯性不可控。
- **复现条件**：① 任取主角未出场、仅有配角对话的镜头，检查其 `prompt_h3`：必含项目第一个角色的 `<Picture 2>`/`<Subject 1>` 且无 `<Subject 2>`；② `mode=keyframe` 任取一镜，对比 `seg.reference_images` 与 `prompt_h3` 的 `<Picture N>` 说明。
- **修复方案**：① 视频链路复用 `_match_shot_chars(shot, char_idx)` 得本镜出场角色，按它构造 `main_char_img` 与对应 `<Subject i>`；说话人 `(Si)` 与 `<Subject i>` 一一对应，第二位说话人至少给出 `<Subject 2>`。② `_h3_picture_defs` 增参 `ref_roles: list[str]`，keyframe 模式输出「`<Picture 1>` = 上一镜结尾画面（承接）」「`<Picture 2>` = 本镜尾帧（动作终点，画面须推进到该帧状态）」，与 `reference_images` 顺序严格同源。
- **验收标准**：① 配角独镜 `prompt_h3` 的 `<Subject 1>` = 该配角名，注入参考图 = 该配角资产；双人对话镜头出现两个 `<Subject>`；② keyframe 模式 `prompt_h3` 出现尾帧/终点语义，且 `<Picture N>` 最大 N == 实际注入参考图张数。
- **附：镜号归一化未收敛（原 O21，归属本条验收）**：仍为 **6 套实现、两种互斥语义** —— `app.py:927 _shot_num_key` 与 `:2896 _shot_seq` 取 `re.search(r"\d+")`；`keyframe.py:161 _seq` 与 `dub_mix.py:166 _shot_seq_num` 取「拼接全部数字」；`app.py:3372 _norm_shot_key` 只处理纯数字串。`"S01-C02"` → 前者 `1`、中者 `102`、后者原样。**落盘/读取已实测错位**：`keyframe.py:150-152` 用 `_seq` 写 `shot_102_end.png`，`app.py:3507` 用 `_shot_seq` 找 `shot_01_end.png` → keyframe 模式尾帧永远找不到、链式静默退化。当前 `shot_id` 由 `novel_to_script._norm_shots`(`:831`) 生成整数，故为**潜伏缺陷**；一旦来自前端传参 / 手工剧本 / `_load_legacy_flat_script` 即触发。修复：收敛为单一语义并全库替换。

---

### 【P1-8】【图片·幂等】分镜任务复用守卫不含 `episode_no` → 第 2 集请求被第 1 集运行中任务吞掉

- **类型**：业务流程逻辑缺陷　|　**所属模块**：分镜图生成（路由幂等守卫）
- **文件路径:行号**：`app/app.py:3275-3284`、`:3285-3295`
- **问题描述**：
  ```python
  with lock:
      _existing_sb = next((tid for tid, st in generation_state.items()
                           if st.get("status") == "running"
                           and st.get("project_name") == project_name
                           and st.get("step") == "storyboard"), None)   # ← 无 episode_no
      if _existing_sb:
          return jsonify({"task_id": _existing_sb, "status": "started", "reused": True, ...})
  ```
  且 `generation_state[task_id]` 里**根本不登记集号**（只有 status/progress/total/current/phase/results/qc/project_name/step/refs_available）。
- **风险影响**：ep01 分镜 worker 还在跑（一集几十分钟）时请求 ep02 → 接口返回 `{"status":"started","reused":true}` + ep01 的 task_id，**ep02 一镜都不生成**；前端进度条展示的还是 ep01 的结果。用户以为第 2 集在生成，直到成片才发现没图。
- **复现条件**：项目 P 的 ep01 分镜 running 时 POST `/api/storyboards/generate`，body 带 `episode_no=2`。
- **修复方案**：守卫键纳入集号并存进任务字典：
  ```python
  _existing_sb = next((tid for tid, st in generation_state.items()
                       if st.get("status") == "running"
                       and st.get("project_name") == project_name
                       and st.get("step") == "storyboard"
                       and int(st.get("episode_no") or 1) == int(ep_no or 1)), None)
  generation_state[task_id] = {..., "episode_no": int(ep_no or 1), "overwrite": bool(data.get("overwrite"))}
  ```
  视频路由同类守卫（`app/app.py:3408-3417`）一并处理。
- **验收标准**：ep01 running 时请求 ep02 → 返回**新** task_id，ep02 的 `shot_XX.png` 实际落盘。

---

### 【P1-9】【并发/契约】pipeline 内部任务写进公共任务表且 `init` 缺字段 → 双向故障（漏命中→双 worker；误命中→前端 404）

- **类型**：架构稳定性隐患　|　**所属模块**：pipeline ↔ app 任务表
- **文件路径:行号**：`app/pipeline.py:195-201`、`:208-210`、`:589`、`:673`；`app/app.py:3275-3280`、`:8394-8396`、`:8880-8882`
- **问题描述**：
  ```python
  # pipeline.py:195
  tid = f"{prefix}_{int(time.time() * 1000)}_{os.getpid()}"
  state = {"status": "running", "progress": 0, "phase": "准备中",
           "results": [], "pipeline": True, "created_at": _now()}
  ...
  registry.pop(tid, None)      # 内部任务不留在 UI 列表（:208-210）
  ```
  - **漏命中**：分镜/视频内部任务的 `init` 不含 `project_name`/`step`（`pipeline.py:589`、`:673`），而路由复用守卫要求同时匹配这两个字段 → 托管正在跑时用户再点一次会**另起 worker**，两线程写同一批 `shot_NN.png` 与同一份 manifest。
  - **误命中**：配音/混音守卫按 `project_name` / `video_path` 匹配，而内部任务恰好带这两个字段 → 手动请求**复用一个随后被 `registry.pop(tid)` 删除的内部 task_id** → 前端轮询 `/api/tts/status/pipe_dub_*` 永久 404；且忽略 episode/out_name，第 3 集的配音可能被第 2 集的内部任务吃掉。
- **风险影响**：重复 GPU 任务 + 同路径并发写 + 任务结果「人间蒸发」（用户看到 started 却查不到任务），排障成本极高。
- **复现条件**：托管跑第 2 集 tts/mix 时 POST `/api/tts/generate`（同项目）→ 响应 `reused:true` 且 `task_id` 以 `pipe_dub_` 开头 → GET `/api/tts/status/<该 id>` 返回 404。反之托管跑 storyboard 时点前端「生成分镜」→ 日志出现两个分镜 worker。
- **修复方案**：① 内部任务改用独立注册表（或把 `pipeline=True` 作为复用守卫的**排除条件**）；② 统一要求 `init` 带 `project_name`/`step`/`episode_no`；③ 守卫同时校验 `episode_no`（配音）与 `out_name`（混音）；④ 内部任务完成后保留终态 N 秒再 pop，避免前端拿到即 404。
- **验收标准**：并发下手动接口不复用 `pipe_*` 任务（或复用后仍可查到终态）；托管运行时手动重复触发分镜/视频被复用而非再起线程。

### 【P1-10】【数据流转】配音清单 manifest 非原子整文件覆盖 + `probe_tts` 解析失败静默 `done:False` → 一次崩溃重烧整集配音

- **类型**：代码bug / 架构稳定性隐患　|　**所属模块**：配音链路（`_dub_worker` ↔ `pipeline.probe_tts`）
- **文件路径:行号**：`app/app.py:8139-8141`、`app/pipeline.py:402-415`、`app/pipeline.py:782-785`
- **问题描述**：manifest 用普通 `open("w")` 整文件覆盖（无 tmp + 原子替换、无 fsync），而它同时是「配音是否完成」的唯一判据与混音/质检的唯一真相源：
  ```python
  # app.py:8139
  with open(os.path.join(out_dir, f"ep{int(episode):02d}_dub_manifest.json"), "w", encoding="utf-8") as f:
      json.dump(manifest, f, ensure_ascii=False, indent=2)
  # pipeline.py:406
  except Exception:  # noqa: BLE001
      return {"total": 0, "ready": 0, "done": False, "file": p}    # ← 静默当"没做"
  ```
- **风险影响**：一次崩溃/写半截（或断电）使已完成的数十句配音全部重烧（TTS 很慢且占 GPU），并可能连锁使 `step_mix` 报「未找到有效配音清单」→ 成片缺失配音；解析失败无任何告警。
- **复现条件**：在 `_dub_worker` 写 manifest 中途 kill 进程（或注入写入截断），再让托管续跑该集。
- **修复方案**：manifest 改原子写（复用 P1-1 的 `atomic_io.write_json_atomic`）；`probe_tts` 区分三态 `missing / corrupt / ok`（`corrupt` 打 error 日志并返回 `state:"corrupt"`）；`step_tts` 在 `corrupt` 时以 `only_missing=True` 只补缺失句（已存在且非空的 wav 不重合成）。
- **验收标准**：截断 manifest 后 probe 返回 `state="corrupt"` 且有 error 告警；续跑只补缺失句；并发/中断后 manifest 始终可 `json.load`。

---

### 【P1-11】【数据安全/长时无人值守】死信落盘失败被静默吞掉 + **计数器清零** → 同一集无限重跑，且「需人工介入」列表永远为空

- **类型**：架构稳定性隐患　|　**所属模块**：托管（autopilot）× 死信（pipeline）
- **文件路径:行号**：`app/autopilot.py:716-720`、`:761-766`、`:747-755`；`app/pipeline.py:1261`、`:1323`
- **问题描述**：
  ```python
  # autopilot.py:716
  tries = _ATTEMPTS.get(project, {}).get(no, 0)
  if tries >= int(plan.get("max_episode_attempts") or 2):
      _mark_dead(project, no, f"连续 {tries} 次生产失败，已挂起等人工处理")
      _ATTEMPTS.setdefault(project, {})[no] = 0      # ← 无论 _mark_dead 是否成功都清零
      continue
  ```
  `_mark_dead` 的 `except Exception` 只记 warning；底层 `pipeline.mark_dead_letter` 用固定 `p + ".tmp"` + `os.replace` 且**无 `PermissionError` 退避**（对比 `project_store._atomic_replace` 的 8 次重试），Windows 上被并发读者占用即失败。
- **风险影响**：「失败达上限 → 标记死信失败 → 计数清零 → 再跑」无限循环，24/7 下持续烧 GPU；用户端「需人工介入」列表**永远为空** → 无补救入口，只有日志一行 warning。
- **复现条件**：让 `output/autopilot/<project>/dead_letter.json` 所在目录不可写，或让该文件被长时打开（Windows 共享冲突），再让某集连续失败到 `max_episode_attempts`。
- **修复方案**：`_mark_dead` 改为返回 bool，**仅在成功落盘后**清零计数；失败则保留计数 + 指数退避 + 写内存兜底死信表使 `/api/autopilot/exceptions` 可见；死信/交付物落盘一并接入 P1-1 的公共原子写（带退避）。
- **验收标准**：注入死信写盘失败后 `_ATTEMPTS` 不再被清零，异常接口仍能列出该集，不再同一集无限重跑。

---

### 【P1-12】【并发/资源】单 GPU 无任何并发闸门：`TaskQueue` 从未接线、线程无上限、`/free` 无条件卸载他人模型

- **类型**：架构稳定性隐患　|　**所属模块**：任务编排（task_store）× 全部 GPU worker
- **文件路径:行号**：`app/app.py:175-176`、`:853`、`:876`、`:3429-3439`；`app/config.py:114`；`app/task_store.py:358-366`；`app/upscale_client.py:601-608`；`app/tts_client.py:605-622`
- **问题描述**（实测 grep）：
  ```
  task_queue 仅出现在 app.py:176（创建）、:853/:876（读 .status()）
  TASK_QUEUE_CONCURRENCY 仅被 import（app.py:31）与定义（config.py:114），零使用
  TaskQueue.start() / .submit() 全库零调用点
  ```
  所有重任务都是裸 `threading.Thread(..., daemon=True).start()`，**没有并发上限**、不 join、无背压；`/api/tasks` 的 queue 字段永远 `running:false, queued:0`（失真的可观测性）。超分/TTS 还会无条件请求卸载模型：
  ```python
  requests.post(f"{self.base_url}/free", json={"unload_models": True, "free_memory": True}, timeout=30)
  ```
- **风险影响**：手动接口 + 托管守护线程 + 总控 AI 三方并发时线程数无界增长，同时打 ComfyUI/GPU → OOM、任务随机失败或耗时暴涨；`/free` 卸掉另一个任务的已加载模型触发反复换入换出；`pipeline` 只在 autopilot 内部对 `GPU_STEPS` 互斥，**手动链路不受任何保护**。
- **复现条件**：对两个项目同时 POST `/api/videos/generate`，其中一路再并发 POST `/api/upscale/video`；或连续 POST 各接口后查看线程数线性增长、`/api/tasks` 的 `queue.queued` 恒为 0。
- **修复方案**：① 二选一 —— 真正接线（重任务统一 `task_queue.submit(...)`，`concurrency=TASK_QUEUE_CONCURRENCY`，启动时 `start()`），或删除该模块与 `/api/tasks` 的 queue 字段避免契约失真；② 引入进程级 `threading.Semaphore(TASK_QUEUE_CONCURRENCY)` 覆盖手动链路；③ `/free` 仅在本进程无其它 running GPU 任务时发送；④ 排队/运行计数暴露到状态接口。
- **验收标准**：并发提交 N 个重任务时任一时刻实际执行的 GPU 任务数 ≤ 配置上限；`/api/tasks` queue 字段与实际排队数一致；不再 `/free` 打断他人任务。

---

### 【P1-13】【图片/视频·ComfyUI 调用层】三连缺陷：轮询结局不可区分 / `interrupt()` 误伤他人任务 / 标量超时无 connect 超时

- **类型**：代码bug + 架构稳定性隐患（同文件，合并一单）　|　**所属模块**：`app/comfyui_client.py`
- **文件路径:行号**：`app/comfyui_client.py:730-759`、`:699-707`、`:316-324`、`:669`
- **问题描述**
  1. **轮询结局不可区分**：`wait_for_completion` 三种结局只有两种返回形态 —— 成功返回 entry、`status_str == "error"` 也**返回 entry**（`:745-748`）、超时返回 `{}`（`:759`）；轮询异常一律 `logger.debug` 吞掉（`:751-752`）；从不查 `/queue` 判断「排队慢」还是「服务离线」。出错任务若已产出部分文件，`get_output_files` 仍取得到 → 上层当成功。
  2. **`interrupt()` 语义错**：
     ```python
     if prompt_id is None:
         self._post("/interrupt")
     else:
         self._post("/interrupt")      # ComfyUI 的 /interrupt 打断"当前正在执行的"，与 id 无关
         self.delete_queued(prompt_id)
     ```
     多 worker/项目共享同一台 ComfyUI 时，A 的「超时清理」会打断 B 正在执行的 GPU 任务。
  3. **标量超时**：提交/上传/查询都用标量 `timeout`（同时作 connect 与 read 超时），而同文件 `get_status` 已给出正确写法 `timeout=(min(2, timeout), timeout)`（`:330-335`）。主机不可达（防火墙丢包）时单次 `queue_prompt` 可挂 300s、`upload_image` 挂 120s。
- **风险影响**：① 离线/网络异常与排队慢不可区分，只能干等 900s/1800s，且超时被上层标成 `qc_blocked=True`（`app/app.py:3183`）→ 基础设施故障混进质检阻断统计并触发无意义重试，运维无法定位；② 24h 托管下互相打断会随机化失败分布；③ 83 镜离线批次几乎全天挂起。
- **复现条件**：① 生成中途 kill ComfyUI（或让节点报错、同时 SaveImage 已执行）；② A 任务超时瞬间 B 正在执行；③ `COMFYUI_URL` 指向丢包/不存在的内网 IP。
- **修复方案**：① 返回体统一带 `status/status_str/timed_out/last_error`，调用方按四类结局分流；超时**不计入 `qc_blocked`**，单列 `infra_failed`；连续 N 次连接失败即整批快速失败。② 清理前 `GET /queue` 判断 running/pending —— pending 只 `delete_queued`，running 才 `/interrupt`（且仅用于用户主动取消，不用于超时清理）。③ 全部 HTTP 调用改 `(connect, read)` 元组（如 `(5, 300)`），批量提交前 `get_status()` 可达性预检。
- **验收标准**：停机跑一镜 → 结果可明确读到「不可达/超时/出错」，且该镜不进入 `qc_blocked`，出错任务残留产物不被判成功；A 超时清理期间 B 产物正常产出；断开 ComfyUI 后秒级抛连接错误。

---

### 【P1-14】【图片·跨项目污染】多视角基础图上传到 ComfyUI output 根目录时**不含项目名** → 跨项目同名资产互相覆盖

- **类型**：代码bug　|　**所属模块**：多视角生成 `comfyui_client.generate_multiview`
- **文件路径:行号**：`app/comfyui_client.py:1147-1156`（配 `:664-668` 的 `upload_image` 固定 `overwrite=true`）
- **问题描述**：
  ```python
  base_name = f"comic_drama_{asset_type}_{asset_name}_base.png"     # ← 无项目名
  output_name = self.upload_image(base_image_path, base_name, image_type="output")
  ```
  不同项目的同名资产（「主角」「村民」「森林」「街道」等高频名）并发生成时互相覆盖同一个 output 文件；紧随其后的多视角工作流以 `comic_drama_character_主角_base.png [output]` 读取它。
- **风险影响**：**A 项目的多视角图按 B 项目的角色形象生成** → 角色一致性静默错乱，质检只看单图往往判不出来；污染写进正式 `output/assets/.../<名>/*.png`，后续分镜/H3 拿错锚点。
- **复现条件**：两个项目各有一个叫「主角」的角色，同时（或交错）执行资产多视角生成。
- **修复方案**：上传名纳入项目键（`upload_image` 已支持 `subfolder=`，一并利用）：
  ```python
  base_name = f"comic_drama_{_safe_project(project_name)}_{asset_type}_{asset_name}_base.png"
  output_name = self.upload_image(base_image_path, base_name, image_type="output",
                                  subfolder=f"mjscxt/{_safe_project(project_name)}")
  ```
  `generate_multiview` 需把 `project_name` 透传进来（worker 已持有）。
- **验收标准**：两项目同名资产并发生成 → 各自多视角图与各自 base 一致；ComfyUI output 下无跨项目共用文件名。

---

### 【P1-15】【图片·幂等】资产任务 ID 仍用秒级时间戳（G5 只改了分镜/视频/配音/混音，资产漏改）

- **类型**：代码bug　|　**所属模块**：资产批量生成（路由）
- **文件路径:行号**：`app/app.py:2665-2671`
- **问题描述**：
  ```python
  task_id = f"{asset_type}_{project_name}_{int(time.time())}"
  with lock:
      generation_state[task_id] = {"status": "running", "asset_type": asset_type, ...}
  ```
  同文件 `app.py:3272` 注释已明确记录该坑：「秒级时间戳同秒双 POST 会覆盖 `generation_state` 且双线程并发抢同一目标路径」—— 资产路由漏改。
- **风险影响**：同秒两次「生成资产」（双击按钮 / 前端重试）→ **同一 `task_id`**：`generation_state[task_id]` 被覆盖并另起线程，两线程共享进度字典且写同一 `asset_dir`、同一 `assets_scratch`、同一 output 前缀 → 进度/结果串台，UI 的 `success_count` 与磁盘不一致，质检阻断统计被覆盖。
- **复现条件**：同一秒内对同一 `asset_type` + 项目连续两次 POST `/api/assets/generate`。
- **修复方案**：`task_id = f"{asset_type}_{_safe_project(project_name)}_{uuid.uuid4().hex[:12]}"`，并补「同项目同类资产运行中即复用/拒绝」守卫（参照 `app.py:3275-3284`）。
- **验收标准**：同秒两次 POST → 两个独立 task_id、可独立查询进度；`asset_dir` 只被一个 worker 写。

---

### 【P1-16】【提示词·资产固定】`characters_in_shot` / `location` 匹配失败时静默兜底取错对象（并架空下游 S6 保护）

- **类型**：代码bug　|　**所属模块**：剧本归一化 `novel_to_script._norm_shots`
- **文件路径:行号**：`app/novel_to_script.py:899`、`:858-862`
- **问题描述**：
  ```python
  # :899  仅精确同名才保留；不匹配 → 整个列表被清空并填入 chars[:1]（通常是主角）
  "characters_in_shot": [c for c in (s.get("characters_in_shot") or []) if c in chars] or chars[:1],
  "items_in_shot": [i for i in (s.get("items_in_shot") or []) if i in items],   # ← 无兜底，两侧不一致
  # :858  场景同理静默取 scenes[0]
  if scenes and loc and loc not in scenes:
      loc = next((n for n in scenes if n and n in loc), scenes[0])
  ```
  角色匹配用严格 `in chars`，比下游 `_match_shot_chars` 的别名归一化**更严** → 本可匹配的名字也被换成主角。
- **风险影响**：分镜图参考图槽位、尾帧提示词的「出场人物」、配音音色兜底（`tts_client.py:482`）全部拿到错误角色 → 换脸/错音色；同时 `app.py:2798-2808` 的 S6 `_no_reference` 分支在 chapter 链路**永不触发**（列表恒非空）→ S6「禁止静默 take-first」被上游架空。
- **复现条件**：剧本 bible 未收录的配角名出现在 `characters_in_shot`；或 `location` 写「破败的大殿」而 scenes 为 `["后山","大殿"]`（子串不中 → 取首个「后山」）。
- **修复方案**：角色侧去掉 `or chars[:1]`（保留空列表，交给 S6 `no_reference` 分支显式告警/跳过），或先做别名归一化再匹配；场景侧匹配不到时**保留原 loc** 并记 warning，不回落 `scenes[0]`。
- **验收标准**：构造未登记角色名 → 分镜图链路给出明确提示而非拿到主角参考图；场景不匹配时提示词里的 location 仍是原文。

---

### 【P1-17】【AI质检】`parse_verdict` 把 `score is None` 当作满足分数条件 → 模型漏回分数即放行

- **类型**：代码bug　|　**所属模块**：AI 质检（结论解析）
- **文件路径:行号**：`app/qc_client.py:1433-1435`、`:1453-1454`
- **问题描述**：
  ```python
  # :1453
  return _finalize_verdict({
      "passed": bool(passed) and (score is None or score >= pass_score),   # ← 缺分数 = 不参与分数判定
      "score": score, ...
  ```
  与同函数 `:1433-1435`「`passed` 缺失时用分数兜底」形成**相反的缺省方向**。
- **风险影响**：截断/偷懒的模型输出（`{"pass": true}` 或 `"score": null`）可让整条「分数合格线 + 客观层兜底」失效，无分数产物直接入库；记录里留下 `score=None / passed=True`，事后无法回溯质量。
- **复现条件（实测，已执行）**：`parse_verdict('{"score": null, "pass": true, "reason": "ok", "issues": []}', 70)` → `{'passed': True, 'accepted': True, 'blocked': False, 'score': None}`；补 `ok=True` 后 `_qc_gate` 返回 `accept=True / label='质检达标'`。
- **修复方案**：`score is None` 时 fail-closed：
  ```python
  _scored = score is not None and score >= pass_score
  "passed": bool(passed) and _scored,
  "reason": (str(obj.get("reason") or "") if score is not None
             else "模型未返回分数，按不通过处理")[:500],
  ```
  或要求补一次「只回分数」的重试。
- **验收标准**：`{"pass": true}`（缺 score）不再放行 → `blocked=True` 且 reason 指明缺分数；正常带分数结论行为不变。

---

### 【P1-18】【AI质检】整集/尾帧把「不可判定」当作「不达标」→ 换种子重跑整集（ffmpeg 缺失或接口 5xx 时白烧 N 遍）

- **类型**：架构稳定性隐患（重画口径不一致）　|　**所属模块**：整集视频质检 × 尾帧质检（对照分镜/资产/逐镜视频）
- **文件路径:行号**：`app/app.py:3603-3645`、`app/comfyui_client.py:1852-1883`、`app/keyframe.py:472-528`；对照 `app/app.py:3893-3899`、`:3148-3150`、`:2443-2444`
- **问题描述**：`_seg_qc_fn` 把 `gate.accept`（`ok=False` 时必为 False）当作 `passed` 返回，comfyui 据此**换种子重生成整集**；`qc_fn` 抛异常也 `continue` 重生成。尾帧同理：`_verify` 返回 `(False, reason)`，keyframe 无法区分「不达标」与「不可判定」→ 换 seed 重画。而分镜/资产/逐镜视频在 `not verdict.get("ok")` 时一律 `break`（保留暂存、阻断、**不重画**）。
- **风险影响**：最常见触发是 **ffmpeg 未安装 / 质检接口 5xx / 抽帧失败**（与内容无关）。整集模式一次白烧 20~44 段 H3 × `(max_retries+1)` 次（默认 3 次，配置上限 10 次）；尾帧每镜多画 2 次。四路口径不一致，同一故障在不同链路代价差两个数量级。
- **复现条件**：QC 接口就绪（`qc_on=True`）但 PATH 无 ffmpeg，或接口返回 500 → 整集路径 `check_video` 返回 `ok=False` → 整集生成 3 遍（三次 `segments[*].prompt` 完全一致，仅 seed 不同）。
- **修复方案**：`_seg_qc_fn` 增加 `"unverifiable": not verdict.get("ok")`，comfyui 在不可判定时 `break` 并保留成片、标 `qc_unavailable`；尾帧 `verify_cb` 返回三元组或写 `r["qc_unavailable"]`，与图片/视频统一为「不可判定 → 不重试」。
- **验收标准**：拔掉 ffmpeg 后跑整集/尾帧，产物只生成 **1 次**且状态标 `qc_unavailable`，不再整集重跑；`max_retries` 仅作用于「内容不达标」。

---

### 【P1-19】【全局】镜号归一化仍有 6 套实现、两种互斥语义 → 落盘名与读取名对不上（keyframe 模式尾帧永久失效）

- **类型**：架构稳定性隐患（同一概念多份实现）＋ 业务流程逻辑缺陷（潜伏触发）　|　**所属模块**：全局（分镜/尾帧/配音/视频）
- **文件路径:行号**：`app/app.py:927`（`_shot_num_key`）、`app/app.py:2896`（`_shot_seq`）、`app/app.py:3372`（`_norm_shot_key`）、`app/keyframe.py:161`（`_seq`）、`app/dub_mix.py:166`（`_shot_seq_num`）、`app/dub_mix.py:174`（`_shot_seq_num_from_filename`）
- **问题描述**：**6 套实现、两种互斥语义**：

  | 实现 | 取号方式 | `"S01-C02"` 结果 |
  |---|---|---|
  | `app.py:927 _shot_num_key` | `re.search(r"\d+")` | `"1"` |
  | `app.py:2896 _shot_seq` | `re.search(r"\d+")` | `1` |
  | `app.py:3372 _norm_shot_key` | 仅纯数字串才归一 | `"S01-C02"`（原样） |
  | `keyframe.py:161 _seq` | 拼接**全部**数字 | `102` |
  | `dub_mix.py:166 _shot_seq_num` | 去非数字 | `102` |
  | `dub_mix.py:174 _shot_seq_num_from_filename` | `shot[_\s]?(\d+)` | `1` |

  **写侧/读侧已实测错位**：`keyframe.py:150-152` 用 `_seq` 写出 `shot_102_end.png`，而 `app.py:3507` 用 `_shot_seq` 去找 `shot_01_end.png` → **keyframe 模式下尾帧永远找不到**，`kf_end_map` 为空 → 链式「上镜尾帧 = 下镜首帧」静默退化为无尾帧。
- **风险影响**：当前 `shot_id` 由 `novel_to_script._norm_shots`（`:831 sid=start_id`）生成为整数，故为**潜伏缺陷**；一旦 shot_id 来自前端手工剧本 / legacy 导入（`_load_legacy_flat_script`）/ 外部工具，立刻表现为：尾帧链断、配音分段与视频片段对错镜、`kf_end_map` 空导致 H3 参考图缺失。且因**不报错**，排障成本极高。
- **复现条件**：构造 `shots = [{"shot_id": "S01-C02"}]` 调用 `keyframe.end_frame_path(dir, "S01-C02")` → 得 `shot_102_end.png`（`keyframe.py:161`）；而 `_shot_seq("S01-C02", 1)` → `1`（`app.py:2896`）→ 读不到。
- **修复方案**：**收敛为唯一实现**，放在 `comfyui_client.py` 或新建 `shot_key.py`，全项目 `from shot_key import shot_seq`；语义定为「取**第一段**数字」（与落盘命名 `shot_%02d` 对齐）。`_norm_shot_key` 扩展为「非纯数字串亦按同一规则归一」。保留旧函数名为一行代理以兼容外部引用。
- **参考代码片段**：
  ```python
  # app/shot_key.py —— 全项目唯一的镜号归一化实现
  import re
  from typing import Optional

  def shot_seq(shot_id, fallback: Optional[int] = None) -> Optional[int]:
      """镜号归一化：取**第一段**数字（'S01-C02'→1，'shot_03'→3，'03'→3）。

      全项目唯一实现：落盘命名与读取匹配必须共用本函数，
      否则会出现「写 shot_102_end.png、读 shot_01_end.png」的静默错位。
      """
      if isinstance(shot_id, int):
          return shot_id
      m = re.search(r"\d+", str(shot_id or ""))
      if not m:
          return fallback
      return int(m.group(0))

  def norm_shot_key(key) -> str:
      """键归一化：纯数字去前导零；含字母串按 shot_seq 归一为字符串。"""
      s = str(key).strip()
      if s.isdigit():
          return s.lstrip("0") or "0"
      n = shot_seq(s)
      return str(n) if n is not None else s
  ```

  ```python
  # app/keyframe.py —— 替换本地 _seq
  from shot_key import shot_seq as _seq      # 原 _seq 函数体删除

  # app/dub_mix.py —— 替换本地 _shot_seq_num
  from shot_key import shot_seq as _shot_seq_num

  # app/app.py:2896 —— _shot_seq 改为代理
  def _shot_seq(shot_id, fallback: int) -> int:
      return shot_seq(shot_id, fallback) or fallback
  ```
- **验收标准**：
  1. `grep -rn "def _seq\|def _shot_seq\|def _shot_num_key\|def _norm_shot_key\|def _shot_seq_num" app/*.py` 只剩 `app/shot_key.py` 一处定义；
  2. 新增离线用例：对 `1 / "1" / "01" / "shot_03" / "shot_1" / "S01-C02"` 六种输入，`shot_seq` 结果与 `keyframe.end_frame_path` 落盘序号**完全一致**；
  3. 用 `shot_id="S01-C02"` 跑一遍 keyframe 模式，尾帧文件被正确写入并被读侧命中（`kf_end_map` 非空）；
  4. 跑 `verify_camera_framing.py` / `verify_h3_sequential.py` 保持全绿。

---

## 四、缺陷条目 · P2 优化级

> P2 不阻断流水线，但会**放大 API 消耗 / 造成磁盘与状态腐化 / 掩盖真实故障**。建议批次 2 统一清理，**优先级从高到低排列**。

---

### 【P2-1】【配置】`save_config` 存在永不可达的重复分支（`api_backoff` 写两次）

- **类型**：代码 bug（死分支 / 复制粘贴残留）　|　**所属模块**：AI 质检配置
- **文件路径:行号**：`app/qc_client.py:667-680`
- **问题描述**：`elif k in ("image_pixel_std_min", "video_max_drift", "api_backoff")` 已把 `api_backoff` 归入 float 组，紧接着又有一条 `elif k == "api_backoff":` → **第二条永远不可达**。同一函数里 `_as_bool` 的归一化注释（`:655-665`）恰好说明这里历史上发生过「改一处漏一处」，本条是该模式的又一残留。
- **风险影响**：无功能故障，但后续若把 `api_backoff` 从 float 组移走只改上面不改下面，会**静默改变配置语义**；阅读者会误判该键的处理路径。
- **复现条件**：静态阅读即得；`python -m pyflakes app/qc_client.py` 不报（语法合法）。
- **修复方案**：删除 `elif k == "api_backoff":` 整段（4 行）。
- **参考代码片段**：
  ```python
  # 删除以下整段
  elif k == "api_backoff":
      try:
          cfg[k] = float(v)
      except Exception:  # noqa: BLE001
          continue
  ```
- **验收标准**：`grep -c 'k == "api_backoff"' app/qc_client.py` 返回 `0`；保存配置后 `api_backoff` 仍能正常写入（回归手工验证一次）。

---

### 【P2-2】【质检】`_qc_prune_attempts` 用未排序的 `os.listdir` → 保留的不是「最后 N 次」而是「名字最小的 N 个」（**已实测**）

- **类型**：代码 bug（排序假设错误）　|　**所属模块**：AI 质检（暂存区清理）
- **文件路径:行号**：`app/app.py:5320-5331`
- **问题描述**：`nums = []` 来自 `for fn in os.listdir(scratch_dir)` 的**目录顺序**（NTFS 上近似按名排序，但无契约保证），随后 `keep_set = set(nums[-keep:]) if len(nums) > keep else set(nums)` —— 直接对**未排序**的 list 取尾切片。实测构造 `1..10` 共 10 个 `_try` 文件、`keep=3`，删除后保留的是 `[1, 2, 3, 10]`（而非期望的 `[8, 9, 10]`）。
- **风险影响**：暂存区应保留「最近 3 次尝试」用于排障，实际保留的可能是**最老的尝试**，最新的失败证据反被删掉 → 质检问题无法回溯；量大时（44 镜 × 10 次）磁盘清理效果也从「保最新」退化为「保最旧」。
- **复现条件**：`_qc_prune_attempts(dir, keep=3)` 前在 dir 里造 `a_try1.png … a_try10.png` → 剩余 `try1/try2/try3/try10`。
- **修复方案**：取尾切片前显式排序。
- **参考代码片段**：
  ```python
  groups.setdefault((prefix, ext), []).append(num)
  info[(prefix, ext, num)] = os.path.join(scratch_dir, fn)
  ...
  for (prefix, ext), nums in groups.items():
      nums = sorted(nums)          # ← 补这一行
      keep_set = set(nums[-keep:]) if len(nums) > keep else set(nums)
  ```
- **验收标准**：新增/扩充离线用例——造 `_try1.._try10`、`keep=3`，断言剩余集合为 `{8,9,10}`。

---

### 【P2-3】【分镜】先置 `success=True` 再落盘，落盘异常时 `success` 不回滚 → 「成功且失败」的矛盾条目

- **类型**：异常捕获缺陷　|　**所属模块**：图片生成（分镜）
- **文件路径:行号**：`app/app.py:3114-3123`、`:3193-3198`
- **问题描述**：`shutil.move(...)` 之后立刻 `item.update({"success": True, "file": dst, "url": ...})`，然后才 `shutil.copy2(scratch_png, dst)` 写入正式目录。若 `copy2` 因权限/磁盘满抛异常，走最外层 `except Exception as shot_err: item["error"] = str(shot_err)` —— **只写 `error`，不撤销 `success=True` 与 `file/url`**。前端与下游见到 `success:True` 即认为该镜已有正式图。
- **风险影响**：真正未落盘的镜被当作已完成 → 后续成片环节读到**不存在的路径**（或读到上一轮遗留的旧图），产出的集出现单镜缺失/串镜；`manifest` 与实际磁盘状态不一致，断点续跑判断失真。
- **复现条件**：对 `output/storyboards/<项目>/` 置只读（或把磁盘写满），触发一次分镜生成 → `results[i]` 同时含 `success: true` 与 `error`。
- **修复方案**：把 `success/file/url` 的赋值**移到 `copy2` 成功之后**；或在 `except` 分支显式回滚。
- **参考代码片段**：
  ```python
  except Exception as shot_err:
      app.logger.error(f"镜头 {shot_id} 分镜图生成失败: {shot_err}")
      item["success"] = False          # ← 回滚
      item.pop("file", None)           # ← 回滚
      item.pop("url", None)            # ← 回滚
      item["error"] = str(shot_err)
  ```
- **验收标准**：制造落盘失败后，`/api/generation/status/<id>` 中该镜为 `success:false` 且无 `file/url` 字段；正常路径行为不变。

---

### 【P2-4】【图片】「资产就绪」判据与「取参考图」判据口径不一致 → 判就绪却取不到图

- **类型**：业务流程逻辑缺陷　|　**所属模块**：图片生成（资产 → 分镜参考图）
- **文件路径:行号**：`app/app.py:1294-1301`（就绪判据）、`:2699-2703` 与 `:2730-2735`（取图判据）
- **问题描述**：判定某角色/道具「资产是否就绪」时用的文件清单，与真正去收集参考图时的清单**不是同一套**（一边认 `base.png` 即可，另一边要求 `front.png` 优先、`base.png` 兜底）。结果：就绪判定通过 → 进入分镜生成 → 参考图收集为空 → 该镜**退化为无参考图生成**（角色一致性直接失守）。
- **风险影响**：画风/角色漂移的隐性来源——**不报错、不阻断**，只是质量静默下滑；批量跑时表现为「部分镜次人物对不上」，极难定位。
- **复现条件**：对某角色只存在 `base.png`（无 `front.png`）时触发分镜生成，观察该镜 `ref_count == 0` 而资产状态显示 ready。
- **修复方案**：抽出**唯一**的参考图收集函数（含优先级与兜底），就绪判据与取图判据都调用它；就绪 = 收集结果非空。
- **参考代码片段**：
  ```python
  def _collect_ref_images(asset_dir: str) -> list[str]:
      """唯一的参考图口径：front.png 优先，base.png 兜底；空表示未就绪"""
      out = []
      for name in ("front.png", "base.png"):
          p = os.path.join(asset_dir, name)
          if os.path.isfile(p) and os.path.getsize(p) > 0:
              out.append(p)
      return out
  ```
- **验收标准**：仅有 `base.png` 的角色，就绪判定与分镜取图结果一致（要么都 ready 并取到 `base.png`，要么都 not-ready）；新增离线用例覆盖「仅 base / 仅 front / 都无」三态。

---

### 【P2-5】【元数据】尾帧产物不写 `.meta.json`，与分镜/资产/逐镜视频的元数据链断裂

- **类型**：架构一致性缺陷　|　**所属模块**：图片生成（尾帧）× 元数据
- **文件路径:行号**：`app/app.py:3142-3146`（分镜写 meta 的范式）vs `app/keyframe.py:550-554`、`:498-505`（尾帧落盘处无 meta）
- **问题描述**：分镜图落盘后会调用 `_write_artifact_meta(...)` 写入 `seed / prompt / workflow_key / qc` 旁路元数据；尾帧（`shot_NN_end.png`）落盘路径**完全没有这一步**。于是尾帧一旦出问题，无法回答「当时用的哪个 seed、哪版提示词、哪版工作流、质检结论如何」。
- **风险影响**：h3 `keyframe` 模式下尾帧是**下一镜首帧的输入**（链式），是整条链最关键的中间产物，却恰恰唯一没有溯源信息；复现一次坏链几乎要靠猜。
- **复现条件**：跑任一 `keyframe` 模式任务 → `output/keyframes/<项目>/` 下只有 `*.png`，无 `*.meta.json`。
- **修复方案**：在尾帧落盘（`shutil.move` 进正式目录）成功后，比照分镜调用 `_write_artifact_meta(dst, kind="keyframe", ...)`。
- **参考代码片段**：
  ```python
  shutil.move(scratch_end, dst)
  _write_artifact_meta(
      dst, kind="keyframe", project_name=project_name,
      seed=seed, prompt=prompt_used, workflow_key="keyframe_gen",
      qc=r.get("qc"), shot_id=shot_id)
  ```
- **验收标准**：`keyframe` 模式跑完后 `output/keyframes/<项目>/` 每个 `*_end.png` 旁均有同名 `.meta.json`，且含 `seed` 与 `qc` 字段。

---

### 【P2-6】【质检】尾帧质检结论只在「失败」时落盘 → 达标记录不留痕

- **类型**：业务流程逻辑缺陷　|　**所属模块**：AI 质检（尾帧）
- **文件路径:行号**：`app/app.py:5747-5757`、`:6351-6356`
- **问题描述**：尾帧质检的记录写入逻辑只在「不通过」路径执行（为了给止损/排障留证），达标路径直接 `break` 走人，**不产生任何质检历史**。同一事实在分镜/资产链上已由 `_qc_record_verdict(...)` 无条件调用修复过。
- **风险影响**：`output/qc/<项目>/` 里尾帧目录长期为空或只有失败样本 → 质检总览面板失真；「这集尾帧到底查没查」无从确认；若质检接口实质未生效（比如 endpoint 静默降级），**现场没有任何痕迹可反证**——这正是 P0-1 类 fail-open 最难被发现的原因。
- **复现条件**：跑一次全部尾帧达标的任务 → `output/qc/<项目>/keyframe*/` 目录为空。
- **修复方案**：把记录调用提到 gate 判定**之前**，无论成败都写 `_qc_record_verdict(project_name, "keyframe", shot_id, ...)`。
- **参考代码片段**：
  ```python
  v_ok, v_reason = verify(...)
  try:
      _qc_record_verdict(project_name, "keyframe", shot_id, "尾帧质检",
                         attempt + 1, seed, scratch_end, {"ok": v_ok, "reason": v_reason})
  except Exception:  # noqa: BLE001
      pass    # 记证失败绝不影响主流程
  ```
- **验收标准**：达标与不达标两种情况下，`output/qc/<项目>/keyframe*/` 均出现对应记录文件。

---

### 【P2-7】【质检】尾帧「止损（连续同因失败则停）」是空壳 —— `critical_issues` 永远取不到（**已实测**）

- **类型**：代码 bug（数据结构不匹配 / 功能未生效）　|　**所属模块**：AI 质检 × 尾帧重试
- **文件路径:行号**：`app/keyframe.py:491-498`（写入端）、止损判读端同文件
- **问题描述**：止损逻辑读的是 `r.get("qc", {}).get("critical_issues")` 与 `issues`，但上面刚赋值的 `r["qc"] = {"ok": ..., "reason": ..., "attempt": ...}` 里**根本没有这两个键** → 每次取到的都是 `[]`。实测 `_qc_retry_hopeless({...})` 恒返回 `(False, '')`，即**永不判定为无望**。
- **风险影响**：与 P1-18 叠加时最贵——尾帧每次失败都换 seed 重画，最坏 `max_retries` 次全跑完（配置上限 10），而本应「同一崩坏原因连出 2 次即放弃」的止损从不触发 → 单个坏镜白烧 10 次 GPU 渲染。
- **复现条件**：构造连续两次相同 `reason` 的尾帧失败，观察止损不生效、重试次数跑满。
- **修复方案**：`r["qc"]` 里补齐止损所需字段（或改为直接读 `verdict` 原对象）。
- **参考代码片段**：
  ```python
  r["qc"] = {"ok": bool(v_ok), "reason": v_reason or "",
             "attempt": attempt + 1,
             # ← 补齐止损判读所需字段
             "critical_issues": (verdict or {}).get("critical_issues") or [],
             "issues": (verdict or {}).get("issues") or []}
  ```
- **验收标准**：构造两次同因失败后，第三次不再发起渲染，`results[i]` 标记 `stopped_early / hopeless`；离线用例断言 `_qc_retry_hopeless` 对同因输入返回 `(True, 原因)`。

---

### 【P2-8】【质检】视频客观层的「致命等级」靠中文子串推断 → 改一句提示文案就会静默降级

- **类型**：架构稳定性隐患（隐式耦合到文案）　|　**所属模块**：AI 质检（视频客观层）
- **文件路径:行号**：`app/qc_client.py:2042-2046`
- **问题描述**：`obj_fatal = [s for s in obj_issues if ("无音轨" in s or "时长" in s or "帧率" in s or "未通过校验" in s)]` —— 靠**中文字符串包含**判定「是否致命」。上游 `_video_objective_issues` 若把文案改成「音轨缺失」「总时长超差」「FPS 异常」，致命集合立刻变空。
- **风险影响**：`blocked` 不再置位 → 客观层闸门失效（又一条 fail-open 的潜在入口）。这类改动看起来是纯文案优化，**不会有人意识到它同时改了安全语义**。
- **复现条件**：把 `"无音轨"` 改成 `"音轨缺失"` 后跑无音轨视频 → `objective_fatal == []`、`blocked=False`。
- **修复方案**：`_video_objective_issues` 返回结构化项（`{"code": "no_audio"|"duration_drift"|"fps_abnormal"|"frames_unverified", "fatal": True, "msg": "..."}`），致命判定读 `code`/`fatal` 字段；为兼容保留旧字符串入口。
- **参考代码片段**：
  ```python
  obj_issues = _video_objective_issues(video_path, expected_duration, cfg, fr)
  obj_fatal = [it for it in obj_issues
               if isinstance(it, dict) and it.get("fatal")]
  ```
- **验收标准**：任意修改客观层**提示文案**后，`objective_fatal` 集合内容不变；离线用例覆盖 4 类致命码各一条。

---

### 【P2-9】【质检】一致性降级（pHash）阈值低于随机基线 → 噪声图被判「一致」（**已实测**）

- **类型**：代码 bug（阈值设置错误）　|　**所属模块**：AI 质检（画面一致性 / `consistency.py`）
- **文件路径:行号**：`app/consistency.py:51`（`WARN_SCORE=55` / `PHASH_ALARM=40`）、`:223-231`（`_verdict`）、`:288-293`（降级调用点）
- **问题描述**：语义层不可用时回退到 pHash，`_verdict` 用 `threshold_pass = PHASH_ALARM(40)`、`threshold_warn = 0`——**warn 阈值被写成 0**。实测：两张**互不相关**的噪声图 `score=49.2` → `_verdict → "pass"`。而 `score >= 0` 恒成立，所以降级模式下**永远不可能出现 `fail`**。
- **风险影响**：语义质检接口一挂（含临时 5xx / 超时），一致性校验立刻退化为**恒通过**，角色/画风漂移完全无人拦截。这是「接口不可用」这个最常见故障下的**静默失效**。
- **复现条件**：`_verdict(49, semantic=False)` → `'pass'`；任意两张噪声图 `check_pair_phash` 结果不判 `fail`。
- **修复方案**：为降级模式设独立且合理的阈值（如 `PHASH_WARN = PHASH_ALARM // 2`），并在**降级时把结论标为 `degraded`**，不允许降级结论等同于语义通过。
- **参考代码片段**：
  ```python
  def _verdict(score: int, semantic: bool) -> str:
      if semantic:
          threshold_pass, threshold_warn = PASS_SCORE, WARN_SCORE
      else:
          threshold_pass, threshold_warn = PHASH_ALARM, PHASH_ALARM // 2   # 原为 0
      if score >= threshold_pass:
          return "pass" if semantic else "degraded"   # 降级不冒充通过
      if score >= threshold_warn:
          return "warn"
      return "fail"
  ```
- **验收标准**：噪声图对在降级模式下**不返回 `pass`**；`semantic_available=False` 时结论带 `degraded` 标记且被质检总览单独统计；离线用例补「噪声图必须 fail」。

---

### 【P2-10】【磁盘】全链路无磁盘水位控制 → 长时无人值守必然写满盘（历史上已发生一次 80GB 事故）

- **类型**：架构稳定性隐患（资源耗尽）　|　**所属模块**：视频/超分/ComfyUI 通用
- **文件路径:行号**：`app/video_postprocess.py:152-159`、`app/upscale_client.py:566-567` 与 `:631-638`、`app/pipeline.py:921-925`、`app/comfyui_client.py:1845-1846`
- **问题描述**：四处**大体积产物**（拼接中间片、超分输出、整集 H3 分段、ComfyUI `output/` 临时）在写入前**都不检查剩余空间**；仅依赖「跑完再清理」，且清理是尽力而为（异常即跳过）。`comfyui_client` 一侧更没有对 `output/comic_drama_*` 的容量上限。
- **风险影响**：托管模式 24/7 无人值守下，任一次批量失败残留即可累积；磁盘写满后**不是「任务失败」而是「全线异常」**（原子写失败、json 损坏、源文件被截断——项目历史上就出现过一条 ffmpeg 无限写把 C 盘写满、把正在编辑的源文件截成 0 字节的事故）。
- **复现条件**：不清理 `output/`，连续跑若干轮整集生成，观察 `output/` 单调增长且无任何告警。
- **修复方案**：加统一守卫 `_ensure_disk_headroom(path, need_bytes, min_free_gb=10)`，在四处写入前调用；不足时**阻断并记入「需人工介入」列表**，同时触发一次 `_qc_prune_attempts` + `output/comic_drama_*` 清理再重试一次。
- **参考代码片段**：
  ```python
  import shutil as _sh, os as _os
  def _ensure_disk_headroom(path: str, need_bytes: int, min_free_gb: float = 10.0) -> None:
      free = _sh.disk_usage(_os.path.abspath(path)).free
      if free - need_bytes < min_free_gb * 1024 ** 3:
          raise RuntimeError(
              f"磁盘余量不足（剩余 {free/1024**3:.1f}GB，本次需要 {need_bytes/1024**3:.2f}GB）")
  ```
- **验收标准**：人为把可用空间压到阈值以下后触发生成 → **快速失败并给出中文告警**（而非中途 IO 异常）；日志出现一次清理动作；不产生半成品正式产物。

---

### 【P2-11】【磁盘】失败路径不清理中间产物 → 孤儿文件长期堆积且无人认领

- **类型**：资源泄漏　|　**所属模块**：视频/音频/超分
- **文件路径:行号**：`app/dub_mix.py:372-375`、`app/app.py:8934-8937`、`app/sfx_isolate.py:216-221`、`app/video_postprocess.py:271`、`app/app.py:3960-3966`
- **问题描述**：五处在异常分支（`except` / 提前 `return` / `continue`）**直接跳出，不删除已生成的临时片**。典型是音频分段、ffmpeg 中间层、SFX 隔离输出——这些文件不会被 `_qc_prune_attempts` 命中（命名不带 `_tryN`，也不在 scratch 目录），因此**永不被清理**。
- **风险影响**：与 P2-10 同源叠加：磁盘被「无主中间文件」吃掉，且这些文件**不出现在任何清单里**，用户与运维都看不到；项目删除也不会带走（不在 `project_kind_roots()` 登记表内）。
- **复现条件**：让混音/超分步骤在中途抛一次异常 → 中间文件留在 `output/temp` 或 `output/upscale/<项目>/`。
- **修复方案**：统一用 `try/finally` 或上下文管理器收口临时产物；把中间目录**纳入 `project_kind_roots()` 登记表**，使其可随删项目一起清理。
- **参考代码片段**：
  ```python
  tmps: list[str] = []
  try:
      ...
      tmps.append(seg_path)
      ...
  finally:
      for p in tmps:
          try:
              if os.path.isfile(p):
                  os.remove(p)
          except OSError:
              pass
  ```
- **验收标准**：在五处各注入一次异常，跑完后对应中间目录**无新增残留文件**；`python .workbuddy/test/verify_project_delete_coverage.py` 覆盖新建中间目录。

---

### 【P2-12】【成片】字幕版成片成为孤儿文件 → 用户拿到的是无字幕版且无提示

- **类型**：业务流程逻辑缺陷　|　**所属模块**：视频成片（字幕烧制）
- **文件路径:行号**：`app/video_postprocess.py:562-567`、`app/app.py:4006-4032`
- **问题描述**：字幕烧制结果写在一个**独立文件名**（`*_sub.mp4` 之类），但成片的**对外登记**（`deliverables.json` / 前端播放地址 / 集号索引）指向的是无字幕版本。烧制成功后**没有任何调用方引用字幕版**，也没有「应该引用哪一版」的显式配置。
- **风险影响**：用户点开成片看不到字幕（AI 短剧无字幕基本不可用），而系统认为一切正常；字幕版文件占着磁盘但没有任何入口能访问——**典型的「做了但没接上」**。
- **复现条件**：跑完整整条流水线 → `output/final/<项目>/` 同时存在两版 mp4，前端只播到无字幕那版。
- **修复方案**：明确成片口径——字幕烧制成功后**替换**成片路径（或登记为 `deliverable` 的正式产物），并把无字幕版降级为中间产物；若两版都要保留，则在 `deliverables.json` 中登记两条并标明 `subbed: true/false`。
- **参考代码片段**：
  ```python
  if os.path.isfile(subbed_path) and os.path.getsize(subbed_path) > 0:
      _atomic_replace(subbed_path, final_path)   # 字幕版上位为正式成片
      final_path_used = final_path
  else:
      logger.warning("字幕烧制未产出，成片保持无字幕版本：%s", final_path)
  ```
- **验收标准**：流水线跑完后，前端成片播放地址指向**带字幕版本**；`deliverables.json` 中该集记录与磁盘实际文件一致；字幕烧制失败时有明确日志与前端提示。

---

### 【P2-13】【NLE】导出工程不带集号 + 按下标对位 → 多集导出必然串镜

- **类型**：数据流转缺陷（下标对位假设）　|　**所属模块**：视频/NLE 导出
- **文件路径:行号**：`app/nle_export.py:130-145`、`:165-181`、`:89`、`app/app.py:2042-2048`
- **问题描述**：NLE（剪辑工程）导出时，素材清单的**文件名不含集号**（`shot_03.mp4` 之类），而与被引用的素材之间是**按下标（序号）对位**建立的。第 2 集起共用同一命名空间 → 一旦某集有镜次缺失/跳镜（P2-3 类问题会造成），下标即整体错位。
- **风险影响**：导出的剪辑工程**时间轴全错**（第 2 集引用第 1 集素材），且因为是「相似文件名 + 数量对得上」，错误的隐蔽性极高；外部剪辑软件打开不会报错。
- **复现条件**：导出第 2 集 NLE 工程，检查素材引用路径 → 指向 `ep01/` 或指向同名但不同集的素材。
- **修复方案**：NLE 内部的素材命名统一带集号（复用 `_ep_dir` 的口径），引用关系改为**按显式 id/镜号映射**而非下标。
- **参考代码片段**：
  ```python
  def _nle_asset_name(ep: int, shot_key: str, ext: str) -> str:
      """NLE 素材名必须全局唯一：带集号"""
      return f"ep{ep:02d}_{shot_key}{ext}"
  ```
- **验收标准**：导出 ep02 的 NLE 工程，所有素材引用路径均在 `ep02/` 下且文件名带 `ep02` 前缀；导出 ep01 与 ep02 后两者素材**无重名**。

---

### 【P2-14】【并发】`api_generation_status` 在锁外序列化任务状态 → 可能读到半更新的字典

- **类型**：并发安全隐患　|　**所属模块**：全局状态机 / Flask API
- **文件路径:行号**：`app/app.py:2684-2688`
- **问题描述**：`with lock: state = generation_state.get(task_id, {})` 只把**引用**取出，随后 `jsonify(state)` 在**锁外**执行。worker 线程此时正在 `with lock:` 块内对同一 dict 做 `results.append(...)` / `progress` 更新 —— 序列化过程中 dict 被并发变更。
- **风险影响**：`jsonify` 需遍历 dict 的 keys/items，遍历中 list 增长在 CPython 下多数情况不抛（`RuntimeError: dictionary changed size during iteration` 主要作用于 dict 本身），但**字段间不一致的快照**必然出现（如 `progress=100` 而 `results` 只有 3 条）→ 前端进度条与结果列表偶尔对不上，属「偶现」类问题，难复现。
- **复现条件**：生成过程中高频轮询 `/api/generation/status/<id>`，偶发看到 `progress` 与 `results` 长度不匹配。
- **修复方案**：在锁内完成**深拷贝**后再序列化（`copy.deepcopy`）；`results` 较大时改为按需分页读取。
- **参考代码片段**：
  ```python
  import copy
  @app.route('/api/generation/status/<task_id>', methods=['GET'])
  def api_generation_status(task_id):
      with lock:
          state = copy.deepcopy(generation_state.get(task_id, {}))
      return jsonify(state)
  ```
- **验收标准**：压测（生成中高频轮询 200 次）始终满足 `len(state["results"]) == state["progress"] * total / 100` 的一致关系（允许 ±1 镜误差），无字段错位快照。

---

## 五、⚡ 历史修复回归核查表

> 本表只列**能给出现场证据**的历史条目。判定用「核查方式」自证，不采信「曾声称已修」。
> 结论口径：**已修** = 当前代码正确；**部分修** = 修了样板没推广；**空壳修** = 代码在但逻辑不生效；**未修** = 仍复现。

| 历史项 | 内容 | 本次核查结论 | 核查方式 / 证据 | 关联 |
|---|---|---|---|---|
| **S8** | 落盘原子化：唯一临时名 + 锁 | ⚠️ **部分修（未推广）** | `grep -n '\.tmp"' app/*.py` → 仅 `ai_chat.py:134`、`ai_config.py:186`、`project_store.py:156`、`prompt_memory.py:400` 用 `pid.thread.rand.tmp`；**其余 17 处仍是固定 `path + ".tmp"`**：`autonomous.py:60`、`autopilot.py:148`、`consistency.py:364`、`continuity.py:108`、`llm_client.py:217/273`、`pipeline.py:1261/1278/1323/1369/1403`、`qc_client.py:692/845/864`、`secret_store.py:155/338`、`watermark_cleanup.py:155`。锁覆盖：`qc_client`/`pipeline`/`secret_store`/`consistency`/`continuity`/`llm_client`/`autonomous` **均为 0 个 `Lock()`** | **P1-1** |
| **G5** | 任务 ID 唯一化（秒级时间戳 → 唯一 ID） | ⚠️ **部分修（资产漏改）** | `app.py:3284` 分镜 `uuid[:12]` ✅、`:3407` 视频 ✅、`:8402` 配音 ✅、`:8877` 混音 ✅；**`:2665` 资产仍是 `f"{asset_type}_{project_name}_{int(time.time())}"`** ❌；`:1421` 尾帧、`:4472` 超分仍用时间戳 | **P1-15** |
| **G14** | 配置文件损坏 → fail-loud（不静默关质检） | ✅ **已修** | `qc_client.py:592-594`「文件损坏则必须 fail-loud」+ `:877` 标记供 UI 高亮 | **P1-2**（同类机制未推广到项目索引，故索引更脆弱） |
| **O21** | 镜号归一化收敛为单一实现 | ❌ **未修（仍 6 套 / 2 种互斥语义）** | `grep -n "def _seq\|def _shot_seq\|def _shot_num_key\|def _norm_shot_key\|def _shot_seq_num" app/*.py` → 6 处定义；`keyframe.py:150-152` 写 `shot_102_end.png` 而 `app.py:3507` 读 `shot_01_end.png` | **P1-19** |
| **G1** | 尾帧质检止损（连续同因失败即放弃） | ❌ **空壳修（逻辑不生效）** | `keyframe.py:491-498` 读 `r.get("qc",{}).get("critical_issues")`，但上一行 `r["qc"]` 只写 `{ok, reason, attempt}` → 恒取 `[]`；实测 `_qc_retry_hopeless(...)` 恒返回 `(False, '')` | **P2-7** |
| **G9 / O1** | 视频客观层确定性闸门（零模型依赖） | ⚠️ **已修但有隐患** | `qc_client.py:2040-2046` 闸门在位；但 `obj_fatal` 靠 `"无音轨"/"时长"/"帧率"` **中文子串**推断 → 改文案即静默降级 | **P2-8** |
| **G8②** | 分镜消费 ComfyUI output 源用 `move` 而非 `copy2` | ✅ **已修**（并引入新缺陷） | `app.py:3111-3113` `shutil.move(result["files"][0], scratch_png)`；但 `:3114-3123` 先置 `success=True` 再落盘 | **P2-3** |
| **S7** | 尾帧质检通过 → 暂存产物移入正式 `keyframes/` | ✅ **已修** | `keyframe.py:498-505` | — |
| **S6** | 提示词侧「资产固定」保护（防角色/场景被模型改写） | ⚠️ **被上游架空** | 保护本身在位，但 `characters_in_shot` / `location` 匹配失败时静默兜底取错对象 → 保护对错对象生效 | **P1-16** |
| **O2** | 产物旁路元数据（seed / 提示词 / 工作流 SHA / 质检结论） | ⚠️ **部分覆盖** | `app.py:3142-3146` 分镜写了 `_write_artifact_meta` ✅；**尾帧落盘路径无此调用** ❌ | **P2-5** |
| **O3 / G7 / G10 / G13 / G18 / G19 / S11 / S13** | 历届审计已确认修复项（镜号收敛辅助、日志、超分/水印/取消等） | ✅ **本轮未复现** | 本轮深挖未取得反例；**未逐项独立复核**，故不列入增补范围 | — |
| — | **已排除的非缺陷项**（避免开发误改） | — | 见下方说明 | — |

### 已排除的非缺陷项（**请开发不要"顺手修"**）

1. **`output/{assets,continuity,dub,final,final_dub,scripts,storyboards,upscale,videos}` 空目录**：这是应用启动自建的标准结构（`app.py` 模块级 `os.makedirs`），**不是脏数据**，删了下次启动还会回来。
2. **「未使用变量」告警**：`A = _A()` 是**前置断言**（有意为之）；`state_path = _ensure_state_dir(...)` 有 `os.makedirs` **副作用**。二者均**不得**当死代码删除。
3. **`task_queue`「只被创建和 `.status()` 读取」**：确属**事实性死代码**，但**不在本轮范围**（改造涉及并发模型，风险高于收益），已在 P1-12 中作为「接线建议」而非「删除建议」。
4. **`ai_qc_judge` 的 4 个函数**：其中 3 个经 `judge_qc_result → create_execution_plan` **可达**，非死代码。（历史教训：只数直接引用会误报。）
5. **`_keyframe_qc_verifier` 在接口不可用时 `return None, 0`**：**不是**回归缺陷，而是 P1-18 的另一面（与图片/视频链路口径不一致），已在 P1-18 统一收口。
6. **`_safe_project('')` 返回字面量 `'project'`**：**当前不是待修 bug**，但**是编码红线**——任何 `_safe_project(x) or 兜底` 写法兜底永不生效；改代码时**必须**改用 `_project_or_400` 或判断原始入参。

---

## 六、✅ 批次任务汇总表（可直接复制下发）

> 共 **38 条**：P0 5 条、P1 19 条、P2 14 条。
> **打勾即可作为工单**。条目详情见第三章（P0/P1）、第四章（P2）。

### 批次 1 —— 开发 A（**24 条**：P0 阻断 + P1 严重）

> **目标**：流水线**跑得通、跑得对、不误判、不白烧 API**。
> **交付纪律**：每修完 1 条跑对应离线用例；**每 3~5 条提一次 commit**（`git add <自己的文件>`，**禁止 `git add -A`**）。

| # | 优先级 | 类型 | 模块 | 文件:行号 | 一句话问题 | 验收要点 |
|---|---|---|---|---|---|---|
| [ ] P0-1 | P0 阻断 | 质检闸门 | `app/app.py:3121-3123`、`:3852-3855`、`:3661`、`:3690-3694` | 「声明开启质检但接口未就绪」时未质检产物直接入库 | `qc_declared && !qc_on` 时必须 fail-closed，与资产链路口径一致 |
| [ ] P0-2 | P0 阻断 | 提示词预检 | `app/prompt_qc.py:227-232`、`:248-249` | 空壳镜判据不含 `dialogue` → 「仅台词」镜被判死刑、整集卡死分镜步 | 仅台词镜必须通过预检；`verify_prompt_qc.py` 全绿 |
| [ ] P0-3 | P0 阻断 | AI质检 | `app/qc_client.py:160-169` | 否定语境无分句边界 → 崩坏描述被吞、崩坏图正常入库 | `find_critical_issues(['五官不自然，面部畸变'])` 必须非空 |
| [ ] P0-4 | P0 阻断 | 全局状态机 | `app/app.py` 成片复核路径 | 成片 `rejected` 后幂等短路 → 空转、`review` 不复位、误报「状态同步异常」 | 打回后可重跑，`review` 正确复位 |
| [ ] P0-5 | P0 阻断 | 并发/幂等 | `app/app.py` `/api/autopilot/run-once` | 与守护线程无集级互斥 → 同集双流水线并发写同一批路径 | 同集任何时刻只有一个执行体；加集级互斥锁 |
| [ ] P1-1 | P1 严重 | 数据安全 | `app/secret_store.py:152-162`（+ 17 处固定 `.tmp`） | 固定 `.tmp` + 无锁；密钥库读改写竞态 + 解析失败静默清空整库 | 抽公共 `atomic_write_json`；并发写不再丢密钥；损坏时不静默清空 |
| [ ] P1-2 | P1 严重 | 数据安全 | `app/project_store.py:114-131`、`:181-190` | 项目索引损坏 → 静默清空 → 写回空索引 → 全部项目永久消失 | 解析失败必须 fail-loud + `.bak` 轮转；不清空索引 |
| [ ] P1-3 | P1 严重 | 视频·成片 | `app/video_postprocess.py:84-87`、`:302-312` | 音轨/视频一致性判定是死代码 → `-c copy` 拼出音轨错位成片 | 判定生效；不一致时走重编码而非 `copy` |
| [ ] P1-4 | P1 严重 | 视频·成片 | `app/video_postprocess.py:374-386` | 降级重编码硬编码 `1280:720@30` → 竖屏项目被改横屏加黑边 | 分辨率/帧率从源片实测推导 |
| [ ] P1-5 | P1 严重 | 视频·时间轴 | `app/pipeline.py:741-746` | 字幕/混音/质检用剧本 duration 累加，与成片实测时长基准分裂 | 统一以 ffprobe 实测为准 |
| [ ] P1-6 | P1 严重 | 视频·混音 | `app/app.py:8506-8518` | `_mix_segments_dir` 不带集号 → 第 2 集起时间轴/成片源系统性错配 | 目录带 `epNN`；ep01/ep02 素材无重名 |
| [ ] P1-7 | P1 严重 | 提示词/图片/视频 | `app/app.py:3487-3488`、`:3539`、`:3275-3284` | H3 锚点恒取第 1 角色 + 尾帧被当角色锚点 → 串脸；复用守卫缺 `episode_no` | 按本镜出镜角色逐镜匹配；守卫加集号 |
| [ ] P1-8 | P1 严重 | 图片·幂等 | `app/app.py:3275-3284` | 分镜任务复用守卫不含 `episode_no` → 第 2 集请求被第 1 集运行中任务吞掉 | 守卫键含集号 |
| [ ] P1-9 | P1 严重 | 并发/契约 | `app/pipeline.py` 任务表写入 | 内部任务写进公共任务表且 `init` 缺字段 → 漏命中双 worker / 误命中前端 404 | 内部任务隔离；`init` 字段补全 |
| [ ] P1-10 | P1 严重 | 数据流转 | `app/app.py:8139-8141` | 配音 manifest 非原子整文件覆盖 + `probe_tts` 静默 `done:False` → 一次崩溃重烧整集 | 原子写；解析失败 fail-loud |
| [ ] P1-11 | P1 严重 | 数据安全 | `app/pipeline.py:1261/1278` 等 | 死信落盘失败被吞 + 计数器清零 → 同集无限重跑，「需人工介入」永远为空 | 死信落盘失败必须可见；计数器不清零 |
| [ ] P1-12 | P1 严重 | 并发/资源 | `app/app.py:175-176`、`:853`、`:876` | 单 GPU 无并发闸门：`TaskQueue` 未接线、线程无上限、`/free` 无条件卸载他人模型 | 接入队列；线程上限；`/free` 只卸自己的 |
| [ ] P1-13 | P1 严重 | ComfyUI 调用层 | `app/comfyui_client.py:1845-1846` 等 | 轮询结局不可区分 / `interrupt()` 误伤他人 / 标量超时无 connect 超时 | 三种结局可区分；interrupt 定向；超时分离 |
| [ ] P1-14 | P1 严重 | 图片·跨项目污染 | 多视角基础图上传路径 | 上传到 ComfyUI `output` 根目录且**不含项目名** → 跨项目同名资产互相覆盖 | 路径含项目名 + 集号 |
| [ ] P1-15 | P1 严重 | 图片·幂等 | `app/app.py:2665` | 资产任务 ID 仍用秒级时间戳（G5 漏改） | 改用 `uuid`，同秒并发不互撞 |
| [ ] P1-16 | P1 严重 | 提示词·资产固定 | `app/app.py` `characters_in_shot`/`location` 匹配 | 匹配失败静默兜底取错对象（并架空下游 S6 保护） | 匹配失败必须告警/阻断，绝不静默取错 |
| [ ] P1-17 | P1 严重 | AI质检 | `app/qc_client.py:1433-1435`、`:1453-1454` | `parse_verdict` 把 `score is None` 当满足分数 → 模型漏回分数即放行 | 缺分数 → fail-closed |
| [ ] P1-18 | P1 严重 | AI质检 | `app/app.py:3603-3645`、`app/keyframe.py:472-528` | 整集/尾帧把「不可判定」当「不达标」→ ffmpeg 缺失时白烧 N 遍整集 | 不可判定 → 不重试，标 `qc_unavailable` |
| [ ] P1-19 | P1 严重 | 全局 | `app/app.py:927/2896/3372`、`keyframe.py:161`、`dub_mix.py:166/174` | 镜号归一化 6 套实现、2 种互斥语义 → keyframe 模式尾帧永久失效 | 收敛为 `shot_key.shot_seq` 单一实现 |

**批次 1 建议修复顺序**（按「阻断程度 × 被依赖度」）：
1. **P0-1 → P1-17 → P1-18 → P0-3**（质检链路四条，先止血，避免误判/白烧）
2. **P1-1 → P1-2**（数据安全两条，独立性强，可并行）
3. **P0-2 → P1-16 → P1-19**（提示词/镜号，**P1-19 必须先于 P1-7 做**，否则锚点修了也对不上）
4. **P1-3 → P1-4 → P1-5 → P1-6 → P1-10**（视频/音频时间轴与落盘）
5. **P1-7 → P1-8 → P1-14 → P1-15**（图片/视频幂等与锚点）
6. **P0-4 → P0-5 → P1-9 → P1-11 → P1-12 → P1-13**（状态机与并发，改动面最大，最后收）
- **依赖关系**：`P1-19 → P1-7`（镜号统一后才能做锚点逐镜匹配）；`P1-1 → P1-2/P1-10/P1-11`（先有公共 `atomic_write_json`，再改各处落盘）。
- **合并交付建议**：`P1-3 + P1-4`（同文件 `video_postprocess.py`）、`P1-5 + P1-10`（同属时间轴/清单）、`P0-4 + P0-5`（同属状态机与互斥）各自合并为一次 commit。

---

### 批次 2 —— 开发 B（**14 条**：P2 优化项）

> **目标**：**降耗、去腐化、让故障可见**。**不改变主流程行为**。
> **前置约束**：批次 2 中 **P2-3 / P2-5 / P2-6 / P2-7 / P2-13** 与批次 1 有耦合（见备注），**必须等批次 1 对应条目合并后再动**。

| # | 优先级 | 类型 | 模块 | 文件:行号 | 一句话问题 | 验收要点 | 前置 |
|---|---|---|---|---|---|---|---|
| [ ] P2-1 | P2 优化 | 死分支 | `app/qc_client.py:667-680` | `elif k == "api_backoff"` 永不可达 | `grep -c 'k == "api_backoff"'` 返回 0 | — |
| [ ] P2-2 | P2 优化 | 排序 bug | `app/app.py:5320-5331` | 未排序 `listdir` → 保留的不是最后 N 次（实测留 `[1,2,3,10]`） | `sorted(nums)` 后保留 `{8,9,10}` | — |
| [ ] P2-3 | P2 优化 | 异常捕获 | `app/app.py:3114-3123`、`:3193-3198` | 先置 `success=True` 再 `copy2`，失败不回滚 | 落盘失败时 `success:false` 且无 `file/url` | **P0-1 之后** |
| [ ] P2-4 | P2 优化 | 口径不一致 | `app/app.py:1294-1301` vs `:2699-2703`/`:2730-2735` | 就绪判据 ≠ 取图判据 → 判就绪却取不到参考图 | 二者共用同一函数 | — |
| [ ] P2-5 | P2 优化 | 元数据 | `app/app.py:3142-3146` vs `keyframe.py:550-554` | 尾帧不写 `.meta.json`，溯源断链 | 每个 `*_end.png` 旁有 `.meta.json` | — |
| [ ] P2-6 | P2 优化 | 质检留痕 | `app/app.py:5747-5757`、`:6351-6356` | 尾帧质检只在失败时落盘，达标无痕 | 达标/不达标均产生记录 | **P1-18 之后** |
| [ ] P2-7 | P2 优化 | 空壳逻辑 | `app/keyframe.py:491-498` | 止损读不到 `critical_issues` → 永不触发（实测） | 同因连续 2 次后停手 | **P1-18 之后** |
| [ ] P2-8 | P2 优化 | 隐式耦合 | `app/qc_client.py:2042-2046` | 致命等级靠中文子串推断 | 改文案后 `objective_fatal` 不变 | — |
| [ ] P2-9 | P2 优化 | 阈值错误 | `app/consistency.py:51`、`:223-231`、`:288-293` | 降级阈值 `<` 随机基线 → 噪声图判「一致」（实测 49.2→pass） | 噪声图不再 `pass`；降级标记 `degraded` | — |
| [ ] P2-10 | P2 优化 | 磁盘水位 | `video_postprocess.py:152-159`、`upscale_client.py:566/631`、`pipeline.py:921-925`、`comfyui_client.py:1845-1846` | 大体积写入前无剩余空间检查 | 空间不足时快速失败 + 中文告警 | **P1-11 之后** |
| [ ] P2-11 | P2 优化 | 资源泄漏 | `dub_mix.py:372-375`、`app.py:8934-8937`、`sfx_isolate.py:216-221`、`video_postprocess.py:271`、`app.py:3960-3966` | 失败路径不清理中间产物 → 孤儿堆积 | 注入异常后无新增残留 | — |
| [ ] P2-12 | P2 优化 | 成片口径 | `video_postprocess.py:562-567`、`app.py:4006-4032` | 字幕版成片成孤儿 → 用户拿到无字幕版 | 前端播放地址指向带字幕版 | — |
| [ ] P2-13 | P2 优化 | 下标对位 | `nle_export.py:130-145`、`:165-181`、`:89`、`app.py:2042-2048` | NLE 导出不带集号 + 按下标对位 → 多集串镜 | 素材名带 `epNN`，按显式映射引用 | **P1-19 之后** |
| [ ] P2-14 | P2 优化 | 并发 | `app/app.py:2684-2688` | 锁外 `jsonify` → 读到半更新快照 | 锁内深拷贝；压测无错位 | — |

---

### 批次 1 与批次 2 的**冲突规避**

| 风险 | 说明 | 规避 |
|---|---|---|
| 同文件并发编辑 | 批次 1/2 同时改 `app/app.py`（P0-1、P1-6/7/8/14/15、P2-2/3/4/5/6/14） | **批次 2 中的 `app.py` 条目一律等批次 1 的 `app.py` 条目合并后再开工** |
| `keyframe.py` 三处交叉 | P1-18（不可判定口径）、P2-6（留痕）、P2-7（止损字段）改的是同一段 | 由**开发 B 一次性提交**，开发 A 只负责 P1-18 的**返回口径**（三元组/`qc_unavailable`） |
| `video_postprocess.py` | P1-3/P1-4（批次 1）与 P2-11/P2-12（批次 2）同文件 | 同上：批次 2 延后 |
| 提交纪律 | 本仓库存在**多会话并发** | **禁止 `git add -A`**；逐文件确认归属；发现同一文件行号在两条命令间漂移 → 有他人正在编辑，**停手并沟通** |

---

## 七、✅ 修复验收标准

### A. 通用门禁（**每次提交前必过**）

```bash
# 1) 语法与未定义名（只看 undefined name）
python -m py_compile app/*.py
python -m pyflakes app/*.py | grep -i "undefined name" || echo "OK: 无未定义名"

# 2) 前端构建（改了 app/static 或前端源码时必做）
cd frontend && npx tsc --noEmit && npx vite build

# 3) 逐条跑受影响模块的离线用例（禁止一次跑完整套：批量删除守卫会误杀后续脚本）
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_<模块>.py
```

- **通用硬性要求**：
  1. 所有新增「静默失败」路径必须改为**可观测**（日志 `warning`+ 或进入「需人工介入」列表），**不得**新增 `except: pass`；
  2. 所有新增落盘必须走**公共 `atomic_write_json` / `_atomic_replace`**（唯一临时名 + 锁）；
  3. **不得**引入新的 `_safe_project(x) or 兜底` 写法（兜底永不生效，改用 `_project_or_400`）；
  4. 不得依赖用户真实数据做测试（样本本地合成到 `.workbuddy/test/_out/`，用完即清）。

### B. 批次 1 逐条验收（开发自测清单）

| # | 判定条件（自测通过即算解决） |
|---|---|
| P0-1 | 关掉质检接口（清空 `base_url`/`api_key` 但保持 `enabled && image_enabled`）跑分镜/逐镜/整集/尾帧 → 正式目录**无新增产物**，`qc_gate.blocked=True`，暂存图保留；资产链行为不变 |
| P0-2 | 构造 `{"dialogue":"你好","visual":""}` 类镜头 → 预检**通过**；`verify_prompt_qc.py`（167 项）全绿 |
| P0-3 | `find_critical_issues(['五官不自然，面部畸变'])` 非空；`find_critical_issues(['画面清晰，五官自然'])` 为空；「不是五官不自然」不再被吞 |
| P0-4 | 成片置 `rejected` 后重跑 → 不再空转，`review` 复位为待复核，无「状态同步异常」 |
| P0-5 | 同时打 `/api/autopilot/run-once` 且守护线程正在跑同一集 → 只有一个执行体，另一路立即返回「该集正在执行」 |
| P1-1 | 并发 20 线程各写不同 key → 无丢 key；`_read_store` 遇损坏文件 → **报错 + `.bak` 保留**，不返回空 dict；`grep -c '\.tmp"' app/*.py` 中固定名归零（或集中于公共工具） |
| P1-2 | 手工把 `projects/index.json` 改成非法 JSON → 启动后**报错并保留原文件**，不写回空索引；`.bak` 存在 |
| P1-3 | 构造视频流/音频流时长不一致的两个分片 → 判定命中，走重编码而非 `-c copy` |
| P1-4 | 竖屏 9:16 项目触发降级重编码 → 输出仍为源片宽高比与帧率（无黑边） |
| P1-5 | 字幕/混音/质检三处的 `expected_duration` 与 ffprobe 实测成片时长差 < 0.5s |
| P1-6 | ep02 混音 → 取到的素材全部在 `ep02/` 下；ep01 与 ep02 素材无重名 |
| P1-7 | 3 角色剧集，第 2/3 角色的镜头其 H3 锚点为**该角色**参考图（非第 1 角色）；同集重复请求不再互撞 |
| P1-8 | 同秒并发提交 ep01 与 ep02 分镜 → 两集各自执行，不出现「第 2 集返回第 1 集 task_id」 |
| P1-9 | 前端任务列表不出现 pipeline 内部任务；`init` 字段齐全（含 `total`/`phase`） |
| P1-10 | 配音中途 kill 进程 → 重启后 manifest 可解析，**不重烧**已完成的段 |
| P1-11 | 让死信落盘失败 3 次 → 该集停止重跑并进入「需人工介入」列表，计数不归零 |
| P1-12 | `TaskQueue` 有真实调用点（`grep -c "task_queue"` 含入队调用）；同 GPU 并发渲染数 ≤ 上限；`/free` 不卸载非本任务模型 |
| P1-13 | ComfyUI 轮询可区分「成功/失败/超时」三态；`interrupt()` 只作用于自己的 `prompt_id`；连接超时与读超时分别可配 |
| P1-14 | 两个项目各有同名角色 → ComfyUI `output` 下不互相覆盖（路径含项目名） |
| P1-15 | 同秒发起 5 次资产生成 → 5 个不同 `task_id`，`generation_state` 无覆盖 |
| P1-16 | 剧本写 `characters_in_shot` 引用了不存在的角色 → **告警/阻断**，不再静默换角色 |
| P1-17 | `parse_verdict('{"pass": true}', 70)` → `blocked=True`，reason 指明「未返回分数」；带分数结论行为不变 |
| P1-18 | 移除 ffmpeg 或让质检接口返 500 → 整集/尾帧只生成 **1 次**，状态标 `qc_unavailable`，不换种子重跑 |
| P1-19 | 对 `1/"1"/"01"/"shot_03"/"shot_1"/"S01-C02"` 六种输入，落盘序号与读取序号**完全一致**；`shot_id="S01-C02"` 跑 keyframe 模式 `kf_end_map` 非空 |

### C. 批次 2 逐条验收（开发自测清单）

| # | 判定条件 |
|---|---|
| P2-1 | `grep -c 'k == "api_backoff"' app/qc_client.py` → `0`；保存配置后 `api_backoff` 仍写入成功 |
| P2-2 | 造 `_try1.._try10`、`keep=3` → 剩余 `{8,9,10}`；离线用例断言 |
| P2-3 | 制造落盘失败 → `success:false` 且无 `file`/`url`；正常路径不变 |
| P2-4 | 仅 `base.png` 的角色：就绪判定与取图结果**一致**；三态用例全绿 |
| P2-5 | `keyframes/<项目>/` 下每个 `*_end.png` 均有同名 `.meta.json`，含 `seed` 与 `qc` |
| P2-6 | 达标与不达标两种情况下，`qc/<项目>/keyframe*/` **都有**记录 |
| P2-7 | 同因连续失败 2 次后第 3 次不再渲染；`_qc_retry_hopeless` 对同因输入返回 `(True, 原因)` |
| P2-8 | 任意改客观层**文案**后 `objective_fatal` 集合不变；4 类致命码各有用例 |
| P2-9 | 噪声图对在降级模式**不返回 `pass`**；结论带 `degraded`；语义可用时行为不变 |
| P2-10 | 把可用空间压到阈值下 → 快速失败 + 中文告警 + 触发一次清理；不产生半成品正式产物 |
| P2-11 | 五处各注入一次异常 → 跑完中间目录无新增残留；`verify_project_delete_coverage.py` 覆盖新目录 |
| P2-12 | 前端成片播放地址指向**带字幕版**；`deliverables.json` 与磁盘一致；烧制失败有日志+提示 |
| P2-13 | ep02 NLE 素材引用全在 `ep02/` 且名字带 `ep02`；ep01/ep02 导出素材无重名 |
| P2-14 | 生成中高频轮询 200 次 → `progress` 与 `results` 长度关系**始终一致**（±1 镜），无字段错位 |

### D. 回归门禁（**批次 1 合并后必须整体过一遍**）

按 **MEMORY.md 纪律**：**一次工具调用只跑一个脚本**（WorkBuddy CLI 批量删除守卫**按轮次累计**计数，整套连跑超阈值会被 `SystemExit(1)` 杀掉 → 环境假红）。

**重点回归清单**（与本次缺陷强相关，共 12 个）：

```bash
# 质检链路
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_bool_parse.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_prompt_qc.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_summary_scan.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_retry_stop.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_ai_qc_sync.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_ref_images.py
# 落盘原子化 / 数据安全
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_atomic_write_locks.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_project_delete_coverage.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_project_audit.py
# 视频 / 音频 / 镜号
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_final_video_episode.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_camera_framing.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_h3_sequential.py
```

- **新增用例要求**：`P1-19`（镜号归一化六态）、`P2-2`（排序保留）、`P2-7`（止损触发）、`P2-9`（噪声图必 fail）**各补一个离线脚本**，不得只做手工验证。
- **门禁通过定义**：上述 12 个脚本 + 4 个新增脚本**全部绿色**；出现「日志只有一行 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`」判为**环境假红**，**逐个单独复跑**确认。

### E. 上线前人工走查（**不可替代**）

1. 用**真实**的一个 3 集项目跑一遍托管，确认：分镜→尾帧→视频→成片→字幕 全链路产物齐全，`deliverables.json` 与磁盘一致；
2. 抽查 3 个 `*.meta.json`（分镜/尾帧各 ≥1）+ 3 条质检记录，确认 `seed`/`prompt`/`qc` 字段齐全；
3. 断开质检接口（改错 `base_url`）再跑一镜，确认 **fail-closed**（无产物入库 + 前端明确告警），**不产生**「未质检但已入库」的静默劣化；
4. 点一次**暂停**，确认秒级生效（`cancellation.py` 路径未被 P2 改动破坏）；
5. 检查 `output/` 在跑完一轮后**没有**新增未被任何清单引用的孤儿文件。



