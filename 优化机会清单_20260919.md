# 漫剧生成系统 · 优化机会清单

**日期**: 2026-09-19 | **范围**: 全项目（后端 `app/` 35940 行 / 前端 `frontend/src` 7907 行）
**方法**: 只读扫描 —— 静态检查（pyflakes）、调用点穷举、配置消费核查、覆盖矩阵比对、热点函数度量、仓库卫生检查。
**原则**: 每条都带**可复现的证据**（文件:行号 / 统计脚本）。不做无证据的猜测；**误判过的项集中记在第四节**。

---

## 一、结论摘要

| # | 问题 | 类型 | 收益 | 风险 | 工作量 |
|---|---|---|---|---|---|
| 1 | **音频质检：配置 × 5、UI 开关、提示词模板全在，功能 0 行实现** | 逻辑/误导 | ★★★ | 低 | 决策 |
| 2 | 质检配置在前端**完全没有入口**（读不到、改不了） | 流程/功能 | ★★★ | 低 | 中 |
| 3 | 19 个离线测试**没有统一入口**，靠手敲 | 流程 | ★★★ | 极低 | 小 |
| 4 | 「生成→质检→换 seed→召回教训→重试」循环**复制 3~4 份** | 逻辑/重复 | ★★★ | 中 | 中 |
| 5 | `_video_generate_worker` 单函数 **446 行**，3 种模式揉在一起 | 代码结构 | ★★ | 中 | 中 |
| 6 | 热循环里**每次读盘 + 解密**取质检配置（无缓存） | 性能 | ★★ | 低 | 小 |
| 7 | `character_manager` 有**死类 + 假实现**（`return 0.85`） | 逻辑/陷阱 | ★★ | 低 | 小 |
| 8 | 测试覆盖偏科：**持久化与安全底座零测试** | 质量 | ★★ | 低 | 中 |
| 9 | 前端 2108 行巨型文件内联 **9 个组件**（未遵循既有约定） | 代码结构 | ★ | 低 | 中 |
| 10 | `app/static` 每次构建「删 2 加 2」，`emptyOutDir` 有白屏风险 | 流程 | ★ | 低 | 小 |
| 11 | 未使用导入 **53 处** | 卫生 | ★ | 极低 | 小 |
| 12 | 根目录堆了 **15 份**一次性报告（含 4 份近乎重复） | 卫生 | ★ | 极低 | 小 |
| 13 | 依赖未锁定（9 条全是 `>=`） | 可复现性 | ★ | 低 | 小 |

---

## 二、逐项详述

### 1. 音频质检：配置、UI 开关、提示词模板都在，唯独功能没实现 ★★★

**证据**（这是本次最"误导"的发现）

对 `qc_client.CONFIG_KEYS` 的 **31 个配置项**逐项核查「被声明区之外是否真有人读」，结果是 **恰好 5 项零消费，且全部属于音频质检**：

| 配置项 | 声明区命中 | 真实消费 |
|---|---|---|
| `audio_enabled` | 2 | **0** ⚠️ |
| `audio_prompt` | 2 | **0** ⚠️ |
| `audio_min_speech_ratio` | 2 | **0** ⚠️ |
| `audio_min_mean_db` | 2 | **0** ⚠️ |
| `audio_max_drift` | 2 | **0** ⚠️ |

（其余 26 项均有真实消费；`script_enabled` 2 处，`keyframe_qc_enabled` 1 处。）

配套旁证：

- `frontend/src/components/AudioTab.tsx:366` 直接渲染 **「音频质检功能正在开发中...」**；`qcResult` / `qcLoading` 两个 state（:34-35）声明后**从未被赋值或读取**（死 state）。
- 前端却已经把 **`音频质检 开/关` 徽标渲染出来了**（`ProjectWorkbenchPage.tsx:881`，读 `cfg.audio_enabled`）。
- `qc_client` 里为它写了完整的 `DEFAULT_AUDIO_PROMPT`（"频谱图+波形图"判定的整段提示词，数百字）与 3 个阈值，**全部无人使用**。
- 后端**没有任何音频质检端点**（`grep -i audio` 于 `@app.route` 结果为空），19 个测试也无覆盖。
- 对照：`image_enabled`→`image_qc_ready`、`video_enabled`→`video_qc_ready`、`script_enabled`→`script_qc_ready` 都有就绪判定；**音频连 `*_ready` 函数都不存在**（`grep "^def .*_ready" app/qc_client.py` 只有 3 个）。

**影响**：用户看到"音频质检 开关"，会以为配音有质检兜底 —— 实际上配音链路（TTS → 混音）全程没有质量门。这属于**功能可信度**问题，比普通死代码严重。

**建议**（二选一，都要做决策）

- **做**：按已写好的 `DEFAULT_AUDIO_PROMPT` + 3 个阈值实现客观层（有声占比 / 平均电平 / 时长偏差，零模型依赖，`ffmpeg`+`numpy` 即可）+ 复用质检接口的 AI 层，仿 `image_qc_ready` 加 `audio_qc_ready`。
- **不做**：把 5 个配置项从 `CONFIG_KEYS`/前端徽标里摘掉或标注"未启用（开发中）"，避免继续误导。**不要停在现状。**

---

### 2. 质检配置在前端完全没有入口 ★★★

**证据**

- `frontend/src/api/client.ts:508-530` 定义了 `qcApi.config / updateConfig / clearConfig / resetEndpoint / syncFromAI / test / history / frames`。
- 全前端检索 `qcApi.` **只有 2 处**：`history`（ProjectWorkbenchPage.tsx:787）、`test`（:802）。其余 6 个方法**零调用**。
- `QcTab`（ProjectWorkbenchPage.tsx:775-948，174 行）内**可编辑控件命中 0 处** —— 纯只读看板。
- 后端 `/api/qc/config` 的 GET / POST / clear / reset-endpoint / sync-from-ai **全部已实现**（`app/app.py:4928-4986`）→ 纯前端没接。

**影响**

- 质检总开关 `enabled`、合格线 `pass_score`、重试次数 `max_retries`、分项开关、以及新增的 `prompt_mode`（`warn/repair/block`）**只能手改 `qc_config.json`**。
- 该文件在 `.gitignore`（`.gitignore:5`），而 `_empty_config()` 默认 **`"enabled": False`** →
  **新部署开箱即"质检全关"**（`image_qc_ready` / `video_qc_ready` / `script_qc_ready` 都以 `cfg["enabled"]` 为硬前提，`app/qc_client.py:726-734, 2098-2101`）。
- 更别扭：用户会在「AI 设置」页配置 QC 模型（`/api/ai/config` 的 `qc` 模块），但**没有任何按钮**把它同步到质检配置 —— `syncFromAI` 正是干这个的。

**建议**：`QcTab` 增加「质检设置」区块（总开关 / 分项开关 / 合格线 / 重试次数 / 预检模式 + 「从 AI 设置同步接口」按钮），直接消费已就绪的 `qcApi`。**用户感知最强的一项。**

---

### 3. 19 个离线测试没有统一入口 ★★★

**证据**：`.workbuddy/test/` 下 19 个 `verify_*.py`，**无** `run_all` / `suite` / 任何批量脚本；项目**无 CI 配置**（无 `.github/` 等）、**无 lint 配置**（无 ruff / eslint 配置）。

**影响**：约定是"每次改动后必跑全套"，实际成本 = 手敲/手写 19 条命令（本次审计即为此手写 for 循环）。漏跑的风险随脚本数增长。

**建议**：新增 `.workbuddy/test/run_all.py` —— 遍历 `verify_*.py`，统一注入 `MJSCXT_AUTOPILOT=0`，汇总 PASS/FAIL，任一失败即非零退出。约 30 行，当天回本，也是第 4/5 项重构的安全网。

---

### 4. 质检重试循环被复制 3~4 份 ★★★

**证据**（同一套「不达标 → 换 seed → 召回教训 → 改写提示词 → 重试」）

| 位置 | 说明 |
|---|---|
| `app/app.py:2226,2235` | 资产基础图（`_generate_asset_task`） |
| `app/app.py:2335` | 资产多视角 |
| `app/app.py:2708,2720` | 分镜图（`_storyboard_worker`） |
| `app/app.py:3320-3330` | 视频（`_video_generate_worker`） |

每份都各自处理：`prompt_memory.suggest` / `learned_prompt`、以 `orig_*_prompt` 为基准（防建议块一轮轮累积）、`_qc_record_verdict` 落盘、换 seed、重试上限、失败后 `qc_blocked` 标记。

**影响**：**改一处漏一处**。历史症状已出现过 —— "视频 worker 漏补风格强化建议"就是这个复制模式造成的（见 `.workbuddy/memory/TOPICS.md`「风格/质检」节）。本轮加尾帧预检时同样要逐处确认。

**建议**：抽 `_qc_retry_loop(kind, gen_fn, qc_fn, desc_fn, max_retries, ...)`，四处改为调用。风险中等（核心可靠性链路），但现有测试（`style_qc` 57 / `qc_prompt_feedback` 44 / `qc_memory` 99）正好覆盖该路径。

---

### 5. `_video_generate_worker` 单函数 446 行 ★★

**证据**：`app/app.py:3033`，函数体 **446 行**（全项目最长）。内部结构：

- 三分支 `if mode == 'keyframe'` / `if mode == 'episode'` / `else`（per_shot）（L3085 / L3157 / L3328）
- 段构造闭包 `_shot_segment`（L3102）、逐镜提示词预检（L3135）、逐镜质检重试（L3297）
- H3 音轨策略（L3430）、音效分离（L3438）

其余超长函数：`_generate_asset_task` 302 行（L2113）、`_storyboard_worker` 226 行（L2612）、`_episodes_worker` 160 行（L5674）。

**影响**：三种模式的差异被 `if/else` 交织，读一处必须同时读另两处。注释密度很高，说明维护者在靠注释对抗复杂度。

**建议**：按模式拆成 `_video_worker_per_shot` / `_episode` / `_keyframe`，并把 `_shot_segment`（现为闭包）与音轨策略提为模块级函数 —— `_shot_segment` 提出来后还能被其它链路复用。

---

### 6. 热循环里每次读盘 + 解密取质检配置 ★★

**证据**

- `app/qc_client.py:427 load_config()`：打开并解析 `qc_config.json`；随后 `secret_store.get_store(...).get_api_key("qc")` —— 而 `_read_store()` **每次读盘并解析 `secrets.enc`**（`app/secret_store.py:138-150`，无缓存）+ `_decrypt()` **每次做一次 Fernet 解密**（:173-183）。**全链路无缓存**。
- `app/app.py` 有 **15 处** `_qc_load_cfg()`；其中两处在**逐镜循环内**：`_storyboard_worker` L2676、视频 worker L3298。
- 对照：资产路径在循环外 hoist 并注明"任务级读取一次，本任务内所有资产共用"（L2143-2144）；整集视频也在循环外读（L3168）。

**影响**：一集 56 镜 × 最多 5 次重试 → 最多数百次重复「读 2 文件 + 1 次解密」。单次毫秒级，但纯属浪费；且同任务内反复读取可能读到不一致的中间态（用户中途改配置）。

**建议**：给 `load_config` 加 **mtime 缓存**（mtime/size 未变则返回缓存）—— 一处修全部 15 个调用点，且保留"改配置立即生效"语义。或至少把 L2676/L3298 提到循环外与 L2143 对齐。

---

### 7. `character_manager` 死类 + 假实现 ★★

**证据**

- `app/app.py:98` `from character_manager import CharacterManager, CharacterConsistencyEngine` —— `CharacterConsistencyEngine` **仅此一处出现**，从未实例化。
- `app/character_manager.py:198` 起该类（46 行）的 4 个方法 `get_consistency_prompt` / `calculate_similarity` / `should_use_reference` / `get_seed_strategy` **全仓零调用**（含 autopilot / agent_core / 测试）。
- `calculate_similarity`（:225）：
  ```python
  def calculate_similarity(self, img1: str, img2: str) -> float:
      """计算两张图片的相似度 (简化版: 实际应使用CLIP或其他模型)"""
      # TODO: 集成实际的图像相似度计算
      return 0.85  # 默认高相似度
  ```

**影响**：不是"留待完善"，而是**看起来像功能、实际返回常数的陷阱** —— 谁把它接进一致性判断就会拿到恒为 0.85 的假相似度且不报错。`CharacterManager` 本身在用（app.py:7770+ 五个角色 CRUD 路由）。

**建议**：删除该类与对应 import。若要做角色一致性，另建实现 + 测试，不要在死代码上继续 TODO。

---

### 8. 测试覆盖偏科：持久化与安全底座零测试 ★★

**证据**：19 个测试实际 import 的后端模块约 17 个（`agent_core / comfyui_client / keyframe / llm_client / novel_to_script / prompt_memory / prompt_qc / qc_client / sfx_isolate / style_kit / tts_client / video_postprocess / watermark_cleanup / dialogue_utils / ai_config / config / dub_mix`）。**零测试引用**的模块 19 个，风险最高：

| 模块 | 行数 | 为什么该测 |
|---|---|---|
| `secret_store.py` | — | 密钥加解密 + **明文自动迁移**（幂等性、主密钥更换后的降级） |
| `task_store.py` | 464 | 任务状态机 + 断点续跑（状态写错最难排查） |
| `project_store.py` | 743 | 项目元数据读写（"迁移项目读不到剧本"这类历史缺陷就出在这里） |
| `novel_parser.py` | 560 | 解析用户上传的小说原文 |
| `nle_export.py` | 592 | 导出交付物 |
| `upscale_client.py` | 742 | 超分链路 |

**建议**：优先 `secret_store`（加密正确性 + 迁移幂等）与 `task_store`（状态迁移合法性）—— 出错时现象最隐蔽、定位成本最高。

---

### 9. 前端巨型文件未遵循自身约定 ★

**证据**：`frontend/src/pages/ProjectWorkbenchPage.tsx` **2108 行**内联定义 **9 个组件**：
`OverviewTab`(233) / `AssetCard`(610) / `AssetPreviewModal`(653) / `QcTab`(775) / `StoryboardHubTab`(949) / `KeyframesTab`(988) / `StoryboardTab`(1104) / `UpscaleTab`(1319) / `ChatPanel`(1835)

而项目**已在用**「一个 tab 一个文件」的约定：`components/AudioTab.tsx`(384)、`components/OutputReviewTab.tsx`(389)、`components/RelationGraphTab.tsx`(476) —— 只有这个大文件没遵守。

**建议**：把 9 个组件拆到 `components/tabs/`。顺带收敛 `any`：全前端 54 处，此文件与 `api/client.ts` 各占 19。

---

### 10. `app/static` 构建产物的 diff 噪声与白屏风险 ★

**证据**：`frontend/vite.config.ts:22-23` 为 `outDir: '../app/static'` + `emptyOutDir: true`；构建产物**已入库**（`git ls-files app/static` = 4 个文件），文件名带 hash → 每次构建产生「删 2 个旧 hash + 加 2 个新 hash」的 diff（当前 `git status` 正是如此）。

**影响**：`emptyOutDir: true` 会**先清空**目录 —— 构建中断或只跑了 `tsc` 未跑 `vite build`、或忘记提交时，仓库里的 `app/static` 可能停在"指向不存在资源"的半成品状态，线上直接白屏，只能靠 `git status` 发现。

**建议（可选）**：本地单实例服务无 CDN 缓存诉求，可固定入口文件名（`build.rollupOptions.output.entryFileNames` 等）→ diff 简化为"改 2 个文件"，同时消除半成品状态。若保留 hash，建议在 README 构建步骤里显式写"构建后必须 `git status` 确认 `app/static` 完整"。

---

### 11. 未使用导入 53 处 ★

**证据**：`python -m pyflakes app/*.py | grep -c "imported but unused"` = **53**。集中在 `app.py`（`math`、`flask.render_template`、`config.TASK_QUEUE_CONCURRENCY`、`llm_client` 的 4 个 config 函数、`export_manager`、`upscale_client`、`dub_mix`、`h3_prompt_kit`、`ai_memory`、`character_manager.CharacterConsistencyEngine`），以及 `video_postprocess` / `upscale_client` / `project_store` 里的 `config.*`。

**影响**：零功能风险，但制造"以为在用"的误判 —— 项目记忆里已明确记过一次同类坑（`comfyui_client` 实例 vs 模块级函数的混淆）。清理后 pyflakes 输出接近零噪声，更容易发现真问题。

**建议**：批量清理（**先排除有意为之的**，见第四节），然后把它从"只看 undefined name"放宽为"全量零告警"作为新基线。

---

### 12. 根目录堆了 15 份一次性报告 ★

**证据**：`ls *.md` = **15** 个。近乎重复的：`对标分析报告_20260915.md` / `_v2_` / `_最终版_` / `_深度版_`（4 份）、`端到端测试报告_*`（5 份），另有 `审计报告_20260916.md`、`功能模块分析报告.md`、`实施报告_优化方案落地情况.md` 等。

**影响**：新人/新会话难判断哪份是当前有效结论；被取代的版本与最新版并列，容易引用到过期内容。

**建议**：建 `docs/` 归档（`docs/audits/`、`docs/design/`），删除被取代的版本；每份头部加「**状态**: 已落地 / 部分落地 / 已废弃（被 X 取代）」。

---

### 13. 依赖未锁定 ★

**证据**：`requirements.txt` 共 9 条依赖，**全部**是 `>=`（`requests>=2.31.0`、`anthropic>=0.25.0`、`Pillow>=10.0.0`、`numpy>=1.24.0`、`opencv-python>=4.9.0` …），无上限、无 lock 文件。

**影响**：与"19 套离线测试充当回归基线"的诉求冲突 —— 隔一段时间重装可能拿到不兼容的大版本（`numpy 2.x` 对 `opencv`/`Pillow` 生态就是典型断点），此时测试失败会被误判成代码问题。

**建议**：`pip freeze > requirements.lock` 固化可用组合；或关键项改 `==`（至少给 `numpy`/`opencv-python` 加上限）。

---

## 三、建议的执行顺序

1. **第 1 项**（音频质检）—— 先做决策：实现 or 摘除。这是唯一涉及"功能可信度"的项。
2. **第 3 项**（测试统一入口）—— 30 行，之后每次改动都受益；也是后续重构的安全网。
3. **第 2 项**（质检设置 UI）—— 用户感知最强，后端接口已就绪，纯前端。
4. **第 7、11、12、10、13 项**（清理类）—— 低风险，可合并成一次"仓库卫生"提交。
5. **第 6 项**（配置缓存）—— 一处改动修 15 个调用点。
6. **第 4、5 项**（重试循环抽取 + 视频 worker 拆分）—— 收益最高但风险中等，**必须在第 3 项之后做**。
7. **第 8 项**（补 `secret_store` / `task_store` 测试）—— 长期投入，按缺陷历史排。
8. **第 9 项**（前端拆文件）—— 纯结构调整，与功能改动解耦后随时可做。

---

## 四、已核实**不是**问题的项（避免后人重复误判）

扫描中怀疑过、但**证据表明没问题**的，集中记在此以免重复排查：

- **`A = _A()` 未使用局部变量**（`app/autopilot.py:360/445/764`，pyflakes 报 3 处）：`_A()` 的定义是「取宿主模块 app，取不到就 `raise RuntimeError`」（:118-124）→ 这是**前置断言**，故意不用返回值。**非死代码，不要删。**
- **质检配置项整体健康**：31 项里除音频那 5 项外**全部有真实消费**（`enabled` 54、`max_retries` 26、`video_frame_count` 8、`api_retries` 7、`api_backoff` 8、`disable_thinking` 7、`min_tokens_when_thinking` 3、`script_categories` 2 …）。不要凭"名字看起来没人用"下结论 —— 我第一次统计时误排除了 `qc_client.py` 本身，导致 10 项被误判为"无人消费"，**必须把库内消费算进去**。
- **硬编码机器路径**：全 `app/*.py` 未发现 `C:\` / `/Users/` 之类硬编码；`127.0.0.1` 仅出现在 `config.py` 的 `COMFYUI_URL` 默认值（可被环境变量覆盖）与「本地地址不走系统代理」判断里。**可移植性 OK。**
- **异常吞噬**：`app.py` 中「`except` 后紧接 `pass`」共 6 处，逐一查看后仅 `api_keyframes_generate`（L1315/L1332）的 task_store 写入属完全静默（best-effort 语义，可接受）；其余为 `_qc_style_of` 的风格回落等有意降级。**错误处理整体健康**，不建议为"消灭 pass"机械改造。
- **参考图分配重复**：`_allocate_storyboard_refs`（`app/app.py:1614`）已被单镜重跑与批量分镜共用。**未发现重复。**
- **`.gitignore`**：密钥、`output/`、`node_modules/`、`qc_config.json` 等均已正确忽略；`frontend/src_backup/` 存在磁盘但**未入库**（本地残留，可随手删）。

---

## 五、复现方式

```bash
# 静态检查（未使用导入 / 未定义名 / f-string 缺占位符）
C:/Python314/python.exe -m pyflakes app/*.py

# 前端 API 死方法：定义 vs 页面调用
grep -rn "qcApi\." frontend/src | grep -v api/client.ts

# 配置项消费核查（⚠️ 必须含 qc_client.py 自身，否则库内消费会被误判为 0）
#   见本次使用的脚本：按 CONFIG_KEYS 逐项统计「声明区之外」的引用数

# 音频质检是否真的不存在
grep -rn "audio_enabled\|audio_prompt\|audio_min_speech_ratio" app/ frontend/src
grep -n "^def .*_ready" app/qc_client.py        # 只有 image/video/script 三个

# 热路径配置读取
grep -n "_qc_load_cfg()" app/app.py

# 超长函数度量
wc -l app/*.py | sort -rn | head

# 全套离线测试（⚠️ 当前需逐个跑，见第 3 项）
for t in h3_prompt_kit prompt_qc qc_memory style_injection gateway_failfast style_qc \
         sfx_pipeline script_audit prompt_script_opt reasoning_effort qc_prompt_feedback \
         narration_dub project_audit h3_sequential step_final_episode deliverable_register \
         reasoning_effort_http async_poll asset_skip_outcome; do
  MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_$t.py
done
```

**注意**：涉及后端的验证必须在 `MJSCXT_AUTOPILOT=0` 下运行，否则启动会恢复生产自动化。
