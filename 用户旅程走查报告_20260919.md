# 用户旅程走查报告 · 2026-09-19

**被测链路**（按用户原话）：
新建项目 → 上传小说 → 与 AI 总控沟通风格 → AI 总控自主按章节生成视频 →
用户只看成片是否符合预期 → 不符合时由用户指出哪个图片 / 场景有问题。

**方法**：真实浏览器操作（agent-browser 逐屏点击）+ 接口实证 + 产物取证 + 审计日志回溯。
全程操作线上实例 `http://127.0.0.1:5000`（未用任何测试桩）。

---

## 一、逐环节结论

| # | 环节 | 结果 | 说明 |
|---|------|------|------|
| 1 | 新建项目 | ✅ 通 | 实测建成 `E2E_journey_0919`（12:16:30），「项目名 + 上传文件 + 创建」三步即入工作台 |
| 2 | 上传小说 | ✅ 通 | 表单回显 `tongling_novel.txt 2 KB`，建项目后自动绑定 `novel_id=20260919_121630_6dee03`，原文可读 |
| 3 | 与 AI 总控沟通风格 | ⚠️ **界面通、内容错** | 流程与产品设计都对（总控先问风格再启动，还会读当前 plan 提建议），但它**按另一部小说**给方案 → 见第二节（已修） |
| 4 | 自主按章节生成 | ⚠️ 在跑但失败率高 | 24h 内 **成 28 集 / 败 18 集（≈39%）**；最近报错是 LLM 网关 `api.agnes-ai.cn` ReadTimeout（环境问题，非代码） |
| 5 | 用户看成片 | ⚠️ 能看到，但**旁白与画面对不上** | 交付物索引进 `output/final_dub/*.mp4`（有音轨，路径正确）；但配音时长是画面的 7 倍 → 大量旁白被截 → 见第三节 |
| 6 | 用户指出问题 | ⚠️ 只能粗粒度反馈 | 「输出与验收」页只有**整集 接受/打回 + 自由文本原因**；逐镜定点修在「分镜管理」页（每卡「重做分镜图 / 重做视频」），AI 总控也有 `retry_shot` 工具 —— 但验收页无入口，打回会**重跑整集** |

---

## 二、主 Bug（已修）：AI 总控把别的小说当成你的项目

### 现象
用《铜铃巷》（民国悬疑，主角陈默 / 阿棠，信物铜铃）新建项目后问
「我想用这部小说做一部竖屏短剧，你先跟我说说该定什么风格、怎么拍」，
总控回了一整篇**《蛊真人》**（暗黑仙侠，古月方源 / 血祖 / 南宫逸）的风格方案。

### 排查：先证伪「工具绑错项目」
审计日志 `output/agent/audit-20260919.jsonl` 显示该轮只调了三个工具，**全部正确绑定新项目**：

```
12:21:57  E2E_journey_0919  get_status       ok=True
12:21:57  E2E_journey_0919  get_plan         ok=True
12:21:57  E2E_journey_0919  list_characters  ok=True
```

`GET /api/autopilot/plan/E2E_journey_0919` 返回的确是**本项目**的默认 plan
（`style:""` / `enable_keyframe:false` / `upscale:true`）——与总控复述的参数逐项一致。
**所以不是读错项目。**

### 真正病根（两处叠加）
1. **总控没有任何读原著的手段。** 31 个工具里没有一个是「读本项目小说」：
   `get_plan` 只有风格与开关，`list_characters` 在新项目是空的。
   模型想知道「这部小说讲什么」，**无路可走**，只能拿上下文里出现的名字猜。
2. **`GET /api/autopilot/status?project=X` 泄漏了别人的项目。**
   该接口虽然把计数 / 交付物 / 曲线收敛到了 X，但 `current`、`last_error`
   直接来自全局状态，回显的是**当时正在生产的另一个项目** + 它的章节标题。
   实测该段落在 agent 工具结果 `_trim()`(1600 字符) 窗口的**第 151 字符**，模型看得见：

   ```
   {"checked_at": "...", "current": {"episode": 8, "phase": "start",
    "project": "蛊真人精校版", "step": "bible", ...
   ```

   于是「本项目」= 上下文里唯一的小说名 = 蛊真人。

### 修复
- `app/autopilot.py` `status(project)`：正在生产的项目 ≠ 本项目时，`current` 置空，
  只回中性标志 `other_project_running: true`，`last_error` 一并清空。
  （前端只用不带 `project` 的全局视图 → **行为不变、零回归**。）
- 新增 `GET /api/projects/<key>/novel-brief`：书名 / 章节数 / 开篇正文 / 已定风格。
  正文取样由 `config.NOVEL_BRIEF_CHARS = 800` 控制（**必须 ≤ agent 结果窗口**，
  否则正文会把字段挤出去）。
- 新增 agent 工具 `get_novel`（总控 31 → 32 个功能）。
- `SYSTEM_PROMPT` 两条纪律：谈风格 / 角色 / 题材前**必须先调 `get_novel`**；
  `other_project_running: true` 表示别的项目在跑、与本项目无关。

### 验证（清空该测试项目对话后重问原句）
| 关键词 | 修复前 | 修复后 |
|---|---|---|
| 铜铃 / 陈默 / 阿棠 / 民国 | 0 / 0 / 0 / 0 | 3 / 1 / 2 / 4 |
| 蛊真人 / 古月方源 / 血祖 | 多次 | **0 / 0 / 0** |

修复后总控开场：「我把原著摸了一遍……这是个**民国悬疑寻亲戏**，调性偏冷、偏压抑」，
并逐条引用了青石巷、绸缎庄、车马、「铃舌撞铃壁却没响」等**原著细节**；
审计日志出现 `get_novel`；面板显示「32 个功能」。

**门禁**：`verify_project_audit.py` 42 → **58 项全过**
（新增 G1–G4：含「注入外来 `_STATE['current']` 断言本项目视图不泄漏」
+ 「全局视图行为不变」的反向断言）。回归：`deliverable_register` 30/0、`async_poll` 16/0。

---

## 三、成片质量两条硬伤（未修 · 需产品决策，证据已留存）

样本：`output/final_dub/蛊真人精校版/`（每条结论都能被管线自己的报告复现）。

### 3.1 配音时长 ≫ 画面时长 → 大量旁白被 `-shortest` 截掉
| 集 | 成片时长 | 配音覆盖 | 倍率 |
|---|---|---|---|
| ep01 | 20.0s | 9.5s | 48% |
| ep02 | 115.0s | 823.9s | **717%** |
| ep03 | 105.8s | 773.3s | **731%** |
| ep04 | 85.2s | 606.4s | **712%** |

管线自己在 `*_mix_report.json` 里就写了：
> `末句结束 837.522s 超出视频时长 114.958s，超出部分会被截断`

**成因**：每个镜头的台词是**整段小说原文**（单条语音 18–35 秒），而镜头只有 4–5 秒。
用户实际观感：旁白只覆盖开头约 1/5，后面画面在推进、旁白还停在章节开头。

### 3.2 分镜时间轴与真实成片长度不一致
混音用的是**剧本名义时长**逐镜累加，而真实成片更短（部分镜头缺视频，拼接后更短）：

| 集 | 时间轴合计 | 真实成片 | 差 |
|---|---|---|---|
| ep02 | 153.5s | 115.0s | +38.6s |
| ep03 | 141.5s | 105.8s | +35.8s |
| ep04 | 116.0s | 85.2s | +30.8s |

→ 旁白时间轴整体越界，越到后面错位越严重。

### 3.3 附带发现
- `output/final/蛊真人精校版/ep01–03_final.mp4` **完全没有音轨**（默片），只有 ep04 有；
- `original_audio_kept` 仅 ep04 为 `True` —— H3 原生音效（打斗 / 雨声）在 ep01–03 被丢弃；
- `output/sfx/` 始终未生成（分离音效钩子只挂逐镜路径，整集模式走不到）。

---

## 四、建议下一步（按性价比排序）

1. **让旁白"塞得进"镜头**：给每镜台词设字数上限（按语速反推），
   或让混音把超长旁白**压缩/分句**而不是直接越界到片长之外；至少要在**出片前**用
   双向覆盖率判定拦下（`_mix_audio_qc` 已能识别 ⩾115% 越界）。
2. **验收页加"指哪打哪"**：成品页支持点某一镜 → 直接「重做分镜图 / 重做视频」，
   把现在的「打回整集 + 打字描述」升级为定点反馈（后端 `retry_shot` 已具备）。
3. **H3 原生音效别丢**：ep01–03 的 `final/` 无音轨问题需要和 `H3_EMIT_AUDIO` 策略一并看；
   整集模式的音效分离钩子补上。
4. **网关可用性**：39% 失败率主要来自 `api.agnes-ai.cn` 超时，建议在 AI 设置里做
   多上游轮换 / 熔断提示（现有熔断机制已存在，缺的是"换路"能力）。

---

## 附：本次走查产生的文件

- 走查脚本：`.workbuddy/test/e2e_journey.sh`、`e2e_journey2.sh`、`e2e_chat2.sh`
- 屏幕证据：`.workbuddy/test/_out/e2e/*.png`、`p4_text.txt`（总控回复全文）
- 对话历史备份：`.workbuddy/test/_out/e2e/chat_history.bak.json`
- 测试项目 `E2E_journey_0919` 保留在系统内（可在「项目中心」直接删除）
