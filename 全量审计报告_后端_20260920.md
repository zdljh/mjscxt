# 漫剧生成系统 · 全量审计报告（后端业务逻辑 + 代码缺陷）

**审计时间**：2026-09-20
**审计范围**：`app/` 下全部后端模块（Flask 84 条路由 + 核心 Python 模块），前端/桌面端已在前份《审计报告_20260916》覆盖
**审计人视角**：资深全栈架构师（业务漏洞 + 代码缺陷 + 架构数据流 + 数据一致性 + 边界 case 五维度）

---

## 一、项目整体业务流程简图（文本形式）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        用户入口（Web / Electron 桌面端）                │
│  上传小说（.txt/.docx/.pdf/.epub）或 选择已有小说 → 创建项目            │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 1：剧本生成（novel_to_script.py + continuity.py）              │
│  分块 → 逐块提炼 outline → 汇总 bible（角色/物品/场景）→ 逐块写分镜   │
│  截断自动提额 + 二次二分 + 网关熔断兜底 + 原文覆盖率校验 + 遗漏补生成  │
│  落盘：output/scripts/<项目键>/第N集.json                               │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 2：资产生成（comfyui_client.py）                                │
│  角色三视图 / 物品 / 场景 参考图 → ComfyUI H3 工作流 → 图片落盘        │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 3：分镜图生成（comfyui_client.py + nine_grid_storyboard.py）   │
│  每镜分镜图（首帧）+ 可选九宫格批量                                    │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 4：关键帧（keyframe.py）                                        │
│  尾帧生成（Qwen Edit 图像编辑）+ 跨镜链式串帧（auto/always/off）        │
│  首帧 = 上一镜尾帧（同场景 auto / 无条件 always / 每镜独立 off）        │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 5：视频生成（comfyui_client.py + h3_prompt_kit.py）            │
│  H3 结构化六段式提示词 → ComfyUI 视频工作流 → 视频落盘                 │
│  音轨策略：H3_EMIT_AUDIO 保留 / H3_STRIP_AUDIO 剥离（_h3_audio_policy）│
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 6：质检门禁（qc_client.py）                                     │
│  AI 质检（图片/视频/音频/剧本）→ 关键缺陷硬闸 → 风格 gate              │
│  失败自动重试（换 seed / 换提示词）→ 仍失败则隔离该镜                   │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 7：配音（tts_client.py）                                        │
│  角色音色一致性（preset / design）+ 情绪化配音 + 旁白补声              │
│  ComfyUI Qwen-TTS 节点 → 逐句合成 → 批量分批 → 降级逐句重试            │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 8：混音（dub_mix.py + video_postprocess.py）                     │
│  音轨对齐 → ffmpeg concat demuxer（-c copy）→ 音轨补齐（静音 AAC）     │
└──────────────────────────────┬────────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Stage 9：超分 + 成片（upscale_client.py + video_postprocess.py）      │
│  FlashVSR 超分（ComfyUI）→ ffmpeg 合并 → 字幕烧录 → 最终成片          │
└─────────────────────────────────────────────────────────────────────────┘

横向支撑层（贯穿全流程）：
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │  pipeline.py  │  │ autopilot.py │  │ task_store.py│  │secret_store.│
  │  步骤编排 +   │  │  无人值守     │  │  SQLite 任务 │  │  Fernet 加密│
  │  失败隔离 +   │  │  守护循环 +   │  │  断点续跑 +   │  │  密钥库     │
  │  断点续跑     │  │  自动复活     │  │  串行队列     │  │             │
  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │ agent_core.py│  │ autonomous.py│  │  llm_client  │
  │  总控 AI      │  │  一键全自动  │  │  熔断 + 重试  │
  │  function-call│  │  生产总控    │  │  思考模型支持 │
  └──────────────┘  └──────────────┘  └──────────────┘
```

---

## 二、问题分级清单

### 【严重】

#### S-01：`video_postprocess.concat_videos` 使用 `concat demuxer + -c copy`，音轨不一致时拼接错位或失败

**文件位置**：`app/video_postprocess.py` L212–243

**问题描述**：
`concat_videos` 用 ffmpeg concat demuxer（`-f concat -safe 0 -i list_file -c copy`）合并视频。
`-c copy` 意味着**不做重编码**，直接拼接容器。当输入视频音轨参数不一致（采样率、声道数、编码器不同），
拼接后音轨会错位甚至 ffmpeg 报错返回非 0。虽然 `_h3_audio_policy` 在生成阶段做了音轨策略统一，
但**用户手动上传的视频片段**（如 `/api/final/video` 重新拼接、用户混剪）不经过该策略，
直接拼进 concat list 就可能触发音轨参数不一致。

**风险影响**：
- 拼接后音画不同步，或 ffmpeg 直接报错（`-c copy` 失败）→ 成片无声或音画错位
- 用户手动上传片段绕过音轨策略时尤为严重

**修复建议**：
拼接前先用 `probe_media` 检测每个片段音轨参数，不一致时重编码统一，或改用 concat filter 重编码。

**参考修改代码片段**：
```python
def concat_videos(self, video_paths, output_path, audio_paths=None):
    # 检测音轨一致性：任一不同则走重编码路径
    probes = [probe_media(p) for p in video_paths]
    audio_params = {
        (p.get("audio_codec"), p.get("sample_rate"), p.get("channels"))
        for p in probes if p.get("has_audio")
    }
    if len(audio_params) > 1:
        # 音轨参数不一致 → 走 concat filter 重编码（可统一采样率/声道）
        return self._concat_reencode(video_paths, output_path)
    # 参数一致 → 走 concat demuxer（快，不重编码）
    ...
```

---

#### S-02：`video_postprocess.strip_audio` 原地替换用 `os.replace`，备份失败时中止但无 `os.replace` 回滚

**文件位置**：`app/video_postprocess.py` L91–136

**问题描述**：
`strip_audio` 在 `in_place=True`（默认）时：
1. 写 `tmp_out = target + ".noaudio.tmp.mp4"`
2. 成功后 `os.replace(tmp_out, target)` 原子替换原文件

如果 `tmp_out` 写入成功但 `os.replace` 前进程崩溃（如 OOM、断电），原文件仍在但 `tmp_out` 残留；
若 `os.replace` 成功但后续质检又读 `tmp_out`（已被 rename），`tmp_out` 不存在 → 静默失败。
更严重的边界：**备份 `shutil.copy2(video_path, bak)` 失败时 `report["error"]` 直接返回**（L102），
这是正确行为；但如果备份成功、`strip_audio` 的 ffmpeg 失败（`last_err`），`tmp_out` 在 L129–133 清理，
但 `os.replace` 未执行 → 原文件保留，这是正确的。真正的隐患是：
**`os.replace` 在 Windows 下目标文件若被占用（如正在被其他线程读）会 `OSError`，但代码未捕获该异常**
→ 整个 `strip_audio` 抛异常，调用方 `_h3_audio_policy` 用 `except Exception` 兜底但不会重试。

**风险影响**：
- Windows 多任务场景（autopilot 跑图 + 前端查看同一视频）时 `os.replace` 抛 `OSError`
- 原视频音轨剥离静默失败，后续拼接仍含音轨 → 成片音画错位

**修复建议**：
`os.replace` 包裹 try/except，失败时保留原文件（不清理 `tmp_out`）并在 report 中标注。

**参考修改代码片段**：
```python
if in_place:
    try:
        os.replace(tmp_out, target)
    except OSError as e:
        # Windows 文件锁：保留 tmp_out，等下次运行清理
        report["error"] = f"os.replace 失败（原文件保留，tmp 残留 {tmp_out}）：{e}"
        logger.error(report["error"])
        return report
```

---

#### S-03：`tts_client.build_dub_plan` 中 `shot_id` 转 int 可能抛 `ValueError`

**文件位置**：`app/tts_client.py` L514–515

**问题描述**：
```python
out_name = (f"ep{int(episode):02d}_shot{int(shot_id):02d}{suffix}"
            f"_{safe_name(char_name, 12)}.wav")
```
`shot_id` 在剧本 Schema 中是 `int`（`_norm_shots` 写入 `sid`），但：
1. 若用户通过 `/api/novels/<id>/convert` 路径产出的剧本里 `shot_id` 是字符串（旧版 schema）；
2. 或 `/api/storyboard/retry-shot` 重生成时 `shot_id` 未正确写入；
3. 或 coverage 补生成镜头追加时 `shot_id` 用 `str(sid)` 形式。

`int("abc")` → `ValueError`，整个 `build_dub_plan` 抛异常，配音计划无法生成。

**风险影响**：
- 配音链路完全阻断（`TTSError` 之外的 `ValueError` 不被上层兜底）
- 前端 `/api/tts/plan` 返回 500

**修复建议**：
`shot_id` 归一化：
```python
try:
    sid_int = int(shot_id)
except (TypeError, ValueError):
    sid_int = 0
out_name = f"ep{int(episode):02d}_shot{sid_int:02d}{suffix}_{safe_name(char_name, 12)}.wav"
```

---

#### S-04：`keyframe.generate_end_frame` 创建 `ComfyUIClient` 实例时未传入 `comfyui_url`，使用默认值可能与配置不同步

**文件位置**：`app/keyframe.py` L180

**问题描述**：
```python
def generate_end_frame(start_frame, prompt, out_path, seed=None, timeout=900):
    ...
    client = comfyui_client.ComfyUIClient()   # ← 未传 comfyui_url
    result = client.generate_storyboard(
        prompt_zh=prompt, ref_images=[start_frame], ...)
```
`ComfyUIClient.__init__` 默认 `comfyui_url=COMFYUI_URL`（来自 `config.py`，读环境变量）。
若用户在运行时通过环境变量覆盖了 ComfyUI 地址（如 `COMFYUI_URL=http://192.168.1.100:8188`），
`config.py` 在模块导入时已缓存该值 → 正常。
但 **`keyframe.py` 在 `generate_keyframes` 循环中每次 `comfyui_client.ComfyUIClient()` 都新建实例**，
每次实例化都会重新读 `COMFYUI_URL` 常量（模块级）→ 若 `comfyui_client` 内部有状态缓存（如 `_CALL_STATS`、
HTTP session），反复创建会丢失连接复用，且 `COMFYUI_URL` 变更后未生效。

更关键：**`generate_end_frame` 每次新建一个 `ComfyUIClient`，而 `comfyui_client` 模块已有全局实例
`app.comfyui_client`（`app.py` L182 初始化）**，两者是独立实例。
若 ComfyUI 侧排队有状态（如 `client_id`、历史 prompt 去重），两个实例互不感知。

**风险影响**：
- 连接不复用（TCP 握手开销 × 每镜一次）
- 与全局 `app.comfyui_client` 状态不共享（如队列监控、熔断）

**修复建议**：
复用全局 `app.comfyui_client` 实例，或在 `generate_keyframes` 中传入已有 client。

**参考修改代码片段**：
```python
def generate_end_frame(start_frame, prompt, out_path, seed=None, timeout=900,
                       client=None):
    if client is None:
        import comfyui_client
        client = comfyui_client.ComfyUIClient()
    ...
```
或在 `app.py` 注入：
```python
keyframe_client = comfyui_client  # 全局实例
keyframe.generate_keyframes(..., client=keyframe_client)
```

---

#### S-05：`novel_to_script._norm_shots` 中 `dialogue` 用 `_dlg_lines` 归一化，但 `NARRATION_SPEAKER` 未过滤

**文件位置**：`app/novel_to_script.py` L839

**问题描述**：
```python
"dialogue": _dlg_lines(s.get("dialogue"), chars, chars),
```
`_dlg_lines`（`dialogue_utils.normalize_lines`）在归一化时，若 `dialogue` 里 `speaker` 为 `"旁白"`
（TTS 旁白补声生成的 `NARRATION_SPEAKER`），而 `chars`（角色表）不含 `"旁白"`，
则 `_dlg_lines` 会把 `speaker` 设为空或丢弃该行。

但 `tts_client.build_dub_plan` 在 `dlg_rows` 为空时，用 `NARRATION_SPEAKER` 补声
（L484–487），此时 `char_name = NARRATION_SPEAKER`，`default_voice_map` 用 `stable_seed` 为
`"旁白"` 生成固定 seed → 正常。
问题在于：**`_norm_shots` 把 `dialogue` 归一化后写进剧本 JSON 落盘，`"旁白"` 这个 speaker 被丢弃**，
下游 `build_dub_plan` 读 `dialogue` 字段发现为空 → 走旁白补声路径 → 正常。
**但如果用户手动编辑剧本 JSON 加了 `"旁白"` 台词，`_norm_shots` 会把它丢掉**（因为不在 `chars` 里）。

**风险影响**：
- 用户手动编辑剧本添加旁白台词时被静默丢弃
- 配音链路读不到该旁白 → 该镜无声

**修复建议**：
`_dlg_lines` 归一化时，保留 `NARRATION_SPEAKER` 不被丢弃：
```python
# dialogue_utils.normalize_lines 里增加对 NARRATION_SPEAKER 的白名单
NARRATION_WHITELIST = ("旁白", "Narration", "narration")
if speaker in NARRATION_WHITELIST:
    # 保留旁白行（不进角色表）
    rows.append({"speaker": speaker, "text": text})
```

---

#### S-06：`app.py` 多处文件路由用 `os.path.normpath(filename)` 防目录穿越，但 `normpath` 在 Windows 下 `/` 和 `\` 混用可能失效

**文件位置**：`app/app.py` L3437、L3449、L3461、L5038、L6568 等所有 `send_file` 路由

**问题描述**：
```python
safe_path = os.path.normpath(filename)
if safe_path.startswith('..'):
    abort(403)
filepath = os.path.join(PROJECT_OUTPUT_DIR, "assets", safe_path)
```
`os.path.normpath` 在 Linux 下 `a/../../etc/passwd` → `../etc/passwd` → 以 `..` 开头 → 拦截 ✓
在 Windows 下 `..\..\..\Windows\System32\cmd.exe` → `normpath` 保留 `..\..\..\Windows\System32\cmd.exe`
→ `startswith('..')` → 拦截 ✓

但 **`<path:filename>` 在 Flask 路由中是 URL 解码后的值**，若前端传 `%2e%2e%5c%2e%2e%5cWindows`（URL 编码的 `..\..\Windows`），
Werkzeug 解码后变成 `..\..\Windows`，`normpath` 在 Windows 下处理 `\` 为分隔符 → 路径穿越仍可能绕过：
- `safe_path = os.path.normpath("..\\..\\Windows")` → `..\\..\\Windows` → `startswith('..')` → 拦截 ✓

真正隐患：**`os.path.join(PROJECT_OUTPUT_DIR, "assets", safe_path)` 中 `safe_path` 若含绝对路径前缀**
（如 `/etc/passwd` 在 Linux 或 `C:\Windows` 在 Windows），`os.path.join` 会**丢弃前几段**直接返回绝对路径。
`normpath` 不会把 `C:\Windows` 识别为 `..` 开头 → 穿越成功。

**风险影响**：
- 目录穿越读取系统任意文件（`/etc/passwd`、`C:\Windows\...`）
- 虽然本地部署（`127.0.0.1:5000`）风险较低，但 Electron 打包后若前端被注入可触发

**修复建议**：
用 `send_from_directory`（Werkzeug 自带目录穿越防护），或手动校验：
```python
from werkzeug.utils import secure_filename
def _safe_serve(base_dir, filename):
    base = os.path.abspath(base_dir)
    target = os.path.abspath(os.path.join(base, filename))
    if not target.startswith(base + os.sep):
        abort(403)
    return send_file(target, conditional=True)
```

---

### 【一般】

#### G-01：`novel_to_script.build_chunks` 零丢弃合并只处理尾块，未处理中间块

**文件位置**：`app/novel_to_script.py` L131–149

**问题描述**：
```python
if len(chunks) >= 2:
    merged = []
    for c in chunks:
        if merged and len(c["text"]) < MIN_CHUNK_CHARS:
            prev = merged[-1]
            prev["text"] = (prev["text"] + "\n" + c["text"]).strip()
            ...
        else:
            merged.append(c)
    if len(merged) >= 2 and len(merged[0]["text"]) < MIN_CHUNK_CHARS:
        merged[1]["text"] = (merged[0]["text"] + "\n" + merged[1]["text"]).strip()
        ...
```
尾块（最后一个）过短并入前一块；首块过短并入后一块。
**但中间块过短时只并入前一块，若前一块也是过短的（两个连续 < 300 字的块），合并后可能仍 < 300 字**，
不影响正确性但可能产生"碎片块"送模型（`MAX_CHARS_PER_CHUNK_PROMPT = 3400`，300 字远低于上限，
模型浪费 context 但不出错）。

**风险影响**：低。中间碎片块仍会被模型处理，只是分块粒度不均匀。

**修复建议**：
循环合并时若 `merged[-1]["text"]` 合并后仍 < `MIN_CHUNK_CHARS`，继续合并 `merged[-2]`（如存在）。

---

#### G-02：`novel_to_script.extract_chunk_outline` 截断二分递归无 `LLMError` 兜底

**文件位置**：`app/novel_to_script.py` L361–372

**问题描述**：
```python
except LLMTruncatedError:
    subs = _split_chunk_in_half(chunk) if depth < CHAPTER_SPLIT_MAX_DEPTH else []
    if not subs:
        raise LLMTruncatedError(...)
    return _merge_outlines(
        [extract_chunk_outline(client, s, novel_title, events, depth + 1) for s in subs], chunk)
```
二分递归中若子块再次抛 `LLMTruncatedError`（depth < 3 时继续二分），
**二分到 depth=3 且 `_split_chunk_in_half` 返回 `[]` 时抛 `LLMTruncatedError`**，
调用方 `convert_novel_to_script`（L965–970）捕获 `LLMError` 后降级为空 outline：
```python
except LLMError as e:
    warnings.append(f"第 {chunk['index']} 块大纲提炼失败（改用空大纲...）")
```
这是**正确兜底**（outline 非关键路径，分镜阶段仍全量送原文）。
但 `convert_chapter_to_script`（L1269–1278）捕获 `LLMTruncatedError` 后 `continue`，
`outlines` 列表可能全空 → L1281 抛 `LLMError("本章所有子块提炼均失败...")` → 整集失败。
**与 `convert_novel_to_script` 的行为不一致**：整本书路径允许全空降级，章节路径则硬失败。

**风险影响**：
- 章节路径（`convert_chapter_to_script`）在模型连续失败时整集中止
- 整本书路径（`convert_novel_to_script`）则降级继续

**修复建议**：
统一行为：`convert_chapter_to_script` 全空时降级为"按原文逐句兜底生成"而非硬失败。

---

#### G-03：`tts_client.synthesize_batch` 中 `keep_model_loaded` 只在最后一句时 `unload_model_after_generate`，但批量失败后未恢复模型

**文件位置**：`app/tts_client.py` L656、L671

**问题描述**：
```python
unload = not self.params.get("keep_model_loaded", False) and is_last
```
`is_last = (i == len(items) - 1)`，即最后一句卸载模型。
若 `synthesize_batch` 中 ComfyUI 执行到第 N 句时失败（`_wait` 抛 `TTSError`），
**模型已被前 N-1 句加载但未卸载**（因为未走到最后一句）→ GPU 显存被占用。
`synthesize_lines` 降级逐句重试时，再次调用 `synthesize_batch([one])`，
此时 `is_last=True`（单句）→ `unload=True` → 卸载模型。
**但如果降级重试也失败且未走 `synthesize_batch`（直接 `res.append({"ok": False, ...})`）**，
模型仍未卸载。

**风险影响**：
- GPU 显存泄漏（长期 autopilot 运行下累积）
- 后续任务 OOM 或排队等待

**修复建议**：
`synthesize_batch` 用 try/finally 确保模型卸载：
```python
def synthesize_batch(self, items, ...):
    prompt_id = self._submit(prompt)
    try:
        history = self._wait(prompt_id, timeout, ...)
        ...
    finally:
        # 无论成功失败都卸载模型（最后一句节点已 unload，但前面失败需补卸载）
        if not self.params.get("keep_model_loaded"):
            self._unload_model()  # 新增：调 ComfyUI /unmodel 或 触发模型卸载工作流
```

---

#### G-04：`video_postprocess.add_subtitles` 中 SRT 文件名可能含特殊字符（中文/空格），未转义

**文件位置**：`app/video_postprocess.py` L260–272

**问题描述**：
```python
srt_file = output_path + ".srt"
...
srt_dir = os.path.dirname(os.path.abspath(srt_file))
srt_name = os.path.basename(srt_file)
filter_arg = "subtitles=filename='" + srt_name.replace("'", r"\'") + "'"
```
`output_path` 来自 `project_name`（用户可控），若含空格、单引号、中文：
- 空格：`srt_name` 含空格，ffmpeg 滤镜参数 `filename='My Project.srt'` → ffmpeg 可能解析失败
- 单引号：`replace("'", r"\'")` 已处理 ✓
- 中文：ffmpeg 默认 ASCII 文件系统下中文文件名可能找不到

代码注释已提到"路径里的中文/空格也一并规避"（切 cwd 到 SRT 目录），
但 **`cwd` 切换只在 Windows 下规避反斜杠问题，Linux/macOS 下中文路径仍可能因编码问题失败**。

**风险影响**：
- 含中文项目名的视频字幕烧录失败
- 空格项目名在 ffmpeg 滤镜解析时可能失败

**修复建议**：
SRT 文件统一用 ASCII 安全名（如 `subtitle.tmp.srt`），生成后再 `os.rename` 回原名，
或在滤镜参数中用 `\ ` 转义空格（ffmpeg 滤镜语法）。

---

#### G-05：`keyframe.plan_keyframes` 链式首帧镜像 `start_frame_link_path` 未处理 `shot_id` 格式不匹配

**文件位置**：`app/keyframe.py` L249–257、L371

**问题描述**：
```python
mirror = start_frame_link_path(keyframes_dir, seq)
if os.path.isfile(mirror):
    start, chained = mirror, True
elif os.path.isfile(chain_src):
    start, chained = chain_src, True
```
`start_frame_link_path(keyframes_dir, seq)` 生成 `shot_{seq:02d}_start.png`。
`_seq(shot_id)` 把 `shot_id` 归一为整数：`"shot_01"` → 1、`"1"` → 1、`1` → 1。
但若 `shot_id` 是 `"shot_100"`（超过 99 镜），`_seq` 提取数字 `100` → `shot_100_start.png` ✓。
若 `shot_id` 是 `"abc"`（非法），`_seq` 返回 `0` → `shot_00_start.png`。
**`shot_00_start.png` 与 `shot_01_start.png` 不同**，但 L371 `_mirror(start, start_frame_link_path(keyframes_dir, seq))`
把实际首帧镜像到 `shot_00_start.png` → 下一镜（`shot_01`）链式时找 `shot_00_end.png`（`chain_src`）
而非 `shot_00_start.png`（`mirror`）→ 行为不一致。

**风险影响**：
- 低。仅在 `shot_id` 非数字（极端输入）时触发，正常剧本 `shot_id` 是连续整数。

**修复建议**：
`_seq` 返回 `0` 时跳过链式（不串帧），避免 `shot_00` 命名歧义。

---

#### G-06：`app.py` L7112 `@app.errorhandler(BadRequest)` 返回 HTML 错误页（已修复，但需确认覆盖所有 4xx）

**文件位置**：`app/app.py` L7112

**问题描述**：
前份审计报告已修复 `request.get_json()` → `get_json(silent=True)`（15 处）+
`@app.errorhandler(BadRequest)` 返回 JSON 400。
但 **`<path:filename>` 路由中 `send_file` 抛 `NotFound`（404）时**，
若 Flask 默认错误页是 HTML，前端无法解析。

**风险影响**：
- 文件不存在时返回 HTML 404 而非 JSON 404
- 前端 `fetch` 解析失败 → 控制台报错

**修复建议**：
增加 `@app.errorhandler(404)` 返回 JSON：
```python
@app.errorhandler(404)
def _not_found(e):
    return jsonify({"success": False, "error": "Not Found"}), 404
```

---

#### G-07：`novel_to_script._split_chunk_in_half` 中 `re.finditer` 在句末标点处截断，若句末标点恰在 `mid` 后很远则碎片不均

**文件位置**：`app/novel_to_script.py` L293–298

**问题描述**：
```python
mid = len(text) // 2
cut = mid
for m in re.finditer(r"[。！？；!?;\n]", text):
    if m.end() >= mid:
        cut = m.end()
        break
```
若 `text[:mid]` 内无句末标点（如长段无标点的散文），`finditer` 遍历到 `text[mid:]` 中第一个标点，
`cut` 可能距 `mid` 很远（最坏情况：`text[:mid]` 全文无标点 → `cut` 到 `text` 末尾附近）
→ 二分极不均匀（`text[:cut]` 远大于 `text[cut:]`），递归深度耗尽后仍被截断。

**风险影响**：低。`CHAPTER_SPLIT_MAX_DEPTH = 3` 层后 `MIN_CHARS = 400` 硬切兜底。

**修复建议**：
`finditer` 只搜 `text[:mid+500]` 范围（中点附近 500 字内），找不到则按 `mid` 硬切。

---

### 【优化】

#### O-01：`app.py` L182 全局 `comfyui_client = ComfyUIClient()` 在 `import app` 时初始化，与 `agent_core.bind_app` 耦合

**文件位置**：`app/app.py` L180–183

**问题描述**：
```python
script_gen = ScriptGenerator()
comfyui_client = ComfyUIClient()
video_processor = VideoPostProcessor()
```
这三个全局对象在 `import app` 时立即创建。若 `ComfyUIClient.__init__` 内有 HTTP 探测
（确认 ComfyUI 在线），则**短命脚本 `python -c "import app"` 会阻塞探测 ComfyUI**。
前份报告已提到 `MJSCXT_AUTOPILOT=0` 逃生口（autopilot），但 `ComfyUIClient()` 构造本身可能也需防护。

**风险影响**：
- `import app` 耗时增加（ComfyUI 离线时探测超时）
- 短命批处理脚本被拖累

**修复建议**：
`ComfyUIClient` 构造改为懒初始化（首次方法调用时才探测），或 `app.py` 用延迟导入。

---

#### O-02：`tts_client.check_environment` 每次调用都 `urlopen` 到 `/object_info`，无缓存

**文件位置**：`app/tts_client.py` L179–232

**问题描述**：
`check_environment` 返回 `available` 状态。若前端频繁轮询（如 `/api/tts/env` 每 10s 刷新），
每次 `urllib.request.urlopen` 到 ComfyUI `/object_info`（可能 60s 超时）→ 拖慢前端响应。

**风险影响**：
- 前端轮询 TTS 环境时阻塞

**修复建议**：
加 30s 内存缓存（`_TTS_ENV_CACHE`），TTL 过期后重新探测。

---

#### O-03：`video_postprocess.probe_media` 对每个文件 `ffprobe`（超时 120s），无并发控制

**文件位置**：`app/video_postprocess.py` L21–60

**问题描述**：
`generate_final_video` 对每个视频片段调 `probe_media`，100 个片段 → 100 次 `ffprobe`（各 120s 超时）。
若 ffmpeg 缺失或文件损坏，每次 120s 超时 → 最长 20 分钟阻塞。

**风险影响**：
- 大批量片段时拼接阶段长时间阻塞

**修复建议**：
`probe_media` 失败立即返回（已做 ✓ `returncode != 0` 时 `return info`），
但 `ffprobe` 缺失（`FileNotFoundError`）应提前 `shutil.which("ffprobe")` 检测，避免逐文件 `subprocess` 异常。

---

#### O-04：`keyframe.generate_keyframes` 串行逐镜生成，无并发但无失败重试

**文件位置**：`app/keyframe.py` L348–413

**问题描述**：
`generate_keyframes` 串行生成尾帧（设计如此：单 GPU），但单镜失败后不重试。
若 ComfyUI 临时超时（如 OOM），该镜永久失败 → 后续链式镜全部退化为"首帧回退到自己分镜图"。

**风险影响**：
- 单镜 OOM 导致后续所有镜链式断裂

**修复建议**：
失败时重试 1–2 次（换 seed），仍失败则记录但继续。

---

## 三、高风险问题清单（优先修复项）

| 序号 | 问题 | 文件 | 影响 | 优先级 |
|------|------|------|------|--------|
| S-01 | `concat_videos` 音轨参数不一致时 `-c copy` 拼接失败/错位 | `video_postprocess.py` | 成片无声/音画错位 | P0 |
| S-06 | `send_file` 目录穿越（`os.path.join` 绝对路径丢弃前缀） | `app.py` 多处 | 读取系统任意文件 | P0 |
| S-03 | `build_dub_plan` `int(shot_id)` `ValueError` | `tts_client.py` | 配音链路 500 | P0 |
| S-04 | `keyframe` 每镜新建 `ComfyUIClient`，不复用全局实例 | `keyframe.py` | 连接泄漏 / 状态不同步 | P1 |
| S-05 | `_norm_shots` 丢弃 `"旁白"` speaker | `novel_to_script.py` | 用户手动旁白被静默删 | P1 |
| S-02 | `strip_audio` `os.replace` Windows 文件锁未兜底 | `video_postprocess.py` | 音轨剥离静默失败 | P1 |
| G-01 | `build_chunks` 中间碎片块未进一步合并 | `novel_to_script.py` | 分块粒度不均 | P2 |
| G-02 | 章节路径 vs 整本书路径失败行为不一致 | `novel_to_script.py` | 章节路径硬失败 | P2 |
| G-03 | `synthesize_batch` 模型未卸载（失败路径） | `tts_client.py` | GPU 显存泄漏 | P1 |
| G-04 | `add_subtitles` SRT 文件名含空格/中文 | `video_postprocess.py` | 字幕烧录失败 | P2 |
| G-05 | `keyframe` `_seq` 返回 0 时链式命名歧义 | `keyframe.py` | 极端输入下链式断裂 | P2 |
| G-06 | `404` 返回 HTML 而非 JSON | `app.py` | 前端解析失败 | P2 |
| G-07 | `_split_chunk_in_half` 无标点时二分歧点远 | `novel_to_script.py` | 递归深度耗尽 | P2 |

---

## 四、修复建议汇总（按优先级）

### P0（立即修复）

1. **S-01 音轨一致性检测**：`concat_videos` 拼接前 `probe_media` 每个片段，音轨参数不一致时走 concat filter 重编码。
2. **S-06 目录穿越防护**：所有 `send_file` 路由改用 `send_from_directory` 或 `os.path.abspath` 前缀校验。
3. **S-03 shot_id 归一化**：`build_dub_plan` 中 `int(shot_id)` 包裹 try/except，失败给 `0`。

### P1（尽快修复）

4. **S-04 复用全局 ComfyUIClient**：`keyframe` 接收注入的 client，不再每镜新建。
5. **S-05 旁白 speaker 白名单**：`dialogue_utils` 保留 `"旁白"` 行不被丢弃。
6. **S-02 os.replace 兜底**：`strip_audio` 的 `os.replace` 包 try/except，失败保留原文件。
7. **G-03 模型卸载兜底**：`synthesize_batch` 加 `finally` 卸载模型。

### P2（排期修复）

8. **G-01 碎片块再合并**
9. **G-02 章节路径降级行为对齐**
10. **G-04 SRT 文件名 ASCII 化**
11. **G-05 `_seq` 返回 0 跳过链式**
12. **G-06 404 返回 JSON**
13. **G-07 二分搜索范围限制**
