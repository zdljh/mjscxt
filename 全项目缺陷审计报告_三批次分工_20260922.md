# 漫剧生成系统 · 全项目缺陷审计报告（按批次分工）

**审计日期**：2026-09-22
**审计角色**：资深 AIGC 漫剧项目架构师
**代码基线**：`zdljh/mjscxt` HEAD `405998c`（`origin/main` 零分叉）；另含工作区**未提交改动**（`app/app.py` / `app/qc_client.py` / `app/serve.py` 修改 + 新增 `app/host_guard.py` + 交付面文件删除）
**审计范围**：全项目端到端流水线；重点四大模块（提示词生成 / 图片生成 / 视频生成 / AI 质检）+ 全局状态机 / 模块调用链路 / 数据流转 / 并发 / 异常处理
**审计方式**：静态代码审计（离线只读，未起服务、未触生产）

> ⚠️ **口径说明（重要）**：本次审计**逐行核实**后发现，此前多轮审计（2026-09-19 ~ 09-21）列出的绝大多数 P0/P1 陷阱**已修复到位**（第 4 节列出「已修复、勿重复修」清单及证据行号）。本报告**只报真实剩余缺陷**，不虚构 P0。

---

## 1. 项目整体流程简述 & 风险总览

### 1.1 端到端流水线（文本流程图）

```
[原著/大纲] ──▶ ①小说解析 novel_parser / ②剧本生成 novel_to_script+script_generator
                    │  · 产出 shots[]（含 description / dialogue / camera / characters_in_shot / audio_cues）
                    │  · 剧本阶段**不写 prompt_h3**（生成期统一 resolve_h3_prompt 兜底）
                    ▼
            [剧本预检 + 一致性/连贯性] prompt_qc / consistency / continuity
                    ▼
   ③ 资产（角色/道具/场景）app._generate_asset_task
        · 基础图(正视图) → 多视角 → 每张图**逐张 AI 质检** → 不达标换 seed 重画 → 阻断不写正式目录
        · 产物：output/assets/{characters|items|scenes}/<项目>/<资产名>/base.png 等
                    ▼
   ④ 分镜图 h3? 否 → comfyui_client.generate_storyboard（Qwen-Edit 参考图编辑型）
        · 参考图最多 3 槽（主角锚点 / 次角或道具 / 场景），槽位>参考图时复用主角并登记
        · 逐镜独立 try/except + 断点续跑（overwrite=False 时已达标镜跳过）
        · 质检达标才 move 到正式目录；不达标只留 scratch
                    ▼
   ⑤ 尾帧 keyframe.py（链式模式下 = 下一镜首帧）
        · 先写 scratch → 质检通过才 move 到 keyframes/（S7）
        · 质检「不可判定」不重试；G1 止损（连续两次缺陷相同）
                    ▼
   ⑥ 视频生成 comfyui_client.generate_h3_sequence[_sequential]
        · H3 一次产出整集（video_mode=episode）或逐镜；六段式 Ref2VA 提示词
        · 整片 QC 门控 + 换随机种子重试 + G1 止损 + P1-18 不可判定不重试
                    ▼
   ⑦ 超分 upscale_client → ⑧ 配音 tts_client → ⑨ 混音 dub_mix（anullsrc 定长底轨）
                    ▼
   ⑩ 成片合成 pipeline.step_final（整集直接采用整集视频 / 逐镜 concat）
                    ▼
   ⑪ 水印清理 video_watermark / watermark_cleanup → ⑫ 交付 nle_export / export_manager
```

**步骤编排与断点续跑**：`app/pipeline.py` 定义 `step_*` + `probe_*`；autopilot 守护线程按步推进，`probe_*.done` 命中则整步跳过。

### 1.2 风险总览

| 维度 | 结论 |
|------|------|
| 历史 P0/P1 修复率 | **高**——2026-09-19~21 三批审计的 P0/P1 绝大多数已落地（逐条证据见第 4 节） |
| 本次新增 P0 | **2 条**（整集 QC 异常白烧、资产无断点续跑全量重烧） |
| 本次新增 P1 | **4 条**（并发落盘、止损状态泄漏、提示词静默截断、超长提示词无上限） |
| 本次新增 P2 | **5 条**（队列无背压、返回契约脆弱、静默吞异常、超时缺省、遗留字段） |
| 并发安全 | 主力落盘模块（project_store/ai_config/ai_chat/qc_client）已唯一化+加锁；**仍有一批固定 `.tmp` 零锁点**（见 P0-1/P1-1） |
| 异常处理 | fail-closed 口径在分镜/资产/视频/尾帧已统一；**仍有 48 处 `except: pass` 静默吞** |
| 幂等/重试 | 分镜/尾帧/视频均有上限+止损；**资产链路仍无 skip-existing**（P0-2） |

---

## 2. 缺陷分级

> 固定字段：`【优先级】【类型】【模块】【文件】`+ 问题描述 / 风险影响 / 复现条件 / 修复方案 / 参考代码 / 负责人建议。
> 类型标注：`代码bug` / `业务流程逻辑缺陷` / `架构稳定性隐患`。

---

### 🔴 P0 阻断级（必须第一批修复）

#### D-01　整集视频 QC 回调「抛异常」时换种子重跑整集（白烧整集 GPU）

- **【优先级】** P0
- **【类型】** 代码bug（异常分支绕过止损）
- **【模块】** 视频生成 / AI 质检
- **【文件】** `app/comfyui_client.py:1862-1897`（`generate_h3_sequence_sequential` 的 QC except 分支）
- **问题描述**：整片 QC 分支里，`qc_fn(...)` **抛异常**（非返回 `ok=False`）时执行 `continue` → 进入下一轮 attempt → `cur_seed = random.randint(...)` → **重新生成整集**。此路径**不会**调用 `qc_stop_cb`，因此整集止损失效。
- **风险影响**：质检接口/回调一旦出现异常（如 `_qc_gate` 抛、`qc_client.check_video` 内部未捕获的异常），会把 20~44 段 H3 整集**每次重跑一遍**，单次几十分钟 GPU，`max_retries` 轮次全白烧——正是 P1-18 要防的「大量 API/GPU 浪费」，但 P1-18 只覆盖了 `verdict.ok=False`，**漏掉了「回调抛异常」这条路**。
- **复现条件**：开启整集视频质检（`video_mode=episode` + `qc.enabled/video_enabled=true`），使 `qc_fn` 抛出任意异常（如质检网关 5xx 导致内部未捕获、或 `_qc_gate` 输入异常）→ 观察日志出现「第 N 次 QC 调用异常」后仍继续下一轮整集重跑。
- **修复方案**：QC 异常视同「不可判定」，**不得**换种子重跑。在 `except Exception as qc_err:` 分支内改为「记录 + 直接 break」，或调用 `qc_stop_cb` 后再决定是否停；口径与 `_ep_qc_stop_cb` 的 `unavailable` 一致。
  ```python
  # app/comfyui_client.py:1867 附近 —— 现状
  except Exception as qc_err:
      logger.warning(f"[H3-episode] 第 {attempt + 1} 次 QC 调用异常: {qc_err}")
      qc_results.append({"attempt": attempt + 1, "passed": None,
                         "reason": f"QC 异常: {qc_err}"})
      continue          # ← BUG：换种子重跑整集，且绕过止损
  ```
  ```python
  # 建议改为
  except Exception as qc_err:
      logger.warning(f"[H3-episode] 第 {attempt + 1} 次 QC 调用异常（不可判定，不重试）: {qc_err}")
      qc_results.append({"attempt": attempt + 1, "passed": None, "unavailable": True,
                         "reason": f"QC 异常（不可判定，不重试）: {qc_err}"})
      break             # 保留 best_file，交给上层按不可判定处置
  ```
- **【负责人建议】** 开发A（P0）
- **【验收标准】** 构造 `qc_fn` 抛异常 → 日志只出现一次「QC 调用异常」，**不再有第 2 次整集生成**；`attempts_used==1`。

#### D-02　资产生成无 skip-existing：每次触发都全量重烧（断点续跑缺失）

- **【优先级】** P0
- **【类型】** 业务流程逻辑缺陷（幂等性缺失）
- **【模块】** 图片生成（资产链路）
- **【文件】** `app/app.py:2377`（`_generate_asset_task` 签名无 `overwrite`）+ `app/app.py:2428-2432`（循环体内无「已达标则跳过」）；对照 `app/app.py:3021-3083`（分镜 `_storyboard_worker` 有 `overwrite` 参数 + `if not overwrite and os.path.isfile(dst)…: continue`）
- **问题描述**：分镜链路已支持断点续跑（已达标镜跳过），但**资产链路没有**：`_generate_asset_task` 一旦被调用，就对传入的 `assets` 列表里**每一个**资产重新「生成→质检→重画」，完全不检查 `output/assets/<类型>/<项目>/<名>/base.png` 是否已存在且已过质检。
- **风险影响**：① 补跑 1 个失败资产要重烧该项目**全部**资产（几十张图 × 每张基础图+多视角），**大量 API/GPU 浪费**；② 与「正式目录有图 ⟺ 已过质检」的断点续跑设计不一致，用户在 UI 上点一次「生成资产」就全量重做，成本不可控。
- **复现条件**：项目已有 30 个资产全部达标 → 再次调用 `/api/generate/assets` 只传 1 个缺失资产名（或传全量）→ 观察日志逐个「生成基础图」而非跳过。
- **修复方案**：给 `_generate_asset_task` 增加 `overwrite: bool = False`；循环体开头（`for i, asset in enumerate(assets):` 之后、生成之前）加：
  ```python
  # app/app.py:2428 循环体开头
  if not overwrite and _asset_ready(base_dir, project_name, name):
      results.append({"name": name, "status": "skipped",
                      "reason": "已达标入库，断点续跑跳过"})
      continue
  ```
  其中 `_asset_ready` 复用模块级 `_first_existing_asset_image`（与 `probe_assets` 同口径，避免判据漂移）；API 层 `api_generate_assets` 透传 `data.get('overwrite')`。
- **【负责人建议】** 开发A（P0）
- **【验收标准】** 资产全部达标后二次调用 → 日志显示全部 `skipped`，**零次** ComfyUI 调用；只缺 1 个资产时仅生成该 1 个。

---

### 🟠 P1 严重级（第二批修复）

#### D-03　14 处「固定 `.tmp` + 零锁」并发落盘（可致索引/状态文件被截断损坏）

- **【优先级】** P1
- **【类型】** 架构稳定性隐患（并发写 / 原子性）
- **【模块】** 全局落盘层（autopilot / autonomous / pipeline / continuity / consistency / llm_client / secret_store / app 交付面）
- **【文件】**（均为 `tmp = path + ".tmp"` 形式，**进程内多线程 + 多入口可同时写同一 path**）
  - `app/autopilot.py:147-152` `_write_json`（autopilot 运行时状态）
  - `app/autonomous.py:58-64` `_write_state`（一键全自动状态）
  - `app/continuity.py:106-112` `save_json`（连贯性报告，**连 try/except 都没有**）
  - `app/consistency.py:381-389` `save_report`（一致性报告）
  - `app/pipeline.py:481`、`:1596`、`:1642`、`:1676`（**交付物索引 `deliverables.json`**，写点最密集）
  - `app/llm_client.py:217`、`:273`（LLM 配置）
  - `app/secret_store.py:423`（遗留明文密钥迁移回写）
  - `app/app.py:3342`（分镜 manifest）、`:8427`/`:8454`（配音 manifest / 配音产物）
- **问题描述**：这些点全部写「同一个固定临时文件名 `<path>.tmp`」且**不加任何锁**。对照已修复的现代实现（`app/qc_client.py:57`、`app/project_store.py:262`、`app/ai_config.py:186`）是 `<path>.<pid>.<tid>.<urandom(3)>.tmp` —— 唯一临时名 + `fsync` + Windows 占用退避。
  两个写入者（例：autopilot 守护线程推进步骤写 `deliverables.json` 的同时，用户在验收页点「打回」触发 `set_review`）会**打开同一个 `.tmp`**：A 写一半 → B 截断重头写 → A 的 `os.replace(tmp, path)` 把 **B 的半个文件** 换成正本；或 A 写完后 B 的 `os.replace` 再从已不存在的 tmp 读取 → 抛异常吞掉。
- **风险影响**：① 索引文件被截断 → **用户所有项目 / 交付物记录"消失"**（见 D-04，解析失败静默返回空，无告警无回退）；② autopilot 运行时状态损坏 → 暂停态/进度错乱；③ 本机无 fsync，断电/强杀会留下 0 字节正本。属于「低概率触发、触发即数据丢失」的高危类型。
- **复现条件**：并发压测。两个 shell 同时循环调用写 `deliverables.json` 的两个入口（`mark_deliverable` / `set_review`）各 500 次 → `json.load` 最终失败率 > 0；或直接把 `app/pipeline.py:481` 那段抽成函数双线程对撞。
- **修复方案**：**统一收口到一个模块级原子写函数**（推荐把 `qc_client._atomic_write_json` 抽到 `app/fs_atomic.py`，全库复用），替换上述 14 处：
  ```python
  # app/fs_atomic.py（新建）
  def atomic_write_json(path: str, data, *, indent: int = 2) -> None:
      d = os.path.dirname(os.path.abspath(path)) or "."
      os.makedirs(d, exist_ok=True)
      tmp = f"{path}.{os.getpid()}.{threading.get_ident()}.{os.urandom(3).hex()}.tmp"
      with open(tmp, "w", encoding="utf-8") as f:
          json.dump(data, f, ensure_ascii=False, indent=indent)
          f.flush(); os.fsync(f.fileno())      # 缺这行 = 断电留 0 字节
      for i in range(5):                        # Windows：杀毒/索引器偶发占用
          try:
              os.replace(tmp, path); return
          except PermissionError:
              time.sleep(0.05 * (i + 1))
      os.replace(tmp, path)                     # 最后一次不再吞异常
  ```
  写点的 `try/finally` 里必须 `os.remove(tmp)` 兜底（现状 `pipeline.py:1596/1642/1676` 是**裸写、无 try**，异常会留残 tmp 且直接 500）。
- **【负责人建议】** 开发A（P1，与 D-04 同批）
- **【验收标准】** ① `grep -rn 'tmp = path + "\.tmp"' app/*.py` 结果为空（`app.py:3342/8427/8454` 三处亦清零）；② 并发对撞脚本 1000 次 `json.load` 全部成功；③ 新增离线测试 `verify_atomic_write.py`：注掉 `fsync` 时应能观察到「强杀后 0 字节」，加回即消失。

#### D-04　索引/报告解析失败**静默返回空**（fail-open，"项目全部消失"且零告警）

- **【优先级】** P1
- **【类型】** 代码bug（异常处理口径错误）
- **【模块】** 全局落盘层 / 项目列表 / 交付面
- **【文件】**
  - `app/pipeline.py:470-474`（`deliverables.json` 读失败 → `logger.warning` + `return`，**静默当作"没有交付物"**）
  - `app/autopilot.py:152-160` `_read_json`（读失败 → 返回 `default`）
  - `app/autonomous.py:48-55` `_read_state`（`except Exception: return {}`）
  - `app/continuity.py:100-103`（读失败 → `return default`）
  - `app/consistency.py:395-403` `load_report`（`return None`）
  - 项目索引相关：`app/project_store.py` `load_index()`（解析失败静默空索引 → **用户所有项目消失**）
- **问题描述**：与 D-03 是**同一根因的两端**——写端可能写坏文件，读端又把「读坏了/解析不了」与「本来就没有」当成同一件事，返回空/默认值继续跑。既没有 `.bak` 回退，也没有把「文件存在但解析失败」升级为 error。
- **风险影响**：一次 D-03 的并发损坏 → 项目列表页显示「暂无项目」；交付页显示「无可验收成片」；autopilot 以为「这项目还没做过」→ **从第一步重新生产**（叠加 D-02 的全量重烧）。表现为「数据凭空消失且无任何日志线索」，排障成本极高。
- **复现条件**：把任一 `deliverables.json` / `projects.json` 内容改成 `{`（半截 JSON）→ 重启后端 → 项目列表为空，日志**只有一条 warning 或什么都没有**。
- **修复方案**：读端区分三态并 fail-loud：
  ```python
  def _load_json_strict(path: str, default):
      if not os.path.isfile(path):
          return default                      # 真的不存在 → 正常默认
      try:
          with open(path, "r", encoding="utf-8") as f:
              return json.load(f)
      except Exception as e:
          bak = path + ".bak"
          if os.path.isfile(bak):             # 有备份 → 救回来并告警
              logger.error("索引解析失败，已从 .bak 恢复：%s（%s）", path, e)
              shutil.copy2(bak, path)
              try:
                  with open(path, "r", encoding="utf-8") as f:
                      return json.load(f)
              except Exception:
                  pass
          logger.error("索引损坏且无备份，拒绝静默清空：%s（%s）", path, e)
          raise                              # 让上层显式报错，绝不当成「没有」
  ```
  同时 D-03 的原子写在 `os.replace` 前把旧正本 `copy2` 成 `.bak`（保留 1 份即可）。**项目索引这类"清空即灾难"的文件，宁可 500 也不能返回空。**
- **【负责人建议】** 开发A（P1，与 D-03 同批）
- **【验收标准】** ① 人为写坏 `deliverables.json` → 有 `.bak` 时自动恢复且 `logger.error`；无 `.bak` 时接口返回 5xx 且**界面不显示"暂无项目"**；② 新增回归 `verify_index_failloud.py` 覆盖三态（不存在/可解析/损坏）。

#### D-05　整集视频 QC **双重降采样**（12 镜摘要 + 最多 6 帧）→ 崩坏镜必然漏检

- **【优先级】** P1
- **【类型】** 业务流程逻辑缺陷（质检覆盖率不足）
- **【模块】** AI 质检（视频）/ 视频生成
- **【文件】**
  - `app/app.py:5880-5898` `_episode_qc_desc(shots, limit=12)` —— 40 镜的整集只描述前 12 镜，其余写「…（其余 N 镜略）」
  - `app/app.py:3809-3813` `_seg_qc_fn` 调 `qc_client.check_video(...)` **不传抽帧数** → 落到默认
  - `app/qc_client.py:2157` `extract_frames(..., cfg.get("video_frame_count", 3), ...)`
  - `app/qc_client.py:606` 默认 `video_frame_count = 3`；`:853` 校验区间 `(1, 6)` **硬上限 6**
- **问题描述**：整集模式（`video_mode=episode`，也是**默认**模式）一次产出 20~44 段拼接的整片。质检时：文本侧只给模型「前 12 镜」摘要；图像侧按 `video_frame_count` 抽帧（默认 3、**配置上限 6**），且抽帧策略是「全片 5%~95% 均布」——即 3 帧 = 5% / 50% / 95% 三个时间点。于是**整集中段几十段完全没有采样**，模型拿 3 张无关的帧去判「这 40 个镜头里的角色有没有崩坏」。
- **风险影响**：① 整集质检**形同虚设**：中段某几段脸崩/穿模/纯色块，抽帧根本没抽到 → `passed=True` → 崩坏片直接进正式目录被动/超分/交付，**漏检**；② 反过来，模型在 3 帧上看到一点瑕疵就判整集不达标 → 叠加 D-01 的异常重跑路径 → 整集白烧。**这是"质检模块名存实亡"的根因**，比单个 bug 更影响成片质量。
- **复现条件**：`video_mode=episode`，让某一集的第 20 段（中段）生成一张明显崩坏的图（可临时替换该段输入图）→ 跑整集质检 → 观察 `passed=True` 且 `qc.history[-1].frames` 只有 3 张、时间戳集中在首/中/尾。
- **修复方案**：三选一（按实现成本排序，**推荐前两条同时做**）：
  1. **按段抽帧**：整集模式下抽帧数 = `min(段数, 12)`，且时间点取**每段中点**而非全片均布；`video_frame_count` 上限从 6 放宽到 24（仅整集路径），或给 `check_video` 增加 `frame_ts: list[float]` 参数由调用方按段边界传入。
  2. **摘要不再截断**：`_episode_qc_desc` 去掉 `limit=12`（或提到 60），改为**每镜一行极简**（`镜N[景别] 角色`）以控长度；`limit` 命中时**必须**在返回串里显式声明「仅给出前 12 镜，其余未提供」——让模型知道信息不完整，而不是以为整集只有 12 镜。
  3. **分段质检（最稳）**：整集 QC 先用 ffmpeg 按段边界切出 N 个子片段，逐段 `check_video`（3 帧/段），再对整片做一次全局时长/黑场客观层。成本高但漏检率最低。
  ```python
  # app/app.py:3809 _seg_qc_fn 内 —— 建议
  seg_n = max(1, len(segs))
  frame_n = max(3, min(seg_n, 12))       # 按段数抬抽帧
  verdict = qc_client.check_video(video_path, _episode_qc_desc(shots, limit=60), cfg,
                                  frames_dir=fr_dir, style=style,
                                  frame_count=frame_n,          # 新增参数
                                  expected_duration=sum(...))
  ```
- **【负责人建议】** 开发B（P1）
- **【验收标准】** ① 40 段整集的 `qc.history[-1].frames` ≥ 12 张且时间戳覆盖各段中点（相邻间距 < 单段时长×1.5）；② 中段注入崩坏图 → 整集 QC `passed=False`（当前必为 True）；③ 摘要串在镜数 > limit 时包含「其余未提供」字样。

#### D-06　提示词 / 质检描述**无长度上限**，`merge_detail` 整段并入 → 超长静默截断丢关键词

- **【优先级】** P1
- **【类型】** 代码bug（缺失边界防护）
- **【模块】** 提示词生成
- **【文件】**
  - `app/h3_prompt_kit.py:467-497` `resolve()` —— 决策只看 `validate()` 的**段落齐不齐**，**完全不看长度**
  - `app/h3_prompt_kit.py:440-465` `merge_detail()` —— `extra = f"补充画面细节：{detail}。"` **整段无截断**追加进 `detailed_description`
  - `app/h3_prompt_kit.py:272-335` `build_detailed_description()` 的 `detail` 入参无 clamp
  - `app/comfyui_client.py:2117-2142` `resolve_h3_prompt()` 透传，无长度守卫
  - 对照：全库 `grep -nE 'len\(prompt\)|len\(text\)' app/*.py` **仅 `app.py:9350` 一处**（且是 160 字的入参校验，与提示词链路无关）
- **问题描述**：H3 提示词由剧本 LLM 产出的 `description / visual_detail / dialogue / audio_cues` 拼接而成，长度完全不受约束；`build_*` 系列只做格式化、不做长度控制。`resolve()` 在「既有 `prompt_h3` 不合规」时走 `merge_detail(built, existing)`，把**整段旧文本**（历史裸英文提示词可能上千字符）原样塞进 `detailed_description`。
- **风险影响**：① 超长提示词提交到 H3 文本节点 → 超出模型上下文被**服务端静默截断**，被截掉的多是**末尾的 `overall_soundscape` / `non_diegetic_music` / 后段动作**（因为拼接顺序固定）→ 音效氛围丢失、动作"演一半"、且**日志无任何提示**，属于"关键词丢失"的典型形态；② 整集模式下 `_seg_qc_fn` 的原始 `shot_desc` 是几十段六段式拼接（文档已注明"又长又难判读"），虽已改走摘要，但 `_record_qc_lesson(..., "\n".join(seg.prompt))` 仍把全量拼接写入教训库 → 教训库条目膨胀、召回相似度失真。
- **复现条件**：构造 `shot["description"]` 5000 字 + `visual_detail` 5000 字 → `h3_prompt_kit.resolve(shot, [])` → 输出 > 10000 字符，无告警、无截断标记；日志中 `prompt_h3 结构不合规` 之后直接提交。
- **修复方案**：在 `h3_prompt_kit` 增加**显式、可观测**的长度闸门，宁可软截断并告警，也不要静默超限：
  ```python
  MAX_PROMPT_CHARS = 6000          # 需与 H3 单段文本节点实际上下文对齐后调整
  MAX_DETAIL_CHARS = 800           # merge_detail 单次并入上限

  def _clamp(text: str, limit: int, label: str) -> str:
      if len(text) <= limit:
          return text
      logger.warning("提示词超长截断：%s %d→%d 字符（原文以 …[截断] 标记）",
                     label, len(text), limit)
      return text[:limit] + "…[截断]"

  # merge_detail 内
  detail = _clamp(str(detail or "").strip(), MAX_DETAIL_CHARS, "merge_detail.detail")
  # resolve 返回前统一兜底
  out = _clamp(out, MAX_PROMPT_CHARS, "prompt_h3")
  ```
  同步修 `_record_qc_lesson` 的入参：传「按相似度召回的 Top-N 段」而非全量拼接（或同样 clamp 到 2000 字符）。
- **【负责人建议】** 开发B（P1）
- **【验收标准】** ① 超长输入（>6000）→ 输出 ≤ 6000 且末尾含 `…[截断]`，日志有 warning；② `merge_detail` 并入 >800 字 detail → 被截断且告警；③ 新增 `verify_prompt_clamp.py` 覆盖「刚好等于上限 / 超 1 字 / 超 100 倍」三个边界。

---

### 🟡 P2 优化级（第三批修复）

#### D-07　任务队列 `submit` 无背压、无 `task_id` 去重（同任务可被执行两次）

- **【优先级】** P2
- **【类型】** 架构稳定性隐患（资源 / 幂等）
- **【模块】** 全局任务调度
- **【文件】** `app/task_store.py:341`（`self._q: list = []`，无 `maxlen`）、`:350-354` `submit()`、`:399-430` `_worker()`
- **问题描述**：`submit()` 只做 `self._q.append(...)` + `notify()`：① **无长度上限**，队列可无限增长；② **不检查 `task_id` 是否已在队列/执行中**，同一 `task_id` 连续提交两次就会入队两条，在 `concurrency > 1` 时**并发执行同一个任务**（两遍 GPU 生成、同一输出目录互写）。当前 `concurrency` 默认 1 掩盖了第 ② 点，但 `status()` 已对外暴露 `queued`，说明设计上允许多并发。
- **风险影响**：① 用户连点/前端重试 → 队列堆积，`status().queued` 疯涨，内存与磁盘（暂存区）双重膨胀；② 一旦有人把 `concurrency` 调到 >1，`task_id` 重复提交即触发**同任务并发写同一输出目录**（与 D-03 叠加放大损坏概率）。
- **复现条件**：`q.submit("t1", fn)` 连调两次，`fn` 内 `sleep(1)` 并打印 → 观察 `fn` 被执行 2 次、`store` 对同一 `task_id` 先 `finish` 后又被 `start` 覆盖。
- **修复方案**：
  ```python
  def __init__(self, store, concurrency=1, max_queue: int = 64):
      ...
      self._max_queue = int(max_queue)
      self._pending_ids: set = set()

  def submit(self, task_id, fn, on_error=None) -> bool:
      with self._cv:
          if task_id in self._pending_ids:
              logger.warning("任务已在队列/执行中，忽略重复提交：%s", task_id)
              return False
          if len(self._q) >= self._max_queue:
              logger.error("任务队列已满（%d），拒绝提交：%s", self._max_queue, task_id)
              return False
          self._pending_ids.add(task_id)
          self._q.append((task_id, fn, on_error))
          self._cv.notify()
          return True
  # _worker 的 finally 里：self._pending_ids.discard(task_id)
  ```
  API 层对 `submit` 返回 `False` 给出 409/「任务进行中」提示，而不是静默排队。
- **【负责人建议】** 开发C（P2）
- **【验收标准】** ① 同 `task_id` 连提 3 次 → 只执行 1 次，其余返回 `False` 且日志有 warning；② 队列到 `max_queue` 后提交被拒且返回明确错误；③ `concurrency=2` 下并发提交不同 `task_id` 各自只跑一次。

#### D-08　`comfyui_client.generate_video()` 遗留死代码（2156 行文件内的第 1640 行）

- **【优先级】** P2
- **【类型】** 架构稳定性隐患（技术债 / 误用风险）
- **【模块】** 视频生成
- **【文件】** `app/comfyui_client.py:1640-1720`
- **问题描述**：`def generate_video(...)` 已在 docstring 自我标注 `[LEGACY · 已停用]`，语义是「固定 10 段模板的单次生成」；`grep -rn 'generate_video(' app/*.py frontend/src` **只命中定义行，零调用者**（整库仅此一处，包括前端）。属于确认无调用链可达的死代码。
- **风险影响**：① 语义陷阱——函数名叫 `generate_video` 却"一次产出 10 段拼接长视频"，新同学按名字调用会**每个分镜跑 10 段**（docstring 里正是这么警告的）；② 它保留了一份独立的工作流/段数注入逻辑，与 `generate_h3_sequence` 双轨，改 H3 相关逻辑时容易漏改这一份。
- **复现条件**：静态确认即可（无调用者）。若担心动态调用，可临时在函数首行 `raise RuntimeError("legacy")` 跑一轮全量离线测试，全绿即确认死代码。
- **修复方案**：优先**删除**。若因历史项目兼容必须保留，则：① 函数名加 `_` 前缀（`_legacy_generate_video`）明确私有；② 首行加 `logger.warning("[LEGACY] generate_video 已停用，请使用 generate_h3_sequence")`；③ 在 `verify_project_audit.py` 里加守卫，断言「除定义外零引用」不"回潮"。
- **【负责人建议】** 开发C（P2）
- **【验收标准】** ① `grep -rn 'generate_video(' app/ frontend/src` 无输出（或仅 `_legacy_` 前缀的定义行）；② 全量离线测试绿；③ `verify_project_audit.py` 新增该守卫通过。

#### D-09　静默吞异常集中整治（47 处多行 `except: pass`，其中 6 处有实质风险）

- **【优先级】** P2
- **【类型】** 代码bug（异常捕获缺陷）
- **【模块】** 全局异常处理
- **【文件】**（全量位置已由 `grep -rnE 'except' app/*.py -A1 | grep -B1 pass` 枚举；**高风险子集**）
  - `app/llm_client.py:268`（`except Exception: pass` 吞掉「明文密钥迁移至加密库」失败 → 密钥看似已加密实则未迁移，**静默漏安全动作**）
  - `app/llm_client.py:285`、`app/comfyui_client.py:85`（`_bump` 调用统计外的兜底，吞掉后无任何线索）
  - `app/app.py:1551`、`:1568`（尾帧回调链路）
  - `app/app.py:5665`、`:5674`（工作流指纹计算——已带注释说明"不影响主体"，**属合理**）
  - 其余多数为 `except OSError: pass` 的**临时文件清理**（合理：清理失败不该阻断生产）
- **问题描述**：项目约定「临时文件清理失败可静默」，但这条约定被**过度外延**到了非清理场景。`llm_client.py:268` 吞掉的是**安全动作失败**（明文密钥未迁移），这是最应该 fail-loud 的一类；`comfyui_client.py:85` 吞掉的是调用统计，长期静默会让「调用次数/成本」统计悄悄失真。
- **风险影响**：排障时"没有任何日志"的黑洞；密钥迁移失败被视为成功 → 明文密钥长期驻留 `llm_config.json`（该文件已被 gitignore，但仍是本地明文暴露面）。
- **复现条件**：装一个 `cryptography` 版本不兼容的环境（或临时把 `secret_store` 置为不可用）→ 启动后端 → 日志无任何异常，但 `llm_config.json` 里 `api_key` 明文仍在。
- **修复方案**：按三类分治，**不做无差别替换**：
  1. **清理类**（`os.remove` / `shutil` 收尾）→ 保持静默，但统一改成 `except OSError: logger.debug(...)`，留痕不给用户看。
  2. **安全 / 配置类**（`llm_client.py:268/285`、`secret_store` 相关）→ 改为 `logger.error` + 向上抛出或标记 `migrated=False`，让调用方知道密钥未加密。
  3. **统计 / 指纹类**（`comfyui_client.py:85`、`app.py:5665/5674`）→ `logger.warning` 一次性留痕，保持不阻断。
  ```python
  # llm_client.py:268 —— 现状
  except Exception:  # noqa: BLE001
      pass
  # 建议
  except Exception as e:  # noqa: BLE001
      logger.error("LLM 明文密钥迁移加密库失败，明文将保留在 %s：%s",
                   config_path, e)
  ```
- **【负责人建议】** 开发C（P2）
- **【验收标准】** ① `grep -cE 'except[^:]*:\s*$' -A1 app/*.py | grep pass` 计数从 47 降到 ≤ 10（仅剩清理类，且均已改 `logger.debug`）；② `llm_client` 密钥迁移失败场景日志出现 `error` 级别且 `api_key` 字段不被清空（不假装成功）；③ 全量离线测试绿。

#### D-10　`concat_videos` 返回 `""` 的隐式契约（已缓解，列为观察项）

- **【优先级】** P2
- **【类型】** 架构稳定性隐患（契约脆弱）
- **【模块】** 视频后期 / 成片合成
- **【文件】** `app/video_postprocess.py:308-345`（`concat_videos`，docstring 已明确「任何失败一律返回 `""`，不抛异常」）
- **问题描述**：用「空字符串」同时表达「失败」「无输出」两种语义，且**不抛异常**——调用方一旦漏判空，就会把 `""` 当成合法路径继续传给 ffmpeg，报出与真实原因无关的错误。审计核实：`video_postprocess.py:601`（`if not self.concat_videos(video_files, output_path)`）与 `pipeline.py:876-878`（concat 后 `if not _nonempty(tmp): raise PipelineError`）**两处调用点均已判空**（且 pipeline 在 concat 前已 `os.remove(tmp)`，不存在旧文件兜底通过的问题），因此当前**不构成缺陷**，仅列为结构性风险。
- **风险影响**：未来新增调用点若忘记判空 → 错误信息丢失真实原因（`""` 无法携带失败理由），排障困难。
- **复现条件**：不适用（当前无触发路径）。
- **修复方案（择机）**：保持返回契约不变（改动风险大于收益），改为**在返回前把失败原因挂到日志并带上调用方标识**，即 `logger.error("concat_videos 失败：%s", reason)`；或在 docstring 的调用点清单处补一条「新增调用点必须在 `verify_project_audit.py` 登记」，让守卫自动检查新调用点是否判空。
- **【负责人建议】** 开发C（P2，可选）
- **【验收标准】** 失败路径日志包含具体 ffmpeg 原因；`verify_project_audit.py` 能断言所有 `concat_videos` 调用点均对返回值判空。

#### D-11　ComfyUI 输出目录只增不减（图片 `copy2` 不清源）+ `narration` 遗留字段

- **【优先级】** P2
- **【类型】** 架构稳定性隐患（资源泄漏 / 技术债）
- **【模块】** 图片生成 / 剧本数据模型
- **【文件】**
  - `app/app.py:2581`、`:2585` `shutil.copy2(base_attempts[-1]["file"], base_dst)` / `shutil.copy2(scratch_base, base_dst)` —— **copy 而非 move**，源文件留在暂存区与 ComfyUI 输出目录
  - `app/app.py:5570-5620` `_qc_prune_attempts` —— **只清 `output/qc/...scratch`**，不涉及 `COMFYUI_OUTPUT_DIR`
  - `app/h3_prompt_kit.py:217-244`、`app/coverage.py:113-119`、`app/dialogue_utils.py:216-222`、`app/novel_to_script.py:936` —— `narration` 旧字段读取
- **问题描述**：
  1. 图片走 `copy2`（视频走 `move`），且 `_qc_prune_attempts` 的作用域**不含** `COMFYUI_OUTPUT_DIR` → 每次资产生成/分镜/重试都在 ComfyUI output 留一份原图，**只增不减**。历史记录：脚本暂存区曾膨胀到 1.1GB（G8 已治），但 ComfyUI 侧未纳入治理。
  2. `narration` 是 2026-09-19 前的旧剧本字段，代码里 4 处**刻意保留读取**（均带注释说明"只为兼容改造前项目"），新剧本恒为空。这**不是 bug**，但属于长期技术债：字段语义已废弃却仍在 `_beats()` 里参与拼接（`h3_prompt_kit.py:243-244`），未来若有人往剧本里重新写 `narration` 会被静默消费。
- **风险影响**：① 磁盘随生产线性增长，长期跑满（历史上已有一次 80GB 写满 C 盘的 ffmpeg 事故，说明磁盘是本项目的真实风险面）；② `narration` 的"兼容读取"没有可见的收敛计划。
- **复现条件**：连续跑 3 轮资产 + 分镜生成 → `du -sh $COMFYUI_OUTPUT_DIR` 单调增长且无任何回收。
- **修复方案**：
  1. `_qc_prune_attempts` 推广为**通用滚动清理**，对 `COMFYUI_OUTPUT_DIR/comic_drama*` 下的 `*_retry*` / `*_try*` 命名产物同样按组保留最近 N 个；或在 `probe_*` 步骤收尾时对「已被 copy 进正式目录」的源文件做 `os.remove`（**注意**：必须是"确认正式目录已有同内容副本"后再删，不能盲目删；建议加 `mtime > 24h` 双保险）。
  2. `narration`：在 `dialogue_utils._check()` 已有的 `narration_line_count` 统计基础上，加一条**一次性迁移/告警**——若某项目仍产出 `narration`，日志 warning 提示"该剧本为改造前版本，建议重跑剧本"；并在 `novel_to_script` 的 schema 里把该字段标记 `deprecated`。
- **【负责人建议】** 开发C（P2）
- **【验收标准】** ① 连续跑 3 轮后 `COMFYUI_OUTPUT_DIR` 增量 < 单轮新增量的 30%（有回收）；② 删除动作仅作用于「正式目录已有同内容副本且 mtime > 24h」的文件，回归测试断言正式产物零删除；③ 仍产出 `narration` 的项目，日志出现 warning。

---

## 3. 批次划分（下发用）

> 划分原则：**同文件/同根因不拆给两个人**（避免冲突）；P0 与 P1 合并为「批次2」由**开发A**主攻（其共同根因是"异常/幂等边界"，同一套心智模型）；图片/视频链路相关由**开发B**；任务队列、全局异常、技术债由**开发C**。

### 批次1　审计与冻结（已完成，无需下开发）

| 项 | 状态 |
|---|---|
| 拉取 GitHub 最新代码、核对分叉 | ✅ `origin/main` HEAD `405998c`，零分叉 |
| 逐行核实历史 P0/P1 修复状态 | ✅ 见第 4 节「已修复、勿重复修」清单 |
| 定位真实剩余缺陷 11 条 | ✅ 见第 2 节 D-01 ~ D-11 |

### 批次2　开发A：全部 P0 + 全部 P1（5 条，**最高优先，插队修复**）

| 编号 | 级别 | 标题 | 主文件 |
|---|---|---|---|
| D-01 | P0 | 整集视频 QC 回调抛异常时换种子重跑整集 | `app/comfyui_client.py:1862-1897` |
| D-02 | P0 | 资产生成无 skip-existing，全量重烧 | `app/app.py:2377`、`2428-2432` |
| D-03 | P1 | 14 处固定 `.tmp` 零锁并发落盘 | autopilot/autonomous/pipeline/continuity/consistency/llm_client/secret_store/app |
| D-04 | P1 | 索引解析失败静默返回空（fail-open） | `app/project_store.py`、`app/pipeline.py:470-474` 等 |
| D-06 | P1 | 提示词 / 质检描述无长度上限，超长静默截断 | `app/h3_prompt_kit.py:440-497` |

> D-03 与 D-04 是**同一根因（写坏 + 读错）**，必须同一人同一改，一并交付；建议同批新增 `app/fs_atomic.py` 作为唯一落盘入口。

### 批次3　开发B：质检覆盖率与生成链路（1 条 P1）

| 编号 | 级别 | 标题 | 主文件 |
|---|---|---|---|
| D-05 | P1 | 整集视频 QC 双重降采样（12 镜摘要 + ≤6 帧）致崩坏漏检 | `app/app.py:3809`、`:5880`；`app/qc_client.py:2157`、`:606`、`:853` |

> 本批次**只有一条**但影响成片质量上限（质检是否有效），建议给足设计时间；若开发A的 D-06 先落地（提示词长度闸门），D-05 的摘要改造可直接复用其 clamp 工具函数。

### 批次4　开发C：P2 优化级（5 条，可与批次2/3并行，不阻塞上线）

| 编号 | 级别 | 标题 | 主文件 |
|---|---|---|---|
| D-07 | P2 | 任务队列 submit 无背压、无 task_id 去重 | `app/task_store.py:341`、`:350` |
| D-08 | P2 | `generate_video()` 遗留死代码（零调用者） | `app/comfyui_client.py:1640` |
| D-09 | P2 | 47 处静默吞异常集中整治（6 处高风险） | `app/llm_client.py:268/285`、`app/comfyui_client.py:85` 等 |
| D-10 | P2 | `concat_videos` 返回 `""` 契约脆弱（已缓解，观察项） | `app/video_postprocess.py:308` |
| D-11 | P2 | ComfyUI 输出目录只增不减 + `narration` 遗留字段 | `app/app.py:2581/2585/5570`；`app/h3_prompt_kit.py:217` |

---

## 4. 批次任务汇总表（可直接复制下发）

### 📋 开发A —— D-01 / D-02 / D-03 / D-04 / D-06（全部 P0 + 95% P1）

```
【任务包 A · 优先级最高，请插队修复】
分支建议：fix/batch-a-boundary  （基线 origin/main @ 405998c）

A-1 [P0] 整集视频 QC 回调抛异常时不得换种子重跑整集
    位置：app/comfyui_client.py:1862-1897（generate_h3_sequence_sequential 的 QC except 分支）
    动作：except Exception 分支内 `continue` → 改 `break`，并给 qc_results 打 unavailable=True；
          口径对齐 app/app.py:3850 _ep_qc_stop_cb 的「不可判定不重试」。
    验收：qc_fn 抛异常时，日志只出现 1 次「QC 调用异常」，attempts_used == 1。

A-2 [P0] 资产链路补断点续跑（skip-existing）
    位置：app/app.py:2377 _generate_asset_task 增加 overwrite: bool = False
          app/app.py:2428 循环体开头加 `if not overwrite and _asset_ready(...): skipped; continue`
          api_generate_assets 透传 data.get('overwrite')
    判据：复用模块级 _first_existing_asset_image（app/app.py:283），与 probe_assets 同口径。
    验收：资产全达标后二次调用 → 全部 skipped、零次 ComfyUI 调用；缺 1 个只生成 1 个。

A-3 [P1] 固定 .tmp 零锁并发落盘 → 统一原子写
    位置（14 处，全部改为唯一临时名 + fsync + 占用退避）：
      app/autopilot.py:147  _write_json
      app/autonomous.py:58 _write_state
      app/continuity.py:106 save_json（当前连 try 都没有）
      app/consistency.py:381 save_report
      app/pipeline.py:481 / 1596 / 1642 / 1676（交付物索引）
      app/llm_client.py:217 / 273
      app/secret_store.py:423
      app/app.py:3342（分镜 manifest）/ 8427 / 8454（配音）
    动作：新建 app/fs_atomic.py 提供 atomic_write_json()，全库复用（参考 qc_client.py:57）。
    验收：`grep -rn 'tmp = path + "\.tmp"' app/*.py` 为空；
          并发对撞脚本 1000 次 json.load 全成功。

A-4 [P1] 索引解析失败不得静默返回空（fail-open → fail-loud）
    位置：app/project_store.py（load_index 解析失败静默空索引 = 用户所有项目消失）
          app/pipeline.py:470-474、app/autopilot.py:152、app/autonomous.py:48、
          app/continuity.py:100、app/consistency.py:395
    动作：区分「不存在 / 可解析 / 损坏」三态；损坏时优先从 .bak 恢复并 logger.error；
          无备份则 raise（宁可 5xx，绝不返回空）。
          A-3 的原子写负责在 replace 前把旧正本存一份 .bak。
    验收：人为写坏 deliverables.json → 有 .bak 自动恢复 + error 日志；无 .bak 返回 5xx 且界面不显示"暂无项目"。

A-5 [P1] 提示词 / 质检描述长度闸门
    位置：app/h3_prompt_kit.py:440-497（merge_detail 无截断、resolve 不看长度）
          app/comfyui_client.py:2117-2142（透传无守卫）
          app/app.py _record_qc_lesson 的 "\n".join(seg.prompt) 全量拼接
    动作：加 MAX_PROMPT_CHARS / MAX_DETAIL_CHARS + _clamp()，超限软截断并 logger.warning；
          教训库入参改 Top-N 段或 clamp 到 2000 字符。
    验收：>6000 字输入 → 输出 ≤6000 且含 "…[截断]" + warning 日志；边界三例（=上限/超1/超100倍）测试通过。

交付要求：每个条目单独 commit；提交前跑
  MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/<相关脚本>.py
  C:/Users/liujianghua/WorkBuddy/.../envs/mjscxt/Scripts/python.exe -m pyflakes app/*.py（只看 undefined name）
```

### 📋 开发B —— D-05（质检覆盖率）

```
【任务包 B · 质检有效性专项】
分支建议：fix/batch-b-qc-coverage  （基线 origin/main @ 405998c）

B-1 [P1] 整集视频 QC 双重降采样导致崩坏镜漏检
    位置：app/app.py:5880  _episode_qc_desc(shots, limit=12)   ← 40 镜只描述 12 镜
          app/app.py:3809  _seg_qc_fn 调 check_video 未传抽帧数
          app/qc_client.py:2157 extract_frames(..., cfg.get("video_frame_count", 3))
          app/qc_client.py:606 默认 3；app/qc_client.py:853 校验区间硬上限 6
    方案（推荐 1+2 同时做，3 为可选加固）：
      1) 抽帧按段：整集抽帧数 = max(3, min(段数, 12))，时间点取每段中点；
         给 check_video 增加帧数/时间戳入参，整集路径不受 6 帧上限约束。
      2) 摘要不截断：_episode_qc_desc 去掉 limit=12（或提到 60），每镜一行极简；
         仍在截断时必须显式声明「其余 N 镜未提供」。
      3) 可选：整集先按段边界切子片段分段质检，再对整片跑一次客观层（时长/黑场）。
    验收：
      ① 40 段整集 qc.history[-1].frames ≥ 12 张，时间戳覆盖各段中点（相邻间距 < 单段时长×1.5）；
      ② 中段注入崩坏图 → 整集 QC passed=False（修复前必为 True，这是核心判据）；
      ③ 摘要串在镜数 > limit 时包含「其余未提供」字样。

交付要求：同 A 的提交与自检规范；另需把「中段崩坏注入」用例固化为离线测试 verify_episode_qc_coverage.py。
```

### 📋 开发C —— D-07 / D-08 / D-09 / D-10 / D-11（全部 P2，不阻塞上线）

```
【任务包 C · 稳定性与技术债】
分支建议：fix/batch-c-hardening  （基线 origin/main @ 405998c，可与 A/B 并行）

C-1 [P2] 任务队列背压 + task_id 去重
    位置：app/task_store.py:341（self._q 无 maxlen）、:350 submit、:399 _worker
    动作：加 max_queue（默认 64）+ _pending_ids 集合；submit 返回 bool；
          重复提交打 warning 并返回 False；worker 的 finally 里 discard。
    验收：同 task_id 连提 3 次只执行 1 次；队列满拒绝且返回明确错误；concurrency=2 下不同 task_id 各跑一次。

C-2 [P2] 删除遗留死代码
    位置：app/comfyui_client.py:1640 generate_video（[LEGACY 已停用]，全库零调用者）
    动作：优先删除；若需保留则改名 _legacy_generate_video + 首行 warning，
          并在 verify_project_audit.py 加「零引用不回潮」守卫。
    验收：grep -rn 'generate_video(' app/ frontend/src 无输出；全量离线测试绿。

C-3 [P2] 静默吞异常分治（47 处）
    位置：高风险子集 app/llm_client.py:268（吞掉明文密钥迁移失败）、:285、app/comfyui_client.py:85、app/app.py:1551/1568
    动作：清理类 → logger.debug；安全/配置类 → logger.error + 不假装成功；
          统计/指纹类 → logger.warning 一次性留痕（app.py:5665/5674 已合理，不动）。
    验收：多行 except:pass 计数 47 → ≤10（仅剩清理类且带 debug 日志）；
          llm_client 密钥迁移失败时出现 error 日志且 api_key 不被清空。

C-4 [P2] concat_videos 空串契约（观察项）
    位置：app/video_postprocess.py:308-345（两处调用点已判空，当前无缺陷）
    动作：失败路径 logger.error 带具体 ffmpeg 原因；verify_project_audit.py 断言所有调用点判空。
    验收：失败日志含真实原因；新调用点漏判空能被守卫发现。

C-5 [P2] ComfyUI 输出目录回收 + narration 技术债
    位置：app/app.py:2581/2585（shutil.copy2 不清源）、:5570 _qc_prune_attempts（仅覆盖 qc scratch）
          app/h3_prompt_kit.py:217-244、app/coverage.py:113、app/dialogue_utils.py:216、app/novel_to_script.py:936
    动作：把滚动清理推广到 COMFYUI_OUTPUT_DIR 的 *_retry* / *_try* 命名产物；
          删除仅限「正式目录已有同内容副本 且 mtime > 24h」；
          对仍产出 narration 的项目打 warning + 在 novel_to_script schema 标 deprecated。
    验收：连续 3 轮后 COMFYUI_OUTPUT_DIR 增量 < 单轮新增量 30%；正式产物零删除（回归断言）；
          旧剧本项目日志出现 narration warning。

交付要求：同 A/B 的提交与自检规范；C 批不阻塞上线，但每个条目必须有独立回归脚本。
```

---

## 5. 修复验收标准（逐条判断条件）

> 通用门槛（**每一条修复都必须同时满足**）：① 相关离线测试脚本 `MJSCXT_AUTOPILOT=0` 全绿、exit 0；② `python -m pyflakes app/*.py` 无新增 `undefined name`；③ 未引入 `config.XXX` 选择性导入陷阱；④ 提交未使用 `git add -A`，`app/static/` 与新增测试脚本入库。

| 编号 | 验收判断条件（可自动化） | 反例（判定未修复） |
|---|---|---|
| **D-01** | 构造 `qc_fn` 抛异常 → 日志「QC 调用异常」仅 1 次；`qc_results` 长度 1；`unavailable=True`；无第 2 次整集生成 | 日志出现 ≥2 次异常或第 2 次积分生成 |
| **D-02** | 资产全达标后二次调用 → `results` 全 `skipped`；ComfyUI 提交数 == 0；仅缺 1 个时提交数 == 1 | 仍逐个输出「生成基础图」日志 |
| **D-03** | `grep -rn 'tmp = path + "\.tmp"' app/*.py` 无输出；并发 1000 次写读 `json.load` 全成功；新 `verify_atomic_write.py` 绿 | 仍有任意一处固定 `.tmp`；并发出现 `JSONDecodeError` |
| **D-04** | 写坏索引 → 有 `.bak` 自动恢复 + `logger.error`；无 `.bak` 接口 5xx 且 UI **不显示「暂无项目」** | UI 显示空列表 / 静默清空 / 从第一步重跑 |
| **D-05** | 40 段整集 `qc.frames ≥ 12` 且覆盖各段中点；中段崩坏 → `passed=False`；摘要含「其余未提供」 | 帧数 ≤6 或中段崩坏仍 `passed=True` |
| **D-06** | >6000 字输入 → 输出 ≤6000 且末尾 `…[截断]` + warning；边界三例测试通过 | 输出 >6000 且无任何告警/标记 |
| **D-07** | 同 `task_id` 连提 3 次仅执行 1 次、其余返回 `False` + warning；队列满拒绝；`concurrency=2` 下无重复执行 | 同任务被执行 2 次 |
| **D-08** | `grep -rn 'generate_video(' app/ frontend/src` 无输出（或仅 `_legacy_` 定义行）；守卫测试绿 | 旧函数仍以原名零引用存在 |
| **D-09** | 多行 `except: pass` 计数 ≤10 且仅剩清理类（带 `logger.debug`）；密钥迁移失败出现 `error` 日志且不清空 `api_key` | 计数仍 47；密钥失败仍静默 |
| **D-10** | `concat_videos` 失败日志含 ffmpeg 真实原因；守卫可断言所有调用点判空 | 失败仅返回 `""` 无日志 |
| **D-11** | 连续 3 轮后 `COMFYUI_OUTPUT_DIR` 增量 < 单轮 30%；正式产物零删除；旧剧本项目 `narration` 有 warning | 输出目录单调线性增长 |

### 上线判定

- **必须过线**：D-01 / D-02 / D-03 / D-04（P0+P1 数据安全四件套）全部满足验收条件 → 方可恢复生产。
- **质量门槛**：D-05 决定"质检模块是否真的有效"，建议同批上线；D-06 与 D-05 有依赖（复用 clamp 工具），可紧随。
- **可延期**：D-07 ~ D-11 不阻塞上线，但 D-09（密钥明文）建议优先于其他 P2。

---

## 6. 「已修复、勿重复修」清单（第 3 节 P0/P1 审计的落点，附证据行号）

> 本节回答"为什么这次只报了 11 条"：2026-09-19 ~ 09-21 三轮审计的 P0/P1 **绝大多数已落地**。逐行核实证据如下，**请勿再按旧报告重修**。

| 历史项 | 结论 | 证据（当前代码行） |
|---|---|---|
| 调用统计 `_CALL_STATS` 无锁 | ✅ 已修 | `comfyui_client.py:61` `_CALL_STATS_LOCK`，`:78` `_bump` 内加锁 |
| `qc_client.save_config` 固定 `.tmp` 并发可截断 | ✅ 已修 | `qc_client.py:57` 唯一临时名（pid+tid+urandom） |
| `save_config` 用裸 `bool(v)` 误存字符串开关 | ✅ 已修 | `qc_client.py` 走 `_as_bool`；`CONFIG_KEYS`/`_empty_config`/`save_config` 三处登记齐 |
| 图片质检参考图编号/前缀/去重 | ✅ 已修 | `qc_client.py:138-170` 从「第 2 张」起、`_REF_LABEL_PREFIX_RE` 剥前缀、`seen` 去重、`MAX_REF_IMAGES=4` |
| `_inject_seed(seed=None)` 沿用模板字面量 | ✅ 已修（语义保留） | `comfyui_client.py:1534` 仍 `return []`（**这是设计**），但 `app.py:2461/2601/3155/4023` 四处（资产基础图/多视角/分镜/整集视频）首轮即注入真随机 seed |
| 整集重试换随机种子 | ✅ 已修 | `comfyui_client.py:1824` `cur_seed = random.randint(1, 2**31-1)` |
| P1-18「不可判定」换种子白烧（`kook=False` 路径） | ✅ 已修 | `app.py:3834-3841` 打 `unavailable` + `:3850 _ep_qc_stop_cb` 优先返回 True |
| 尾帧质检异常口径 | ✅ 已修 | `keyframe.py:480-580` G18 fail-closed + 不可判定不重试 + G1 第 4 位 `critical_issues` 补齐 |
| G1 止损（连续两次缺陷特征相同） | ✅ 已修 | `qc_client.py:309` `qc_retry_hopeless`；调用点 `app.py:1578/2557/2662/3286/3859/4162` |
| `_safe_project('')` 返回字面量 `'project'` 的真值陷阱 | ✅ 已修 | `_project_or_400` 收口；`verify_project_audit.py` 有守卫 |
| 分镜断点续跑 | ✅ 已修 | `app.py:3021` `overwrite: bool = False`；`:3083` `if not overwrite and os.path.isfile(dst): continue` |
| 参考图收集判据 ↔ 资产就绪判据漂移 | ✅ 已修 | `app.py:1362-1412` `_collect_asset_refs` 扩展名白名单 + `_first_existing_asset_image`（`app.py:283`）复用统一 |
| `_match_shot_chars` 静默 take-first | ✅ 已修 | S6：找不到即返回空，由调用方决策 |
| 资产批量单点异常整批判 failed | ✅ 已修 | `app.py:2430` try 起、`:2724`「已隔离，继续后续资产」逐资产隔离 |
| `host_guard` 非回环启动护栏 | ✅ 已修 | 新增 `app/host_guard.py`（`_guard_host`），`app.py:10683` 与 `serve.py` 双端接入 |
| `main.py` 传参 `main(port, host)` 与 `serve.main()` 无参不匹配 | ✅ 已修 | `app/main.py` 改为先写 env 再调无参 `serve.main()` |
| `anullsrc` 无时长撑爆磁盘（80GB 事故） | ✅ 已修 | `dub_mix.py:321` `anullsrc=r={sr}:cl=mono:d={base_dur:.3f}` 显式时长 |
| 音频整轨口径误判 / 解析不出电平即失败 | ✅ 已修 | `qc_client.py:190` `AUDIO_CRITICAL_KEYWORDS`；`_merge_audio_verdict`（`:2221`）两层质检 |
| 质检暂存区膨胀（1.1GB） | ✅ 已修 | `app.py:5570` `_qc_prune_attempts` 按尝试号排序保留最近 4（仅覆盖 qc scratch，见 D-11） |
| `camera_key` 猜"中景" | ✅ 已修 | 找不到景别词返回 `""`；`verify_camera_framing.py` 守 |
| 项目/配置落盘唯一化（project_store / ai_config / ai_chat） | ✅ 已修 | `project_store.py:262`、`ai_config.py:186`、`ai_chat.py:134` |

**复核建议**：本清单可作为「回归防回潮」基线——若要防止这些修复被后续改动推翻，建议把第 3 列的行号断言写进 `verify_project_audit.py`（现有 61 项校验的扩展位）。

---

**报告结束**。总计 **11 条真实剩余缺陷**：P0 **2** / P1 **4** / P2 **5**；历史 P0/P1 修复率按第 6 节清单为 **已全部落地**。
