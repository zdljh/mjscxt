# 漫剧生成系统 · 缺陷总报告（合并版）+ 双人分工

> **日期**：2026-09-21　|　**代码基线**：`zdljh/mjscxt` HEAD `295933d`（与 `origin/main` 零分叉，工作树干净）
>
> **合并来源**
> ① 《漫剧生成系统 · 上线前全检总报告（代码审查 + 安全审计 + QA 测试）》——`prelaunch-check-mjscxt-2026-09-21`
> ② 《全项目缺陷审计报告 · 三批次分工》——`全项目缺陷审计报告_三批次分工_20260921`（38 条：P0 5 / P1 19 / P2 14）
>
> **合并结果**：去重后 **49 条**（上线闸门 3 + 阻断 5 + 严重 21 + 优化 20），按 **2 名研发** 切成两个互不重叠的工作包。

---

## 零、合并口径说明（先看这节，能省掉半天）

### 0.1 两份报告的本质差异（为什么不能简单相加）

**报告① 上线前全检**：方法 = 静态代码审查 + OWASP/STRIDE 安全审计 + 39 个离线脚本回归。
视角 = 「**能不能上线**」（部署安全 + 行为不退化）。结论 = 🟡 条件 Go（纯本机单用户）；🔴 0 / 🟠 6 / 🟡 10 / 🟢 5。
强项：访问控制、凭据卫生、Docker 部署、交付面卫生、回归覆盖。
盲区：**回归脚本未覆盖的分支看不到**。

**报告② 缺陷审计**：方法 = 端到端流水线逐条读源码 + **构造反例实测**。
视角 = 「**业务逻辑对不对**」（跨模块契约 + 边界 + 幂等）。结论 = P0 5 / P1 19 / P2 14。
强项：质检口径、断点续跑、时间轴、镜号归一化、幂等键。
盲区：未做渗透测试、未逐包比对 CVE、未验 ACL 运行时效果。

**两份报告不互相取代**。报告① 的 🟢 项是真实资产，但它的 🟠/🟡 清单里没有报告② 的 5 条 P0 —— 因为那些地方**离线脚本从来没跑过**。

### 0.2 「39 个脚本全绿」与「5 条 P0 阻断」为什么能同时成立

这是合并时最容易被误读的一点，**必须向两个研发讲清楚**：39 个离线脚本验证的是「**已实现路径的行为没有退化**」——它们本质是**回归测试**，不是**覆盖性测试**。报告② 的 5 条 P0 恰好全部落在脚本未覆盖的分支上：

- **P0-1**（质检 fail-open）：所有脚本都在「质检就绪」或「质检完全关闭」两端取值，**从未构造 `qc_declared=True && qc_on=False` 的「半开」态**。
- **P0-2**（仅台词镜头）：`empty_shot_drop`(7 项) 只覆盖「四字段全空」，**未覆盖「只有 dialogue、没有画面描述」**。
- **P0-3**（否定语境）：`qc_prompt_feedback` / `qc_bool_parse` 用的都是**正向命中**样例。
- **P0-4**（成片打回后重跑）：回归脚本**从不打回**，只跑 happy path。
- **P0-5**（`run-once` 与守护线程并发）：单线程串行跑，脚本里**不存在并发探针**。

> **结论**：报告① 的「全绿」可信，报告② 的「P0」也可信，二者不冲突。
> **纪律**：修 P0 时**必须同时补离线脚本**，否则下次回归仍然抓不到。
> 本项目已有先例：G1「尾帧止损」当年就是这么"修"成了**空壳**（代码在、逻辑不生效），本轮审计才把它翻出来（即 P2-7）。

### 0.3 一处表述冲突的澄清（不要让研发误解）

报告① 的 🟢 良好实践列里写着「**尾帧/图片/视频 QC fail-closed 口径一致**」；报告② 的 P0-1 写的是「**分镜/逐镜/整集视频在质检未就绪时未质检产物直接入库**」。**两者说的是不同分支，都不算错**：

- **分支 A —— 质检回调抛异常**（质检接口 up，但调用过程报错）：✅ fail-closed（保留暂存、不交付、不重试）。这是 G18 修的口径，报告① 验收的就是这条。
- **分支 B —— 质检接口未就绪**（`enabled && image_enabled` 为真，但 `base_url`/`api_key`/`model` 缺失或网关不通 → `qc_on=False`）：⚠️ **fail-open**，分镜/逐镜/整集视频直接入库；资产链路是 fail-closed。**报告② P0-1 说的是这条。**

分支 B 才是高危的那个：它**不报错、不告警**，直接把未质检的图/视频当成品交付，并破坏「正式目录有产物 ⟺ 已过质检」这条断点续跑赖以成立的不变量。**2026-09-20 刚真实发生过同类配置故障**（`ai_config` 与 `qc_config` 两套数据 + 网关切换）。

### 0.4 去重映射（5 处，已合并为单条工单）

- **报告① H2**（`qc_client.py:692` `save_config` 固定 `.tmp`）→ 并入 **P1-1**：与「17 处固定 `.tmp`」同一件事，**一次改完**。
- **报告① M1**（`secret_store.py` `_write_store` 固定 `.tmp` + 无锁）→ 并入 **P1-1**：P1-1 已含此文件，且更严重（解析失败会**静默清空整个密钥库**）。
- **报告① M2**（`autopilot.py` `_write_json` 固定 `.tmp`）→ 并入 **P1-1**：autopilot 为单线程守护，实际并发概率低，但统一改掉成本几乎为零。
- **报告① F-05**（`task_store.py` `TaskQueue.submit` 无上限）→ 并入 **P1-12**。⚠️ **不要单独做 F-05**：P1-12 已查明 `TaskQueue` **从未接线**（只被创建和 `.status()` 读取），只加 `MAX_QUEUED` 是**无效改动**——必须先接线，上限才有意义。
- **报告① M4**（`app.py:164` `generation_state` 嵌套 dict 锁外改风险）→ 并入 **P2-14**：两者是同一类问题的「风险提示版」与「实例版」，合并后按 P2-14 的实测口径改。

### 0.5 合并后总览

- **总量**：49 条
- **🔴 上线闸门**：3 条（F-01 路径穿越 / F-02 零鉴权 / F-03 明文密钥）—— 上线前必须完成
- **P0 阻断**：5 条 —— 流水线跑不通、产出完全异常、死循环、大量 API 浪费
- **P1 严重**：21 条 —— 偶现异常、画风漂移、质检误判、数据错乱
- **P2 优化**：20 条 —— 性能、日志、磁盘、可维护性、监控告警
- **🟢 保持项**：6 项 —— 报告① 确认的良好实践，禁止在重构中回潮
- **分工**：开发 A **25 条**（质检线 + 提示词线 + 数据安全 + 上线闸门）；开发 B **24 条**（图片线 + 视频音频线 + 并发与 ComfyUI）

---

## 一、合并后统一分级清单

### 1.1 分级分布

- 🔴 **上线闸门**　3 条　—— 影响上线的安全/凭据问题，上线前必须完成（开发 A 3 条）
- **P0 阻断**　5 条　—— 流水线跑不通、产出完全异常、死循环、大量 API 浪费（开发 A 3 / 开发 B 2）
- **P1 严重**　21 条　—— 偶现异常、画风漂移、质检误判、数据错乱（开发 A 8 / 开发 B 13）
- **P2 优化**　20 条　—— 性能、日志、磁盘、可维护性、监控告警（开发 A 11 / 开发 B 9）
- **合计 49 条**（开发 A 25 / 开发 B 24）
- 🟢 **保持项 6 条**　—— 报告① 确认的良好实践，禁止在重构中回潮

### 1.2 一页速览（按开发分组）

**开发 A（25 条）**
- 上线闸门：`F-01`　`F-02`　`F-03`
- P0：`P0-1`　`P0-2`　`P0-3`
- P1：`P1-1`　`P1-2`　`P1-11`　`P1-16`　`P1-17`　`P1-18`　`P1-19`　`F-04`
- P2：`P2-1`　`P2-2`　`P2-5`　`P2-6`　`P2-7`　`P2-8`　`P2-9`　`P2-14`　`F-09`　`M3`　`M5`

**开发 B（24 条）**
- P0：`P0-4`　`P0-5`
- P1：`P1-3`　`P1-4`　`P1-5`　`P1-6`　`P1-7`　`P1-8`　`P1-9`　`P1-10`　`P1-12`　`P1-13`　`P1-14`　`P1-15`　`H1`
- P2：`P2-3`　`P2-4`　`P2-10`　`P2-11`　`P2-12`　`P2-13`　`F-06`　`F-07`　`F-08`

### 1.3 两人之间唯一的一条强依赖

**`P1-19`（镜号归一化）→ `P1-7`（H3 参考图锚点）**

镜号归一化收敛前，`keyframe.py` 写出的尾帧文件名与 `app.py` 读取时用的镜号规则**不一致**（`"S01-C02"` 一处算 `102`、一处算 `1`）。开发 B 的「H3 锚点逐镜匹配」依赖尾帧能被正确读出，因此 **`P1-19` 未完成前不要启动 `P1-7`**。除此之外两个工作包**完全独立**。

---

## 二、开发 A 工作包 —— 质检线 · 提示词线 · 数据安全 · 上线闸门

**包内 25 条**：上线闸门 3 + P0 3 + P1 8 + P2 11。

### 2.1 你的文件范围（**只碰这些**）

```
app/qc_client.py        质检客户端（判据、配置解析、客观层）
app/prompt_qc.py        提示词预检
app/keyframe.py         尾帧：生成、质检、止损、元数据
app/secret_store.py     密钥库
app/project_store.py    项目索引
app/consistency.py      画面一致性（pHash 降级层）
app/serve.py            启动入口（F-02 护栏）
app/app.py              ⚠️ 仅限下列行号段：
                          927 / 2595-2597 / 2684-2688 / 2896 / 3121-3123 / 3186-3189 /
                          3372-3379 / 3603-3645 / 3661 / 3690-3694 / 3852-3855 /
                          5320-5331 / 5747-5757 / 6351-6356 / 8139-8141 / 9456-9880
.gitignore              交付面卫生
```

**明确不属于你**（属开发 B，别顺手改）：
`video_postprocess.py`、`dub_mix.py`、`nle_export.py`、`upscale_client.py`、`comfyui_client.py`、
`task_store.py`、`sfx_isolate.py`、`docker-compose.yml`、`Dockerfile`，
以及 `app.py` 的 `:164`、`:1294-1301`、`:2042-2048`、`:2665`、`:2699-2703`、`:2730-2735`、
`:3114-3117`、`:3275-3284`、`:3487-3488`、`:3539`、`:3960-3966`、`:4006-4032`、`:8506-8518`、`:8934-8937` 段。

### 2.2 任务清单（已按**建议执行顺序**排列，从上往下做）

#### A-01　`F-01`　【🔴 上线闸门】　安全

- **位置**：`app.py:9456-9880`
- **问题与修复要点**：裸 `project` 入参直接 `os.path.join` 拼路径写盘，**绕过全系统 `_safe_project` 收口**（G4 漏网之鱼）→ `project=../../evil` 可越界写盘。改走 `_project_or_400`，或在 `CharacterManager.__init__` 做 `abspath` 前缀校验越界即 400
- **验收标准**：越界 project 返回 400 且不写盘

#### A-02　`F-02`　【🔴 上线闸门】　安全

- **位置**：`app.py:108`、`serve.py`、`.env`
- **问题与修复要点**：全应用零鉴权 + `CORS(app)` 无 origin 限制 + `APP_HOST` 可被 env 覆盖为 `0.0.0.0`。单机版只做**护栏**：`serve.py` 对非回环 host 启动校验/强告警
- **验收标准**：非回环 host 启动被拦截或强告警

#### A-03　`F-03`　【🔴 上线闸门】　凭据（运维）

- **位置**：根目录 `qc_config.backup_20260914.json`
- **问题与修复要点**：残留真实明文 API key（`sk-vZx9…`），已 gitignore 但机器上仍是活凭据
- **验收标准**：删除文件 + **轮换该 key**（视为已泄露）

#### A-04　`P0-1`　【P0】　质检闸门

- **位置**：`app.py:3118-3123`、`:3852-3855`、`:3661`、`:3690-3694`、`:2966-2977`、`:3186-3189`
- **问题与修复要点**：`qc_declared=True` 但 `qc_on=False`（接口未就绪）时，分镜/逐镜/整集视频把**未质检**产物写进正式目录；资产链路是 fail-closed → 口径不一致。统一为 fail-closed，并修正与事实相反的报错文案
- **验收标准**：四条链路均阻断且不写正式目录

#### A-05　`P0-3`　【P0】　AI质检

- **位置**：`qc_client.py:160-169`
- **问题与修复要点**：否定语境判定**无分句边界**。实测 `find_critical_issues(['五官不自然，面部畸变']) → []` → 崩坏图 `blocked=False` 正常入库。加否定词作用域（子句级）
- **验收标准**：崩坏描述必命中；「不是五官不自然」不误杀

#### A-06　`P1-17`　【P1】　AI质检

- **位置**：`qc_client.py:1433-1435`、`:1453-1454`
- **问题与修复要点**：`parse_verdict` 把 `score is None` 当满足分数。实测 `{"score":null,"pass":true}` → `passed=True, accept=True` → 模型漏回分数即放行
- **验收标准**：缺分数 fail-closed

#### A-07　`P1-18`　【P1】　AI质检

- **位置**：`app.py:3603-3645`、`keyframe.py:472-528`
- **问题与修复要点**：整集/尾帧把「**不可判定**」当「不达标」→ 换种子重跑。ffmpeg 缺失或接口 5xx（与内容无关）时白烧 20~44 段 H3 × `max_retries`
- **验收标准**：不可判定 → 不重试，标 `qc_unavailable`

#### A-08　`P0-2`　【P0】　提示词预检

- **位置**：`prompt_qc.py:227-232`、`:248-249`
- **问题与修复要点**：`_has_shot_content` 只看画面三选一（**不含 `dialogue`**）却判 fatal 且不可自愈，而上游 `_norm_shots` 只丢「四字段全空」→「仅台词」镜永远出不了图 → **整集在分镜步永久卡死**
- **验收标准**：仅台词镜可通过预检，或上游根除触发条件

#### A-09　`P1-16`　【P1】　提示词

- **位置**：`app.py:2595-2597`
- **问题与修复要点**：`characters_in_shot` / `location` 匹配失败时**静默取「资产索引第一个角色」**当外观锚点 → 整集以错误角色为锚点，日志/界面看不出来（并架空下游 S6 保护）
- **验收标准**：匹配失败告警/阻断，不静默取错

#### A-10　`P1-19`　【P1】　全局

- **位置**：`app.py:927/2896/3372`、`keyframe.py:161`、`dub_mix.py:166/174`
- **问题与修复要点**：镜号归一化**6 套实现、2 种互斥语义**（`"S01-C02"` 一处算 `102`、一处算 `1`）→ keyframe 模式写 `shot_102_end.png` 读 `shot_01_end.png`，**尾帧永久找不到**。收敛为 `shot_key.shot_seq`
- **验收标准**：★**开发 B 的 P1-7 前置**，做完立即通知

#### A-11　`P1-1`　【P1】　数据安全

- **位置**：`secret_store.py:152-162` 等 **17 处**
- **问题与修复要点**：固定 `.tmp` + 无锁。最高危：`secret_store._read_store` 解析失败**静默返空** → 读改写**清空整个密钥库**。（吸收报告① H2 / M1 / M2）抽公共 `atomic_write_json` 后全项目复用
- **验收标准**：并发 20 线程写不同 key 无丢失；损坏文件报错并留 `.bak`

#### A-12　`P1-2`　【P1】　数据安全

- **位置**：`project_store.py:114-131`、`:181-190`
- **问题与修复要点**：`_read_json` 对 `(OSError, ValueError)` 静默返默认 → 索引损坏 → 写回空索引 → **全部项目永久消失**
- **验收标准**：解析失败 fail-loud + `.bak` 轮转，不清空索引

#### A-13　`P1-11`　【P1】　数据安全

- **位置**：`pipeline.py:1261/1278` 等
- **问题与修复要点**：死信落盘失败被静默吞掉 **+ 计数器清零** → 同一集无限重跑，且「需人工介入」列表永远为空
- **验收标准**：落盘失败可见；计数不清零

#### A-14　`P2-1`　【P2】　质检配置

- **位置**：`qc_client.py:667-680`
- **问题与修复要点**：`elif k == "api_backoff"` **永不可达**（`:671` 已含该键），复制粘贴残留
- **验收标准**：死分支删除

#### A-15　`P2-2`　【P2】　质检

- **位置**：`app.py:5320-5331`
- **问题与修复要点**：用**未排序**的 `os.listdir` 取尾切片。实测 `1..10` + `keep=3` 保留 `[1,2,3,10]`（保最旧而非最新）→ 最新失败证据反被删
- **验收标准**：排序后保留 `{8,9,10}`

#### A-16　`P2-5`　【P2】　元数据

- **位置**：`keyframe.py:550-554`（对照 `app.py:3142-3146`）
- **问题与修复要点**：尾帧不写 `.meta.json`，与分镜/资产/逐镜视频的元数据链断裂。尾帧是链式下一镜首帧，**最关键的中间产物却唯一无溯源**
- **验收标准**：每个 `*_end.png` 旁有 `.meta.json`，含 `seed`/`qc`

#### A-17　`P2-6`　【P2】　质检

- **位置**：`app.py:5747-5757`、`:6351-6356`
- **问题与修复要点**：尾帧质检结论**只在失败时落盘**，达标无痕 → 「这集尾帧到底查没查」无从确认（也掩盖了 P0-1 类 fail-open）
- **验收标准**：达标/不达标均产生记录

#### A-18　`P2-7`　【P2】　质检

- **位置**：`keyframe.py:491-498`
- **问题与修复要点**：止损读取的 `critical_issues` 字段**没人写**（`r["qc"]` 只写 3 个键）。实测 `_qc_retry_hopeless(...)` 恒返回 `(False,'')` → 永不判定无望
- **验收标准**：同因连续 2 次失败后停手

#### A-19　`P2-8`　【P2】　质检

- **位置**：`qc_client.py:2042-2046`
- **问题与修复要点**：`obj_fatal` 靠**中文子串包含**推断（`"无音轨"` / `"时长"` / `"帧率"`）→ 改一句提示文案即静默降级为不致命
- **验收标准**：改文案后 `objective_fatal` 集合不变

#### A-20　`P2-9`　【P2】　质检

- **位置**：`consistency.py:51`、`:223-231`、`:288-293`
- **问题与修复要点**：降级 pHash 的 warn 阈值被写成 `0`。实测噪声图 `score=49.2` → `pass`，且 `score >= 0` 恒成立 → **降级模式永远不可能 fail**
- **验收标准**：噪声图不返回 `pass`；结论标 `degraded`

#### A-21　`P2-14`　【P2】　并发

- **位置**：`app.py:2684-2688`
- **问题与修复要点**：锁外 `jsonify` 序列化 `generation_state` → 读到半更新快照（如 `progress=100` 但 `results` 只有 3 条）。（吸收报告① M4）
- **验收标准**：锁内深拷贝；压测无字段错位

#### A-22　`M5`　【P2】　代码卫生

- **位置**：`app.py:682/1481/1610/2922/3764/4746/5738`、`autopilot.py:363/448/776`
- **问题与修复要点**：未使用变量 / 无占位符 f-string。⚠️ 部分「未使用变量」是**有意为之**（`A = _A()` 前置断言、`state_path = _ensure_state_dir()` 有 `os.makedirs` 副作用），**勿盲删**；`:5738` 需确认意图
- **验收标准**：逐个人工确认意图后再改

#### A-23　`M3`　【P2】　API 契约

- **位置**：`video_postprocess.py` `concat_videos`
- **问题与修复要点**：失败返回 `""`。现有调用方已检查，外部若直接调用需逐一确认 `if not`。⚠️ **先 `grep` 全部调用点再决定**，勿盲改抛异常
- **验收标准**：全部调用点确认；或改抛 `RuntimeError`

#### A-24　`F-09`　【P2】　交付面

- **位置**：`.gitignore:21-22`
- **问题与修复要点**：`!output/projects/index.json` 否定规则因父目录被整体忽略而**失效**（`git check-ignore` 实测命中第 21 行）→ 索引实际不在版本控制内
- **验收标准**：删除失效否定或改选择性忽略

#### A-25　`F-04`　【P1】　凭据（运维）

- **位置**：`.env`、`output/secrets.enc`
- **问题与修复要点**：Fernet 主密钥明文落盘且 ACL 偏宽；`.env` + `.secret_key` + `secrets.enc` 三者齐泄即可破整个密钥库
- **验收标准**：`icacls` 收紧至当前用户；部署文档声明最小授权


### 2.3 交付顺序与依赖

1. **上线闸门 3 条**（`A-01` F-01 / `A-02` F-02 / `A-03` F-03）：F-01 在 `app.py:9456-9880`、F-02 在 `serve.py`，**与开发 B 零冲突，第一天就能做**；F-03 是运维动作，你只需提交删除 + 轮换清单。
2. **`A-04` ~ `A-07`：质检链路四条一次成型**（P0-1 / P0-3 / P1-17 / P1-18），避免反复改 `qc_client.py`。
3. **`A-08` ~ `A-10`：提示词与镜号**（P0-2 / P1-16 / P1-19）。⚠️ **`A-10`（P1-19）做完立即通知开发 B**，解除他的阻塞。
4. **`A-11` ~ `A-13`：数据安全**（P1-1 / P1-2 / P1-11）。先抽公共 `atomic_write_json`，再改各处。
5. **`A-14` ~ `A-21`：优化项收尾**（P2-1 / P2-2 / P2-5 / P2-6 / P2-7 / P2-8 / P2-9 / P2-14）。
6. **`A-22` ~ `A-24`：代码卫生与交付面**（M5 / M3 / F-09）。
7. **`A-25`：F-04（运维）**，与以上并行推进即可。

**合并交付建议**（同文件、同机制的一起提交）：
`A-04 + A-05 + A-06 + A-07`（质检四条）｜`A-11 + A-12 + A-13`（落盘原子化）｜`A-15 + A-14`（`qc_client.py` 两处）｜`A-18 + A-19 + A-20`（尾帧质检三处）。

### 2.4 ⚠️ 包内不要盲改的 3 条

1. **`app.py` 的「未使用变量」不要随手删**（`A-22` M5）。其中至少两类是**有意为之**：`A = _A()` 形式是**前置断言**；`state_path = _ensure_state_dir(...)` 有 `os.makedirs` **副作用**。改前必须逐个确认意图；`app.py:5738` 的「无占位符 f-string」需先确认到底是真 bug 还是待补文案。
2. **`A-23` M3（`concat_videos` 返回 `""`）不要直接改成抛异常**。先 `grep` 全部 `concat_videos(` 调用点，确认每一处都有 `if not` 判空；确认后再决定是保留 `""` 还是抛 `RuntimeError`。现有调用方已检查，盲改会引入新崩溃。
3. **`A-02` F-02 在单机场景只做「护栏」不做「鉴权」**。本次目标是「非回环 host 启动校验 / 强告警」，**不要**顺手引入用户体系——那会改变所有端点的契约与前端行为。最小鉴权属网络部署，独立立项。

---

## 三、开发 B 工作包 —— 图片线 · 视频音频线 · 并发与 ComfyUI

**包内 24 条**：P0 2 + P1 13 + P2 9。

### 3.1 你的文件范围（**只碰这些**）

```
app/video_postprocess.py   拼接 / 降级重编码 / 字幕烧制
app/dub_mix.py             配音混音
app/nle_export.py          剪辑工程导出
app/upscale_client.py      超分
app/comfyui_client.py      ComfyUI 调用层（含 H1 统计锁）
app/task_store.py          任务队列
app/sfx_isolate.py         音效分离
app/pipeline.py            时长基准 / 任务表 / 死信
app/app.py                 ⚠️ 仅限下列行号段：
                             164 / 1294-1301 / 2042-2048 / 2665 / 2699-2703 / 2730-2735 /
                             3114-3117 / 3275-3284 / 3487-3488 / 3539 / 3960-3966 /
                             4006-4032 / 8506-8518 / 8934-8937
docker-compose.yml, Dockerfile
```

**明确不属于你**（属开发 A，别顺手改）：
`qc_client.py`、`prompt_qc.py`、`keyframe.py`、`secret_store.py`、`project_store.py`、`consistency.py`、
`serve.py`、`.gitignore`，
以及 `app.py` 的 `:927`、`:2595-2597`、`:2684-2688`、`:2896`、`:3118-3123`、`:3186-3189`、`:3372-3379`、
`:3603-3645`、`:3661`、`:3690-3694`、`:3852-3855`、`:5320-5331`、`:5747-5757`、`:6351-6356`、
`:8139-8141`、`:9456-9880` 段。

> ⚠️ **`:3114-3123` 区间有交界**：开发 A 改 `:3118-3123`（质检闸门），你改 `:3114-3117`（`success` 回滚）。
> **约定：串行、不并行**。你先做（`B-15` P2-3），提交并 push 之后**明确告知开发 A**，他再在其上改闸门。

### 3.2 任务清单（已按**建议执行顺序**排列，从上往下做）

#### B-01　`P1-12`　【P1】　并发/资源

- **位置**：`app.py:175-176`、`:853`、`:876`；`task_store.py`
- **问题与修复要点**：实测 `TaskQueue` **全库零入队调用点**（只被创建 + `.status()` 读取）→ 起任务直接 `Thread().start()`，**线程数无上限**；`/free` **无条件卸载他人正在用的模型**。吸收报告① F-05：⚠️ **先接线再谈上限**，只加 `MAX_QUEUED` 是无效改动
- **验收标准**：队列真正入队 + 线程上限 + `/free` 只卸自己的

#### B-02　`P0-5`　【P0】　并发/幂等

- **位置**：`app.py` `/api/autopilot/run-once`
- **问题与修复要点**：与托管守护线程**无集级互斥** → 同一集两条流水线并发写同一批路径（正式目录 + scratch）。加**集级**锁（键 = `项目 + 集号`），不可用全局锁（会让不同项目互相阻塞）
- **验收标准**：同集任何时刻只有一个执行体

#### B-03　`P0-4`　【P0】　全局状态机

- **位置**：`app.py` 成片复核路径
- **问题与修复要点**：成片置 `rejected`（打回）后重跑走**幂等短路** → 空转、`review` 状态**永不复位**、最终误报「状态同步异常」
- **验收标准**：打回后可重跑，`review` 正确复位

#### B-04　`P1-3`　【P1】　视频·成片

- **位置**：`video_postprocess.py:84-87`、`:302-312`
- **问题与修复要点**：`probe_media` 未取 `sample_rate`/`channels`，拼接前的音轨/视频一致性判定是**死代码** → `-c copy` 拼出音轨错位成片**且被判就绪**
- **验收标准**：判定生效；不一致时走重编码而非 `copy`

#### B-05　`P1-4`　【P1】　视频·成片

- **位置**：`video_postprocess.py:374-386`
- **问题与修复要点**：降级重编码**硬编码** `scale=1280:720` + `fps=30` → 竖屏 9:16 项目被改成横屏加黑边（成片已落盘，**不可逆**）
- **验收标准**：分辨率/帧率由源片实测推导

#### B-06　`P2-12`　【P2】　成片

- **位置**：`video_postprocess.py:562-567`、`app.py:4006-4032`
- **问题与修复要点**：字幕烧制写在**独立文件名**，但对外登记（`deliverables.json`/前端地址/集号索引）指向**无字幕版** → 字幕版成孤儿，用户拿到无字幕成片
- **验收标准**：前端播放地址指向带字幕版；两版都留则显式标注

#### B-07　`P1-5`　【P1】　视频·时间轴

- **位置**：`pipeline.py:741-746`
- **问题与修复要点**：字幕/混音/质检的 `expected_duration` 用**剧本 duration 累加**，与成片实测时长拼接基准分裂 → 字幕整体漂移
- **验收标准**：统一以 ffprobe 实测为准（差 < 0.5s）

#### B-08　`P1-10`　【P1】　数据流转

- **位置**：`app.py:8139-8141`
- **问题与修复要点**：配音 manifest **非原子整文件覆盖** + `probe_tts` 解析失败**静默 `done:False`** → 一次崩溃要**重烧整集配音**
- **验收标准**：原子写；解析失败 fail-loud

#### B-09　`P1-9`　【P1】　并发/契约

- **位置**：`pipeline.py` 任务表写入
- **问题与修复要点**：内部任务写进**公共**任务表且 `init` 缺字段 → 双向故障：漏命中起双 worker；误命中前端 404
- **验收标准**：内部任务隔离；`init` 字段补全

#### B-10　`P1-6`　【P1】　视频·混音

- **位置**：`app.py:8506-8518`
- **问题与修复要点**：`_mix_segments_dir` **不带集号** → 第 2 集起取到第 1 集素材，时间轴/成片源系统性错配
- **验收标准**：目录带 `epNN`；ep01/ep02 素材无重名

#### B-11　`P1-8`　【P1】　图片·幂等

- **位置**：`app.py:3275-3284`
- **问题与修复要点**：分镜任务复用守卫**不含 `episode_no`** → 第 2 集请求被第 1 集运行中任务吞掉
- **验收标准**：守卫键含集号

#### B-12　`P1-15`　【P1】　图片·幂等

- **位置**：`app.py:2665`
- **问题与修复要点**：资产任务 ID 仍用**秒级时间戳**（G5 只改了分镜/视频/配音/混音，**资产漏改**）
- **验收标准**：改 `uuid`，同秒并发不互撞

#### B-13　`P1-14`　【P1】　图片·跨项目

- **位置**：多视角基础图上传路径
- **问题与修复要点**：上传到 ComfyUI `output` **根目录**且**不含项目名** → 跨项目同名资产互相覆盖。另：基础图/多视角**从不设 `filename_prefix`** → `output` 只增不减
- **验收标准**：路径含项目名 + 集号

#### B-14　`P2-4`　【P2】　图片

- **位置**：`app.py:1294-1301` vs `:2699-2703`/`:2730-2735`
- **问题与修复要点**：**就绪判据 ≠ 取图判据** → 「判资产齐全却收不到参考图」，全程无报错 → 该镜退化为无参考图生成（质量静默下滑）
- **验收标准**：二者共用同一函数；三态用例全绿

#### B-15　`P2-3`　【P2】　分镜

- **位置**：`app.py:3114-3117`
- **问题与修复要点**：先置 `success=True`/`file`/`url` **再** `copy2`，落盘异常时 `except` 只写 `error` **不回滚** → 同一 item 同时含 `success:true` 与 `error`。⚠️ **与开发 A 的 `:3118-3123` 交界，串行不并行**
- **验收标准**：落盘失败时 `success:false` 且无 `file`/`url`

#### B-16　`P2-11`　【P2】　磁盘

- **位置**：`dub_mix.py:372-375`、`app.py:8934-8937`、`sfx_isolate.py:216-221`、`video_postprocess.py:271`、`app.py:3960-3966`
- **问题与修复要点**：五处失败路径**不清理**中间产物，且命名不带 `_tryN`、不在 scratch → **永不被清理，也不出现在任何清单里**
- **验收标准**：注入异常后中间目录无新增残留

#### B-17　`P2-13`　【P2】　NLE

- **位置**：`nle_export.py:130-145`、`:165-181`、`:89`、`app.py:2042-2048`
- **问题与修复要点**：素材名**不带集号** + 与引用**按下标对位** → 第 2 集起串镜，时间轴全错，且外部剪辑软件**不会报错**
- **验收标准**：素材名带 `epNN`，按显式 id/镜号映射引用

#### B-18　`P1-7`　【P1】　图片/视频

- **位置**：`app.py:3487-3488`、`:3539`、`:3275-3284`
- **问题与修复要点**：三问题叠加：① 锚点恒取 `character_refs[:1]`，**与本镜实际出镜角色无关** → 串脸；② H3 参考图上限 2 张被 `[分镜图, 第1角色]` 占满，本镜角色**无锚点**；③ 尾帧被当角色锚点。分镜链路有 `_match_shot_chars`(`:2757`) 逐镜匹配，**视频链路没有**
- **验收标准**：★**等 A-P1-19 完成后启动**，否则镜号对不上白做

#### B-19　`H1`　【P1】　并发

- **位置**：`comfyui_client.py:76-81`
- **问题与修复要点**：`_CALL_STATS` 全局 dict 多线程读改写**未取 `_CALL_STATS_LOCK`** → 统计偶发少计（无业务阻断）
- **验收标准**：两个函数体包 `with _CALL_STATS_LOCK`（约 5 行）

#### B-20　`P2-10`　【P2】　磁盘

- **位置**：`video_postprocess.py:152-159`、`upscale_client.py:566/631`、`pipeline.py:921-925`、`comfyui_client.py:1845-1846`
- **问题与修复要点**：四处大体积写入**前不检查剩余空间**，只依赖「跑完再尽力清理」。**历史已发生**：一条无时长 ffmpeg 源把 C 盘写满 **80GB** 并截断源文件。加 `_ensure_disk_headroom(...)`
- **验收标准**：空间不足时快速失败 + 中文告警 + 触发一次清理

#### B-21　`P1-13`　【P1】　ComfyUI 调用层

- **位置**：`comfyui_client.py:1845-1846` 等
- **问题与修复要点**：三连：轮询结局不可区分 / `interrupt()` 误伤他人任务 / 标量超时无 connect 超时
- **验收标准**：三态可区分；`interrupt` 定向；超时分离

#### B-22　`F-06`　【P2】　交付面

- **位置**：`test_page.html`、`h3_ref_submit.py`、`app/templates/index_old.html`、`index_v2.html`
- **问题与修复要点**：调试/历史产物随交付面扩大侦察面
- **验收标准**：上线前从仓库/静态目录剔除

#### B-23　`F-07`　【P2】　数据完整性

- **位置**：`project_store.py` `PROJECT_TRASH_DIR`
- **问题与修复要点**：删除「可恢复」且 `output/` 宽 ACL → 本机他账号可悄悄还原/篡改
- **验收标准**：多用户机收紧 ACL；如需不可恢复语义补独立 `purge` 端点

#### B-24　`F-08`　【P2】　部署正确性

- **位置**：`docker-compose.yml`、`Dockerfile`
- **问题与修复要点**：容器内应用绑 `127.0.0.1` 而宿主发布 `5000:5000` → **转发不通**（功能 bug）；`5000:5000` 也未收口宿主回环
- **验收标准**：容器走 `APP_HOST=0.0.0.0` + 宿主 `127.0.0.1:5000:5000`（**仅采用 Docker 时处理**）


### 3.3 交付顺序与依赖

1. **`B-01` ~ `B-03`：并发与状态机**（P1-12 / P0-5 / P0-4）。改动面最大，**先做能避免后面反复冲突**。
2. **`B-04` ~ `B-06`：成片质量**（P1-3 / P1-4 / P2-12，同属 `video_postprocess.py`，可合并一次提交）。
3. **`B-07` ~ `B-10`：时间轴与清单**（P1-5 / P1-10 / P1-9 / P1-6）。
4. **`B-11` ~ `B-14`：图片幂等与隔离**（P1-8 / P1-15 / P1-14 / P2-4）。
5. **`B-15` ~ `B-17`**（P2-3 / P2-11 / P2-13）。其中 `B-15`（P2-3）**尽早做**，尽快腾出 `app.py:3114-3117` 区间并通知开发 A。
6. **`B-18`（P1-7）**：⚠️ **等开发 A 的 `A-10`（P1-19）交付后再启动**，否则镜号对不上，逐镜匹配做了也白做。
7. **`B-19` ~ `B-24`：收尾**（H1 / P2-10 / P1-13 / F-06 / F-07 / F-08）。

**合并交付建议**：`B-04 + B-05`（`video_postprocess.py` 两条视频质量）｜`B-07 + B-08`（时长基准 + manifest）｜`B-11 + B-12`（图片幂等两条）｜`B-23 + B-24`（凭据/部署 ACL 两条，均偏运维）。

### 3.4 ⚠️ 包内不要盲改的 2 条

1. **`B-01` P1-12 不要只加 `MAX_QUEUED`**。`TaskQueue` 当前**从未接线**（全库零入队调用点），队列不在链路上。必须**先把它真正接入渲染任务**，上限才有意义。同时注意 `/free` 端点当前**无条件卸载他人模型**——这是比"无上限"更直接的伤害来源。
2. **`B-06` P2-12 成片口径要一次定清**。字幕版与无字幕版**不能两版都留却不标注**。二选一：① 字幕烧制成功后**替换**成片路径（推荐）；② 两版都保留，则 `deliverables.json` 必须登记两条并标 `subbed: true/false`。改前先确认前端播放地址从哪里取。

---

## 四、上线闸门（P0，上线前必须完成）

#### 闸门 `F-01`　角色/关系端点 `project` 入参统一改走 `_project_or_400`（或在 `CharacterManager.__init__` 做 abspath 前缀校验）

- **负责方**：开发 A　|　**紧急度**：**P0**　|　**期望完成**：上线前
- **说明**：本次**唯一写原语**：`project=../../evil` 可越界写盘

#### 闸门 `F-02`　`serve.py` 增加非回环 `APP_HOST` 启动校验 / 强告警

- **负责方**：开发 A　|　**紧急度**：**P0**　|　**期望完成**：上线前
- **说明**：本次只做单机护栏，**不引入用户体系**（最小鉴权属网络部署，独立立项）

#### 闸门 `F-03`　删除 `qc_config.backup_20260914.json` 并**轮换其中 API key**

- **负责方**：运维（开发 A 提交清单）　|　**紧急度**：**P0**　|　**期望完成**：上线前
- **说明**：视为已泄露；部署清单加「备份不得携带明文 key」

#### 闸门 `F-04`　收紧 `.env` / `.secret_key` / `secrets.enc` 的 Windows ACL 至当前用户

- **负责方**：运维　|　**紧急度**：**P1**　|　**期望完成**：上线前（多用户机）
- **说明**：`icacls`；部署文档声明最小授权

#### 闸门 `F-05`　并入 **P1-12**（`TaskQueue` 先接线，再加 `MAX_QUEUED` + 告警）

- **负责方**：开发 B　|　**紧急度**：**P2**　|　**期望完成**：下迭代
- **说明**：⚠️ 队列当前**从未接线**，单独做本条无效

#### 闸门 `F-06 + F-09`　清理交付面调试/历史产物 + 修复 `.gitignore` 失效否定

- **负责方**：开发 A（F-09）/ 开发 B（F-06）　|　**紧急度**：**P2**　|　**期望完成**：下迭代
- **说明**：两条同属「交付面卫生」，可合并一次评审

#### 闸门 `F-08`　若走 Docker：补端口收口 + 逐包 CVE 比对（`pip audit`，本次未做）

- **负责方**：运维　|　**紧急度**：**P2**　|　**期望完成**：采用容器时
- **说明**：不用 Docker 则本条不适用


> **`F-01` 为什么是最高优先**：`/api/characters`、`/api/relations*`（`app.py:9456–9880`）用裸 `project` 入参直接 `os.path.join` 拼路径写盘，**绕过了全系统统一的 `_safe_project` 收口**（属 G4 收口的漏网之鱼）。`project=../../evil` 即可越界写盘。这是本次合并报告中**唯一的写原语**，也是它的优先级高于所有业务缺陷的原因。
>
> ⚠️ **改它时注意**：`_safe_project('')` 会返回字面量 `'project'`（**真值**），所以**绝不要**写成 `_safe_project(x) or 兜底`——兜底永不生效。正确写法是 `_project_or_400`。

---

## 五、同文件协作规则（**两人共用一个 `app.py`，必须遵守**）

`app.py` 有 10400+ 行，两个工作包都会碰它。这是本次分工**唯一的协作风险点**。

### 5.1 行号段归属

- **开发 A 独占**：`927`、`2595-2597`、`2684-2688`、`2896`、`3121-3123`、`3186-3189`、`3372-3379`、`3603-3645`、`3661`、`3690-3694`、`3852-3855`、`5320-5331`、`5747-5757`、`6351-6356`、`8139-8141`、`9456-9880`
- **开发 B 独占**：`164`、`1294-1301`、`2042-2048`、`2665`、`2699-2703`、`2730-2735`、`3114-3117`、`3275-3284`、`3487-3488`、`3539`、`3960-3966`、`4006-4032`、`8506-8518`、`8934-8937`
- **交界处**：`:3114-3123`。按 3.1 的约定**串行处理**，不做并行编辑。

### 5.2 四条硬纪律

1. **禁止 `git add -A`**。提交前 `git status` 逐个确认改动归属（本仓库常有并发会话）。用 `git add <具体文件>`。
2. **发现行号漂移立即停手**：若同一文件的行号在你连续两条命令之间发生变化 → 说明对方正在编辑该文件 → **停手、沟通、等对方提交后再 `git fetch` + merge**。
3. **小步提交**：每 3~5 条提一次 `commit`，不要攒一大坨。`commit` 后**必须** `git push origin main`。
4. **push 前必做**：`git fetch origin` → `git rev-list --left-right --count origin/main...HEAD`；落后先 `git merge origin/main`；**推送前跑 `python -m py_compile app/*.py` + `python -m pyflakes app/*.py`（只看 `undefined name`）**。**绝不 `push -f`**。

---

## 六、验收与回归门禁

### 6.1 通用门禁（每次提交前）

```bash
python -m py_compile app/*.py
python -m pyflakes app/*.py | grep -i "undefined name" || echo "OK: 无未定义名"
# 改了前端或 app/static 时
cd frontend && npx tsc --noEmit && npx vite build
```

**三条硬性要求**：

1. 所有新增的「静默失败」路径必须改为**可观测**（`logger.warning` 及以上，或进「需人工介入」列表），**不得**新增 `except: pass`；
2. 所有新增落盘必须走公共 `atomic_write_json` / `_atomic_replace`（唯一临时名 + 锁）；
3. **不得**引入新的 `_safe_project(x) or 兜底` 写法。

### 6.2 回归脚本（12 个强相关 + 4 个必须新增）

⚠️ **一次工具调用只跑一个脚本**。一次 bash 连跑多个会触发 CLI 沙箱的批量删除守卫（单轮累计删除 ≥50 即杀）→ 后续脚本被 `SystemExit(1)` 直接杀掉。
**判据**：日志只有一行 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]`、无 traceback 也无汇总行 → **这是环境假红，不是回归失败**，逐个独立复跑即复绿。

```bash
# 质检链路
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_bool_parse.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_prompt_qc.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_summary_scan.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_retry_stop.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_ai_qc_sync.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_qc_ref_images.py
# 落盘原子化 / 数据安全
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_atomic_write_locks.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_project_delete_coverage.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_project_audit.py
# 视频 / 音频 / 镜号
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_final_video_episode.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_camera_framing.py
MJSCXT_AUTOPILOT=0 C:/Python314/python.exe .workbuddy/test/verify_h3_sequential.py
```

**必须新增的 4 个脚本**（否则下次回归仍然抓不到这些 P0）：

1. `verify_qc_gate_declared_off.py` —— `qc_declared=True && qc_on=False` → 四条链路均阻断且不写正式目录（**开发 A**）
2. `verify_shot_content_dialogue.py` —— 「仅台词、无画面描述」镜头 → 预检通过（或上游已丢弃）（**开发 A**）
3. `verify_negation_boundary.py` —— `find_critical_issues(['五官不自然，面部畸变'])` 非空；「不是五官不自然」为空（**开发 A**）
4. `verify_shot_key_unify.py` —— `1` / `"1"` / `"01"` / `"shot_03"` / `"shot_1"` / `"S01-C02"` 六种输入的落盘序号与读取序号**完全一致**（**开发 A**）

### 6.3 必须人工走查的 5 项（**脚本替代不了**）

1. 用**真实**的一个 3 集项目跑一遍托管：分镜 → 尾帧 → 视频 → 成片 → 字幕 全链路产物齐全，`deliverables.json` 与磁盘一致；
2. 抽查 3 个 `*.meta.json`（分镜 / 尾帧各 ≥1）+ 3 条质检记录，确认 `seed` / `prompt` / `qc` 字段齐全；
3. **断掉质检接口**（把 `base_url` 改错）再跑一镜，确认 fail-closed（无产物入库 + 前端明确告警）；
4. 点一次**暂停**，确认秒级生效（`cancellation.py` 路径未被改坏）；
5. 跑完一轮后检查 `output/` **没有**新增未被任何清单引用的孤儿文件。

---

## 七、🟢 保持项（**禁止在后续重构中回潮**）

这些是报告① 确认的良好实践，**改代码时不要顺手破坏**：

1. FFmpeg 全链路**零 `shell=True`**（一律 list 形式调用）；
2. agent 工具白名单**无 RCE 原语**；
3. `_serve_safe` / `_safe_upload_name` 防**读**穿越；
4. 密钥 Fernet 库权限 `0o600`；
5. `serve.py` 的 `atexit` + 信号**优雅停机**；
6. `_safe_project` **无 `or` 回潮**（改代码时警惕：`_safe_project(x) or 兜底` 的兜底永不生效）。

---

## 八、⚠️ 已知局限（合并时如实保留，勿删）

1. **安全审计为静态 + 落盘核查**，未对运行中服务做主动渗透；未逐包比对 CVE 库（`pip audit` 未做）；未实测 Windows 多用户 ACL 的运行时效果。
2. **报告② 的 5 条 P0 全部为读源码 + 构造反例实测**，未在完整流水线上端到端复现。其中 `P0-2`、`P0-4`、`P0-5` 因缺少对应输入 / 并发条件，属**代码路径推断 + 局部实测**。
3. **上线前需确认线上 5000 实例是否已重启加载 `295933d`** —— 此前批次改过 `app/*.py`，未重启则跑的是旧逻辑（`app/*.py` 改动必须重启 Flask 才生效，**注意重启会打断在跑的长任务**）。
4. **关于报告①「外部文档未采信」的说明**：报告① 中提到的「疑似另一并发会话产物」的《全项目缺陷审计报告_三批次分工_20260921.md》，**即为本次合并的来源②**，来源已确认，无需再核对归属。
