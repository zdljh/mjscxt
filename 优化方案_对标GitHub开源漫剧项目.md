# 本地漫剧自动化生成系统 · 对标优化方案

> 对标对象：GitHub 上 10 个主流开源 AI 漫剧/短剧项目
> 标的系统：`漫剧生成系统` v2.0.0（Flask + ComfyUI 本地全链路）
> 日期：2026-09-14

---

## 一、开源项目优点提炼（按能力维度归并）

不是逐个罗列功能，而是把 10 个项目的做法抽成 8 个可复用的**能力维度**——这才是能拿来改造自己系统的部分。

| · | 能力维度 | 代表项目 | 具体做法 → 可迁移要点 |
|---|---------|---------|---------------------|
| ① | **多智能体编排** | Novella | Hub-and-Spoke 架构 + ProjectBlackboard 共享黑板；主控导演 Agent 调度剧本识别/角色设计/分镜运镜/配音/压制等专职 Agent，且支持用户自定义扩展 Agent → **把线性流水线升级为「有编排层的 Agent 协作」** |
| ② | **角色一致性锚定** | Moyin Creator / waoowaoo | 6 层身份锚点机制；角色形象档案 + consistency 技术保证跨分镜不「变脸」→ **一致性要有可量化的校验闭环，而非只靠参考图** |
| ③ | **多模型可插拔** | LumenX / waoowaoo | 统一 Provider 网关聚合 Wan2.7 / Kling V3 / Vidu Q3 / 豆包 / Veo 等 10+ 模型，按环节选最优；配音可换 CosyVoice / Qwen3-TTS → **模型层抽象，避免被单一引擎锁死** |
| ④ | **关键帧驱动视频** | CineGen | 先出每镜首帧 + 尾帧，再让视频模型在两端之间插值，构图与角色位置可控 → **比纯参考图（Ref2VA）稳定性更高，值得作为并行模式** |
| ⑤ | **可视化编排 + 人工干预** | Anime AI Studio / ArcReel | 无限画布自由排序分镜、调整节奏；资产跨镜头复用 → **流程要有「可回退、可局部重跑」的交互层** |
| ⑥ | **外部 NLE 导出** | ArcReel / CineGen | 一键导出**剪映草稿**继续精修；导出高清关键帧 + MP4 片段导入 Premiere/AE → **成片不是终点，要能交接到专业后期** |
| ⑦ | **工程化与可运维** | waoowaoo / ArcReel / 灵果短剧 | Docker Compose 一键部署；Redis + Asynq 异步任务队列；**费用追踪**；中英文双语界面 → **一键部署 + 异步队列 + 成本可观测** |
| ⑧ | **产品化形态** | Novella / Toonflow / CineGen | Tauri v2 桌面端打包；多租户 + 用户系统 + 支付等 SaaS 能力（商业化部署）→ **可选：桌面端 / 多租户两条产品化路径** |

### 一句话总结这些项目的共性

> 都把「**一致性**」「**可控性**」「**可交接**」当成三个核心命题：
> 一致性靠锚定 + 校验，可控性靠 Agent 编排 + 关键帧 + 人工干预点，可交接靠 NLE 导出 + 成本可视。

---

## 二、你的系统现状体检

### ✅ 已经领先的地方（务必保住，这是差异化护城河）

| 长板 | 所在模块 | 为什么稀有 |
|------|---------|-----------|
| **原文覆盖率守门人** | `coverage.py` | 逐句核对原文是否被镜头承载，遗漏自动补生成，`max_rounds` 兜底。开源的 10 个项目**没有一个做这个**——它们普遍会丢原文。这是「忠于原著」型漫剧的核心卖点 |
| **跨集连贯性 Bible** | `continuity.py` | 项目级设定库 / 风格指南 / 金句清单 / 口吻词典 / 运镜术语表 + state_in/out + 时间线锚点校验。比单纯「角色一致性」更完整，覆盖叙事层 |
| **多模态 AI 质检 + 自动重试** | `qc_client.py` | 图片逐张、视频抽 3 帧送多模态模型打分，不达标自动换 seed 重试，**质检不可用时阻断入库而非静默跳过**——这个「宁缺毋滥」的取舍很关键 |
| **本地全链路零视频 API 成本** | ComfyUI + H3 + FlashVSR | 数据不出本机，边际成本≈电费。开源项目里只有 Jellyfish/Toonflow 同路线 |
| **H3 动态段数无缝拼接** | `h3_episode_builder.py` | 按分镜数动态重建 1/22/44 段并重建 join 链，工程难度高 |
| **配音→混音时间轴对齐** | `tts_client.py` + `dub_mix.py` | 逐句按镜头时间轴对齐 + 逐句 offset 微调 + 校准落盘复用 |

### ⚠️ 明显短板（对照上表逐条）

| 短板 | 现状证据 | 对应开源优点 |
|------|---------|-------------|
| **架构单文件化** | `app.py` 215 KB、`index.html` 5020 行；无 Blueprint 分层 | ⑦ 工程化 |
| **路径硬编码** | `config.py` 写死 `D:\ComfyUI_portable_TE_v260619`，换机即崩 | ⑦ 一键部署 |
| **密钥明文落盘** | `ai_config.json` 里 `nvapi-...`、`sk-...` 明文 | ⑦ 安全 |
| **无任务队列** | task_id 在内存，重启即丢，长任务无法断点续跑 | ⑦ 异步队列 |
| **无成本/耗时统计** | 只见 `calibration.json`，无调用次数/GPU 秒数/单集成本看板 | ⑦ 费用追踪 |
| **无一一致性校验闭环** | QC 是单图打分，不做「跨镜头角色是否同一人」的比对 | ② 一致性锚定 |
| **无关键帧驱动模式** | 只有 H3 Ref2VA（参考图），无首尾帧插值 | ④ 关键帧驱动 |
| **无可视化画布** | 纯按钮式线性流程，不能拖拽重排/单镜重跑 | ⑤ 可视化编排 |
| **无外部 NLE 导出** | 导出只有 mp4 + 字幕 | ⑥ NLE 导出 |
| **无模型抽象层** | 强绑 ComfyUI 固定模型，无 Provider 接口 | ③ 多模型可插拔 |
| **无编排层/无插件** | 流程硬编码在 app.py 的 if-else 里 | ① 多智能体编排 |
| **无 i18n/无桌面端** | 中文硬编码，浏览器 + 手工双进程启动 | ⑧ 产品化 |

---

## 三、优化方案

按「**先补底座 → 再补体验 → 后补生态**」三层递进。P0 是地基，不做后面全是沙上筑塔。

### P0｜架构底座（地基，建议优先）

#### P0-1 拆分 `app.py` → Flask Blueprint 分层

```
app/
├── __init__.py            # create_app() 工厂 + 蓝图注册
├── blueprints/
│   ├── projects.py        # /api/projects/*
│   ├── novels.py          # /api/novels/* /episodes /continuity /coverage
│   ├── scripts.py         # /api/script/*
│   ├── assets.py          # /api/assets/* /storyboards
│   ├── videos.py          # /api/videos/* /final/video
│   ├── audio.py           # /api/tts/* /mix/*
│   ├── postprocess.py     # /api/upscale/* /watermark/*
│   └── admin.py           # /api/ai/* /qc/* /status
├── services/              # 现有业务模块平移（continuity/coverage/qc_client...）
├── models/                # SQLite 数据访问层（新增）
└── config.py + profiles/   # 配置与环境档案
```

- **验收**：`app.py` ≤ 200 行（仅剩工厂与启动）；各蓝图可独立单测。

#### P0-2 配置外置 + 路径配置化 + 部署档案

- 引入 `pydantic-settings` 或 `.env`，把 `COMFYUI_WORKFLOWS_DIR` / `MODELS_DIR` / `COMFYUI_URL` 全部改为环境变量驱动；
- 提供 `profiles/local-8g.env`、`profiles/local-16g.env`、`profiles/server-a100.env` 三档（显存档位你已经有了 `TE_UPSCALE_LOWVRAM_PARAMS`，正好合并）；
- **验收**：删除所有 `D:\ComfyUI_portable_TE_v260619` 字面量；换机只改 `.env`。

#### P0-3 密钥安全

- `ai_config.json` / `llm_config.json` / `qc_config.json` 的 api_key 迁移到 `.env` 或系统 keyring；
- 落盘时用对称加密（如 `cryptography.Fernet`，密钥存环境变量），前端接口继续只回显脱敏值（你已有的脱敏逻辑直接复用）；
- **验收**：仓库内不再出现任何 `nvapi-` / `sk-` 明文；`.gitignore` 覆盖所有含密钥文件。

#### P0-4 持久化任务队列 + 断点续跑

这是**投入产出比最高的一项**——H3 单集生成动辄数小时，现在重启就全丢。

- SQLite 存 `tasks(id, project, kind, payload, status, progress, started_at, finished_at, result_path, error)`；
- 后台 worker 串行消费（GPU 独占，避免与 ComfyUI 抢显存）；
- 每个**最小可复用单元**（单角色图 / 单镜视频 / 单句配音）落盘产物即视为「已完成」，重跑时按产物存在性跳过 → 断点续跑；
- 前端进度从「轮询内存 task_id」改为「订阅 tasks 表」；
- **验收**：中途 kill 进程重启后，点「继续」能从中断镜头接着跑，已完成镜头不重算。

---

### P1｜一致性与可控性（核心体验）

#### P1-1 一致性校验闭环（补 ②）

现状 `qc_client.py` 只做「单张图好不好」，缺「这一张和那张是不是同一个人」。

- 引入视觉嵌入比对：对角色 `front.png` 建参考向量，对每个分镜图/视频抽帧裁剪出的角色区域算 **CLIP/DINO 余弦相似度**；
- 阈值内自动通过并写 `output/continuity/<项目>/consistency.json`；越阈触发重试（复用现有 QC 重试机制）或标记人工复核；
- 同时校验**服饰状态**是否与 `bible.json` 的「当前服装状态」一致（你已有状态字段，只差校验动作）；
- **验收**：UI 出现「一致性看板」，每镜头显示角色相似度分数与判定。

#### P1-2 新增「关键帧驱动」视频模式（补 ④）

- 保留现有 H3 Ref2VA（`per_shot` / 整集）作为默认；
- 新增 `keyframe` 模式：先由分镜图生成每镜**首帧 + 尾帧**，再用 H3 首尾帧插值（fl2v）生成运动，构图可控性显著高于纯参考图；
- 在前端视频模式下拉里加一项，与「逐镜头生成 / 整集生成」并列；
- **验收**：同一分镜在 keyframe 模式下的角色位置漂移明显小于 Ref2VA 模式。

#### P1-3 可视化分镜画布（补 ⑤）

- 新增「分镜画布」页：卡片式展示每镜（分镜图 + camera + duration + 台词），支持**拖拽排序、单镜重跑、时长微调、替换参考图**；
- 每张卡片显示其 QC 分数 + 一致性分数 + 覆盖率归属的原文句，做到「一屏看穿质量」；
- **验收**：无需重生成整集即可只重跑第 7 镜。

#### P1-4 模型 Provider 抽象层（补 ③）

- 定义接口 `ImageProvider / VideoProvider / TTSProvider / LLMProvider`；
- 本地实现：`ComfyUIQwenProvider`、`ComfyUIH3Provider`、`QwenTTSProvider`（现有代码平移）；
- 预留云实现：`WanProvider`、`KlingProvider`、`ViduProvider`、`VeoProvider`——按环节选优（如：关键镜用云、量镜用本地）；
- **验收**：不改业务代码，仅改配置即可把「视频生成」从本地 H3 换成云端 Wan。

---

### P2｜生态与产品化（按需，非必须）

| 编号 | 事项 | 参考项目 | 说明 |
|------|------|---------|------|
| P2-1 | **剪映草稿导出** | ArcReel | 生成 `draft_content.json`（含轨道/片段/字幕/配音），创作者可直接在剪映精修——这是**给专业用户的最大钩子** |
| P2-2 | **Premiere XML / 帧序列导出** | CineGen | 导出 FCPXML 或图片序列，交接 AE/PR |
| P2-3 | **成本与耗时看板** | ArcReel | 统计 GPU 秒数、ComfyUI 调用次数、单集总耗时、电费估算，落 `output/analytics/`，前端出折线图 |
| P2-4 | **插件化扩展 Agent** | Novella | 把「资产生成 / 分镜 / 质检 / 配音」抽象为可注册插件，社区可加「表情包生成」「音效自动配」等 Agent |
| P2-5 | **i18n 中英双语** | waoowaoo | 前端文案抽 `locales/zh-CN.json` / `en-US.json`，为出海铺路 |
| P2-6 | **桌面端打包** | Novella / Toonflow | Tauri v2 打包成 exe，内置「一键启动 ComfyUI + 后端」双进程编排，消灭手工开两个黑窗的体验 |
| P2-7 | **Docker / 多租户** | waoowaoo / CineGen | 若要做 SaaS：后端容器化（ComfyUI 因 GPU 依赖单独部署为服务），加用户系统 + 配额 + 计费 |

---

## 四、目标架构（P0+P1 完成后）

```
┌─────────────────────────────────────────────────────────────┐
│  前端（画布 + 一致性看板 + 成本看板）                          │
│  分镜卡片可拖拽排序 / 单镜重跑 / 一键导出剪映草稿               │
└──────────────────────────┬──────────────────────────────────┘
                           ↓ REST + SSE 进度
┌─────────────────────────────────────────────────────────────┐
│  Flask 应用工厂（Blueprint 分层）                             │
│  projects │ novels │ scripts │ assets │ videos │ audio │ post │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  编排层 Orchestrator（Agent 化，共享 ProjectBlackboard）       │
│  剧本 Agent → 资产生成 Agent → 一致性校验 Agent →             │
│  分镜 Agent → 视频 Agent → 质检 Agent → 配音 Agent → 合成 Agent │
│  （每步产物落盘 = 断点续跑单元；可插入人工复核 Gate）           │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  Provider 抽象层（本地 / 云端双通道，按环节择优）               │
│  ComfyUI(Qwen/H3/FlashVSR) │ Wan │ Kling │ Vidu │ QwenTTS     │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  持久化底座：SQLite 任务队列 + 产物注册表 + 密钥加密 + profiles │
└─────────────────────────────────────────────────────────────┘

  保留并强化：coverage.py（原文守门） · continuity.py（跨集 Bible） · qc_client.py（多模态质检）
```

---

## 五、里程碑建议

| 阶段 | 内容 | 产出判据 |
|------|------|---------|
| **M1 底座** | P0-1 ~ P0-4 | 换机可跑（只改 .env）；kill 进程能续跑；仓库无明文密钥 |
| **M2 可控** | P1-1 ~ P1-4 | 一致性看板上线；能只重跑单镜；视频可选关键帧模式 |
| **M3 交付** | P2-1 / P2-3 | 能导出剪映草稿；能看到单集成本 |
| **M4 生态** | P2-2 / P2-4 ~ P2-7 | 按商业化需要择机 |

---

## 六、今天就能做的 5 件小事（各自 ≤30 分钟）

1. **把 `ai_config.json` / `llm_config.json` / `qc_config.json` 加进 `.gitignore`**——先止血，明文密钥外泄风险最高。
2. **在 `config.py` 顶部加 `load_dotenv()`**，把 `COMFYUI_WORKFLOWS_DIR` 等 6 个路径改成 `os.getenv(..., 默认值)`——为后续配置化铺路，零风险。
3. **把 `output/continuity/<项目>/` 的 bible / 校验结果做成一个只读接口 + 前端一个 Tab**——你已有的能力，先显性化，立刻能向人证明「我的系统比开源项目更懂原著」。
4. **给 `qc_client.py` 的每次判定补记 `model` + `耗时` + `token 用量`**——为 P2-3 成本看板提前攒数据，成本几乎为零。
5. **写一份 `docs/DIFFERENTIATION.md`**，把「原文覆盖率守门 + 跨集连贯性 Bible + 质检阻断入库」三条讲清楚——这是你对标开源项目的**答辩材料**，也是后续选型/招商的抓手。

---

## 七、一句话结论

> 开源漫剧项目卷的是「**生成能力**」，而你的系统已经在「**忠于原著的连贯叙事**」这条线上领先——`coverage.py` 和 `continuity.py` 是别人没有的。
> 因此优化的正确姿势**不是照抄它们的多模型堆料**，而是：**先用 P0 把工程底座补平（配置化 / 任务队列 / 密钥安全 / 架构分层），再用 P1 把一致性闭环和可控性（关键帧 + 画布）补齐，最后用 P2 打开交付出口（剪映导出 / 成本看板）**。
> 短板补完之日，你就是「开源漫剧项目里最懂故事、又最工程化」的那一个。
