# 漫剧生成系统 · 四大模块专项审计报告

> 审计日期：2026-09-19
> 审计范围：`app/` 全量（38,277 行）+ 工作流 JSON + 磁盘产物实证
> 聚焦模块：**① 提示词生成 ② 图片生成 ③ 视频生成 ④ AI 质检**，并附**全局**（状态机/幂等/透传/日志/资源清理）
> 审计方法：逐文件通读 + 全库 grep + Python AST 异常吞没扫描 + 工作流 JSON 解析 + **磁盘产物实证**（读 `output/` 下真实 manifest / QC 历史 / ComfyUI output 目录）

## 〇、可信度标注（重要）

报告中每条问题的可信度分两档，请按此判断优先级：

- **✅ 直查** = 审计人**亲自读源码并在报告中贴出原始片段**，已交叉验证，可立即修。
- **⚠️ 代理** = 由分模块深挖得到、附精确行号与代码片段，但未逐条二次复核；**修改前请先按行号看一眼**。

> ⚠️ 审计期间 `app/app.py`、`app/video_postprocess.py` 正在被其他会话修改（行号有漂移，`video_postprocess.py` 从 408→490 行）。**本报告行号为审计时刻快照，修改前请以 `grep` 定位为准。**

---

## 一、流水线整体流程图（文本版）

```
┌─ 原著输入 ─────────────────────────────────────────────────────────────┐
│ output/novels/<小说>/  →  novel_parser.py（编码探测/分章）              │
└──────────────────────────────┬─────────────────────────────────────────┘
                               ▼
┌─ ①【提示词生成模块】剧本阶段 ───────────────────────────────────────────┐
│ novel_to_script.py  ── convert_chapter_with_continuity（continuity.py） │
│   ├─ LLM 拆章 → 角色/道具/场景 bible + shots[]                          │
│   ├─ _norm_shots()：丢弃 narration、时长取 max(模型值,required_)        │
│   └─ script_prompt_analyzer.py：为每镜生成 prompt_h3（**六段**）        │
│ script_generator.py ── generate_script（**剧本质检是死代码，见 S1**）    │
│                    ↓ 产物 output/scripts/<项目>/第N集.json              │
├─ ①② 交界：资产提示词 ───────────────────────────────────────────────────┤
│ style_kit.apply_asset_style_all() → 角色/道具/场景 的 appearance 加风格  │
│ prompt_qc.preflight("asset")  ← 确定性预检（warn/repair/block）          │
└──────────────────────────────┬─────────────────────────────────────────┘
                               ▼
┌─ ②【图片生成模块】 ─────────────────────────────────────────────────────┐
│ _generate_asset_task（角色/物品/场景基础图 + 多视角）                    │
│   ├─ _generate_base_image → comfyui_client.queue_prompt → ComfyUI       │
│   ├─ _harden_no_watermark（**逻辑反转，见 S2**）                        │
│   └─ 资产质检 qc_client.check_image → 不达标重画                        │
│ _storyboard_worker（分镜图）                                            │
│   ├─ comfyui_client.build_storyboard_prompt（景别/机位/风格/画幅四注入） │
│   ├─ 参考图：_collect_asset_refs（**只认 4 个文件名，见 S6**）           │
│   ├─ chars_in 兜底（**匹配不上取第一个角色，见 S6**）                    │
│   ├─ prompt_qc.preflight("storyboard") → 出图 → QC → _qc_retry_hopeless │
│   └─ 达标 shutil.copy2 → output/storyboards/<项目>/[epNN/]shot_XX.png   │
│ keyframe.generate_end_frame（尾帧，**直写正式目录，见 S7**）             │
└──────────────────────────────┬─────────────────────────────────────────┘
                               ▼
┌─ ③【视频生成模块】 ─────────────────────────────────────────────────────┐
│ _video_generate_worker（mode = per_shot | keyframe | episode）           │
│   ├─ per_shot : 每镜一次 generate_h3_sequence(segments=[seg])            │
│   ├─ episode  : generate_h3_sequence_sequential(**N 段一次提交**)        │
│   │             └─ h3_episode_builder：previous_latent/handover 张量接力 │
│   ├─ 提示词：h3_prompt_kit.resolve_h3_prompt（六段/base 三段）           │
│   ├─ 参考图槽位填充（**空槽沿用模板示例图，见 S12**）                    │
│   ├─ 视频质检 qc_client.check_video（**只抽 3 帧，见 G9**）              │
│   └─ watermark_cleanup.clean_video（**探测失败即丢音轨，见 G6**）        │
├─ 音频链路 ──────────────────────────────────────────────────────────────┤
│ tts_client（逐句 TTS，**只读 dialogue**）→ dub_mix.mix_video_with_entries │
│ audio_qc（客观层恒跑 + AI 层）→ _mix_audio_qc                           │
│ ⚠️ dub_mix.shot_timeline 按文件名排序+下标对位（**整集模式错位，见 S5**） │
├─ 后期 ──────────────────────────────────────────────────────────────────┤
│ video_postprocess.concat_videos / add_subtitles                          │
│ upscale_client.upscale（FlashVSR 视频超分）                              │
│ video_postprocess.generate_final_video（**丢集号，见 S4**）              │
└──────────────────────────────┬─────────────────────────────────────────┘
                               ▼
┌─ ④【AI 质检模块】贯穿全链路，四个接入层 ────────────────────────────────┐
│ L1 确定性预检  prompt_qc.preflight()       零成本，出图前，fail-open     │
│ L2 客观层      audio_qc.quick_check()      零模型，永久执行              │
│ L3 视觉 AI 层  qc_client.check_image/video 多模态打分 + 关键缺陷词表     │
│ L4 硬闸        _qc_gate → 单向收紧 → 暂存区留证，不达标不入正式目录      │
│                                                                          │
│ 编排（autopilot.py）：_loop → _one_round → pipeline.step_*              │
│ 取消（cancellation.py）：LLM 调用前 / 退避休眠前检查（**不断 GPU 渲染**）│
└─────────────────────────────────────────────────────────────────────────┘

四大模块调用关系（谁给谁喂参数）：
  ①提示词 ──prompt_zh/prompt_h3──▶ ②图片 ──shot_XX.png──▶ ③视频
  ①提示词 ──style/aspect────────▶ ②③（style_kit 双端注入）
  ①提示词 ──style/desc/camera───▶ ④质检（作为判定基准）
  ②产物 ◀──重画──── ④质检（不达标 → 换 seed / 换提示词 → 重入 ②）
  ③产物 ◀──重配/重生成── ④质检
```

---

## 二、严重问题清单（P0）

> 判据：会导致**产出失败 / 画风错乱 / 死循环 / 大量 API·GPU 浪费 / 交付物被误登记**。

### S1 ✅ 剧本质检整条链路从未接入生产（死代码）
- **文件**：`app/script_generator.py:71`（定义 `generate_script_with_qc`）、`app/script_generator.py:136`（`check_script` 唯一调用点）、`app/app.py:2146`（实际入口）
- **证据（全库 grep 实测）**：`generate_script_with_qc` / `script_qc_ready` **零外部调用点**；`qc_client.check_script` 仅在死代码内部被调用。
- **问题**：剧本质检（结构/逻辑/风格/提示词质量/可执行性 + AI 深检 + `ai_qc_judge` 重试闭环）写了一整套，但生产路径调用的是无质检的 `generate_script`。
- **风险**：用户以为剧本已质检，实际从未执行。结构缺陷（角色引用未定义、台词超预算、时长不合规）**直接流入分镜/视频**，在烧掉数十分钟 GPU 后才暴露。
- **复现**：任意 `/api/script/generate` → `output/qc/<项目>/` 下永远不出现 `script_*.json`。
- **修复**：把 `app.py:2146` 改为调用 `generate_script_with_qc(...)`；或至少在 `generate_script` 返回前插入 `check_script` 并把 `qc_result` 透出；同时 `/api/qc/config` 的 `public_view` 如实暴露 `script_qc_active`。

---

### S2 ✅ 去水印逻辑反转：含"水印"的**正向**提示词被判为负向槽位
- **文件**：`app/comfyui_client.py:93`、`:841-846`、`:1364`、`:1456`（调用点）
- **原始代码**：
```python
# :93
NEGATIVE_HINTS = ("模糊", "水印", "blurry", "watermark", "low quality", "worst quality", "低质量", "噪点")

# :841-846
low = value.lower()
is_negative = any(h.lower() in low for h in NEGATIVE_HINTS)
if is_negative:
    ...
    inputs[field] = value.rstrip("，,;； ") + ", " + NO_WATERMARK_NEGATIVE_EXTRA
    changed[str(nid)] = f"{field}: 负向追加水印排除词"

# :1364  build_storyboard_prompt 产出的正向提示词里
"画面中不得出现任何文字、字幕、台词文本、水印、logo 或标识"
```
- **问题**：判据是「文本含 `NEGATIVE_HINTS` 任一关键词即视为负向槽位」，而 `水印` 同时出现在**负向词表**和**正向提示词的否定句**里。分镜图的正向提示词 100% 含"水印"二字 → 必然被判为负向 → 把 `NO_WATERMARK_NEGATIVE_EXTRA`（`"水印, watermark, logo, 标志, 签名, 日期戳, 文字, 字幕, 半透明水印, AI生成…"`）拼到**正向提示词尾部**。
- **风险**：
  1. 正向提示词里出现一整串裸负面词，Qwen-Edit 会把「水印 / logo / 签名 / AI生成字样」当**正向内容**绘制 —— 与"彻底去水印"目标完全相反；
  2. 连锁：`_find_positive_text_node` 用同一份 `NEGATIVE_HINTS` 排除节点，该节点此后会被判为"负向节点"，同一 `api_prompt` 内再做任何正向操作都会**定位错节点**。
- **复现**：任意分镜图生成（`app.py:1695` 单镜重跑 / 批量）→ 日志出现 `[去水印强化] …: 负向追加水印排除词`，而分镜工作流**根本没有负向节点**。
- **修复**：负向判定不能只看关键词命中，必须先排除"否定式正向前缀"：
```python
_NEG_PHRASE_RE = re.compile(r"(不得|严禁|禁止|不要|避免)[^，。；]{0,20}(水印|文字|字幕|logo)")

def _is_negative_slot(value: str) -> bool:
    if _NEG_PHRASE_RE.search(value):   # 「不得出现…水印」= 正向约束
        return False
    return any(h.lower() in value.lower() for h in NEGATIVE_HINTS)
```
`clean_conflict_negative_tokens`(:768) 必须同步改用同一判定，避免两端判据分叉。

---

### S3 ✅ H3 整集的"重试"是空操作 —— 三次重试跑的是同一支视频
- **文件**：`app/comfyui_client.py:1688-1690`（重试）→ `:1410-1411`（`_inject_seed`）
- **原始代码**：
```python
# :1688-1690
for attempt in range(max_retries + 1):
    attempts_used = attempt + 1
    if attempt > 0:
        cur_seed = None   # 重试换随机种子，避免重复失败结果
        logger.info(f"[H3-episode] 第 {attempt + 1} 次整片重试（随机种子）")

# :1410-1411
def _inject_seed(api_prompt: dict, seed) -> List[str]:
    if seed is None:
        return []          # ← 传 None 等于「不注入」，沿用工作流 JSON 里的字面量 seed
```
- **问题**：注释写"换随机种子"，代码却把 `cur_seed` 置为 `None`；`_inject_seed` 对 `None` 直接返回 → 沿用模板 `RandomNoise.noise_seed` 的**固定常量**（实测模板如 `201922334278287`）。前端随机化靠 `widgets_values` 的 `'randomize'`，而 `_ui_to_api` 走 `widgets_values_named`，该伪控件**不会提交**，API 模式下 ComfyUI 按字面量执行。重试时提示词也未改写（`_seg_qc_fn` 只写教训库，`generate_h3_sequence` 不读 `prompt_memory`）。
- **风险**：整集 QC 不通过后，第 2、3 次"重试"提交的是**完全相同的 prompt + 相同参考图 + 相同 seed** → 产物逐字节相同。22~44 段 H3 一次跑几十分钟，属**纯白烧 GPU**。
- **复现**：`app.py:3348` 起 episode 模式传 `seed=None`，`max_retries=2` → 同一支视频被跑 3 遍，最终仍 `failed`。
- **修复**：
```python
if attempt > 0:
    cur_seed = random.randint(1, 2 ** 31 - 1)      # 真正换种子
    # 同时把 _seg_qc_fn 沉淀的教训回写进各段提示词，否则换 seed 只是换骰子
```
并顺手给每段 `noise_seed` 加段序号偏移，避免全片段间 seed 相同。

---

### S4 ✅ 成片合成丢失集号 + 忽略失败返回值，坏产物被登记为验收交付物
- **文件**：`app/video_postprocess.py:422-445`、`:467`
- **原始代码**：
```python
# :422
def generate_final_video(self, script_path: str, project_name: str) -> str:   # ← 没有 episode_no
    ...
    videos_dir = os.path.join(PROJECT_OUTPUT_DIR, "videos", project_name)     # ← 平铺目录
    video_files = sorted([os.path.join(videos_dir, f)
                          for f in os.listdir(videos_dir) if f.endswith('.mp4')])
    ...
    output_path = os.path.join(final_dir, f"{project_name}.mp4")
    self.concat_videos(video_files, output_path)      # ← 返回值被忽略
    ...
    return output_path                                # ← 无条件 return
```
- **问题**：写入侧遵守"第 1 集平铺、第 2 集起 `epNN/`"（`app.py` 的 `_ep_dir`），而合成侧签名无 `episode_no`，只 `os.listdir` 平铺目录。且 `concat_videos` 失败返回 `""`，此处完全忽略。
- **风险**：
  1. 多集项目点"生成成片"，**第 2 集及以后完全漏掉**，接口仍返回 `success: true` 并自动 `register_final_deliverable` 登记进验收队列；
  2. 拼失败时返回一个**不存在的 URL** 且照样登记；
  3. `videos_dir` 里同时有 `epNN_full.mp4` 与逐镜文件时，会把整集和它的各镜**一起拼**，产出内容重复的成片 —— 体积够大能通过 100KB/2s 硬闸。
- **复现**：项目有 3 集，完成第 2 集 per_shot 视频 → `POST /api/final/video {"script_path":".../第2集.json"}` → 成片是第 1 集内容（或报"没有可合并的视频片段"）。
- **修复**：签名加 `episode_no` 并复用 `_ep_dir`；文件筛选只取 `shot_\d+.mp4` 且按 shot_id **数字**排序（参照 `nle_export.py:141-143` 的正确写法），显式排除 `*_full.mp4`；`if not self.concat_videos(...): return ""` 把失败上抛。

---

### S5 ✅ 分期时间轴按"文件名排序 + 下标对位"，整集模式下第 1 集时间轴彻底错位
- **文件**：`app/dub_mix.py:88-97`、`app/app.py` 的 `_mix_prepare`（`timeline = shot_timeline(script, videos_dir=seg_dir)`）
- **原始代码**：
```python
# dub_mix.py:88-97
files = sorted(os.path.join(videos_dir, f) for f in os.listdir(videos_dir)
               if f.lower().endswith((".mp4", ".mov", ".mkv")))
...
for i, shot in enumerate(shots):
    real = real_durations[i] if i < len(real_durations) and real_durations[i] > 0 else None
```
- **问题**：先按**文件名字典序**排序文件，再用**列表下标** `real_durations[i]` 去匹配 `shots[i]`，完全不看 `shot_id`。
- **风险**：
  1. **整集模式**：目录里只有 1 个 `ep01_full.mp4` → `shots[0]` 拿到**整集时长**，`shots[1..n]` 的 `start` 全部 ≥ 整集时长 → 后续镜台词全落在视频之外被 `-shortest` 截掉，**只有第 1 镜的台词能听到**；
  2. 目录里多/少一个文件（某镜被阻断、或混入 `*_full.mp4`）→ 后续所有镜头整体错位；
  3. `shot_100.mp4` 排在 `shot_11.mp4` 之前（>99 镜时）。
  且 `_mix_prepare` 只打一条 warning，**不阻断**，产物照样落盘并登记。
- **复现**：`video_mode=episode` 跑第 1 集 → `step_mix`。
- **修复**：`shot_timeline` 改为按 `shot_id` → 文件路径建映射（`_shot_seq` 归一化），逐一取该镜 mp4 时长，取不到才回落剧本时长；**整集模式（单文件）显式按剧本时长铺时间轴**，禁止把整集文件当成"第 1 镜的时长"。

---

### S6 ✅ 参考图静默丢失 / 静默用错角色 —— 外观锚点整集级污染
- **文件**：`app/app.py:1252-1264`（收集）、`app/pipeline.py:282`（探测）、`app/app.py:2595-2597`（兜底）
- **原始代码**：
```python
# app.py:1257 —— 只认 4 个文件名
for cand in ("front.png", "base.png", "front.jpg", "base.jpg"):
    p = os.path.join(d, cand)
    if os.path.isfile(p):
        ref = p; break

# pipeline.py:282 —— 却认 4 种扩展名
if fn.lower().endswith((".png", ".jpg", ".jpeg", ".webp")) ...

# app.py:2595-2597 —— 角色匹配不上就静默取第一个
chars_in = [n for n in (shot.get("characters_in_shot") or []) if n in char_idx]
if not chars_in:
    chars_in = [n for n in char_idx.keys()][:1]
```
- **问题（两个独立缺陷）**：
  1. **判据不一致**：`probe_assets` 认 `.webp`、认任意图片名；`_collect_asset_refs` 只认 4 个固定文件名。→ 出现「`probe_assets` 判定资产齐全 → `step_video` 却收不到任何参考图」的组合，视频退化为**无角色锚点**，全程无报错；
  2. **兜底危险**：`characters_in_shot` 名字对不上（剧本写"方源"、资产目录名"方源_主角"），就静默拿**资产索引里排序第一个角色**当主锚点画分镜图。`char_idx` 顺序来自 `sorted(os.listdir(...))`。
- **风险**：**整部剧的分镜图可能以错误角色为外观锚点**，而 `item["refs"]` 只记目录名，日志与界面看不出"本该是 A 却用了 B"。AI 质检也判不出来（它拿的参考图就是这张错的）。
- **复现**：剧本 `characters_in_shot: ["方源"]`，资产目录名 `方源_主角` → 匹配失败 → 取第一个键（可能是"古月族长"）→ 该镜主体变成另一个人。
- **修复**：
  1. 两处判据统一：`_collect_asset_refs` 的候选扩展为"目录内第一张图片"（同 `probe_assets` 的扩展名集合）；
  2. **匹配失败不要兜底** —— 记为 `error="角色名未命中资产索引：xxx（可用：…）"` 并跳过/告警；或先做别名归一（去 `_主角` 后缀、去空白/《》）；
  3. 收集到空参考图时把 `app.logger.info` 改为 `warning` 并在 `video_item` 里记 `no_reference: true`。

---

### S7 ✅ 尾帧**直写正式目录**、质检不通过仍保留 → 链式污染且永久复用
- **文件**：`app/keyframe.py:413`（写盘）、`:418`（质检）、`:279`/`:262`（"已存在"判据）
- **原始代码**：
```python
# keyframe.py:413-418 —— 先写正式目录，后质检
r = generate_end_frame(start, prompt, item["end_path"], ...)
...
v_ok, v_reason = verify_cb(item["end_path"], shot, item)

# keyframe.py:279 / :262
"end_path": end_p, "has_start": has_start,
has_end = os.path.isfile(end_p) and os.path.getsize(end_p) > 0
```
- **问题**：分镜/视频都是"先落暂存区，达标才入正式目录"；**尾帧直接写 `keyframes/`**，质检失败（`ok=False`）只改内存标记，文件仍在。下一轮 `plan_keyframes(only_missing=True)` 据 `has_end` 判"尾帧已存在，跳过"。
- **风险**：一张被质检判为坏图的尾帧被**永久复用**；链式模式下它同时是**下一镜的首帧**（`_mirror` → `start_frame_link_path`），错误沿链扩散到全片，且质检再也无法纠正（永远跳过）。
- **复现**：任一尾帧被判不通过 → 重跑 `/api/keyframes/generate`（默认 `only_missing=true`）→ 该镜不再出现在 `need_gen` 列表。
- **修复**：尾帧也走暂存目录（如 `QC_DIR/<项目>/keyframe_scratch/`），`verify_cb` 通过后再 `shutil.move` 到 `keyframes/`；失败者不落地。使 `has_end` 语义与分镜/视频一致。

---

### S8 ✅ `project_store` / `ai_config` 落盘无锁 + 固定 `.tmp` 名 → 注册表可被写坏
- **文件**：`app/project_store.py:92-97`、`app/ai_config.py:128-133`
- **原始代码**：
```python
# project_store.py:92-97
tmp = path + ".tmp"                      # ← 固定名，多线程共用
with open(tmp, "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
os.replace(tmp, path)
```
- **实测证据**：`grep -c "Lock\|RLock"` → `project_store.py: 0`、`ai_config.py: 0`、`ai_chat.py: 0`。
- **问题**：`os.replace` 本身原子，但**两个线程共用同一个 `.tmp` 文件**：A 写完一半、B 以 `"w"` 截断重写，A 再 `os.replace` → 发布出去的是**交错/截断的 JSON**。且 `load_index()` 捕获 `ValueError` 后**静默返回默认空索引**。`project_store` 所有 CRUD 都是 `load_index → 内存改 → save_index` 的读改写，完全无锁 —— 并发下经典的**丢失更新**。
- **风险**：
  1. `output/projects/index.json` 被写坏 → `load_index()` 返回 `{"projects": []}` → 用户**所有项目从列表消失**（数据还在磁盘，但界面看不到、`get_project` 找不到、托管 `enabled_projects()` 为空）；
  2. 并发新建项目 + 绑定剧本 → 后写覆盖先写，新建的项目凭空消失、`episode_count` 回退；
  3. `ai_config.json` 同理 → 快速连续保存"文本分析/质检"两个模块，后者用旧快照覆盖前者（**表现为"保存了但没生效"**）。
- **复现**：并发 `POST /api/projects/<id>/script/bind` 与 `POST /api/ai/modules/text`，反复压测后 `index.json` 变空对象。
- **修复**：
```python
# 1) 临时名唯一化
tmp = f"{path}.{os.getpid()}.{threading.get_ident()}.{uuid4().hex[:6]}.tmp"
# 2) 模块级 RLock 包住整个读改写
_INDEX_LOCK = threading.RLock()
```

---

### S9 ✅ ComfyUI 远端任务无任何取消手段 —— "暂停"后 GPU 照跑
- **文件**：`app/comfyui_client.py:659-680`（`wait_for_completion`）、`app/upscale_client.py:686-730`
- **原始代码**：
```python
def wait_for_completion(self, prompt_id: str, timeout: int = 1800) -> dict:
    start = time.time()
    while time.time() - start < timeout:
        try:
            history = self.get_history(prompt_id)
            ...
        except Exception as e:
            logger.debug(f"轮询历史失败: {e}")
        time.sleep(3)
    logger.warning(f"等待超时: {prompt_id}")
    return {}          # ← 超时只返回空，不取消远端任务
```
- **问题**：轮询循环里**不引用 `cancellation`**；超时后只 `return {}`，既不 `POST /interrupt` 也不删 `/queue` 项（全项目 grep 无 `/interrupt` 调用）。且返回 `{}` 与"正常但无输出"不可区分 → 上层按失败处理并 **`break` 出重试循环**，即"超时=不重试"。
- **风险**：
  1. 用户在托管页点"暂停"后，ComfyUI 队列里的当前段与后续段**继续占用 GPU**，用户以为已腾显存；
  2. 超时后远端继续跑白烧算力，用户手动重跑 → 队列再叠一个 → 提交数自我放大；
  3. H3 22 段可跑半小时以上，远端产物成为孤儿。
- **复现**：托管跑 H3 整集 → 点"暂停" → `_loop` 在步骤边界停下，但 ComfyUI 继续执行，GPU 占用不降。
- **修复**：
```python
# 1) 轮询内检查中止
if cancellation.should_stop():
    self.interrupt(prompt_id); raise Cancelled(...)
# 2) 超时/取消后清理远端
POST {base_url}/interrupt
POST {base_url}/queue  {"delete": [prompt_id]}
# 3) 区分 None（超时）与 []（真无输出），超时走一次重试
```
> ⚠️ 注意：`cancellation` 检查点**刻意不放进 ComfyUI 渲染循环**（会留半成品）——此处加的是"超时/取消后的远端清理"，两者不冲突。

---

### S10 ✅ `dub_mix` 的无限音频源时长保护是**条件式**的 → 同型历史事故
- **文件**：`app/dub_mix.py:229-232`、`:273-274`
- **原始代码**：
```python
cmd = [FFMPEG, "-y", "-v", "error", "-i", video_path, "-f", "lavfi"]
if vdur > 0:
    cmd += ["-t", f"{vdur:.3f}"]          # ← 输入侧限时，条件式
cmd += ["-i", f"anullsrc=r={sr}:cl=mono"]
...
if vdur > 0:
    cmd += ["-shortest"]                   # ← 输出侧限时，条件式
```
- **问题**：`anullsrc` 是**无限源**。`vdur = float(vinfo.get("duration") or 0)`，而 `probe_media` 把 `format.duration` 解析失败一律归为 `0`，前置校验只查 `has_video`、**不查 duration**。于是当 mp4 有视频流但容器时长读不出时，`vdur=0` → 既不限时长也不 `-shortest`，`amix(duration=first)` 以无限静音底轨为基准 → 音频无限延伸，输出被无限写入。
- **风险**：与历史「无限 lavfi 写盘把 C 盘写满 80GB」**同一类事故**。唯一兜底是 `subprocess.run(timeout=...)`，而 `timeout` 来自请求体 `params`，**调用方可任意放大**。
- **复现**：构造 `format.duration` 缺失但含视频流的 mp4 → `POST /api/mix/generate`，`params={"timeout": 100000000}` → 持续写直至磁盘满。
- **修复**：时长保护改**无条件**：
```python
if vdur <= 0:
    raise DubMixError(f"无法读取成片时长，拒绝合成（避免无限音频流）：{video_path}")
```
并对 `params["timeout"]` 设服务端上限（`min(int(...), 3600)`）。

---

### S11 ✅ 资产批量生成无逐项异常隔离 —— 一次瞬时故障整批判失败、已成功结果丢弃
- **文件**：`app/app.py:2246`（`for i, asset in enumerate(assets)`）；异常兜底在 worker 外层
- **问题**：分镜 worker 每镜有独立 `try/except`，**资产链路没有**。`generate_multiview` 上传基础图失败会主动 `raise RuntimeError`，`queue_prompt` 遇 5xx/超时也会抛。任一处抛出 → 整个批次被标 `failed`，局部变量 `results`（已成功资产的结果）**全部丢弃**。
- **风险**：20 个资产，第 7 个时 ComfyUI 一次连接抖动 → 20 个整体判失败，前 6 个已入 `base.png` 的成果用户不可见，重跑又从第 1 个开始（叠加 S13 的"无逐资产断点续跑"，代价成倍放大）。
- **修复**：`for` 体内部包一层 `try/except Exception as e:`，失败时 `results.append({"name": name, "success": False, "error": str(e)})` 后 `continue`。

---

### S12 ✅ H3 参考图缺失时静默沿用"模板内置示例图"
- **文件**：`app/comfyui_client.py:1836-1845`
- **原始代码**：
```python
# 参考图不足时不再沿用模板自带的示例图（会污染角色外观），
# 用本段第一张参考图补齐空槽（纯单参考即重复同一张）。
fill_src = local_refs[0] if local_refs else None
for j in range(len(slots)):
    is_fill = j >= len(local_refs)
    rp = local_refs[j] if not is_fill else fill_src
    if not rp:
        logger.warning(f"段{i + 1} 无参考图，参考槽 {slots[j]} 沿用模板默认图")
        continue
```
- **问题**：注释承认"沿用模板默认图会污染角色外观"，但实现上 `local_refs` 为空时 `fill_src=None` → 直接 `continue`，**保留模板 `LoadImage` 自带的示例人物图**。而此时提示词走的可能是 T2VA 分支（无任何 `<Picture N>` 声明）—— "提示词说无参考图、工作流里却塞了一张陌生角色图"。
- **风险**：出片人物与设定完全不符，`prompt_qc` 无从发现（它只检查"有参考图却缺 `<Picture 1>`"）。示例图会在画面里生成一个与剧本无关的人物，而 QC 可能因"画面里有个人"而通过。
- **修复**：无任何可用参考图时**拒绝提交该段**（或降级为 T2VA 且把模板 `LoadImage` 从 prompt 里彻底剔除），至少要计入 `video_item["error"]`，不能静默继续。

---

### S13 ⚠️ 资产步骤"partial → 全量重烧"
- **文件**：`app/pipeline.py:499-512`（只按 kind 判 done）+ `app/app.py:2246`（无逐资产跳过）
- **问题**：`probe_assets` 是 **kind 粒度**的"全齐才算 done"。一类里缺 1 个资产，`step_assets` 把该类型**全部 assets** 传给 worker，worker 从第一项起无条件走"预检→出图→质检→多视图"。
- **风险**：30 个角色缺 1 个 → 重跑 30 次基础图 + N 次多视图 + 30 组多模态质检调用。托管场景每轮重试都可能触发。**这是全链路最贵的一处浪费**。
- **修复**：`probe_assets` 升级为返回 `missing` 名单，`step_assets` 只传缺失项；或在 worker 循环内加"该资产 base 非空 → skipped"判据。

---

## 三、一般问题清单（P1 / P2）

### G1 ✅ `_qc_retry_hopeless` 只接入了 2 处，其余 4 个重试循环仍会重复烧 GPU
- **已接入**：分镜 worker、单镜视频 worker
- **未接入**：① 资产基础图循环 ② 资产多视角循环（**任意一个视角不达标就整组重生成**）③ 整集模式循环（N 段一次重生成，最贵）④ 尾帧循环
- **修复**：把 `_qc_retry_hopeless` 抽到公共模块（避免 `app.py` ↔ `comfyui_client` 循环依赖），四处复用。

### G2 ⚠️ `save_config` 用 `bool(v)` 解析开关，`"false"` 会被存成 True
- **文件**：`app/qc_client.py:617-618`：`cfg[k] = bool(v)`
- **讽刺点**：同文件 `_as_bool`(664-680) 的 docstring **恰好记录了这条坑**（"不能用 `bool(v)`：字符串 `"false"`/`"0"` 一律判 True"），但 `save_config` 自己没改。后续 `load_config_dict` → `_normalize` → `_as_bool(True)` 收到的是已污染的真值，**无法回滚**。
- **修复**：改为 `cfg[k] = _as_bool(v, ...)`。

### G3 ✅ 质检总览接口恒返回空统计（目录结构判错 + 死守卫）
- **文件**：`app/app.py:5669-5687`
- **原始代码**：
```python
project = _safe_project(request.args.get('project', ''))
if not project:                                  # ← _safe_project('') 返回字面量 'project'，恒真，死守卫
    return jsonify({"success": False, "error": "缺少 project 参数"}), 400
for entry in os.listdir(QC_DIR):
    shot_dir = os.path.join(QC_DIR, entry)
    if not os.path.isdir(shot_dir) or not entry.startswith('shot_'):   # ← 实际无 shot_* 目录
        continue
```
- **问题**：实际写入路径是 `QC_DIR/<项目>/<kind>_<shot>.json`，而这里枚举的是 `QC_DIR` 下以 `shot_` 开头的**目录** → 一个都匹配不到。叠加 `if not project` 死守卫。
- **风险**：质检总览页永远"0 通过 / 0 失败"，用户**以为质检从未运行**（实际 152 个质检历史文件都在）。
- **修复**：改为枚举 `os.path.join(QC_DIR, project)` 下的 `*.json`，用文件名推 `kind`/`shot_key`；`read_history` 返回的是 `{"records":[...]}` 字典，字段是 `passed`/`time`（不是 `verdict`/`timestamp`）。

### G4 ✅ `_safe_project('')` 恒返回真值 `'project'` → 9 处死守卫
- **实现**：`app/project_store.py:53-58`：`key = key.strip("_") or "project"`
- **死守卫**：`app.py` 的 9 处 `if not project:`（`api_keyframes_plan`、`api_qc_project_summary`、`api_autopilot_start` 等）
- **风险**：请求漏传项目名时不是被拒绝，而是落到共享的 `project` 命名空间 —— `api_autopilot_start` 会创建 `output/autopilot/project/plan.json` 幽灵计划（用户"开启了托管"但没有任何项目被生产）；写类接口可能互相覆盖。
- **修复**：判空必须看**原始入参**。项目内已有正确范式 `_audio_qc_visuals_key`，建议抽出 `_project_or_400(raw)`。
- **注意**：已确认全库**不存在** `_safe_project(x) or 兜底` 写法（grep 零命中），此陷阱已被正确规避过。

### G5 ⚠️ 任务 ID 秒级时间戳 + 无幂等键 → 同秒双 worker
- **文件**：`app.py` 的 `storyboard_/video_/dub_/mix_{project}_{int(time.time())}`
- **问题**：同一秒两次 POST → `generation_state[task_id] = {...}` 第二次**覆盖**第一条记录，但**两个线程都已启动** → 双 worker 并发跑同一批镜头，`results` 数组互相 append、`progress` 来回跳、`shutil.move` 抢占同一目标路径。
- **修复**：`task_id = uuid4().hex[:12]`；接口入口加"同 (project, episode, step) 已有 running 任务 → 返回该 task_id 并 `reused: true`"。
- **另**：`episodes_` 互斥是 check-then-act，判定与登记**不在同一临界区**（TOCTOU），且只查内存表（重启后失明）。

### G6 ✅ `probe_has_audio` 失败即认定"无音轨" → 去水印时静默丢弃 H3 原生音效
- **文件**：`app/watermark_cleanup.py:46-58`、`:136-141`
- **原始代码**：
```python
except Exception:      # pragma: no cover - 环境相关
    return False       # ← "探测失败" 被等价于 "没有音轨"
...
has_audio = probe_has_audio(path)
audio_args = ["-c:a", "copy"] if has_audio else ["-an"]
```
- **问题**：探测异常时 `clean_video` 加 `-an` 并把结果 `os.replace` 回原路径 —— **原视频连同音轨一起被替换掉**。随后补一条**静音** AAC，`has_audio_after=True`，音效分离拿到的是一条静音轨。
- **风险**：成片彻底没有音效，表面看"有音轨" —— 正是该文件注释声称已修复的历史问题。
- **修复**：`probe_has_audio` 改三态（`True/False/None`），`None` 时**不加 `-an`**，跳过本次去水印并记录 `error`，绝不改写原文件。

### G7 ⚠️ 断点续跑判据只判"文件存在 + size>0"，半截文件被当已完成
- 分镜/视频/尾帧/manifest 全部同型；`_episode_video_stats` 稍严（`>1024`）
- **风险**：一次超时/被杀留下半截 mp4（远超 1KB）→ `probe_final`/`probe_mix`/`probe_video` 判"已完成" → 后续步骤**永久跳过**，坏成片进验收。
- **修复**：判据改为"可播放性"（`probe_media().ok and has_video and duration>0`）；所有写产物改"写 `.tmp` → 校验 → `os.replace`"。

### G8 ✅ ComfyUI output / 质检暂存区 / 去水印备份只增不减（实测 1.1GB+395MB）
- **实测磁盘**：`output/qc` = 1.1GB（`_watermark_backup` 604MB + `蛊真人精校版` 471MB，含 `storyboard_scratch` 296MB / `assets_scratch` 168MB）；ComfyUI `output/comic_drama_sb/` = 395MB / 328 文件；基础图 143 张、`sb_ref_*.png` 170 张。
- **根因**：图片链路用 `shutil.copy2`（视频链路用 `shutil.move`）；基础图/多视角**从不设置 `filename_prefix`**；scratch 保存**每一次**尝试的产物，不达标越多残留越多；全项目无任何清理代码。
- **叠加缺陷**：`delete_project` 的 `_match_project_entries` 只扫一层，`output/qc/audio/<项目>` 这种**两层结构永远不被移入回收站**。
- **修复**：基础图覆写 `filename_prefix`；图片链路改 `move`；scratch 只保留最近 N 份；`project_kind_roots()` 增加 `("qc_audio", QC_DIR/"audio")` 与 `_watermark_backup` 条目。

### G9 ⚠️ 视频质检维度缺失：不判时长/帧率/音轨，`frame_meta.verified` 算了不用
- `check_video` = "3 张静帧 + 一份写死的提示词"，**不判**：视频时长与 `shot.duration` 是否一致（H3 只出 1 秒也会通过）、帧率/丢帧、音轨存在/无声/削波、镜头跳变。而提示词里却要求模型判"闪烁、撕裂"——**稀疏静帧在原理上无法体现时域缺陷**。
- **修复**：加确定性闸门（时长偏差阈值 + `ffprobe` 判音频流 + `frame_meta.verified=False` 计入 issues）；时域缺陷改用连续帧差分。

### G10 ⚠️ 重试时用"未自愈的原提示词"覆盖 `prompt_qc` 自愈结果
- 分镜 `orig_prompt = prompt` 取自**自愈之前**，重试时 `prompt = orig_prompt` → 首轮确定性补上的"风格声明 / 禁文字约束 / 剔除台词泄漏"在重试轮**全部丢失**，且不再过预检。
- **风险**：**越重试越差**，白烧 GPU。视频链路无此问题。
- **修复**：`orig_prompt` 记录**自愈后**的提示词（"指纹基准"与"实际生成用提示词"分开存两个变量）；每轮生成前统一跑 `prompt_qc.preflight`（幂等）。

### G11 ⚠️ `parse_verdict` 宽松解析白名单不全 → 合法中文结论被判不通过
- `passed` 白名单缺 `"通过"/"是"/"符合"/"ok"` → 直接 `False`，`score>=pass_score` 救不回来；`style_match` 更脆弱：**任何非白名单字符串**都被折成 `False`，而 `False` 在 `_apply_style_gate` 里被当作**明确的"风格不符"证据**强制失败。
- **风险**：典型的"字段值写法差异 = 100% 判不通过"，直接触发换 seed 重画。
- **修复**：补白名单；字符串无法识别时视为**未知**（`None`）而不是 `False`。

### G12 ⚠️ `{pass_score}` 占位符不存在于任何默认提示词 → 模型不知合格线
- 替换处确实在 `.replace("{pass_score}", ...)`，但 `DEFAULT_IMAGE_PROMPT`/`VIDEO`/`AUDIO` **均无该占位符**。模型只按直觉给 `pass=true, score=68`，代码随后用 `score >= pass_score(70)` 强行翻成 `passed=False`。
- **风险**：典型假失败 —— 画面其实可用，却被判不通过并进入重试循环。
- **修复**：默认提示词里显式写入判定线；或让模型只输出 `score`，由代码统一比较。

### G13 ⚠️ 质检配置逐镜反复读盘 + 密钥解密（`load_config` 无缓存）
- 调用点位于**逐镜循环内**（分镜 worker、视频 worker、`_prompt_preflight`），单镜最多 2 次 → 每次都是"读 JSON + 读 `secrets.enc` + 一次 Fernet 解密"。单集 56 镜 ≈ 112 次。
- **风险**：纯开销（非正确性），但运行中改配置会造成**同批任务新旧配置混用**，更难复现。
- **修复**：加带 mtime 校验的进程内缓存，或 worker 进循环前读一次并传参（资产 worker 已这么做，可对齐）。

### G14 ⚠️ `_empty_config()["enabled"]=False` + 配置读失败静默回退 → 质检可能被静默关闭
- 新部署（无 `qc_config.json`）→ 全部质检静默跳过；**更危险**的是配置损坏/权限异常时同样回退到 `enabled=False`，只有一条 warning，UI 看不到。
- **修复**：区分"文件不存在（正常，默认关）"与"文件存在但解析失败（异常）"，后者 fail-loud。

### G15 ⚠️ 音频 AI 层结论被"图片关键缺陷词表"二次硬闸
- `check_audio` → `_run_vision` → `parse_verdict` → `_finalize_verdict` → `find_critical_issues` 用**默认（图片）词表**；音频结论里的"错位/变形"会命中图片词表 → 跨域误判。
- 而 `find_critical_issues` 本身**支持 `keywords` 参数**，音频侧只是没用上。
- **修复**：`_run_vision` 增加 `kind`/`keywords` 参数，音频传 `AUDIO_CRITICAL_KEYWORDS`。

### G16 ⚠️ 质检历史记录缺关键排查字段
- `_qc_record_verdict` 落盘字段里**没有**：`raw`（模型原始响应，verdict 里其实有）、`model`、`call_url`、`api_attempts`、参考图列表、prompt 指纹。
- 且资产链路有 app 日志（call_url/model/score/latency），**分镜与视频连这条日志都没有**。
- **风险**：出现"同一张图昨天过今天不过"时无法复盘；换质检模型后无法回溯历史结论来源。

### G17 ⚠️ 尾帧质检结论不落盘 `output/qc/`
- 分镜/视频/资产都调 `_qc_record_verdict`，**尾帧既无 `_qc_record_verdict` 也无 `_record_qc_lesson`**，结论只落在内存 `r["qc"]`。
- **风险**：尾帧是最容易污染下游的资产，却无法复盘、无法沉淀教训。

### G18 ⚠️ 三处 fail-open / fail-closed 口径不一致，且无文档说明
- 图片/视频/资产质检异常 → **fail-closed**（阻断）
- 尾帧质检回调异常 → **fail-open**（`logger.warning` 后按通过处理）
- 成片音频质检 → **只记录不阻断**
- **修复**：统一策略并写进 docstring；若坚持尾帧 fail-open，必须落盘 `qc_skipped_due_to_error=True` 并在 UI 高亮。

### G19 ⚠️ 竖屏 9:16 只在风格串含关键词时落地，否则静默回落模板 16:9
- `style_kit.resolve()` 未传 `default_ratio`，只认风格串里的 `竖屏/portrait/9:16`；模板 `ResolutionSelector = 16:9 × 0.5MP` → 960×544 **横屏**。
- **风险**：用户以为选了竖屏、实际产出横屏，无任何告警。
- **修复**：给链路显式默认画幅（项目 config 的 `aspect_ratio`，缺省 `(9,16)`）；`size is None` 时打 warning。

### G20 ✅ `main.py` 与 `serve.main()` 签名不匹配 → EXE/桌面入口启动即 TypeError
- **原始代码**：
```python
# main.py:32-34
def run_web_mode(port, host):
    from serve import main
    main(port, host)          # ← 传 2 个位置参数

# app/serve.py:113
def main() -> int:            # ← 不收参数
```
- **风险**：`TypeError: main() takes 0 positional arguments but 2 were given`。打包 EXE 入口正是 `main.py`（见 `漫剧工坊.spec`），`run_app.bat` 走 `python serve.py` 掩盖了该问题。
- **复现**：`python main.py` 或运行 `dist/漫剧工坊.exe`。
- **修复**：`run_web_mode` 改为通过环境变量传参（`APP_PORT`/`APP_HOST`，`serve._safe_run` 已在读）后调用无参 `main()`。

---

## 四、优化建议清单（P3）

| # | 建议 | 位置 |
|---|---|---|
| O1 | **黑图/纯色图确定性检测**：入库前加 `Image.open().verify()` + `ImageStat.Stat(im.convert("L")).stddev < 3`；当前全库**零像素统计**，只靠多模态模型自觉 | `qc_client.py:1493` |
| O2 | **产物旁路元数据**：每个正式产物写 `<name>.meta.json`（seed/prompt/工作流 SHA256/耗时/生效参数）；当前 manifest 只记工作流**文件名** | `app.py` manifest 构造处 |
| O3 | **seed 规范化与落盘**：第 1 轮也用真实随机 seed 并**始终注入**（不依赖模板常量）；写进 manifest 顶层。实测 `manifest.shots[0].qc.history[0].seed == null` | `app.py` worker 种子初始化 |
| O4 | **暴露可调参数**：steps/cfg/sampler/模型名/LoRA/负面词**全链路无入口**，实际取值 100% 来自工作流模板，不可调不可审计 | `comfyui_client.generate_*` 签名 |
| O5 | **`PSEUDO_WIDGETS` 补 `control_after_generate`**：它不在剔除名单 → 作为未声明输入长期提交，自检报告有噪声（也正是 S3 的证据） | `comfyui_client.py:82-85` |
| O6 | **`camera` 解析统一**：H3 链路未复用 `camera_key/camera_spec`，同一字段在分镜图链路与视频链路被解释成两种意思 | `h3_prompt_kit.py:275` |
| O7 | **景别与画面内容互斥检测**：`camera="中景"` + description 写"抬脚踩进血水、镜头自脚面俯拍"→ 正向提示词同时含"严禁出现脚部"与"脚面俯拍"，模型摇摆。`prompt_qc` 五类 kind 均无此判据 | `prompt_qc._check_storyboard` |
| O8 | **LLM 文本换行归一**：`description` 含 `\n` 会破坏 H3 单行时间轴结构（`coverage.py:216` 已有正确范式，提示词链路全部缺失） | `h3_prompt_kit.py:216-226` |
| O9 | **截断改句边界 + 常量收敛**：全部 `[:N]` 字符切片（200/400/60/120/30…），限长硬编码分散在 4 个文件；`keyframe.build_end_frame_prompt` 只读 `description`，**丢失 `visual_detail`** | `novel_to_script.py` / `keyframe.py:113` |
| O10 | **角色外貌单一来源**：资产 120 字 vs LLM 现场 60 字两条路径，六段齐全的 LLM 版本会被直接采用（`validate` 只校验段名）→ 同角色跨镜外貌串不一致 | `comfyui_client.py:1923` vs `script_prompt_analyzer.py:51` |
| O11 | **风格兜底硬编码 `国漫3D渲染`**：`style_of(shot)` 未传项目风格 → 旧剧本/无 style 时写出"全片风格统一为国漫3D渲染"，与分镜链路"以参考图为准"不一致；`_repair_h3` 只补无文字约束、**不补风格** | `h3_prompt_kit.py:66/495` |
| O12 | **`negative_for_style` 自相矛盾**：`国漫 + 偏写实` 时正向要"偏写实"，负向却压"写实照片/真人摄影/3D 写实渲染"（豁免被第一个分支绕过）；且该组合正是 `style_kit` docstring 的官方示例 | `style_kit.py:275-283` |
| O13 | **空资产提示词被风格后缀"洗白"**：`with_style("")` 返回纯风格后缀 → 长度超 `_ASSET_MIN_LEN` 且 `style_declared` 为真 → 预检放过 → 资产外观完全随机 | `style_kit.py:562-570` |
| O14 | **记忆库过时教训永不过期**：`_MAX_AGE_DAYS=120` **全库从未被引用**；`_flush` 用 `"w"` 全量重写（与 docstring 声称的"追加式"不符），多进程会静默丢教训；合并条件只比 `phash+kind` 不比 `issues` → 同提示词的**其它**缺陷记录被删 | `prompt_memory.py:79-82/159-166/190-194` |
| O15 | **`dub_mix` 零日志、`continuity` 仅 3 条**：混音（最易音画不同步）无任何入参/出参日志；跨集连贯性出错时日志没有"收到哪些前情、写回了什么" | `dub_mix.py` / `continuity.py` |
| O16 | **`autopilot.stop()` 无调用方 + 全库无 `atexit`/`signal` handler** → 无优雅停机，Ctrl+C 硬杀可能留下 SQLite `interrupted` 与半写 JSON | `autopilot.py:567` / `serve.py:23`（`import signal` 未使用） |
| O17 | **内存任务表无界增长**：`generation_state`/`upscale_tasks`/`dub_tasks`/`mix_tasks`/`agent_core._JOBS` **全库无任何 pop/clear**，24/7 挂机常驻内存单调上涨 | `app.py:162/3850/6890/7667` |
| O18 | **`autopilot.status()` 的 `running` 语义错误**：等于"守护线程存活"，暂停/idle 时仍为 `true`；缺 `producing`/`state` 枚举 | `autopilot.py:601-604` |
| O19 | **`_STATE` 除 `paused` 外不落盘**：重启后 `MIN_RERUN_INTERVAL=45s` 节流失效、`_ATTEMPTS` 连续失败计数归零 | `autopilot.py:173-195` |
| O20 | **`delete_project` 不停止在跑 worker** → "删了又回来"（worker 在 move 之后又 `os.makedirs`） | `project_store.py:353-395` |
| O21 | **三套镜号归一化并存**（`_shot_num_key`/`_shot_seq`/`_norm_shot_key`/`keyframe._seq`）：含多个数字的 shot_id（`"S01-C02"`）会算出不同结果 | 4 处 |
| O22 | **`_write_json` 无锁在 AI 对话历史同样存在**：`ai_chat` 全库零 Lock，多项目并发会话会互相覆盖（最长 200 条历史丢失） | `ai_chat.py:158-165` |
| O23 | **阈值"同一口径多处定义"清单**：`SHOT_DURATION_MIN/MAX`(novel_to_script) vs `SHOT_DURATION_MIN_OK/MAX_OK`(qc_client) 值一致但重复；**台词长度 `SHOT_SPEECH_BUDGET_CHARS=30` vs `_AUDIO_MAX_CHARS=60` 值不一致**；语速 4.5 两处；音频三个软阈值默认值 5 处 | 见各文件 |
| O24 | **提示词里的阈值文案与代码阈值无联动**：`"不得超过 8 分"`、`"score ≤ 50"`、`"3-12 秒"`、`"不超过 30 字"` 只写在自然语言里，改常量不会同步 → 模型与代码判定长期漂移 | `qc_client.py:61/127/393/397` |
| O25 | **`parse_verdict` 丢弃 `categories`/`suggestions`** → 剧本 QC 的 AI 五维分与修复建议 100% 丢失；`ai_qc_judge` 恒返回 `should_continue=True`（质检结论无约束力），且在 `score` 为 `None` 时 `None < 50` 抛 `TypeError` | `qc_client.py:1353-1367` / `ai_qc_judge.py:71-93` |

---

## 五、高风险阻塞项汇总（建议立即修）

> 这批问题会**直接导致**：漫剧产出失败 / 画风错乱 / 死循环 / 大量 API·GPU 浪费 / 坏交付物进验收。

| 序号 | 阻塞项 | 后果类型 | 位置 | 可信度 |
|---|---|---|---|---|
| **1** | H3 整集重试是空操作（`cur_seed=None`） | 💸 **纯白烧 GPU**（一次数十分钟×2） | `comfyui_client.py:1688` | ✅ |
| **2** | 成片合成丢集号 + 忽略失败 + 重复拼接 | 🚫 **坏成片被登记为验收交付物** | `video_postprocess.py:422-445` | ✅ |
| **3** | 尾帧直写正式目录、失败仍保留 | 🎨 **链式画风错乱 + 永久复用坏图** | `keyframe.py:413-418/279` | ✅ |
| **4** | 参考图静默丢失 / 静默取第一个角色 | 🎨 **整集外观锚点错误** | `app.py:1257/2595` | ✅ |
| **5** | 分期时间轴按文件名+下标对位 | 🚫 **整集模式只有第 1 镜台词能听** | `dub_mix.py:88-97` | ✅ |
| **6** | 去水印逻辑反转（正向词被当负向） | 🎨 **反而画出水印/文字** | `comfyui_client.py:93/841/1364` | ✅ |
| **7** | `project_store`/`ai_config` 无锁 + 固定 `.tmp` | 🚫 **项目列表全消失 / 配置静默回滚** | `project_store.py:92` / `ai_config.py:130` | ✅ |
| **8** | 剧本质检整条链路是死代码 | 🚫 **结构缺陷直通 GPU** | `script_generator.py:71` | ✅ |
| **9** | 质检总览恒空 + `_safe_project('')` 死守卫 | 📉 **质检能力被误认为没运行** | `app.py:5669-5687` | ✅ |
| **10** | ComfyUI 远端任务无取消 | 💸 **"暂停"后 GPU 照跑** | `comfyui_client.py:659-680` | ✅ |
| **11** | `dub_mix` anullsrc 条件式时长保护 | 💥 **无限写盘（历史事故同型）** | `dub_mix.py:229-232` | ✅ |
| **12** | 资产批量无逐项异常隔离 | 🚫 **一坏全批、已成功结果丢弃** | `app.py:2246` | ✅ |
| **13** | H3 无参考图时沿用模板示例图 | 🎨 **画面出现无关人物** | `comfyui_client.py:1836-1845` | ✅ |
| **14** | 资产 partial → 全量重烧 | 💸 **最贵的一处浪费** | `pipeline.py:499` | ⚠️ |
| **15** | `save_config` 的 `bool("false")→True` | ⚙️ **开关形同虚设（反而多花钱）** | `qc_client.py:617` | ✅ |
| **16** | 重试时用未自愈的原提示词 | 💸 **越重试越差** | `app.py:2817` | ⚠️ |
| **17** | `main.py` / `serve.main()` 签名不匹配 | 🚫 **EXE/桌面入口无法启动** | `main.py:34` | ✅ |

---

## 六、优化建议（按用户四个方向）

### 6.1 强化角色 / 道具资产锁定
1. **参考图收集判据统一**（S6）：`_collect_asset_refs` 与 `probe_assets` 用同一套扩展名集合；在剧本角色 schema 补 `ref_image` 字段由资产步骤回写，彻底消除"靠猜文件名"。
2. **禁止静默兜底**（S6）：角色名匹配失败 → 记 error 并跳过，**绝不取"排序第一个"**；同时做别名归一（去 `_主角`/`_角色` 后缀、去《》空白）。
3. **外貌特征单一事实源**（O10）：`<Subject N>` 的外貌串一律由资产生成，LLM 版本只保留画面描写；把 `[:60]` 提到 ≥120 并改句边界截断。
4. **资产完整性前置硬校验**：资产生成完成后校验"每个角色有 base + N 视角 + 可解码"，不达标**不允许进分镜**（现在是 probe 判 done 但参考图收不到）。
5. **资产版本指纹**：manifest 里记资产图的 hash，分镜前比对；资产变了 → 相关分镜自动标 stale。

### 6.2 提升质检准确率
1. **补确定性闸门**（O1/G9）：黑图/纯色图（像素 stddev）、视频时长与 `shot.duration` 偏差、有无音频流、`frame_meta.verified` —— 这些**不该靠多模态模型自觉**。
2. **修 `parse_verdict` 白名单**（G11）：中文/大小写/带尾字符表述全覆盖；**字符串不可识别时返回 `None` 而非 `False`**，避免"字段写法差异 = 100% 假失败"。
3. **`{pass_score}` 落入默认提示词**（G12）：让模型知道合格线，消除"模型给 68 分 pass=true、代码按 70 判失败"的假失败。
4. **音频走自己的缺陷词表**（G15）：`_run_vision` 加 `kind` 参数，复用已有的 `AUDIO_CRITICAL_KEYWORDS`。
5. **阈值单一事实源**（O23/O24）：把 `30/60`、`4.5`、`3.0/12.0`、音频三阈值收敛到一处，并**从常量生成提示词文案**，杜绝代码与文案漂移。
6. **质检记录补齐排查字段**（G16）：`raw`/`model`/`call_url`/`api_attempts`/参考图/prompt 指纹；尾帧也要 `_qc_record_verdict` + `_record_qc_lesson`（G17）。
7. **统一 fail-open/fail-closed 口径并写进 docstring**（G18）。
8. **补 `prompt_qc` 的"景别 vs 画面内容互斥"判据**（O7），命中时提示人工确认而非静默改词。

### 6.3 减少无效重绘（省钱）
1. **重试止损铺开到全部 6 个循环**（G1）：资产基础图、资产多视角、整集、尾帧 —— 抽公共模块复用。
2. **重试前先做确定性修复**：每轮生成前跑 `prompt_qc.preflight`（幂等，零成本），再考虑换 seed。
3. **重试真的换 seed + 回写教训**（S3）：`random.randint(1, 2**31-1)`，并把 `prompt_memory.learned_prompt` 回写进提示词；否则换 seed 只是换骰子。
4. **重画范围粒度优化**：资产多视角"一坏全重" → 改按视角局部重画；整集模式按段定位重画。
5. **资产逐项断点续跑**（S13）：`probe_assets` 返回 missing 名单，只补缺失项。
6. **超时即取消远端**（S9），避免"本地判失败、远端继续跑"的双重浪费。
7. **入口幂等**（G5）：同秒双 worker、连点重跑、重复超分/混音都要拦。

### 6.4 提升流水线稳定性
1. **原子写 + 锁**（S8/O22）：临时名唯一化 + 模块级 `RLock`，覆盖 `project_store`/`ai_config`/`ai_chat`。
2. **无优雅停机 → 补齐**（O16）：`atexit.register(autopilot.stop)` + `SIGINT/SIGTERM/SIGBREAK` handler。
3. **内存表 LRU 裁剪**（O17）：`generation_state` 等保留 `running` + 最近 200 条。
4. **续跑判据改"可播放性"**（G7）：`probe_media().ok`，配合"写 `.tmp` → 校验 → `os.replace`"。
5. **`_STATE` 完整持久化**（O19）：节流与连续失败计数跨重启保留，否则重启即重置保护。
6. **`delete_project` 先停 worker**（O20）：前置校验无 running 任务，或先 push 中止再等收尾。
7. **资源清理制度化**（G8）：ComfyUI output 覆写 `filename_prefix` 用 `move`；scratch 滚动保留；`project_kind_roots()` 补 `qc/audio` 与 `_watermark_backup`。
8. **`_safe_project` 陷阱收口**（G4）：抽 `_project_or_400(raw)`，把 9 处死守卫一次修完。
9. **`status()` 语义收敛**（O18）：返回 `state: alive|idle|producing|paused` 枚举，取代易误读的 `running`。
10. **`dub_mix` / `continuity` 补日志**（O15）。
11. **ffmpeg 临时文件统一 `finally` 清理**（G8 附带）：`watermark_cleanup` 的 `.clean.*`、`video_postprocess` 的 `.list`/`.srt`/`.tmp.mp4`；临时文件用**点前缀 + 非 `.mp4` 后缀**，避免被 `.mp4` glob 命中当成"分片"。

---

## 七、修复优先级路线图

**第 1 批（本周，止血）** —— 全是"改几行就见效"：
`S3` H3 重试空操作 · `S2` 去水印逻辑反转 · `S10` anullsrc 条件式时长 · `S11` 资产逐项隔离 · `S4` 成片丢集号 · `G2` `bool("false")` · `G20` `main.py` 签名 · `G3` 质检总览目录 · `S8` 原子写+锁

**第 2 批（下周，影响产出质量）**：
`S6` 参考图判据统一+禁止静默兜底 · `S7` 尾帧走暂存区 · `S5` 时间轴按 shot_id · `G1` 止损铺开 · `G10` 重试保留自愈 · `G9`/`O1` 确定性闸门 · `G11`/`G12` 假失败

**第 3 批（能力建设）**：
`S1` 剧本质检接线 · `S9` 远端取消 · `S13` 逐资产续跑 · `G8` 资源清理 · `O16` 优雅停机 · `O2`/`O3` 元数据与 seed 落盘

---

*报告结束。共记录：严重 13 条（✅12 / ⚠️1）、一般 20 条、优化建议 25 条；高风险阻塞项 17 条。*
