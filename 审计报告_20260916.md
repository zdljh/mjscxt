# 漫剧生成系统 · 全项目审计报告

**审计时间**：2026-09-16 19:40 — 21:00
**审计范围**：前端（React + Vite）、后端（Flask，183 条路由）、构建链、数据契约
**回归结果**：29 项接口检查 **全部通过，0 个 5xx**；浏览器实测控制台 **0 错误**

---

## 一、你提出的两点，已修复并实测确认

### 1. AI 配置不在外面了 → 已恢复为全局入口

侧边栏现在是三个与项目无关的全局入口：

| 入口 | 路由 | 说明 |
|---|---|---|
| 📁 项目中心 | `#projects` | 项目列表 + 新建项目 |
| ⚙️ AI 配置 | `#ai` | 文本分析 / 质检 / 对话总控三个模型 + 系统设置 |
| 🧠 AI 记忆 | `#memory` | 记忆统计与列表 |

单项目的功能（关键帧、九宫格、分镜、配音、混音、质检、导出、AI 总控）一律进**项目工作台**的标签页。

顺带修掉一个隐藏 bug：AI 配置页此前读的是 `d.config`，而后端实际把模块嵌在 `d.config.modules` 里，
导致三个模块明明「已配置」却全显示「○ 未配置」。现已正确显示 **✓ 已配置**。

### 2. 上传小说并入「新建项目」→ 已合并

「新建项目」弹窗现在有两个字段：

- **项目名称**
- **小说来源**：`上传新文件` ⇄ `选择已有小说`（二选一切换）
  - 上传新文件：支持 .txt / .docx / .pdf / .epub，单文件 ≤200MB，可拖拽
  - 选择已有小说：下拉列出小说库（当前 41 部可选）

上传路径已修正为 `POST /api/novels/upload`（此前指向不存在的 `/api/novels` → 404），
上传完成后再创建项目，成功即跳转到该项目工作台。

---

## 二、审计新发现并修复的 5 类真实缺陷

### A. 畸形请求体被当成「服务端错误」（500）

**现象**：请求体不是合法 JSON 时，接口返回 **500**，错误文案却是 `400 Bad Request: ...`。

**根因**：`request.get_json()` 未加 `silent=True`，werkzeug 抛 `BadRequest`，
被统一异常兜底 `_autopilot_guard` 的 `except Exception` 捕获，包装成 500。

**影响**：客户端问题被误报成服务端故障，排查方向被带偏。

**修复（三层）**：
1. 15 处 `request.get_json()` 全部改为 `get_json(silent=True) or {}`
2. `_autopilot_guard` 增加 `except HTTPException` 分支，保留原本语义状态码
3. 新增全局 `@app.errorhandler(BadRequest)` 返回 JSON 400
   （此前未被兜底包裹的路由会直接吐 **HTML 错误页**，前端无法解析）

**验证**：6 个畸形用例全部返回 400，无 5xx。

---

### B. 「AI 记忆」整页失效 —— 前后端字段全线错配

**现象**：记忆列表里显示原始键名 `memory.type.undefined`，四个统计卡全空。

**根因（4 处连锁问题）**：

| # | 问题 | 后端实际 | 前端假设 |
|---|---|---|---|
| 1 | 主键字段名 | `mem_id` / `mem_type` | `id` / `type` |
| 2 | 列表返回结构 | 信封 `{success, memories, total}` | 直接 `Memory[]` |
| 3 | 统计返回结构 | `{stats:{total, by_type:{lesson,…}}}` | 扁平 `{total, lessons,…}` |
| 4 | 清空后刷新 | — | `setMemories(整个对象)` → 随后 `.map` 必抛 TypeError |

另外 i18n 只定义了 `lesson / success / insight` 三种类型，而后端类型集合是
`pattern / lesson / success / failure / insight`（数据里还出现了 `test`），
缺键时直接把原始键名渲染到界面上。

**修复**：
- `client.ts` 在 API 层统一拆封（list → 数组、stats → 扁平数字）
- `types/index.ts` 的 `Memory` 改为 `mem_id` / `mem_type`，新增 `MemoryType`
- `MemoryPage.tsx` 读新字段、补 key 兜底、补 i18n 未命中时回退显示原文的保护
- 中英 locale 各补 `pattern / failure / test` 三个键

**验证**：统计显示 `3 总记忆 / 1 教训`，列表 3 条正常，类型显示为「教训 / 测试」。

---

### C. Tailwind 只走 CDN —— 构建产物里 0 个工具类（架构级问题）

**现象**：构建出的 CSS 只有 **3KB**（纯自定义样式），
`.flex` / `.rounded-lg` / `.grid-cols-*` 一个都没有。

**根因**：`index.html` 用 `<script src="https://cdn.tailwindcss.com">` 在**运行时**
现场生成工具类。更矛盾的是 devDependencies 里装的是 tailwindcss **v4** +
`@tailwindcss/vite`，但 `vite.config.ts` 从未注册该插件，`index.css` 又是 v3 的
`@tailwind` 指令 —— 装了等于没装。

**影响**：
- **断网 / 内网 / Electron 离线打包 → 整个界面失去样式**
- 每次打开页面都要在浏览器里做一次 JIT 扫描
- Tailwind 官方明确禁止在生产环境使用该 CDN

**修复选择**：**刻意回退到 v3，而不是迁 v4**。
本项目有 **188 处裸 `border`**（v3 默认 `gray-200`，v4 变成 `currentColor`）
和 **27 处 ring / outline-none**（v4 已重命名），直接迁 v4 会造成整站视觉回归。
v3 能 1:1 复刻既有观感。

**落地**：
```
npm i -D tailwindcss@^3.4.17 postcss autoprefixer   # 并卸载 @tailwindcss/vite
+ frontend/tailwind.config.js   (darkMode: 'class')
+ frontend/postcss.config.js
- index.html 里的 CDN <script>（其内联 config 已并入配置文件）
```

**验证**：CSS 3KB → **37KB**，工具类齐备；外部 `<script src="http…">` 归零；
按钮圆角仍 8px、渐变背景仍在、42 个卡片正常；暗色模式 58 条 `.dark` 规则生效
（body 由 gray-50 切到 gray-950）；**控制台由 1 条 CDN 警告变为 0 条**。

---

### D. 导出是「空壳 + 占位项目名」（用户可见）

**现象**：点「导出文件」后下载到的 FCPXML 里 `<project name="项目">`、
`<clips/>` 为空；EDL 只有表头；JSON 只有 `{}`（2 字节）。

**根因**：工作台导出调 `POST /api/export/<项目>`，该路由把**客户端传来的空 timeline**
直接交给 `ExportManager`。而真正可用的实现 `nle_export.export_all`
（配在 `/api/export/run`）前端从未调用 —— **两套导出实现并存，前端接错了那一套**。

**修复**：
- 新增 `_timeline_for_export()`：客户端未提供 timeline 时，用
  `nle_export.build_timeline()` 据剧本自行构建，并适配成 `ExportManager` 需要的结构
- `export_manager.py` 里写死的 `"项目"` 兜底改为回退到项目 key
- 响应新增 `shot_count` / `total_sec`；前端提示改为
  「导出文件已生成，共 N 个镜头（约 X 秒）」，0 镜时明确提示内容为空

**验证**（剑心初醒，导出前 0 镜）：

| 项目 | 修复前 | 修复后 |
|---|---|---|
| 镜头数 | 0 | **10** |
| 时长 | 0 | **60 秒** |
| project name | `项目` | `剑心初醒` |
| FCPXML `<clip>` | 0 个 | **10 个** |
| EDL CUT 行 | 0 条 | **10 条**（时间码递进正确） |
| 画布 | 1920×1080 | **1080×1920 竖屏** |

---

### E. 迁移项目读不到剧本 → 全链路空数据

**现象**：项目索引登记着「10 集」，界面却显示「**10 集 · 0 分镜**」，
分镜画布 / 导出 / 质检等全部读到空数据。

**根因**：`list_episodes()` 只认现代布局 `output/scripts/<project_key>/第N集.json`，
而迁移遗留项目是**扁平**的 `output/scripts/<name>_<时间戳>.json`，一集都匹配不到。

**修复**：`_load_script_for()` 在现行布局落空时回退 `_load_legacy_flat_script()`，
按修改时间倒序取第一份「含 shots」的扁平剧本并打日志。
**仅在读不到时触发，不会遮蔽正常的 `第N集.json`**。

---

## 三、环境层面的两个重要提醒

### ⚠️ 1. 启动后端会自动「接着生产」

`app.py` 的 `_schedule_autopilot_boot()` 在检测到有启用项目时会自动 `resume()`，
**直接开始跑 ComfyUI 出图**。本次审计期间反复重启后端，每次都触发了跑图。

> 做验证 / 一次性脚本时请务必加 `MJSCXT_AUTOPILOT=0`（代码注释里也写了这个逃生口）。

### ⚠️ 2. ComfyUI 后端进程已不在运行

审计中途 ComfyUI 的 python 进程（端口 8188）消失了，只剩
`ComfyUI_TE模式启动器.exe` 外壳进程。**需要你自行重启 ComfyUI。**
（后端在 ComfyUI 缺失时能优雅降级：`/api/status` 返回 error 字段而不是崩溃。）

**当前服务状态**：后端已在 5000 端口运行（本次以 `MJSCXT_AUTOPILOT=0` 启动，不会自动跑图；
要恢复 24/7 托管，重启时去掉该环境变量即可）。

---

## 四、回归验证

保留脚本 **`_final_regression.py`**（项目根目录，可随时重跑）：

```
A. 只读与配置接口 ............ 15 项
B. 原先缺装饰器的写接口 ....... 4 项
C. 畸形请求不应产生 5xx ....... 3 项
D. 九宫格生成 → 选定 .......... 2 项
E. 导出生成 + 下载 ............ 4 项
F. 视频生成参数校验 ........... 1 项
--------------------------------------
总计 29 项 · 通过 29 · 失败 0 · 5xx 0
```

浏览器实测（Chrome DevTools）：

- 三个全局入口正常，AI 配置三模块显示「✓ 已配置」
- 「新建项目」弹窗含小说来源切换，已有小说下拉 41 部
- 九宫格：生成 9 格（构图 / 机位 / 变焦）→ 点选 → 提示「**✓ 已选定**」
  （修复前该链路生成即 500，选定必然 404）
- 导出标签：提示「导出文件已生成，共 10 个镜头（约 60 秒）」，3 个文件可下载
- 下载用 `<a download>` 真实产出，非 `window.open`（不会被弹窗拦截）
- **控制台 0 错误**（修复前有 Tailwind CDN 警告）

---

## 五、仍遗留 / 建议后续处理

| 优先级 | 事项 |
|---|---|
| 中 | `output/scripts/` 有 52 份含分镜的剧本多为历史遗留，迁移项目靠新增的扁平回退才能读到；建议做一次**剧本目录归一化** |
| 中 | 小说下拉里有 **8 份同名《蛊真人》精校版**（不同 novel_id，非接口去重 bug，是重复导入产生的数据）；部分标题是整句话（取首行的朴素标题提取）——建议改进标题提取 + 去重展示 |
| 低 | 九宫格标签文案写「生成九宫格分镜**参考图**」，但后端实际只产出**文字**机位/构图，文案与能力不符 |
| 低 | `app/app.py` 仍有 19 条 pyflakes「导入未使用 / 局部变量未使用」，本次未清理 |
| 低 | `output/exports/<项目>/` 下 09-16 19:32 前的旧空壳文件仍在（已备份到 `temp/_audit_backup/`），重新点「导出文件」即被覆盖 |

---

## 六、本轮改动文件清单

**后端**
- `app/app.py` — 畸形请求三层修复；`_timeline_for_export()`；`_load_legacy_flat_script()` 回退；
  7 处缺失的 `@app.route` 装饰器补全；`NineGridStoryboard` 导入；`/api/videos/generate` 截断路由补全；
  `_llm_guide_response` → `_ai_guide_response`；`logger` → `app.logger`
- `app/export_manager.py` — 去掉写死的 `"项目"` 占位名

**前端**
- `src/api/client.ts` — `memoryApi` 统一拆封；`aiConfigApi` 路径去重复 `/api` 前缀；
  `novelsApi.upload` 改用 `/api/novels/upload`；`analyticsApi` → `/analytics/summary`；
  `exportApi.generate` 类型补 `shot_count`
- `src/types/index.ts` — `Memory` 改为 `mem_id` / `mem_type`，新增 `MemoryType`
- `src/pages/MemoryPage.tsx` — 字段名、拆封、key 与 i18n 兜底
- `src/pages/ProjectWorkbenchPage.tsx` — 导出提示带镜头数；下载改用 `<a download>`
- `src/pages/GridPage.tsx`、`src/pages/ProjectsPage.tsx`、`src/App.tsx`、`src/components/layout/Sidebar.tsx` — 导航与新建项目合并

**构建链**
- `frontend/tailwind.config.js`（新增）、`frontend/postcss.config.js`（新增）、
  `frontend/index.html`（移除 CDN）、`frontend/package.json`（tailwindcss v3）

**文案**
- `locales/zh-CN.json`、`locales/en-US.json` — 补记忆类型键、新建项目来源相关键

---

# 补充轮（21:00–21:15）：AI 配置语义澄清 + 2 个新缺陷

## 起点：用户指出「文本分析模型就是 LLM 引擎」

**这是对的**，而且系统设置里同时出现「LLM 引擎 / LLM API Key」与「文本分析模型」，
本质是同一份配置展示了两遍。

根因：`config.modules.text` 就是 LLM 引擎（后端注释里 `/api/llm/config` 已标记
「等价于 `/api/ai/config` 的 text 模块，已废弃」）。而系统设置区另建了一套
`llm_provider` / `llm_api_key` 字段——不仅重复，而且**从来就没能用**：

- `POST /api/ai/settings` 一直返回 500：
  `TypeError: load_llm_config() takes 1 positional argument but 2 were given`
- `GET /api/ai/settings` 返回的 `settings` 里没有 llm_provider / comfyui_url /
  watermark_enabled，所以前端那块表单永远是空白

**修法**：删掉重复字段，让语义归一。
- `app/ai_config.py` → text 模块 `label` 改为 `文本分析模型（= LLM 引擎）`，desc 说明它就是 LLM 引擎
- `app/app.py` → 重写 `api_ai_settings` POST：`llm_api_key` 写入 `ai.text` 模块；
  `comfyui_url` 如实返回「由环境变量 COMFYUI_URL 决定」（不落盘，因为没有任何代码读它）；
  水印走 `/watermark/config`
- `AIVaultPage.tsx` → 删除「LLM 引擎」下拉与「LLM API Key」输入；ComfyUI 地址改为
  **只读禁用 + 环境变量说明**；按钮改「💾 保存水印设置」；说明区写明两者是同一件事

> ⚠️ 期间我的测试 POST 误把假 key 写进了 `ai.text` 加密库（掩码从 `nvap******hXIG`
> 变成 `sk-t******real`）。因 text 与 chat 原本同源，已用
> `secret_store.set_api_key('ai.text', chat 的 key)` 还原，**三模块掩码与审计开始时完全一致**。

## 新缺陷 1：favicon 缺失

`index.html` 引用 `/vite.svg`，但 `app/static/` 里没有这个文件；后端只有一个
返回 ⚡ emoji 的行内占位路由。

**修法**：新增 `frontend/public/favicon.svg`（胶片 + 播放三角品牌图标），
引用改为 `/favicon.svg`，后端新增 `/favicon.svg` 路由。

同时加固了 SPA 兜底：原逻辑对「带扩展名的路径」一律 404，会误伤真实静态文件。
改为**先查 static 目录是否真有该文件**（`normpath` + 前缀校验防目录穿越）再决定。
穿越测试 `/../app.py` → 404、不存在的 `/robots.txt` → 404，均正确。

## 新缺陷 2（最严重的视觉缺陷）：设计系统缺类

`card-glass` / `input-field` / `btn-primary` 这三个类在 `AIVaultPage.tsx` 里被引用，
但 `src/index.css` **从来没有定义过**（CSS 里只有 `card-tech` / `input-tech` / `btn-glow`）。

实测后果（浏览器 computed style 取证）：

| 元素 | 修复前 | 修复后 |
|---|---|---|
| 输入框宽度 | **168px**（浏览器 `<input>` 默认宽度），`https://integrate.api.nvidia.com/v1` 被截断成 `https://integrate.api.` | 921px，完整显示 |
| 卡片 | `background: rgba(0,0,0,0)`、无边框、无圆角、无阴影 → 页面「没有卡片」 | 白色玻璃底 + 16px 圆角 + 阴影 |
| 按钮 | 背景全透明，只剩 emoji + 文字 | indigo 渐变实底 + 白字 + 阴影 |

**修法**：在 `index.css` 末尾用 `@layer components` 补齐这三个类，视觉语言对齐既有的
`card-tech` / `input-tech` / `btn-glow`。放在 `components` 层是为了让 `w-48` / `flex-1`
等工具类仍然可以覆盖它们。

> 附带发现：`src/pages/ExportPage.tsx` 是**孤儿文件**——没有任何地方 import 它，
> 工作台里的「导出」标签用的是 `ProjectWorkbenchPage` 的内联 `ExportTab`。
> 该文件也引用同三个类，本次一并被治愈。是否删除待定。

## 本轮验证

- 浏览器硬重载实测：LLM 字段消失 ✅、ComfyUI 只读禁用 ✅、favicon `200 image/svg+xml` ✅、
  水印开关联动真实配置且保存成功 ✅、卡片/输入框/按钮样式恢复 ✅、**控制台 0 错误 0 警告**
- 水印测试后已用 API 恢复原值（`enabled:false`、`text:"漫剧生成系统"`）
- `_final_regression.py` 复跑：**29 项全通过、0 失败、0 个 5xx**
- 前端产物：`index-BCVUrE8s.js` + `index-D5SrEi_H.css`（38.22KB）

> ⚠️ **验证方法学提醒**：Chrome DevTools MCP 的 `navigate_page` 在 URL 只有 hash
> 变化时**不会真正重载**，`ignoreCache` 也随之失效，会一直看到旧 JS 的旧界面
> （本次一度被误导，以为改动没生效）。必须用 `type:"reload"` + `ignoreCache:true`。

## 本轮改动文件

- `app/ai_config.py`、`app/app.py`（`api_ai_settings` 重写、favicon 路由、SPA 兜底加固）
- `frontend/src/index.css`（补齐 3 个组件类）、`frontend/index.html`（favicon 引用）
- `frontend/public/favicon.svg`（新增）、`frontend/src/pages/AIVaultPage.tsx`

---

# 补充轮 2（21:09–21:30）：新建项目弹窗跑出屏幕 + AI 总控改为右侧常驻

用户反馈：「新建项目还是有问题」「点击项目进去后 AI 总控不见了」。
追问确认：前者是**背景变暗但没有弹窗**；后者期望**项目内右侧常驻面板**。

## 缺陷 A（严重，波及 12 个页面）：`.fade-in` 残留 transform 把弹窗挤出视口

```css
.fade-in { animation: fadeIn 0.4s cubic-bezier(0.4,0,0.2,1) both; }
@keyframes fadeIn { from{opacity:0;transform:translateY(12px)} to{opacity:1;transform:translateY(0)} }
```

`animation-fill-mode: both` 会把**结束帧的 `transform: translateY(0)` 永久保留**。
按 CSS 规范，只要祖先元素的 computed `transform !== 'none'`，它就成为 `position: fixed`
后代的**包含块**。于是弹窗不再相对视口定位，而是相对这个超长的页面根节点。

实测取证：

| 项 | 值 |
|---|---|
| `ProjectsPage` 根节点 `.space-y-6.fade-in` 的 computed transform | `matrix(1,0,0,1,0,0)`（≠ `none`，即残留的 `translateY(0)`） |
| 视口高度 | 1381 |
| 弹窗面板实际 `y` | **2204** → 落在超长项目列表的垂直中间，**屏幕之外** |

用户体验即为：点「新建项目」→ 页面变暗 → 找不到对话框（需向下滚很久）。

**修法（两层防护）**
1. `.fade-in` 改用 `animation-fill-mode: backwards`，结束帧去掉 `transform`
   → 动画结束后回落到 `transform: none`。
   同时统一了 `src/index.css`（原 `0.4s … both`）与 `frontend/index.html`
   （原 `0.3s ease-out`，无 fill-mode）两份不一致的定义。
2. `Modal` / `Toast` 改用 `createPortal(…, document.body)` 渲染到 body ——
   不再受任何祖先 transform 影响。这是根治的那一层。

## 缺陷 B：`useMemo` 缓存了语言包加载前的翻译

弹窗修好后，创建按钮显示成原始 i18n key **`project.create`**。

原因：`t()` 是 i18n 模块的**模块级函数，引用永不变化**；而
`ProjectsPage` 写的是 `useMemo(() => t('project.create'), [submitting, source, t])`。
首帧语言包尚未加载 → 算出原始 key → 依赖项不再变化 → 永久缓存。
（实测：切换「上传新文件 / 选择已有小说」会改 `source`，按钮即恢复为「创建」。）

**修法（系统性）**：`i18n/index.ts` 增加 `localeVersion`（每次 `loadLocale` 成功 +1），
`AppContext` 依据它用 `useCallback` 重建 `t` 的引用。这样**任何** `useMemo(..., [t])`
都会在语言包就绪后自动重算。全项目仅此一处有该模式。

## 缺陷 C：`novel_name` 被写成项目名

`api_projects_create` 中 `novel_name=data.get('novel_name') or name`，而前端只传
`novel_id`、不传 `novel_name` → 「小说名」被写成了「项目名」。
实测：项目 `ZZ_排查临时项目` 的 `novel_name` 也是 `ZZ_排查临时项目`。

**修法**：有 `novel_id` 而无 `novel_name` 时回查小说库
`get_novel(NOVELS_DIR, novel_id)` 取真实标题（与 `api_projects_ensure_for_novel` 一致）。
修后实测 `novel_name = 剑心初醒`。

## 需求实现：AI 总控 → 项目内右侧常驻面板

原先它是工作台的第 10 个标签页（排在最末，`flex-wrap` 还会换到第二行，用户找不到）。

- 从 `tabs` 移除 `chat` 项
- `ChatTab` → `ChatPanel({projectKey, onClose})`：`<aside class="w-[340px] shrink-0 … sticky top-0">`，
  头部含「刷新对话 / 收起面板」，消息气泡收窄，输入行独立为底栏
- 工作台主体改为 `flex items-start gap-4`：左列 `flex-1 min-w-0`，右列面板；
  收起后右列变为竖条按钮（`writingMode: vertical-rl`）可重新展开
- 默认展开；**切换标签页时面板保持常驻、对话不丢失**

## 验证结果

- 弹窗 `panelInViewport=true`、`panelY=458`、父节点 `BODY`；`.fade-in` transform 回到 `none`
- 创建按钮文案为「创建」，**全站无原始 i18n key 泄漏**（#/projects、#/ai、#/memory 均已核）
- AI 总控面板位于 `x=885, w=340`；收起→展开往返正常；切到 自动生产/分镜/导出 后仍在
- 控制台 **0 错误 0 警告**；`npx tsc --noEmit` **0 错误**
- `_final_regression.py`：**29 项全通过、0 失败**
- 产物：`index-kGXfsi59.js` + `index-BpeaNWw2.css`

## 测试数据清理

排查期间创建的 4 个测试项目已移入回收站（项目数 42 → 40）；
测试小说 `ZZ_上传测试小说` 因**无删除接口**，已手工移到 `novels/_trash/`（未物理删除）。

## 本轮改动文件

- `app/app.py`（`api_projects_create` 的 novel_name 解析）
- `frontend/src/index.css`、`frontend/index.html`（`.fade-in` 修复，两处同步）
- `frontend/src/components/ui/index.tsx`（Modal / Toast → Portal）
- `frontend/src/i18n/index.ts`、`frontend/src/context/AppContext.tsx`（localeVersion 响应式 t）
- `frontend/src/pages/ProjectWorkbenchPage.tsx`（ChatTab → ChatPanel + 右侧常驻布局）




---

# 补充轮 3 —— 修复《漫剧生成系统_Web功能测试报告》+ AI 总控视觉对齐（2026-09-16）

## 一、测试报告缺陷复核

测试报告（21:35–22:18）共 5 条，其中 **#1 是唯一实体缺陷**，其余为测试工具编码 / 长耗时观察项。

| # | 报告结论 | 复核结果 |
|---|---|---|
| 1 | CharactersPage 白屏风险（后端对象 vs 前端数组） | **成立且比报告更严重**，已修（见下） |
| 2 | PowerShell 提交中文 400 | 测试工具编码问题，非系统缺陷 |
| 3 | 场景首次提交 failed | 同上，已重提成功 |
| 4 | 单资产 26–28 分钟 | 符合本地 ComfyUI + 逐张质检预期 |
| 5 | 历史 serve.py 全站 500 | 本轮全程未复现 |

### 复核中的关键发现：#1 位于**不可达的死代码**

报告建议「后端统一返回数组或前端兼容对象」。深入排查后发现两件报告未察觉的事：

1. **`CharactersPage.tsx` 根本没有被路由引用。** `App.tsx` 的 `SECTIONS` 只有
   `projects / ai / memory`，项目内功能全部收在 `ProjectWorkbenchPage` 的标签页里；
   侧边栏仅 3 项。`CharactersPage` 属于孤儿 page 之一。
   构建产物里 `grep 'character.title'` = **0 命中** → 被 Vite 完整 tree-shake 掉了。
   即：报告 #1 的风险**在运行时不存在**，他们实测「未复现白屏」是正确的。
2. **但文件内还有 3 个比报告更硬的 bug**，一旦该页被重新接上就会立刻爆：
   - `char.images.length` —— 后端角色 JSON **根本没有 `images` 字段**。
     真实字段是：`id / name / role / description / outfit / status / views / references /
     created_at / updated_at`。所以必然 `TypeError: Cannot read properties of undefined
     (reading 'length')` → 白屏。报告的猜测是「后端返回对象 vs 前端按数组渲染」，
     **实际崩点在字段名上**，比报告判断的更确定。
   - `charactersApi.add({name, role, description})` —— 后端 `POST /api/characters`
     **必带 `project`**（缺了直接 400），此处没传 → 添加角色必定失败。
   - `add` 的返回类型被声明为 `Character`，实际后端只回
     `{success, character_id, name}` → 调用方把半成品对象塞进 state，
     卡片 `key`、`role` 全变成 undefined。

## 二、本轮修复

### 1. 角色数据链路（类型层 + 调用方）

上一轮已在 `client.ts` 用 `toAssetArray` 把「字典 / 数组 / 异常形态」统一归一化成数组，
本轮补齐类型层与调用方：

- `frontend/src/types/index.ts` —— `Character` 重写，与后端真实结构对齐：
  `role` 由字面量联合放宽为 `string`（后端不约束枚举，AI 可能产出其它值）；
  `images` 降级为可选并标 `@deprecated`；
  补 `outfit / status / views / references / created_at / updated_at / project(请求参数)`。
- `frontend/src/pages/CharactersPage.tsx` —— 重写：
  - 图片数改按**真实字段**统计：`countImages(c) = 已生成的 views 数 + references 数`，
    不再读 `images.length`；
  - 添加角色补 `project`，且**添加成功后重新拉列表**，不再往 state 塞 `{character_id}` 半成品；
  - 项目 key 从 hash 读取，读不到时返回空串并给提示，**不再兜底成 `'default'`**
    —— 后端并没有名为 `default` 的项目，用它拉数据只会把「没进项目」
    伪装成「项目里没角色」；
  - 监听 `hashchange`（同一 SPA 内切项目能跟随刷新）；添加中禁用按钮。
- `locales/zh-CN.json` / `locales/en-US.json` —— 补 `common.images`
  （此前不存在，会渲染出原始 key）。

### 2. AI 总控面板视觉对齐（用户反馈「怪怪的、和整体不搭」）

| 问题 | 修法 |
|---|---|
| 面板只有 358px 高、悬浮在半空，与左列 531px 参差 | 改 `h-[calc(100vh-7rem)] min-h-[420px]`，**全高 + 与左列顶部对齐**（两者 y 都是 87） |
| 圆角 12px，与全站 8px 卡片不一致 | 统一 `rounded-xl` + `border-gray-200` + `bg-white`，与资产卡同一套 |
| 纯白无层次 | 头部加靛蓝图标徽章 +「AI总控 / 项目级对话」双行标题；消息区改 `bg-gray-50` |
| 空状态只有一行灰字 | 改为图标 + 引导文案 + **3 个可点击示例指令** |
| 发送按钮是描边幽灵按钮 | 改实心 `bg-indigo-600` 主按钮 |
| 消息区莫名出现 12px 滚动条 | 空状态与消息列表改为**互斥容器**（`space-y-3` 曾给滚动哨兵 `<div>` 加了 12px margin） |
| 思考中无反馈 | 加 spinner |

### 3. 回归与验证

- `_final_regression.py`：**29 项全通过 · 0 失败**（只读/写接口、畸形 JSON 不产生 5xx、
  九宫格生成→选定、导出 FCPML 下载、视频参数校验 400）
- `npx tsc --noEmit`：**0 错误**
- 浏览器实测（`#/?p=剑心初醒`）：
  - 面板 `x=885, y=87, w=340, h=1269, bottom=1356`，视口高 1381 → **完整可见**；
    左列 `x=248, y=87` → **顶部对齐**；`panelRightOK / panelBottomOK / panelScrollOK` 全为 true
  - 面板内**无任何溢出元素**（`overflowing: []`）→ 滚动条问题确认修好
  - 结构：头部 2 个 28px 图标按钮 + 3 个示例指令（306px）+ 靛蓝「发送」按钮，
    输入框 `y=1305` 落在面板内
  - 工作台 角色/物品/场景 各 3 个资产、均显示「5个视角」，真实数据渲染正常
  - **控制台 0 错误 0 警告**；全站**无原始 i18n key 泄漏**
- 产物：`index-bBLzwHFb.js` + `index-BQg9w-NQ.css`
  （说明：本轮改动只落在死代码 / 纯类型 / 后端 JSON 上，前端产物内容哈希未变，属预期）

## 三、本轮改动文件

- `frontend/src/types/index.ts`（Character 与后端真实结构对齐）
- `frontend/src/pages/CharactersPage.tsx`（重写）
- `locales/zh-CN.json`、`locales/en-US.json`（补 `common.images`）

## 四、孤儿页面清理（已执行 —— 用户选定方案 A）

`src/pages/` 原本有 22 个页面，实际进入构建的只有 6 个：
`ProjectsPage / AIVaultPage / MemoryPage / OverviewPage / ProjectWorkbenchPage / GridPage`。
其余 **16 个**既无 import 也无动态导入路径，被 Vite 完整 tree-shake。
这类孤儿文件正是本轮「白屏风险」误报的根源 —— 静态审查会看到一堆「坏页面」，
但它们根本不参与构建。

> 注：早前记为 17 个，是把后来确认可达的 `GridPage`（由 workbench import）算了进去。

### 删除前的三项确认

1. `grep -rn "React.lazy\|lazy(\|import("` → **无动态导入**
2. `grep -rn "pages/" --include=*.tsx --include=*.ts | grep -v '^./pages/'`
   → 只命中 `App.tsx` 的 5 条 import
3. 逐个标记可达性 → 可达 6 个、孤儿 16 个

### 已删除（16）

`AutoPage / AutopilotPage / CharactersPage / ChatPage / DeliverPage / EpisodesPage /
ExportPage / KeyframesPage / MixPage / PlaceholderPage / ProjectDetailPage / QcPage /
SettingsPage / StoryboardCanvasPage / TtsPage / UploadPage`

**顺带收益**：删除后 Tailwind 扫描不到这些文件，CSS 产物
**40.36 kB → 36.33 kB**（清掉约 4KB 死样式）；JS 产物字节数**完全不变（226799 B）**，
反证删掉的确实是死代码。

### ⚠️ 两个「功能无替代入口」的已删页面

它们都接着真实接口（不是坏桩），但**工作台里没有对应标签**：

| 页面 | 功能 | 接口 | 现状 |
|---|---|---|---|
| `DeliverPage` | 成品验收（查看/下载成片） | `autopilotApi.deliverables` | 工作台 9 个标签里没有该项，**而 AI 总控的欢迎语仍在宣传「验收成品」** |
| `EpisodesPage` | 剧集管理 | `episodesApi.list / generate` | 同样没有替代入口 |

需要时用 `git checkout a3b009d -- frontend/src/pages/DeliverPage.tsx` 取回。
**建议后续把它们接成工作台标签页，而不是让功能悬空。**

## 五、版本管理（本轮一并处理）

首次把 `frontend/` 整个前端工程与其构建产物 `app/static/` 纳入版本管理，
并补齐 9 个此前未跟踪的后端模块。

⚠️ **`app/static/` 必须入库**：`Dockerfile` 是 `COPY . .` 且**不跑前端构建**，
产物不入库则镜像起来没有界面。

`.gitignore` 新增（此前缺 `node_modules/`，`frontend/node_modules` 有 3965 个文件，
直接 `git add -A` 会把仓库搞爆）：

```
node_modules/
frontend/src_backup/
.workbuddy/          # Agent 工作区（记忆/状态），非源码；但不要删除该目录
nul                  # Git Bash 里 `> nul` 会真的建出文件，而非丢弃输出
```

| 提交 | 说明 | 改动 |
|---|---|---|
| `a3b009d` | `chore: 纳入前端工程与后端新模块；修测试报告缺陷 + AI 总控视觉对齐` | 101 files, +30704 / -7431 |
| `5c963c9` | `refactor: 删除 16 个不可达的孤儿页面` | 20 files, +3 / -2831 |

### 清理后验证（全绿）

- `_final_regression.py`：**29 项全通过 · 0 失败**
- `npx tsc --noEmit`：**0 错误**
- `vite build`：成功，`index-D5_-f3Wo.js` + `index-B_2PiHa0.css`，资源全 200
- 浏览器冒烟：`#/projects` → `#/ai` → `#/memory` → `#/?p=剑心初醒`
  （工作台 9 标签齐全、资产 3/3/3 正常渲染）**全程 0 错误 0 警告**
- 顺带测到窄屏：视口 1024×768 下 AI 总控面板 `bottom=743 ≤ 768` 完美贴合、
  **无横向溢出**，固定 340px 面板在 1024 宽下无问题
