# 漫剧生成系统 · 用户视角端到端测试报告

| 项目 | 内容 |
|------|------|
| 测试日期 | 2026-09-17 |
| 测试视角 | **普通用户**（不看代码，只用产品） |
| 被测对象 | 漫剧生成系统（Flask + React，本机 127.0.0.1:5000） |
| 测试素材 | 自建测试小说《剑影孤城》（3 章 / 1697 字） |
| 运行环境 | Windows · Flask(waitress) :5000 · ComfyUI :8188（RTX 5060 Laptop，显存 8.5 GB） |
| 模型 | text/chat = `nvidia/nemotron-3-super-120b-a12b`；qc = `agnes-2.5-flash` |
| 测试方式 | **全真实链路**：真实后端 + 真实 ComfyUI 出图出片 + 真实 LLM 调用，未使用 mock |

---

## 一、测试目标

完全按用户原话设定旅程，验证「用户什么都不用管，AI 总控自己把小说做成视频」是否成立：

> 新建项目 → 上传小说 → 和 AI 总控沟通好风格 → **AI 总控自己去操作、按章节生成视频** →
> 用户只看视频成品是否符合预期 → 不符合，用户再指出**哪个镜头/场景**有问题

---

## 二、结论速览

| # | 旅程环节 | 结果 | 关键证据 |
|---|----------|------|----------|
| 1 | 新建项目 | ✅ 通过 | 项目 `剑影孤城-测试`（id `p20260917182828b98d`）创建成功，`novel_name` 正确回查 |
| 2 | 上传小说 | ✅ 通过 | novel_id `20260917_182816_f26557`，识别 **3 章 / 1697 字** |
| 3 | 与 AI 总控沟通风格 | ✅ 通过 | 设定落盘：`style=dark wuxia`、`color_palette=低饱和冷调`、`pacing=平缓叙事`，计划写入 `episodes=[1]`、`target_shots=6` |
| 4 | **AI 总控按章节生成视频** | 🟡 **链路全通，质量受模型拖累** | 剧本→资产→分镜→视频→配音→混音 **六个子环节全部打通并落盘**；但剧本因模型 503 走了"原文兜底" |
| 5 | 用户看视频成品 | 🟡 → ✅ **（已修）** | 成片 `剑影孤城-测试_subtitles_ep01_dubbed.mp4`（h264+aac，4.458s）已生成，但**未登记为"交付物"**，`deliverables` 返回 `count:0`。**第二批已修**：成片合成/混音完成即自动登记，实测 `count: 0 → 1` |
| 6 | **用户指出某镜有问题 → AI 总控重做** | ✅ **通过（本轮最大亮点）** | 一句"第 3 镜斗篷颜色不对"，AI 总控自主完成 `get_shots → retry_shot(shot_id=3)`，重出图并质检 85 分通过；**第二批已补**：前端分镜卡也有"重做视频"按钮，且重做后同集成片自动标"已过期" |

**一句话结论**：**"AI 总控自动做视频 + 用户吐槽某张图"这条主链路是真的通的**；卡口不在功能缺失，而在
① 文本大模型不稳定（HTTP 503）导致剧本降级，② 成片没有进"待验收"队列，③ 生成任务缺少互斥保护。
**第二批「继续修复」已把 ②③ 与 N3/N4/N5/N7 全部处理，并补齐 4 个前端小病**（详见 4.3 / 4.4）；
剩余唯一硬约束是 ① —— 模型稳定性属外部依赖，只能靠降级链与重跑策略缓解。

> **2026-09-17 晚补充（4.5 第三批实测，修正了 ① 的定性）**：
> 已定位 ① 的**真实根因是 LLM 网关可达性，而不是某个模型的长 JSON 能力**。
> `text` / `chat` 指向的 `developer.amd.com.cn/radeon/api/v1` 当前上游**没有任何可用算力**
> （`503 No available workers`，同网关三个模型全部超时）；
> 且失败时要等 3 × 300s 超时（单块 672s）才降级 → **整集近 1 小时等待换来 6/6 原文兜底**。
> 已实测出可用的替代路由：`apihub.agnes-ai.cn/v1` 的 `agnes-2.5-flash`
> **13.0s 一次吐出 8 镜合法 JSON**。详见 4.5。

> **2026-09-17 深夜补充（第四 / 五批）**：
> - **4.6**：静默镜的真实根因**不是提示词，而是配音链路从来不读 `narration`**——已修 4 处，
>   新增 `verify_narration_dub.py` **43/43**。
> - **4.7**：用户问"配音为什么没有打斗音效"。查明 **H3 本来就会生成音轨，是被我们主动删掉的**；
>   已按**方案 A 分层保留**实现（保 H3 音轨 → 分离掉其人声 → 音效垫在 QwenTTS 台词下），
>   新增 `verify_sfx_pipeline.py` **57/57**。⚠️ **GPU 实试与重启均未做**。
> - **4.8**：**新发现**——单镜质检过不了会触发**整集从头重跑**，不收敛地烧显卡
>   （实测 `雨夜归人` 第 1 集跑完又重跑，只因 `shot_15` 要"特写"却 6 次都出中景）。
>   **后续核实（4.8.5）**：循环最终在 23:47 终止，但**不是因为它过了**——`shot_15` 两轮
>   共 **12 次全部未通过**（最高 75 分 < pass_score 80）；真正原因是 `qc_config.json` 在
>   **23:25:31** 被改为 `image_enabled=False`，此后 `shot_10–24` **跳过质检直接入库**。
> - **4.9**：顺着"QC 不通过时不该改提示词吗"追下去，**查出真凶**——这条链路两端都断了：
>   ① `prompt_memory.learned_prompt` **模块级根本不存在**（AttributeError 被 except 吞掉）；
>   ② 教训库被喂了 **36 条空记录**（误读不存在的 `rec["verdict"]` 键）。
>   两个失败都被静默兜住，所以"重试 6 次、提示词一次没变"。**已修**，
>   新增 `verify_qc_prompt_feedback.py` **44/44**。⚠️ **需重启才生效**。

---

## 三、逐环节详细记录

### 环节 1｜新建项目 ✅

- 操作：上传小说后建项目，名 `剑影孤城-测试`。
- 结果：项目创建成功，目录独立（`output/scripts/剑影孤城-测试/`）。
- 说明：此前修过的 `novel_name` 兜底 bug 未复现，`novel_name` 正确回查为"剑影孤城"。

### 环节 2｜上传小说 ✅

- 产物：`novel_id=20260917_182816_f26557`，**3 章 / 1697 字 / 581 字（第 1 章）**。
- 章节切分正确，`/api/episodes/<novel_id>` 可正常列出已生成剧集。

### 环节 3｜与 AI 总控沟通风格 ✅

- 用户话术示例：「暗黑武侠风格，色调偏冷，镜头语言克制」。
- AI 总控行为：调用 `apply_creative_settings` + `update_plan`。
- 落盘结果：`style=dark wuxia`、`color_palette=低饱和冷调`、`pacing=平缓叙事`，
  `episodes=[1]`、`target_shots=6`。
- 说明：这一步暴露过"非法键名被静默丢弃"的坑（`color_tone` / `camera_language`），本轮已修并可回归验证。

### 环节 4｜AI 总控按章节生成视频 🟡

拆成六个子环节逐一验证：

| 子环节 | 结果 | 耗时 | 产物 |
|--------|------|------|------|
| 4.1 小说转剧本（第 1 集） | 🟡 成功但降级 | **546.2s** | `output/scripts/剑影孤城-测试/第1集.json`（6 镜 / 27s） |
| 4.2 资产生成（角色 / 场景） | 🟡 部分 | 约 20 min 未完成 | `沈砚/base.png`、`城墙关口/base.png`；**多视角未产出** |
| 4.3 分镜图 | ✅ | 约 5 min/张 | `output/storyboards/剑影孤城-测试/shot_03.png`（质检 85 分） |
| 4.4 单镜视频 | ✅ | **449.6s** | `output/videos/剑影孤城-测试/shot_03.mp4`（960×544 / 107 帧 / 4.46s） |
| 4.5 整集配音（TTS） | ✅ 但**延迟 5 分 15 秒** | 派发 19:37:30 → 完成 19:42:45 | `ep01_dub.wav`、`lines/ep01_shot03_沈砚.wav`、`ep01_dub_manifest.json` |
| 4.6 成片拼接 + 混音 | ✅ | 拼接 ~秒级 / 混音 **0.096s** | `final/剑影孤城-测试.mp4`、`final_dub/..._dubbed.mp4`（h264 + aac 48kHz） |

**4.1 为什么是"降级"**：文本模型连续返回 **HTTP 503 Service temporarily overloaded**，
分镜编写失败后触发系统的**原文兜底**（`_fallback_shots_for_chunk`），
把小说原文逐句当成镜头描述写进剧本。产物可用、覆盖率 100%，但：

- 6 个镜头**全部带 `fallback: true`** 标记
- 其中 5 个镜头 `dialogue = []`、`prompt_h3 = ""`（台词与提示词都空）
- 直接后果：**配音阶段只有 1 句台词可配**（第 3 镜"沈砚：还剩我一个…"）

**4.2 为什么没做完**：角色与场景的 base 图都出来了，但**多视角图反复质检不达标**，
在"第 1/5 次重试"阶段被本次服务重启打断。当前显存只剩 **2.97 / 8.52 GB**，
ComfyUI 队列长期 1 running + 2~4 pending，多视角出图非常慢。

### 环节 5｜用户看视频成品 🟡 —— **发现缺陷**

- 成片确实产出了，而且音视频流完整（h264 视频 + aac 单声道音频，4.458s）。
- 混音还给出了**合理告警**：`末句结束 17.735s 超出视频时长 4.458s，超出部分会被截断`
  （因为只有 1 个镜头有视频，这只是预期内的截断提示，逻辑正确）。
- ❌ **但成片没有登记为"交付物"**：

```
GET /api/autopilot/deliverables?project=剑影孤城-测试
→ {"count": 0, "items": [], "pending": 0}
```

  项目目录下只有 `plan.json`，**没有 `deliverables.json`**。原因是交付物只在两处登记：
  `app.py:6548`（`produce_episode` / run-once）与 `autopilot.py:814`（托管轮转）。
  也就是说：**只要用户走"让 AI 总控逐环节操作"的路径，成片就永远进不了验收队列，
  前端"成品验收"页会是空的** —— 恰好卡住用户旅程的第 5 步。

### 环节 6｜用户指出某镜有问题 → AI 总控重做 ✅ **（本轮最佳验证）**

真实对话：

> **用户**：第 1 集的第 3 个镜头画面不对：黑衣人的斗篷应该是破旧黑斗篷，图上颜色偏灰，帮我重新生成这个镜头的分镜图

AI 总控自主执行的工具链（审计日志 `output/agent/audit-20260917.jsonl`）：

```
get_status({project})                      # 先看项目状态
get_shots({episode: 1, project})           # 定位到具体镜头
retry_shot({shot_id: "3", episode: 1})     # 重出这一镜的图
```

结果：`shot_03.png` 重新生成并写入 manifest，质检 **85 分通过**，AI 回复中明确说明
"提示词已按您的需求加强：斗篷描述为『破旧黑斗篷』，色调偏冷，镜头语言克制"，并给出后续建议。

**结论：用户"看图挑毛病 → 指名重做"的回路完全可用**，且 AI 能正确落到"某一集某一镜"。

---

## 四、缺陷清单

### 4.1 本轮新发现（**第二批「继续修复」已全部处理**）

| 编号 | 级别 | 缺陷 | 状态 |
|------|------|------|------|
| N1 | **P1** | **成片不登记为交付物（手动链路）** | ✅ 已修（见 4.3-B1） |
| N2 | **P1** | **分集生成无互斥保护** | ✅ 已修（见 4.3-B2） |
| N3 | P2 | 前端缺少"单镜视频重做"入口 | ✅ 已修（见 4.3-B3） |
| N4 | P2 | AI 连通测试回显误导 | ✅ 已修（见 4.3-B4） |
| N5 | P2 | 兜底剧本进入下游无预警 | ✅ 已修（见 4.3-B5） |
| N6 | P2 | 多视角资产质检过严 + 显存吃紧 | ⏸ 未处理（硬件约束，需排期） |
| **N7** | **P1** | **"剧本生成成功"实为整集兜底（本轮新发现，最关键）** | ✅ 已加体检预警（见 4.3-B5） |

#### N1 证据与定位（已修）

```
output/autopilot/剑影孤城-测试/  → 只有 plan.json，无 deliverables.json
GET /api/autopilot/deliverables  → count: 0
登记点仅两处：app.py:6634（run-once）、autopilot.py:814（托管轮转）
```
修复：在 `/api/final/video`（成片合成）与 `_mix_worker`（带配音混音完成）两处统一调用
新函数 `register_final_deliverable()`。

**注意**：报告原文建议的"该集所有镜头视频齐全"作为硬条件**实测会把真成片误杀** ——
漫剧单镜常常不到 1 秒（本项目 6 镜合并成片只有 **4.46s**），按时长/镜头数硬判定必然误报。
最终方案改为：硬闸门只卡「文件存在 + ≥100KB + 时长 ≥2s」，
镜头数不齐时**仍登记**但在 `meta` 打 `incomplete_shots` + `warning`，
前端验收页显示琥珀色「可能不完整 1/6」角标，把判断权交回用户。

#### N2 证据与定位（已修）

```
文件写入时间 18:52:23（generated_at）/ mtime 18:54:52
批处理任务 id ..._1789642179 ≈ 18:49:39 才创建
→ 两轮生成时间重叠
产物侧：coverage_percent=100%、coverage_passed=true、已被 /api/episodes 收录
任务侧：status=failed，"模型未返回有效分镜…：未知错误"
```
修复（两条并行）：
1. `api_novel_episodes_generate` 增加同 novel 互斥：扫描 `generation_state` 中
   `episodes_*` 的 running 任务，**章节集合有交集**才复用（返回原 `task_id` + `reused:true`）；
   章节不重叠的批量可以继续并发，不牺牲吞吐。任务记录新增 `picks` 字段用于重叠判定。
2. 判定改为「以产物为准」：新增 `_salvage_episode_script()`，异常分支下若
   `第N集.json` 已落盘（>200 字节）且 `shots` 非空，则回填 `success` 并标 `degraded:true`；
   汇总文案追加"（其中 N 集过程报错但产物可用）"。

#### N7 证据与定位（本轮新发现，最关键）

第二批修复时用 `dialogue_utils.audit_script()` 体检**真实剧本**时发现：

```
output/scripts/剑影孤城-测试/第1集.json
→ 镜头 6 / 可朗读台词 1 / 兜底镜头 6 / 全兜底 all_fallback=True
```

也就是说：本报告环节 4 记录的"AI 总控按章节生成视频 ✅"，
其剧本其实是 **6/6 全部走了 fallback 分支**（模型连续失败后按原文逐句承载），
`dialogue=[]`、`prompt_h3=""`。这直接解释了本轮两个"说不通"的现象：

- 为什么配音"成功"却只有 1 句 → 全剧只有 1 句可读台词；
- 为什么用户"指不出具体哪张图/哪场戏有问题" → 镜头没有分镜设计，画面都是原文照搬。

→ 教训：**脚本生成的"成功"必须区分"模型产出"与"原文兜底"**，否则整条下游都在做无用功。

### 4.2 本轮已修复（附验证）

| 编号 | 级别 | 缺陷 | 修复 | 验证 |
|------|------|------|------|------|
| F1 | **P0** | **AI 总控对异步任务"提前谎报成功"** | `agent_core.py` 新增 `_await_task`，为 `generate_tts` / `mix_audio` / `upscale_video` 补上"轮询到终态"闭环 | **单测 16/16 通过** |
| F2 | P1 | `config.KEYFRAME_CHAIN_MODE` NameError（关键帧页 500） | `app.py` 4 处改为 `KEYFRAME_CHAIN_MODE` | 关键帧页可正常返回 |
| F3 | P1 | `/api/ai/chat/apply` 静默丢弃非法字段 | 返回 `dropped_fields` + `allowed_fields`，AI 可自我纠正 | 非法键被如实上报，合法键正常落盘 |
| F4 | P1 | `get_status` 工具 URL 拼接错误 → 404 → **跨项目串台** | 修正为查询参数 + 项目钉死 + SYSTEM_PROMPT 加"项目纪律" | 对话已能正确报告当前项目 |
| F5 | P2 | `ai_config.json` 明文密钥 | 迁入加密库 `output/secrets.enc` | 配置中 `api_key` 恒为空 |

#### F1 详情（本轮最重要的修复）

**现象**：AI 总控回复"配音已完成"，但 `output/dub/剑影孤城-测试/` 下只有一个 `voice_map.json`，
混音随即报 `未找到配音清单（*_dub_manifest.json）`。

**根因**：`/api/tts/generate` 是**异步接口**，POST 只返回 `{task_id, status:"started"}`。
工具层把"HTTP 200"等同于"任务完成"，于是模型据此宣告诉"已完成"。

**硬证据（时间线）**：

| 时刻 | 事件 |
|------|------|
| 19:37:30 | `generate_tts` 派发，审计 `elapsed_sec = 2.6` |
| **19:37:31** | **AI 回复"配音已完成"** ← 提前 |
| 19:42:45 | `ep01_dub.wav` / `ep01_dub_manifest.json` 才真正落盘 |

→ **提前 5 分 15 秒宣告完成**。这属于用户最反感的"谎报"，且直接导致下游误判。

**修复**：三处工具加 `poll` 声明 + 新增 `_await_task()`：

```python
"poll": {"status": "/api/tts/status/{task_id}", "timeout": 1800}
"poll": {"status": "/api/mix/status/{task_id}", "pick": "task", "timeout": 900}
"poll": {"status": "/api/upscale/status/{task_id}", "timeout": 3600}
```

行为变化：**完成** → 返回真实终态；**失败** → 带回任务自身错误原因；
**超时** → 返回 `timeout: true` 并明确写入
"任务仍在后台运行（当前阶段…进度…），**不要声称已完成**"。
SYSTEM_PROMPT 同步补充约束，HTTP 状态码与任务状态分离存放（`status` vs `task_status`），审计语义不变。

**验证**：`.workbuddy/test/verify_async_poll.py` → **16/16 通过**（覆盖 完成/失败/卡死/无状态接口/
execute_tool 接线/无 task_id/同步工具不回归 七组场景）。
新代码已随服务重启生效（`/api/agent/tools` 描述已更新）。

### 4.3 修复批次二（「继续修复」：N1–N5 + N7 + 4 个连带前端缺陷）

| 编号 | 缺陷 | 改动文件 | 验证 |
|------|------|----------|------|
| B1 | 成片不登记交付物 | `app.py`（`register_final_deliverable` / `_episode_video_stats`） | **单测 30/30** + **线上实测**（见下） |
| B2 | 分集生成无互斥 + 以产物为准 | `app.py`（互斥分支 / `picks` / `_salvage_episode_script` / `degraded`） | `py_compile` + 符号落盘复核 |
| B3 | 前端缺单镜视频重做入口 | `client.ts`（新增 `videoApi`）/ `ProjectWorkbenchPage.tsx`（StoryboardTab 每镜加"重做分镜图/重做视频" + 模式选择） | `tsc --noEmit` 通过 + 构建通过 |
| B4 | 连通测试误判 | `llm_client.test_connection`（改走 `_build_payload`）/ `qc_client.test_vision`（`reasoning_only` 分支）/ `AIVaultPage.tsx` | **单测 41/41** + **线上实测三模块全 ok** |
| B5 | 兜底剧本无预警 | `dialogue_utils.audit_script`（新增体检）/ `app.py`（`/api/tts/plan`、`/api/tts/generate`、分集生成结果）/ `AudioTab.tsx` | **单测 47/47** + 线上实测命中 |
| B6 | 镜头重做后成片未失效 | `pipeline.mark_deliverable_stale` + `/api/video/retry-shot` 自动打标 + 验收页"成片已过期"角标 | 单测覆盖（打标 / 重混自动清除） |

#### B1 线上实测（用户第 5 步旅程闭环）

```
POST /api/final/video  {"project_name":"剑影孤城-测试", ...}
→ {"success":true, "filename":"剑影孤城-测试.mp4",
   "deliverable":{"registered":true,"episode_no":1,
                  "reason":"已登记（镜头覆盖 1/6，可能不完整）"}}

GET /api/autopilot/deliverables?project=剑影孤城-测试
→ count: 0 → 1   pending: 0 → 1
   meta = {shots_total:6, shots_ready:1, incomplete_shots:true,
           duration_sec:4.46, source:"final_video",
           warning:"该集剧本 6 镜，仅发现 1 个镜头视频，成片可能不完整…"}

下载校验：/api/autopilot/deliverable/file/…/剑影孤城-测试.mp4
→ HTTP 200 / video/mp4 / 1,103,732 B（与登记 size 完全一致）
```

#### B4 线上实测（不再把模型自言自语当答案）

| 模块 | max_tokens | verdict | reply | 备注 |
|------|-----------|---------|-------|------|
| text | 1024（思考下限自动抬高） | `ok` | `'连通'` | 1837 ms |
| chat | 1024 | `ok` | `'连通'` | 979 ms |
| qc（视觉探针） | 256 | `ok` | `'OK'`，`vision:true` | 2011 ms |

关键点：`test_connection` 此前**自己拼 body**，绕过了 `_build_payload` 的思考额度下限，
`max_tokens=32` 使正文永远为空、并把 `reasoning_content` 里的独白当错误抛出。
现在统一走 `_build_payload`，并把 `LLMReasoningOnlyError` 单独识别为
`verdict=reachable_but_no_content`（**链路可达**，并非配置错误）。

#### B5 线上实测（兜底剧本被点名）

```
POST /api/tts/plan  {"project_name":"剑影孤城-测试","episode":1}
→ line_count: 1
  audit: {shot_count:6, speakable_lines:1, fallback_shot_count:6, all_fallback:true}
  warnings_ok: false
  W: 该集全部 6 个镜头都是「模型失败后按原文兜底」生成的，剧本实际上未经模型加工
     （无分镜设计、无台词）。建议重新生成剧本，或人工分镜后再进入配音出片。
```

### 4.4 顺带修掉的 4 个前端"小病"（本轮发现）

| # | 缺陷 | 位置 | 用户可见后果 |
|---|------|------|--------------|
| 1 | 步骤导航里硬编码了 `①②③[idx]` | `AudioTab.tsx` | 界面上直接显示这串占位符 |
| 2 | 配音计划列表读 `line.seq`，实际字段是 `shot_id` | `AudioTab.tsx` | 每行显示 `#undefined` |
| 3 | `mixApi.status` 类型声明为 `MixTask`，后端实际返回信封 `{success, task}` | `client.ts` | 一旦有人用就会取到 `undefined` |
| 4 | 成功提示读 `response_time_ms`，后端字段是 `latency_ms` | `AIVaultPage.tsx` | 提示"测试成功 (undefinedms)" |

同时给 `readError()` 补上 `hint` / `guide` 透传：后端精心写的处置建议
（如"可能是兜底剧本、请补台词"）此前会被整体丢弃，用户只看得到问题、看不到办法。

---

### 4.5 第三批：**模型与网关层**（本轮针对「GLM-5.3-Flash 长 JSON 行不行」的实测）

> 起因：用户把 `text` / `chat` 模块切到了 `GLM-5.3-Flash`（`developer.amd.com.cn/radeon/api/v1`），
> 想知道它扛不扛得住分镜那种长 JSON。结论分两层，**必须分开看**。

#### 4.5.1 结论：模型能力够，但**当前网关没有任何可用算力**

| 对象 | 端点 | 实测结果 |
|------|------|----------|
| GLM-5.3-Flash | `developer.amd.com.cn/radeon/api/v1` | ✗ 极小请求（16 tok）秒回 `500 / 503 Service Unavailable No available workers (all circuits open or unhealthy)`；带 2048+ tok 的请求**直接挂到超时**（60s / 90s / 300s 三档都试过） |
| DeepSeek-V4-Flash | 同上 | ✗ 90s 超时 |
| Qwen3.8-Flash-Next | 同上 | ✗ 90s 超时 |
| agnes-2.5-flash | `apihub.agnes-ai.cn/v1` | ✅ **200 / 13.0s / JSON 可解析 / shots=8 / finish_reason=stop** |

网关 `/models` 正常返回 7 个模型，说明**是上游 `self-dploy` 部署没算力，不是模型名写错、也不是密钥问题**。
三个不同模型全挂 → 排除"单模型故障"。

#### 4.5.2 为什么这直接对应「整集 6/6 兜底」

```
LLM 请求 → _post 遇 5xx/超时 → 重试 3 次（每次 300s 超时）
        → 抛 LLMError → build_shots_for_chunk 上抛
        → 调用方 except LLMError → _fallback_shots_for_chunk（按原文兜底）
        → 每个分块都兜底 → 整集 6/6 兜底 → 配音 0 句
```

实测单块耗时 **672.6s**（3 × 300s 超时都打完才放弃）。
**一集 6 块 ≈ 1 小时纯等待，最后产出一份"没有分镜、没有台词"的兜底剧本** —— 这比直接报错更糟。

> 这一条修正了 4.1 中关于"`nemotron-3-super-120b-a12b` 长 JSON 压力大"的判断：
> **模型不是主因，网关可达性才是**。同一失败现象在两个完全不同的模型上都复现了。

#### 4.5.3 模型层面的能力（供换网关后参考，**尚未在真实网关上验证通过**）

| 项 | GLM-5.3-Flash | agnes-2.5-flash（实测通过） |
|----|---------------|------------------------------|
| 上下文 / 最大输出 | 1M / 128K tok | — |
| 原生 JSON mode | 支持 | 实证可用（8 镜 JSON 一次过） |
| 思考模式 | **不可关闭**，只有 `reasoning_effort = low/high/max`（默认 max） | 同样是 always-on（`reasoning_content` 与 `content` 分离） |
| 实测 | 因网关不可用，**无法验证** | 13.0s / 正文 3284 字 / 思考 444 字 / finish=stop |

#### 4.5.4 为此落地的代码改动（已通过 44 + 19 项离线/HTTP 验证）

1. **`llm_client.py` 支持「思考档位」**：新增 `REASONING_EFFORT_LEVELS`、
   `MIN_TOKENS_WHEN_REASONING_EFFORT = 2048`、注入 `chat_template_kwargs.{reasoning_effort, clear_thinking}`。
   与原有 `disable_thinking`（只对 Qwen/vLLM 系有效的 `enable_thinking=false`）**互斥，档位优先**。
2. **非法档位一律归一化成空**：`ai_config.normalize_reasoning_effort()`。
   实测部分模型会把非法值**静默解析成最高档（最贵）**，不拦就等于悄悄按 max 计费。
3. **`ai_config.py` 支持落盘/回显** `reasoning_effort`（`None`=不改动 / `""`=清空，语义区分），
   `public_view` 下发 `reasoning_effort_options`；旧配置文件缺该字段可正常读取。
4. **修掉两处漏传**：`_ai_client_for_module()` 与 `/api/ai/test` 此前只挑 `base_url/api_key/model`
   三个字段 → 档位会被丢掉，造成"测试连接时行为"与"保存后行为"不一致。
5. **前端**：AI 配置页每个模块新增「思考档位（可选）」下拉 + 成本提示；
   `client.ts` 的 save/test 增加该参数；`types/index.ts` 补类型。

> ⚠️ **未验证项**：该网关是否接受 `chat_template_kwargs` 参数 —— 带该参数的请求只观察到超时、
> 没有返回 400，**不能据此断定支持**。等网关恢复后需重跑
> `.workbuddy/test/probe_gateway_health.py` 复核。

#### 4.5.5 换网关后复测：**通了**（21:32 实测，配置已切到 agnes）

用户在 21:26 把 `text` / `qc` / `chat` 三个模块统一切到
`https://apihub.agnes-ai.cn/v1` + `agnes-2.5-flash`（同一把密钥）。随后用**项目真实函数**
`novel_to_script.build_shots_for_chunk`、真实原文（547 字，反推自兜底剧本）、目标 6 镜复测：

| 档位 | 耗时 | 返回镜头 | finish | 体检 | 台词 | prompt_h3 | 画面描述均长 |
|------|------|----------|--------|------|------|-----------|--------------|
| **未注入（当前配置）** | **30.0s** | **11 镜** | `stop` | **ok=True** | 4 句 | 11/11 | 31.9 字 |
| `reasoning_effort=low` | 70.8s | 20 镜 | `stop`（经一次截断修复 4096→8192） | ok=False（10 个静默镜） | 6 句 | 20/20 | 25.1 字 |

**结论**：
1. **长 JSON 能力没问题** —— 一次调用（`attempts=1`）就吐出了 11 镜的完整合法 JSON，
   `fallback_shot_count=0`，**不再有整集兜底**。从「672s 后 0 镜」变成「30s 后 11 镜」。
2. ⚠️ **不要给 agnes 网关设 `reasoning_effort`**：实测 `low` 档**更慢、更长、还触发截断**，
   而且产出 10 个静默镜。该网关对 `chat_template_kwargs` 大概率是忽略或不兼容，
   所以**档位保持留空**（= 不注入）是当前最优解。
   → 这也解释了 4.5.4 的未验证项：**这个网关不该走档位参数**。

#### 4.5.6 新增护栏：网关级快速失败 / 熔断（已实现 + 57 项离线验证）

针对 4.5.2 那条「672 秒/块、一集近 1 小时白等」的根因，在 `llm_client.py` 加了两层拦截：

| 机制 | 行为 |
|------|------|
| 上游标记识别 | 5xx 响应体含 `no available workers` / `all circuits open` / `upstream_error` / `no available channel` / `service unavailable` → 判定「上游没算力」 |
| 单次调用内快速失败 | 判定为上游不可用时**只试 1 次**（原本 3 次），不再把 3 个超时都打完 |
| 远端超时也算 | 远端地址的 Timeout / ConnectionError / `socket.timeout` / OSError 一律计入网关不可用 |
| 跨调用熔断 | 同网关连续 **2** 次不可用 → 熔断 `600s`，期间**一次网络请求都不发**（实测 `len(CALLS)==0`） |
| 不误伤 | `429`、**无标记**的 5xx 仍按原逻辑重试 3 次；本地地址（ollama）即使报 `no available workers` 也**不熔断**（本地冷启动会自愈） |
| 恢复 | 任一次成功即清零；`/api/ai/test`（用户点「测试连接」）会**先清空熔断**，避免改完配置还被旧熔断拦住 |
| 可观测 | `/api/ai/config` 新增 `gateways[].circuit`；测试连接失败时返回 `verdict="gateway_unavailable"` + `hint` |

**并且改了失败处置**（这是比"快速失败"更重要的一半）：
网关不可用**不再是普通的单块失败**。`novel_to_script._gateway_down_fallback` 现在分两种情况：

- **还没产出任何真实镜头** → **直接报错中止**，不再用原文兜底凑出一份"假成功"剧本。
  报错信息里写明「继续下去只会得到无分镜无台词的兜底剧本（配音只能出 0 句）」+ 处置建议。
- **已有真实产出** → 剩余块按原文兜底，保住已完成部分，并在 warnings 里标注
  **「本集为降级产出，网关恢复后建议重跑」**。

单测：`.workbuddy/test/verify_gateway_failfast.py`（**57/57**，假 HTTP 层驱动，
断言「只发 1 次请求」「熔断期 0 次请求」「429/本地不误伤」「成功清零」「空产出必须抛错」等）。
> 该单测在编写过程中**抓到两个真 bug**：① 503 命中标记后 `break` 写在了 `resp = None` **之前**，
> 导致掉进 `status>=400` 分支、熔断整个失效；② 只捕获 `requests.exceptions.Timeout`，
> 而 `socket.timeout` 在 Python 3.10+ 是内置 `TimeoutError`（OSError 子类），会被当成"请求异常"白等一轮。
> 两处均已修。

#### 4.5.7 顺带发现：正在跑的「雨夜归人」第 1 集有 18/24 个静默镜

自动生产此刻正在为 `雨夜归人` 第 1 集生成 24 张分镜图（21:03 开始）。对该集剧本做只读体检：

```
output/scripts/雨夜归人/第1集.json
  镜头=24  可朗读台词=3  兜底镜=0  静默镜=18  ok=False
```

**注意这跟「整集兜底」不是同一个问题**：`兜底镜=0` 说明它是**真实的模型产出**，
但绝大多数镜头 `narration` 与 `dialogue` 都是空字符串（只有 `audio_cues` 里有"雨声"，
而配音链路不读 `audio_cues`）。逐镜抽查确认：

```
shot 6: narration=''  dialogue=[]   audio_cues='雨声持续'
shot 7: narration=''  dialogue=[]   audio_cues='雨滴敲伞声'
```

**后果**：24 个镜头只有 3 句台词，配音阶段基本出不了音频 → 成片大面积"无声片段"。
这不是坏数据（分镜图仍然可用），但提示词层面值得跟进：**当 `narration` 与 `dialogue` 同时为空时，
应强制模型从原文补一句旁白**，而不是允许整镜静默。已列入建议 12（第四批已修复，见 4.6）。

---

### 4.6 第四批：**静默镜的真实根因不是提示词，是配音链路不读 narration**（本轮修复）

#### 4.6.1 先前的判断只对了一半

4.5.7 的结论是"提示词该加硬约束"。但顺着"加了旁白又会怎样"往下查，发现再往前一步还有个大洞：

```
app/tts_client.py  build_dub_plan()   台词来源只有两种：
                                      shot["dialogue"]（结构化）→ shot["dialogue_text"]（旧字段）
                      对 narration 的引用次数：**0**
```

也就是说——**`narration`（旁白）从头到尾没有任何一条路径会进配音**。
同时 `dialogue_utils.audit_script` 的静默判定却是"台词与旁白**同时**为空才算静默"，
即它默认"有旁白就不算静默"。**两边口径互相矛盾**：

| | 判定依据 | 后果 |
|---|---|---|
| `audit_script`（体检） | 台词或旁白有其一即放行 | "只有旁白"的镜头**不报警** |
| `build_dub_plan`（配音） | **只读 dialogue** | "只有旁白"的镜头**不出声** |

结论：**只改提示词＝零效果**。就算模型乖乖补满旁白，成片照样是哑的；
而体检还会告诉用户"没问题"。这是一个比静默镜本身更值得修的缺陷。

#### 4.6.2 修复（4 处，全部离线验证）

1. **`tts_client.build_dub_plan`：新增「旁白补声」**（根因修复）
   该镜没有任何台词时，用默认「旁白」音色把 `narration` 合成进去；
   旁白少于 4 字（`……`/「雨」）视为不值得单独合成，仍不生成。
   新增配音行字段 `source: "dialogue" | "narration"`，便于前端与审计区分。
2. **`novel_to_script.estimate_shot_duration`：无台词时按旁白长度推算时长**
   否则一句 60 字旁白（≈13 秒）会硬贴在 3 秒画面上 → 音画错位、旁白被截断。
   实测同一段文本，旁白镜与台词镜推算出的时长**完全一致**（10.5s == 10.5s）。
3. **`dialogue_utils.audit_script`：口径与配音链路对齐**
   新增 `narration_line_count` / `speakable_line_count_total`；
   `ok` 的判定由"有台词"改为"台词 + 旁白 > 0"；
   删掉已过时的说法"（旁白不计入配音台词）"，改为在"全片只有旁白、一句台词都没有"时
   单独告警，提示用户检查剧本是否漏写台词。
4. **分镜提示词新增「音轨不空约束」**（`novel_to_script`）
   明确写出：配音链路只读 `dialogue` 与 `narration`，`audio_cues` 里的「雨声/风声」
   **不产生任何人声**；每镜必须"有台词 **或** 旁白 ≥8 字"，**严禁两者同时为空**；
   纯画面镜头必须补写旁白。同时把 `narration` 字段说明从"无则空字符串"改为
   "会被合成进成片，无台词的镜头必须填；确实无需旁白时写空字符串，但不得与 dialogue 同时为空"。

#### 4.6.3 验证：新增 43 项离线测试，全绿

新增 `.workbuddy/test/verify_narration_dub.py`（43/43），覆盖：

- 有台词镜头**不被旁白污染**（行数一行都不多、说话人仍是角色本人）
- 无台词但有旁白 → 补出发声行，`character=旁白`、音色能解析出、`source=narration`
- 旁白过短（`''` / `……` / `雨` / `嗯`）→ 不生成（不制造无意义音频）
- 台词与旁白同时为空 → 不生成（**不凭空造声**）
- 旧字段 `dialogue_text` 仍优先于旁白补声（兼容老剧本）
- 时长推算：旁白镜被正确拉长（3.0s → 10.5s），且与等长台词镜一致
- 体检口径：`silent` 只算"两者皆空"、`narration_line_count` 正确、
  全片只有旁白时给出专门告警且不再出现旧说法
- 提示词硬约束确实写进了模板（防止改完没落盘）
- **真实剧本回归**：见下

#### 4.6.4 用真实剧本验证修复效果（`雨夜归人` 第 1 集）

```
24 镜 / 台词镜 3 / 仅旁白镜 3 / 两者皆空 18
配音行数：旧口径 3 句  →  新口径 6 句
```

**要如实说明**：旁白补声只能**多救回 3 句**（3 → 6）。
剩下 **18 镜是"台词与旁白同时为空"**——它们没有任何可朗读文本，
**必须靠重新生成剧本才能解决**（提示词硬约束从下一次生成开始生效）。
对这 18 个镜头，修复后的体检会明确告警，不会再让用户以为"配音生成成功却没有声音"。

#### 4.6.5 回归

| 套件 | 结果 |
|---|---|
| `verify_narration_dub.py`（本轮新增） | **43/43** ✅ |
| `verify_script_audit.py`（受口径变更影响） | **47/47** ✅ |
| `verify_gateway_failfast.py` | **57/57** ✅ |
| `verify_reasoning_effort.py` | **44/44** ✅ |
| `verify_async_poll.py` | **16/16** ✅ |

> 遗留小事项：`chars_out` 只列出剧本角色，所以「旁白」这个伪角色在配音页
> **没有独立的音色选择项**（会走 `default_voice_map` 自动分配）。
> 不影响出片，若需人工指定旁白音色，后续可在配音页补一个「旁白」条目。

---

### 4.7 第五批：**"配音没有打斗音效"不是模型做不到，是音轨被人为剥掉了**（本轮修复）

#### 4.7.1 用户的疑问

> "现在的配音功能是不是没有打斗等音效，帮我看看怎么解决比较好"

顺着查下去，结论很意外：**不是模型不支持**。

#### 4.7.2 根因：H3 本来就会生成音轨，是我们主动删掉的

MiniMax H3 是**音视频联合模型**——工作流 `分镜生成视频_已装加速链.json` 里有一条完整的活音频链路：

```
SamplerCustomAdvanced → VAEDecodeAudio → CreateVideo.audio → SaveVideo
```

音频 VAE `minimax_h3_audio_vae_fp32.safetensors`（605 MB）就在模型目录里。
也就是说，**每一镜本来就带着打斗、雨声、环境音**，只是被人为删了三道：

| # | 位置 | 做法 |
|---|---|---|
| 1 | `config.H3_EMIT_AUDIO = False` | 提交前就断开 `CreateVideo.audio`（`comfyui_client.strip_h3_audio_inputs`）→ 存盘即无声 |
| 2 | `watermark_cleanup.clean_video` | ffmpeg 无条件 `-an` → 去水印时把音轨一并丢掉 |
| 3 | `config.H3_STRIP_AUDIO = True` | `video_postprocess.ensure_no_audio()` 再剥一次兜底 |

**为什么当初要删**：config 注释写得很清楚——"后续配音统一由 QwenTTS 负责，H3 生成阶段即不带音轨，
**避免不同剧情/角色声音不一致**"。这个顾虑是合理的：H3 会顺手把对白也配上，而每一镜的模型语气色
都不相同，跟统一的 QwenTTS 音色混在一起会"一镜一个嗓"。

**所以这不是 bug，是一个当时做得很决绝的设计取舍**——只是代价连"打斗/雨声"一起赔进去了。

#### 4.7.3 方案对比与选定

| 方案 | 做法 | 评价 |
|---|---|---|
| A. 分层保留 ✅ | 保 H3 音轨 → **分离掉 H3 自己的人声** → 只留音效垫在 QwenTTS 台词下 | **选中**：既拿到音效，又保住统一音色 |
| B. 原样保留 | 直接用 H3 原生音轨 | ❌ 人声会与 QwenTTS **双份对白**，明显穿帮 |
| C. 另配 BGM | 装 ACE-Step 生成配乐 | ⚠️ 需另下模型 + 吃显存，且做不出"打击同步" |
| D. 外部音效库 | ElevenLabs 等付费素材 | ⚠️ 付费、且与画面不同步 |

选 **A（分层保留）**。

#### 4.7.4 实现（新增 1 个模块，改 5 个文件，全部离线验证）

**新增 `app/sfx_isolate.py`**——负责"把 H3 音轨里的人声去掉，只留音效"：

- `resolve_stems()`：把配置归一化成合法音轨下标，**永远排除 Vocals(3)**
- `available()`：探测 ComfyUI 是否具备 `LoadAudio` / `AudioSeparation` / `SaveAudio`；
  ⚠️ 结果 **缓存 5 分钟**——`object_info` 有 4000+ 节点、上兆体积，每镜去拉会拖垮出片
- `isolate_sfx()`：**全链路 fail-open**（任一环失败就保留原音轨，绝不因音效把成片搞挂）
- `build_sfx_entries()`：把分离好的逐镜音效对齐到时间轴，产出混音条目（音量 0.3）

分离用的是 ComfyUI 自带的 `AudioSeparation`（torchaudio `HDEMUCS_HIGH_MUSDB_PLUS`），
输出 4 条 stem：`Bass(0) / Drums(1) / Other(2) / Vocals(3)`。
**权重首次运行会自动下载**，无需手工放置。
取 **Drums + Other**（打击 + 环境音）作为音效；`MelBandRoFormer` 虽然也装了但**模型未下载**，不用它。

**改动清单**：

| 文件 | 改动 |
|---|---|
| `config.py` | `H3_EMIT_AUDIO=True` / `H3_STRIP_AUDIO=False` / `H3_SFX_ISOLATE=True` / `H3_SFX_STEMS=(1,2)` / `H3_SFX_DIR` |
| `watermark_cleanup.py` | 新增 `probe_has_audio()`；`clean_video` **不再无条件 `-an`**，有音轨就 `-c:a copy` |
| `dub_mix.py` | 支持 `original_audio_volume`（垫底默认 0.3）与逐条 `volume`；源无音轨时**不失败**，给可读 `warning` |
| `video_postprocess.py` | 新增 `ensure_audio_track()`：给无音轨视频补**等长静音 AAC**，防拼接错位 |
| `app.py` | 新增 `_h3_audio_policy()` / `_isolate_shot_sfx()`；**两个视频落盘点都接**；`_mix_prepare` 在拿到音效轨时强制关原音轨（否则人声会回来） |

#### 4.7.5 三个必须处理的工程坑

1. **拼接错位**：`concat_videos` 用的是 concat demuxer + `-c copy`，
   **各镜音轨有无不一致会直接错位/失败** → 用 `ensure_audio_track()` 给缺音轨的镜头补等长静音。
2. **人声回归**：音效轨是从 H3 原音轨分离出来的，如果混音时又叠一遍原音轨，
   H3 的人声就回来了 → `_mix_prepare` 里**有音效轨时强制 `keep_original_audio=False`**。
3. **音量**：音效要垫在人声下面 → 新增 `volume` 参数（原音轨 0.3、音效 0.3、QwenTTS 台词 1.0）。

#### 4.7.6 验证

新增 `.workbuddy/test/verify_sfx_pipeline.py`，**57/57 全绿**，覆盖：

- 配置断言（`H3_STRIP_AUDIO=False` 等 5 项）
- `resolve_stems` 归一化：非法值回落、传 `('Vocals',)` 会被剔掉
- 分离工作流接线：**全流程没有任何节点引用 Vocals(下标 3)**；单轨时不产生多余 `AudioCombine`
- 用**真实 ffmpeg 生成的正弦波**当素材：去水印后**音轨确实还在**（本次修复核心）
- `ensure_audio_track`：无音轨→补齐成功；已有音轨→不重复改动
- 混音命令里**确实出现 `volume=0.3000`** 与原音轨输入 `[orig]`
- 源视频无音轨时**仍然成功**并给出可读告警
- 源码接线复核：**两个落盘点都改了**（防止只改一处）、`ensure_audio_track` 已定义
- ComfyUI 探测：`LoadAudio/AudioSeparation/SaveAudio` 三节点齐备（在线）

#### 4.7.7 ⚠️ 尚未完成的部分（如实说明）

- **GPU 实试没做**。H3 约 20.9 GB（int8 offload），HDEMUCS 分离还要额外吃显存；
  当时自动生产正在占用显卡，**不能边跑边试**。已确认链路接线与参数正确，
  **但"真跑一遍不 OOM、分离权重能下下来"这一步还欠着**。
- 本批改动**全部需要重启后端才生效**（`app/*.py` 改了 5 个）。
  按你的选择"等本集过完再重启"，目前**还没重启**。

---

### 4.8 新发现：**单镜质检过不了会触发"整集从头重跑"，不收敛地烧显卡**

这是本轮排查音效时**顺带撞见的**，性质比音效更严重（在烧钱）。

#### 4.8.1 现象

`雨夜归人` 第 1 集从 21:03 开始，一小时二十分钟后进度条仍写 `34%`，
而分镜图目录里**已经躺着 22 张图**。两个数字对不上，于是去翻落盘时间：

```
第一轮  shot_01  21:27  ...  shot_23  22:02  shot_24  22:04     ← 24 镜跑完了
第二轮  shot_01  22:06  ...  shot_08  22:24                    ← 又从头开始
```

**跑完一整集之后，它又从头开始跑第二遍。**

#### 4.8.2 原因：`shot_15` 过不了质检

翻 `output/qc/雨夜归人/image_15.json`，6 次尝试记录得很清楚：

| 尝试 | 分数 | 质检理由 |
|---|---|---|
| 1 | 45 | 景别不符合要求（应为特写，实际为中景）… |
| 2 | 55 | 景别与镜头要求不符：镜头要求特写，但画面为胸部到腰部以上的中景 |
| 3 | 55 | 景别不符：镜头要求特写（看不到腰部以下），但画面为腰部以上中近景 |
| 4 | 55 | 景别为近景/中景（腰部以下可见），与镜头要求『特写』不符 |
| 5–6 | 55 | （同上） |

这一镜要求**特写**，但模型连续 6 次都给了中景/近景。
`step_max_retries`（默认 2）耗尽后，**升级成了整集级重跑**——
而重跑并不会让"特写"这件事变容易，于是**极可能原样再失败一次，再重跑一次**。

#### 4.8.3 为什么值得优先修

- **纯烧显卡**：一集分镜 24 镜 ≈ 40 分钟 GPU，循环下去没有终点；
- **进度显示失真**：`percent` 卡在 34%，用户完全看不出"其实已经在跑第二遍"；
- **根因是提示词层面**："特写"这类景别要求，生成端没有对应的强化手段（或强化不足），
  质检端却按字面严格判定 → **两端标准不对齐**，靠重试是碰运气。

#### 4.8.4 建议（待与你确认后实施）

1. **加"整集重跑上限"**：整集级重跑最多 N 次（建议 1），超了就**带着个别不达标镜头出片 + 标警告**，
   而不是无限重试；
2. **让质检对"景别"宽容一档**：`特写/近景/中景` 属同一连续谱，硬判"不符"过于苛刻；
   或改为"景别偏离只扣分不阻断"；
3. **把重跑事实暴露给用户**：进度里明确写"第 2 遍（因 shot_15 未达标）"，别让 34% 骗人；
4. **生成端补强景别**：把"特写"转成模型能听懂的构图短语（如 `extreme close-up, face fills frame`）。

> 本条**尚未修复**——它牵涉质检阈值与出片策略的取舍，属于"要不要放宽标准"的产品决定，
> 所以先如实记在这里，等你定调。
>
> **⚠️ 2026-09-17 深夜追加**：你问"分镜 QC 不通过在质检时就该发现并改提示词重新生成吧"——
> 顺着这句去查，发现**这条链路本该存在，但在两个地方被悄悄掐断了**，见 **4.9**。
> 4.9 修完之后，4.8 的循环才有真正收敛的可能（否则只是拿同一个提示词换种子瞎撞）。

#### 4.8.5 后续核实：循环**终止了**，但原因和我上面的推断不一样（**修正 4.8.1–4.8.3**）

23:47:32，分镜步**确实走完了**（`step` 从 `storyboard` 变为 `video`，24 张图齐）。
我原本据此以为"第二遍终于碰运气过了"。逐条核对后，**这个说法不成立**：

| 核实项 | 实测结果 |
|---|---|
| `shot_15` 累计尝试次数 | **12 次**（2 轮 × 6），**全部 `passed=False`** |
| 历史最高分 | **75 分**（try6，pass_score 为 **80** 分）→ **即便开着质检也过不了** |
| 循环为什么停 | **不是因为过了**，而是 `qc_config.json` 在 **23:25:31** 被改：`image_enabled: True → False` |
| 关闭之后 | `shot_10`–`shot_24` **完全跳过质检**（连质检记录都没产生），直接写入交付目录 |
| 交付的 `shot_15.png` | 与第三轮 `shot_15_try1.png` **字节完全相同**（1157602 B，同 mtime 23:34:49）→ **未经质检的图** |

时间线与证据吻合：

```
21:27–22:04  第一轮 24 镜（shot_15 试 6 次全败）
22:06–23:0x  第二轮 24 镜（shot_15 试 6 次全败）→ 判定整集重跑
23:09–23:26  第三轮 shot_01–shot_09 有质检记录（image_1..image_9.json）
23:25:31     ★ qc_config.json 被改为 image_enabled=False
23:26–23:47  第三轮 shot_10–shot_24 无质检记录，直接入库
23:47:32     storyboard 完成 → 进入 video
```

**结论修正**：

1. **"会永远循环烧显卡"的说法要收回一半**——风险是真的（两轮 12 次一次没过），
   但**这次是被"关掉图片质检"终止的**，不是它自己收敛。若质检一直开着，它还会继续重跑。
2. **交付物里混进了未经质检的图**（shot_10–24，含那张 12 次都没达标的 shot_15）。
   这不算 bug（是配置被改），但要知道**这批成片的分镜质量没有质检背书**。
3. ⚠️ **和 4.9 的重要交互**：图片质检一旦关闭，`qc_on=False` → 质检循环整个不执行
   → **4.9 修好的"缺陷反哺提示词"链路也就不会运行**（没有质检，就没有教训）。
   **4.9 只在"图片质检开启"时才有意义。**

---

### 4.9 你的追问追出了真凶：**"质检不通过 → 改提示词重生成"这条链路两端都断了，而且都被静默吞掉**

#### 4.9.1 链路本来是这样的（设计是有的）

`_storyboard_worker` 里确实有这么一段，注释还写着"从记忆模块获取教训并修改提示词"：

```
重试时 → prompt_memory.suggest()      ← 召回"上次哪儿不对"
       → prompt_memory.learned_prompt() ← 把建议追加到提示词
       → 换新种子 + 用新提示词重新生成
质检不通过 → prompt_memory.record()    ← 把这次的缺陷沉淀成"教训"
```

设计意图完全正确，**所以你的直觉是对的**。问题在于它从来没真正跑通过。

#### 4.9.2 断点一：`learned_prompt` 在模块级根本不存在

| 调用方式 | 是否存在 |
|---|---|
| `prompt_memory.learned_prompt(...)`（生成链路里的写法） | ❌ **不存在** |
| `prompt_memory.PromptMemory.learned_prompt(...)`（只有类方法） | ✅ 存在 |

`prompt_memory.suggest()` 和 `prompt_memory.record()` 都有模块级便捷入口，
**唯独 `learned_prompt` 漏了**。实测：

```
>>> prompt_memory.learned_prompt(kind='storyboard', prompt='x', root_dir='y')
AttributeError: module 'prompt_memory' has no attribute 'learned_prompt'
```

而调用点外面包着 `except Exception as mem_err: logger.warning(f"读取记忆模块失败: {mem_err}")`
—— **异常被吞掉，只留一行 warning，提示词原封不动**。

#### 4.9.3 断点二：教训库被"喂"了 36 条空记录

`prompt_memory.record()` 需要 `issues/reason/score` 才能沉淀出有用的教训。
调用点却是这么取的：

```python
last_verdict = attempts[-1].get("verdict") or {}   # ← 这个键根本不存在
score  = last_verdict.get("score", 0)               # ← 恒为 0
issues = last_verdict.get("issues", [])             # ← 恒为 []
```

因为 `_qc_record_verdict()` 返回的记录把 `score / reason / issues` 放在**顶层**，
**没有** `verdict` 子对象（已用 `image_15.json` 的真实记录逐键核对）。
于是教训库里躺着的 36 条全是空的：

```
project: 雨夜归人  kind: storyboard  score: 0  reason: ""  issues: []
project: 雨夜归人  kind: storyboard  score: 0  reason: ""  issues: []
...（连续多条）
```

**两个断点互为掩护**：即便修好其一，另一个仍会让链路失效；
而且**两个失败都被 try/except 或 `.get(默认值)` 兜住，日志里看不出任何异常**。
这就是"重试了 6 次，提示词一次都没变"的真正原因。

#### 4.9.4 修复（改 2 个文件）

**`app/prompt_memory.py`**：补上模块级 `learned_prompt()`（并写明为什么必须放模块级）。

**`app/app.py`**：

| 改动 | 说明 |
|---|---|
| 新增 `_qc_lesson_from_record()` | 从质检记录里正确取缺陷，**顶层与嵌套 `verdict` 两种形态都兼容**，避免再被字段形态坑 |
| 新增 `_record_qc_lesson()` | 统一沉淀入口，带日志（能看见"记了几条缺陷"） |
| **循环内即时沉淀** | 每次质检不通过**立刻**写库 —— 这样**同一次重试循环的下一次**就能召回它（旧实现只在循环结束后记一次，前 N 次纯瞎撞） |
| 稳定召回基准 `orig_prompt` | 教训库指纹基于**原始提示词**；旧写法在"已叠加建议的提示词"上再叠，会让【历史质检修正建议】一轮轮累积成一大坨 |
| 资产链路同款修复 | `asset` 的质检重试有**完全相同**的两个 bug，一并修 |

#### 4.9.5 验证：新增 44 项离线测试，全绿

新增 `.workbuddy/test/verify_qc_prompt_feedback.py`（**44/44**）。
⚠️ 该脚本**不 import app**（import app 有副作用会短暂停用自动生产），
改为用 AST 从 `app.py` **把函数原样抽出来 exec** —— 既测真实源码，又不打扰在跑的生产。

覆盖的关键断言：

- 模块级 `learned_prompt` 存在且可调用（断点一的回归锁）
- 用**真实质检记录**（`雨夜归人` shot_15）验证：`score=45`、`issues` 3 条、
  缺陷里**含"特写"这个关键线索**（不再是 0 / "" / []）
- 嵌套 `verdict` 形态兼容；空记录不产生垃圾教训；非 dict 输入不炸
- **端到端**：缺陷落库 → `suggest()` 召回非空 → `learned_prompt()` 返回的提示词
  **确实变了**、含【历史质检修正建议】、含原始提示词、含"特写"
- **幂等**：同一提示词连调 3 次结果一致，建议块只出现 1 次（防累积）
- 源码接线复核：`_record_qc_lesson` 共 5 处（定义 + 4 调用）、
  两条链路的**循环内**即时沉淀都在、旧误读写法已归零
- **真实教训库未被污染**（测试全程写临时目录；跑完 `lessons.jsonl` 仍是 36 行）

#### 4.9.6 回归

| 套件 | 结果 |
|---|---|
| `verify_qc_prompt_feedback.py`（本轮新增） | **44/44** ✅ |
| `verify_script_audit.py` | **47/47** ✅ |
| `verify_narration_dub.py` | **43/43** ✅ |
| `verify_sfx_pipeline.py` | **57/57** ✅ |
| `verify_reasoning_effort.py` | **44/44** ✅ |
| `verify_async_poll.py` | **16/16** ✅ |

#### 4.9.7 ⚠️ 生效前提

本批改动在 `app/*.py` 里，**必须重启后端才生效**。
按你的选择，**现在不重启** —— 也就是说：

- 当前正在跑的分镜循环**仍在用旧逻辑**（改提示词依然不会发生）；
- 4.7 的音效改动同样**尚未生效**。

> 另外，4.9 修的是"QC 失败后能不能把缺陷用起来"，**它不会让"特写"这件事自动变容易**。
> 若模型确实做不出特写，重试仍会失败——但至少现在失败原因会**真正进入提示词**，
> 而且不必再等到"整集重跑"这种昂贵的方式才学到教训。

---

## 五、性能与环境基线（供排期参考）

| 环节 | 实测耗时 | 备注 |
|------|----------|------|
| 小说转剧本（1 章 581 字 → 6 镜） | **546.2s** | 含 4 次截断重试 + 3 次 503 重试；**且最终 6/6 走了兜底** |
| 资产 base 图（场景） | 1 次过（92 分） | 质检有效 |
| 资产 base 图（角色） | 3 次过（65 → 75 → **85**） | 质检重试确实在提升质量 |
| 分镜图 1 张 + 质检 | 约 5 min | 85 分通过 |
| 单镜视频（reference 模式，5s） | **449.6s** | 8GB 显存，偏慢 |
| 配音 1 句 | 约 5 min 15s | 被 ComfyUI 队列阻塞 |
| 成片拼接 | 秒级 | |
| 混音 | **0.096s** | |
| ComfyUI 可用显存 | **2.97 / 8.52 GB** | 长期队列 1 running + 2~4 pending |

**结论**：当前硬件下，"一集 6 镜"若要真正跑完，视频环节一项就需约 **45 分钟**。
建议在 UI 上给出更明确的耗时预期，或在显存紧张时降级并集/分辨率。

---

## 六、建议（按优先级）

> 状态标注：✅ 已在第二批修复完成 ｜ ⏸ 待排期

1. ✅ **（P1）给分集生成加互斥**：同 novel + 同章节已有 running 任务时直接复用 task_id，杜绝并发抢配额。
2. ✅ **（P1）成片自动进验收队列**：`/api/final/video` 与混音完成处统一登记交付物，第 5 步旅程闭环。
3. ✅ **（P1）任务状态以产物为准**：`_salvage_episode_script` 兜底回填 `success` 并标 `degraded`，消除"失败但产物有效"的矛盾。
4. ✅ **（P2）前端补"单镜视频重做"入口**：`videoApi.retryShot` + 分镜卡上的两个按钮。
5. ✅ **（P2）AI 连通测试修 `max_tokens`**：改走 `_build_payload`（思考下限 1024），
   并把"只吐思考"如实报为"链路可达、未返回正文"而非失败。
6. ✅ **（P2）兜底剧本预警**：新增 `dialogue_utils.audit_script`，在剧本生成结果、
   `/api/tts/plan`、`/api/tts/generate` 失败提示三处暴露缺口，并在配音页以琥珀色横幅展示。
7. ⏸ **（P0，4.5 实测后升级为最高优先）LLM 网关可达性**：`text` / `chat` 指向的
   `developer.amd.com.cn/radeon/api/v1` **当前无任何可用算力**，导致整集 6/6 兜底。
   ① 立刻换到存活网关（`apihub.agnes-ai.cn/v1` 的 `agnes-2.5-flash` 已实测可出 8 镜 JSON）；
   ② 加"上游连续不可用**快速失败**"护栏 —— 现在要等 3 × 300s 超时才放弃，单块 672s、整集近 1 小时；
   ③ 网关恢复后重跑 `.workbuddy/test/probe_gateway_health.py` 复核。
8. ⏸ **（P1）整集兜底默认标记为需重跑**：现已预警（琥珀横幅 + `all_fallback` 告警），
   但不自动重跑。建议在 `script_audit.ok === false` 时把该集任务标为 `degraded` 并可一键重跑。
9. ⏸ **（P2）模型稳定性**：长 JSON 生成对模型压力大（503 + 截断）。
   建议为"剧本分镜"这类关键长任务配置独立降级链（网关 → 备份网关 → 备份模型），
   而不是直接从"成功"跌到"原文兜底"。
10. ⏸ **（P2）多视角资产质检过严 + 显存吃紧**：8.5 GB 显存下重试 5 次仍未过，
   建议降低多视角要求或允许"基准图通过即放行"。
11. ✅ **（P0，4.5.6 已完成）网关级快速失败 / 熔断**：`llm_client.py` 已加两层拦截
   （上游标记识别 → 单次只试 1 次；连续 2 次不可用 → 熔断 600s 不发请求），
   并把「网关不可用」从"逐块兜底"改为"未产出时直接报错中止"。57/57 离线验证。
   **待后端重启后生效**（当前自动生产在跑，重启会中断）。
12. ✅ **（P1，第四批已完成）静默镜：提示词硬约束 + 旁白真的进配音**（详见 4.6）
   - 关键更正：原先以为"改提示词就够了"，实测发现**配音链路根本不读 `narration`**，
     只改提示词等于零效果 → 于是同时修了 `build_dub_plan`（旁白补声）、
     `estimate_shot_duration`（按旁白长度算时长）、`audit_script`（口径对齐）。
   - `雨夜归人` 第 1 集配音行数 **3 → 6 句**；剩余 18 镜是"台词与旁白同时为空"，
     **必须重新生成剧本**（新提示词从下次生成起生效）。
   - 新增 `verify_narration_dub.py` **43/43**；受口径变更影响的 `verify_script_audit.py` 仍 **47/47**。
13. ⏸ **（P1）整集兜底 / 降级产出可一键重跑**：现已能预警并标注 `degraded`，
   但不自动重跑。建议在 UI 给一个「重跑本集剧本」按钮
   （`雨夜归人` 第 1 集正属于这一类：18 镜无可朗读文本，重跑才能救）。
14. ⏸ **（P3）「旁白」不是角色，配音页没有独立音色项**：`chars_out` 只遍历剧本角色，
   旁白走 `default_voice_map` 自动分配音色，用户无法手动指定。
   建议在配音页补一个「旁白」音色条目。

---

## 七、附录

### 7.1 测试产物清单

```
output/scripts/剑影孤城-测试/第1集.json                     6 镜 / 27s（含 fallback 标记）
output/assets/characters/剑影孤城-测试/沈砚/base.png
output/assets/scenes/剑影孤城-测试/城墙关口/base.png
output/storyboards/剑影孤城-测试/shot_03.png + storyboard_manifest.json
output/videos/剑影孤城-测试/shot_03.mp4                     960×544 / 107 帧 / 4.46s
output/final/剑影孤城-测试/剑影孤城-测试.mp4                 拼接成片
output/final/剑影孤城-测试/剑影孤城-测试_subtitles.mp4
output/final_dub/剑影孤城-测试/剑影孤城-测试_subtitles_ep01_dubbed.mp4   ← 带声成片
output/dub/剑影孤城-测试/{ep01_dub.wav, ep01_dub_manifest.json, voice_map.json, lines/}
output/qc/剑影孤城-测试/asset_image_*.json                  质检记录（含逐次理由）
output/agent/audit-20260917.jsonl                           AI 总控工具调用审计
```

### 7.2 测试脚本

```
.workbuddy/test/剑影孤城.txt                 测试小说（3 章）
.workbuddy/test/thinking_probe.py           思考模式 A/B 对照
.workbuddy/test/agent_feedback_probe.py     AI 总控交互探针（含工具链追踪）
.workbuddy/test/mix_probe.py                混音链路探针
.workbuddy/test/verify_async_poll.py        异步轮询修复单测（16/16）
.workbuddy/test/verify_deliverable_register.py  交付物登记闸门 + 过期打标（30/30）
.workbuddy/test/verify_script_audit.py      剧本体检 + 连通测试误判修复（47/47）
.workbuddy/test/verify_reasoning_effort.py  思考档位全链路（44/44，纯离线：请求体注入/互斥/
                                            非法值归一化/配置落盘/前端契约）
.workbuddy/test/verify_reasoning_effort_http.py 思考档位 HTTP 闭环（19/19，临时实例 5077，
                                            快照 + atexit 还原 ai_config.json）
.workbuddy/test/verify_gateway_failfast.py  网关级快速失败 / 熔断（57/57，假 HTTP 层驱动）
.workbuddy/test/verify_narration_dub.py     旁白补声 / 静默镜修复（43/43，含真实剧本回归）
.workbuddy/test/probe_gateway_health.py     网关健康 + 长 JSON 能力探针（联网，手动跑）
.workbuddy/test/probe_longjson_storyboard.py 用真实分镜函数实测长 JSON（联网，手动跑）
```

> 两个新增脚本都**不联网、不烧 GPU**：`verify_deliverable_register.py` 直接 import `app`
> 并对真实成片做登记/查询/打标（结束时清理探针项目）；
> `verify_script_audit.py` 用假 `_post` 驱动连通测试，最后附加一段对真实剧本的只读体检。

> **2026-09-17 补充（4.5 第三批）**：新增的 `verify_reasoning_effort*.py` 同样离线/离线+临时实例，
> 用假 `_post` 检查真正发出去的 HTTP 请求体；`verify_reasoning_effort_http.py` 会改
> `ai_config.json`，因此**开跑前快照、atexit 还原**，且**只发 base_url/model**、
> 绝不发送 `api_key`（留空 = 保持原密钥，避免冲掉用户密钥）。
> `verify_gateway_failfast.py` 用假 `requests.Session` 驱动，断言"实际发出的请求次数"
> （快速失败=1 次、熔断期=0 次），全程不碰网络。
> `probe_gateway_health.py` / `probe_longjson_storyboard.py` 是**联网**探针，
> 用来一眼判断"网关活着吗 / 长 JSON 吐得出来吗"，不进回归套件、按需手动跑。

> **2026-09-17 补充（4.6 第四批）**：`verify_narration_dub.py` 同样是纯离线单测，
> 直接调用真实的 `tts_client.build_dub_plan` / `novel_to_script.estimate_shot_duration` /
> `dialogue_utils.audit_script`，最后再拿 `output/scripts/雨夜归人/第1集.json`
> 做一次只读回归（核对配音行数是否真的从 3 涨到 6）。
> 它同时会 grep 提示词源码，防止"提示词改完没落盘"这类静默失败。

### 7.3 说明

- 本次为**真实环境实测**，未使用任何 mock；所有结论均以磁盘产物 / 接口响应 / 审计日志为证。
- 测试过程重启过一次后端以加载 F1 修复（使用 `MJSCXT_AUTOPILOT=0` 启动，未触发自动生产）。
- 第二批修复后**再次重启后端**（同样 `MJSCXT_AUTOPILOT=0`；本次把 5000 端口的旧进程一并替换，
  期间短暂不可用）以加载 B1–B6 改动；随后完成 4.3 中列出的全部线上实测。
- 第二批改动集中在：`app/app.py`、`app/pipeline.py`、`app/llm_client.py`、`app/qc_client.py`、
  `app/dialogue_utils.py`，前端 `client.ts` / `types/index.ts` / `ProjectWorkbenchPage.tsx` /
  `AudioTab.tsx` / `OutputReviewTab.tsx` / `AIVaultPage.tsx`；前端已重新构建（`app/static/` 已更新）。
- 测试项目 `剑影孤城-测试` 及测试小说为**测试产物**，可按需删除；
  其中 `output/autopilot/剑影孤城-测试/deliverables.json` 是本次验证 B1 时**主动生成**的，
  仅用于证明验收闭环，可按需删除。
