# 漫剧生成系统 - 功能模块分析与优化方案

> 分析日期：2026-09-17
> 分析对象：`app/` 后端模块 + `frontend/src/` 前端页面

---

## 1. 关键帧 (Keyframes) + 九宫格 (Nine-grid) 分镜功能

### 当前状态

| 组件 | 后端 | 前端 | 状态 |
|---|---|---|---|
| 关键帧管理 | ✅ `keyframe.py` (463行) | ✅ `KeyframesTab` (line 642) | **活跃使用** |
| 九宫格分镜 | ✅ `nine_grid_storyboard.py` (98行) | ✅ `GridPage` (被 ninegrid tab 调用) | **部分使用** |
| 分镜管理 | ✅ `storyboard` API (line 1332+) | ✅ `StoryboardTab` (line 752) | **活跃使用** |

**问题发现：**
- `ninegrid` tab 存在但后端 API `/api/nine-grid/*` **未注册到 `app.py`**（grep 无结果）
- `GridPage.tsx` 实际实现的是九宫格生成功能，但调用的是 `storyboardApi.selectNineGridShot()` 而非独立的 nine-grid API
- 关键帧和九宫格分镜在业务逻辑上是**不同阶段**：
  - 关键帧：单镜头的首尾帧生成（用于视频连贯性）
  - 九宫格：单个镜头的 9 种构图草案（用于导演选择）
  - 分镜：完整的镜头序列管理

### 优化方案

**方案 A：整合为统一「分镜管理」模块（推荐）**

```
Tab: 分镜管理 (Storyboard)
├── 子标签 1: 九宫格构图 (Nine-grid) - 镜头级构图选择
├── 子标签 2: 关键帧生成 (Keyframes) - 镜头首尾帧
└── 子标签 3: 分镜序列 (Storyboard) - 完整镜头列表
```

实施步骤：
1. 修复 `nine_grid_storyboard.py` 的 API 注册（当前未挂载）
2. 将 `GridPage` 重构为 `NineGridTab` 子组件
3. 在 `StoryboardTab` 内增加九宫格/关键帧子标签导航
4. 统一数据模型：`{ storyboard_id, shots[], nine_grid_drafts[] }`

**方案 B：保持分离但修复九宫格 API**

1. 在 `app.py` 注册 `/api/ninegrid/*` 端点
2. 修复 `GridPage.tsx` 的 API 调用路径
3. 添加九宫格数据到项目资产统计

---

## 2. TTS配音 + 音画混音 功能整合

### 当前状态

| 组件 | 后端 | 前端 | 状态 |
|---|---|---|---|
| TTS配音 | ✅ `tts_client.py` + `/api/tts/*` | ✅ `TtsTab` (line 816) | **活跃使用** |
| 音画混音 | ✅ `dub_mix.py` (302行) + `/api/mix/*` | ✅ `MixTab` (line 919) | **活跃使用** |

**问题发现：**
- 两个功能是**顺序依赖**关系：先配音 → 后混音
- 用户需要切换两个 Tab 完成一个完整流程
- `dub_mix.py` 已包含 TTS 播放逻辑（line 52: `ffmpeg_available()`），但前端分开调用

### 优化方案

**方案：合并为「声音处理」统一模块**

```
Tab: 声音处理 (Audio)
├── 步骤 1: TTS 配音生成
│   ├── 环境检查 → 配音计划预览 → 生成配音
│   └── 试听/下载单条配音
├── 步骤 2: 音画混音合成
│   ├── 选择音轨 + 视频源
│   ├── 混音参数（音量/淡入淡出/时序调整）
│   └── 生成混合音频
└── 步骤 3: 音频质检
    ├── 波形预览
    └── 时长/格式校验
```

实施步骤：
1. 新建 `AudioTab.tsx` 组件（含三个子步骤）
2. 后端新增 `/api/audio/pipeline` 端点（一键完成配音+混音）
3. 保留原有 `/api/tts/*` 和 `/api/mix/*` 作为独立接口（供自动化流水线调用）
4. 移除单独的 `tts` 和 `mix` tabs，改为 `audio` tab

**收益：**
- 用户操作从 2 个 Tab 减少到 1 个
- 流程可视化（步骤进度指示器）
- 支持中断恢复（TTS 完成后直接跳转混音）

---

## 3. 导出 + 成品验收 功能合并

### 当前状态

| 组件 | 后端 | 前端 | 状态 |
|---|---|---|---|
| 导出 | ✅ `export_manager.py` + `nle_export.py` | ✅ `ExportTab` (line 1067) | **活跃使用** |
| 成品验收 | ✅ `/api/autopilot/deliverables*` | ✅ `DeliverTab` (line 1224) | **活跃使用** |

**问题发现：**
- 两个功能已是**顺序流程**：导出成片 → 验收/打回
- `DeliverTab` 已包含视频预览和验收操作
- `ExportTab` 仅生成 FCPML/EDL/JSON 文件，不生成视频

**实际工作流程：**
```
导出 (Export)          成品验收 (Deliver)
├── 生成 FCPML 时间轴   ├── 查看成片清单
├── 生成 EDL 编辑日志   ├── 视频预览
└── 生成项目 JSON      └── 接受/打回
```

### 优化方案

**方案：合并为「输出与验收」统一模块**

```
Tab: 输出与验收 (Output & Review)
├── 区域 1: 导出配置
│   ├── 输出格式选择 (FCPML/EDL/MP4)
│   ├── 分辨率/码率设置
│   └── 生成导出文件
├── 区域 2: 成片清单
│   ├── 各集状态 (待验收/已接受/已打回)
│   └── 批量操作 (全部接受/全部打回)
└── 区域 3: 验收操作
    ├── 视频播放器
    ├── 验收意见输入
    └── 提交验收结果
```

实施步骤：
1. 新建 `OutputReviewTab.tsx` 组件
2. 将 `ExportTab` 和 `DeliverTab` 的内容迁移为子区域
3. 后端新增 `/api/output/generate` 端点（同时生成导出文件+触发成片质检）
4. 保持原有 API 向后兼容

---

## 4. 橄榄模块与剧本概览功能

### 当前状态

**「橄榄模块」分析：**
- 在整个代码库中 **搜索不到「橄榄」相关代码**
- 可能是以下情况之一：
  1. 翻译错误（英文 "olive" 应为其他功能）
  2. 历史遗留功能（已删除但未清理）
  3. 第三方模块引用（如 "Olive" AI 服务）

**剧本概览功能分析：**
- 后端存在 `continuity.py` 中的 `build_summary_card()` 函数 (line 806)
- 该函数生成每集摘要卡：`{script, summary, validation, assets, events}`
- 但**前端未暴露此功能**：`OverviewTab` 不显示剧本概览

### 优化方案

**方案 A：实现剧本概览功能（推荐）**

在 `OverviewTab` 中增加剧本概览区域：

```typescript
interface ScriptOverview {
  total_episodes: number;
  total_scenes: number;
  characters: CharacterBrief[];
  episodes: {
    episode_no: number;
    title: string;
    scene_count: number;
    duration_sec: number;
    status: 'pending' | 'generating' | 'done' | 'failed';
  }[];
  summary: string; // 来自 continuity.summary_card
}
```

实施步骤：
1. 后端新增 `/api/script/overview?project=xxx` 端点
2. 前端 `OverviewTab` 增加剧本概览卡片
3. 显示：总集数、总场景数、角色列表、各集状态

**方案 B：澄清「橄榄模块」含义**

需要用户确认：
- 「橄榄」是指哪个具体功能？
- 是否是某个第三方集成（如 Olive AI 视频服务）？
- 如果是历史功能，建议从文档中移除引用

---

## 5. 角色关系图功能

### 当前状态

| 组件 | 状态 | 说明 |
|---|---|---|
| 后端 API | ✅ 完整 | `relation_manager.py` (334行) + `/api/relations/*` |
| 前端 API Client | ✅ 完整 | `client.ts:174` 有 `relations/graph` 调用 |
| 前端 UI | ❌ **缺失** | 无任何组件渲染关系图 |

**后端已实现功能：**
- 关系类型：亲属/朋友/敌人/恋人/师徒/同事/对手/盟友/陌生人/主仆
- 关系强度：close(0.9) → hostile(-0.8)
- 双向关系支持
- 冲突检测 (`RelationConflictDetector`)
- SVG/JSON 导出

### 优化方案

**方案：实现角色关系图前端组件**

```
新建: RelationGraphTab.tsx

功能:
├── 可视化关系图谱 (Force-directed graph)
├── 节点: 角色卡片 (头像 + 名称 + 类型)
├── 边: 关系连线 (颜色/粗细表示关系类型/强度)
├── 交互:
│   ├── 点击节点查看角色详情
│   ├── 拖拽调整布局
│   └── 双击添加/编辑关系
└── 导出: SVG/PNG 图片
```

技术选型：
- **推荐**: `react-force-graph-2d` (轻量、性能好)
- **备选**: `@xyflow/react` (功能强、学习曲线陡)
- **简单方案**: SVG + d3-force (完全控制)

实施步骤：
1. 安装依赖：`npm install react-force-graph-2d`
2. 新建 `RelationGraphTab.tsx` 组件
3. 调用 `/api/relations/graph` 获取数据
4. 在 `ProjectWorkbenchPage.tsx` 添加新 tab（或在 `characters` 页面内嵌）
5. 添加 i18n 翻译键

**必要性评估：高**
- 剧本创作阶段需要管理角色关系
- 视觉上帮助理解人物关系网
- 与现有 `character_manager.py` 功能互补

---

## 总结与建议优先级

| 优先级 | 功能 | 工作量 | 收益 | 建议 |
|---|---|---|---|---|
| **P0** | 角色关系图 | 中 | 高 | 立即实现（后端已完成 80%） |
| **P1** | 导出+验收合并 | 小 | 中 | 短期优化用户体验 |
| **P1** | 剧本概览功能 | 小 | 中 | 补充缺失的前端展示 |
| **P2** | TTS+混音整合 | 大 | 中 | 中期重构 |
| **P2** | 关键帧+九宫格整合 | 中 | 低 | 九宫格 API 需先修复 |
| **?** | 橄榄模块 | ? | ? | 需用户澄清含义 |

---

## 附录：代码位置索引

| 功能 | 后端文件 | 前端文件 | API 端点 |
|---|---|---|---|
| 关键帧 | `app/keyframe.py` | `ProjectWorkbenchPage.tsx:642` | `/api/keyframes/*` |
| 九宫格 | `app/nine_grid_storyboard.py` | `GridPage.tsx` | `/api/nine-grid/*` (未注册) |
| 分镜 | `app/storyboard*.py` | `ProjectWorkbenchPage.tsx:752` | `/api/storyboard/*` |
| TTS | `app/tts_client.py` | `ProjectWorkbenchPage.tsx:816` | `/api/tts/*` |
| 混音 | `app/dub_mix.py` | `ProjectWorkbenchPage.tsx:919` | `/api/mix/*` |
| 导出 | `app/export_manager.py` | `ProjectWorkbenchPage.tsx:1067` | `/api/export/*` |
| 验收 | `app/autopilot.py` | `ProjectWorkbenchPage.tsx:1224` | `/api/autopilot/deliverables*` |
| 关系图 | `app/relation_manager.py` | ❌ 缺失 | `/api/relations/*` |
| 剧本概览 | `app/continuity.py:806` | ❌ 缺失 | 无独立端点 |
