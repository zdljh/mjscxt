# 缺陷修复批次复核与复验报告（漫剧生成系统）

**日期**：2026-09-22
**场景**：QA 测试 + 发布判定（修复批次验收）
**参与成员**：质量门神（QA 与发布）· 独立复核 ×1 + 独立复验 ×1；主理人（编排与修复执行）
**代码基线**：`origin/main` `405998c` → **`f3f9ed9`**（推送后本地/远端零分叉）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 **通过（Go）**
- 阻塞项数量：**0**
- 上游 11 条缺陷（D-01 ~ D-11）**全部落实**；本轮复核另发现 **2 项本批新引入缺陷（B1/B2）**，均已修复并推送
- 复验新增 4 条非阻塞项，其中 3 条（含 2 条为修复者自身产物瑕疵）已修，1 条留待下一批
- 下一步：可放行；建议将 4 条残余小修纳入下一批次

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 4（非阻塞，已修 3） |
| 关键行动项 | 4 条（3 已闭环，1 待排期） |
| 建议负责人 | 已由本批次闭环；残余项建议并入下一批次 |

---

## 1. 各成员核心结论

### ✅ 质量门神（QA 与发布）· 第一轮独立复核
- 核心判断：**🟡 条件 Go** —— D-01/D-02/D-03/D-04 四件套全部满足审计报告第 5 节验收条件（独立读码 + 复跑确认），但**本批自己引入了 2 类新缺陷**，列为阻塞。
- 关键建议：修复 3 处 logger 方法名拼写（B1），并把分镜 manifest 的第二写路径（B2）纳入统一原子落盘。

### ✅ 质量门神（QA 与发布）· 第二轮独立复验
- 核心判断：**🟢 Go / 0 阻塞** —— B1、B2 均确认为真修复，未引入新回归。
- 关键建议：`tts_client` 日志级别偏低（P3）、`verify_atomic_write` 有 1 条死断言（P3）、manifest 损坏时 API 层裸 HTML 500 且部分成功误报（P2）。

### 🔧 主理人（编排与修复执行）
- 核心判断：上游 11 条修复质量高（多为新建独立模块 + 入库守卫脚本），但**同一批次内引入了属性名拼写类假修复与同根因遗漏** —— 说明"修完必须全库复跑 + 守卫反向验证"这一环缺失。
- 关键动作：独立复现并修复 B1/B2；追加防回潮守卫并**实测守卫会红**；修复复验提出的 3 条 P3/P2；全部提交推送。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 处置 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| B1 | 🔴→已修 | 代码bug（回归） | `app/project_store.py:139`、`app/pipeline.py:1450`、`app/tts_client.py:623` | `34b944c`（D-09）写日志时写错方法名 `logger.w(`/`logger.d(`。`logging.Logger` 无此方法，三处均落在 `except` 处理体内 → 走到即二次抛 `AttributeError`，把"可降级/不阻断"变成硬失败并吞掉原始异常。`compileall`/`pyflakes` 均不做属性检查故全绿漏过 | ✅ `fae7fa5` + 新增 `verify_silent_except` 第 5 节守卫（白名单 + `hasattr` 校验 + 守卫自检） | 质量门神 |
| B2 | 🟠→已修 | 业务流程逻辑缺陷 | `app/app.py:1958` `_update_storyboard_manifest_shot` | `storyboard_manifest.json` 有两条写路径，批次只原子化了批量那条。单镜重跑侧仍是裸 `open(w)+json.dump` + `except: manifest={}` fail-open。并发时用残缺 manifest 覆盖 → **其他镜头记录整批丢失** | ✅ `2bc2a56`：读改 `read_json_strict`、写改 `atomic_write_json` + 6 条文本锚点守卫 | 质量门神 |
| C1 | 🟡→已修 | 可观测性 | `app/tts_client.py:623` | `/free` 互斥守卫失败记 `debug` 偏低：该守卫作用是"避免卸掉其它任务正在使用的模型"，失败＝互斥保护失效（安全类）；紧邻同类日志用 `warning` 形成级别倒挂；生产 root 级别 WARNING → `debug` 完全不可见 | ✅ `6e8632e` 升为 `warning` | 质量门神（复验） |
| C2 | 🟡→已修 | 代码bug（守卫失效） | `.workbuddy/test/verify_atomic_write.py` | 新增的正则断言 `except[^:]*:\s*\n\s*manifest\s*=\s*\{\s*\}` 匹配不到原始代码（`except Exception:  # noqa: BLE001` 的 `#` 打断 `\s*`）→ **断言恒真 = 死守卫** | ✅ 改 `except[^\n:]*:[^\n]*\n...`，实测能命中原始 fail-open、不误命中修复后代码 | 质量门神（复验） |
| C3 | 🟡→已修 | 业务流程逻辑缺陷 | `app/app.py:1933-1935` | B2 的 fail-loud 读连带后果：图片**先** `copy2` 进正式目录、**后**更新 manifest，manifest 损坏时抛错冒泡 → **部分成功被误报成整镜失败**，且前端拿到裸 HTML 500（全库仅注册 `BadRequest` 处理器） | ✅ `f3f9ed9`：窄 try + `logger.error` + 响应追加 `manifest_updated`/`manifest_error`（**响亮降级，不 fail-open**） | 质量门神（复验） |
| C4 | ⚪ 待排期 | 代码一致性 | `app/app.py:3083-3096`、`fs_atomic._snapshot_bak` | ① `_storyboard_worker` 读旧 manifest 仍 fail-open（仅用于断点续跑回填，可接受但口径不统一）；② `.bak` 快照失败级别不一致（`fs_atomic` 用 `debug` vs `project_store` 用 `warning`） | 留待下一批次 | 质量门神（复验） |

### 关键印证：B2 缺陷严重性的实测数据

| 写法 | 读到半截 JSON | 镜头被清空 | 最终保留 |
|---|---|---|---|
| 修复前（裸写 + fail-open 读） | **56 次** / 400 轮 | **136 次** | **2 / 10 镜（丢 8 个）** |
| 修复后（原子写 + 严格读） | **0** | **0** | **10 / 10 镜** |

并已核实并发前提成立：批量 worker 在 `gpu_task_gate` 内，但单镜重跑 API（`app.py:1834`）不在闸门内 → 两条路径可真正并发。

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 状态 |
|---|------|--------|--------|------|
| 1 | 修复 B1（3 处 logger 方法名）+ 加 logger 方法名白名单守卫 | 主理人 | P0 | ✅ `fae7fa5` |
| 2 | 修复 B2（manifest 第二写路径原子化 + 严格读）+ 加文本锚点守卫 | 主理人 | P0 | ✅ `2bc2a56` |
| 3 | 修复 C1（tts 日志级别倒挂） | 主理人 | P3 | ✅ `6e8632e` |
| 4 | 修复 C2（死断言）+ C3（部分成功误报 / HTML 500） | 主理人 | P3 / P2 | ✅ `f3f9ed9` |
| 5 | C4：统一 manifest 读取口径与 `.bak` 失败级别 | 下一批次 | P3 | ⏳ 待排期 |

### 提交与推送链条

```
405998c  基线（审计报告发布时）
  └─ 97453d4 … 3551c57   开发A/B/C 修复批次（11 条缺陷，共 11 提交）
9500fa0  ← 拉取并 --ff-only 合并（零分叉、无冲突）
fae7fa5  B1：logger 方法名拼写 + 守卫
2bc2a56  B2：manifest 单镜重跑写路径原子化 + 严格读
6e8632e  C1：tts /free 守卫日志级别 debug → warning
f3f9ed9  C3+C2：manifest 失败响亮降级（不再误报失败/裸 500）+ 修死断言
```

`git push origin main` 全程执行，推送后本地领先 0 / 远端领先 0。

### 守卫计数变化（修复前后）

| 守卫脚本 | 修复前 | 修复后 |
|---|---|---|
| `verify_silent_except.py` | 17 | **20**（+logger 方法名白名单 3 条） |
| `verify_atomic_write.py` | 49 | **58**（+B2 6 条、+部分成功降级 3 条） |

---

## ⚠️ 待完善 / 已知局限

1. **C4 未处理**：manifest 读取口径未完全统一（`_storyboard_worker` 侧仍 fail-open）；`.bak` 快照失败日志级别不一致。
2. **上游审计报告中的非阻塞项**（第一轮复核提出，尚未处理）：
   - `_prompt_preflight` 之后的提示词可能重新 >6000（`prompt_qc` 无长度上限）→ 使 D-06 的"≤6000"不绝对成立；
   - `TaskQueue.submit` 在 `app/` 内**无生产调用方** → D-07 修复存在"已修但不可达"的假象；
   - `llm_client` 密钥迁移失败用 `warning` 而非验收②字面要求的 `error`（未静默、不假装成功）；
   - `watermark_cleanup.py:113/155` 使用 `path + ".clean.png"/".clean.tmp"`（同 path 并发清洗会互踩，观察项）。
3. **审计范围边界**：本轮为**离线只读 + 离线回归**，未起服务、未触生产、未跑真实 GPU 链路。并发结论来自合成对撞探针，非生产流量。
4. **安全提示**：`/api/storyboard/retry-shot` 全程无鉴权与 `_autopilot_guard`（与本批次无关，属既有设计），任何能访问该端口的本机/同网段进程均可触发重跑 —— 建议纳入后续安全评审。
5. **环境现象（非缺陷）**：`.workbuddy/test/` 部分脚本收尾批量删除会被 `CODEBUDDY_SAFE_DELETE_BULK_GUARD` 拦截，表现为断言跑完后出现一行 `[safe-delete]...` 且 exit=1。判据＝汇总行存在且无 traceback，属**环境假红**。

---

## 📚 成员产出索引

- **质量门神（QA 与发布）· 第一轮复核**：D-01~D-11 逐条裁决表 + B1/B2 新缺陷定位（含 `git blame` 追溯至 `34b944c`）+ 8 个新守卫与抽样历史回归结果。原始产出见本次会话内复核报告。
- **质量门神（QA 与发布）· 第二轮复验**：B1 的 **AST 级全库扫描**（662 个日志调用点、0 处错名）+ 守卫三层反向验证（含"污染白名单"验证 5.2 兜底有效）；B2 的**并发实测数据**（旧写法丢 8/10 镜）+ 正常路径 16 条断言 + 断言活性反向验证 + API 500 冒泡链路核实。原始产出见本次会话内复验报告。
- **主理人（编排与修复执行）**：B1/B2 独立复现与修复；4 笔提交；守卫反向验证（注入探针确认守卫会红并精确报行号）；回归汇总（`verify_silent_except` 20/20、`verify_atomic_write` 58/58、`verify_atomic_write_locks` 77/77、`verify_index_failloud` 23/23、`verify_storyboard_skip` 全通过、`verify_asset_skip_existing` 30/30、`verify_narration_dub` 55/55、`verify_step_final_episode` 37/37、`verify_final_video_episode` 37/37、`verify_h3_prompt_kit` 128/128、`verify_project_name_guard` 58/58、`verify_project_audit` 40/40、`compileall` exit 0、`pyflakes` 无 `undefined name`）。
- **缺陷定义权威来源**：`全项目缺陷审计报告_三批次分工_20260922.md`（第 2 节缺陷定义 + 第 5 节验收标准）。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
